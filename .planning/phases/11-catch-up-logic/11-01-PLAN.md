---
phase: 11-catch-up-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bensdorp1/db/schema.py
  - src/bensdorp1/db/engine.py
  - src/bensdorp1/commands/events.py
  - src/bensdorp1/commands/_scan_engine.py
  - tests/test_commands/test_catchup.py
autonomous: true
requirements: [STATE-05, STATE-07, DATA-06]

must_haves:
  truths:
    - "A fresh DB created via metadata.create_all() includes the positions.delisted column defaulting to 0"
    - "Running run_migrations() on an existing DB adds positions.delisted without error and is idempotent on re-run"
    - "events.py exposes 13 pure template functions returning the spec §8.9 verbatim strings"
    - "_query_open_positions() returns _OpenPosition records carrying entry_close, shares, and delisted"
  artifacts:
    - path: "src/bensdorp1/commands/events.py"
      provides: "13 catch-up event template rendering functions + split-notification renderer"
      contains: "def render_initial_stop_violated"
    - path: "src/bensdorp1/db/schema.py"
      provides: "positions.delisted column with server_default text('0')"
      contains: "delisted"
    - path: "src/bensdorp1/db/engine.py"
      provides: "ALTER TABLE positions ADD COLUMN delisted migration"
      contains: "ADD COLUMN delisted"
    - path: "tests/test_commands/test_catchup.py"
      provides: "Wave-0 test scaffold with all 14 named test stubs"
      contains: "def test_apply_splits_math"
  key_links:
    - from: "src/bensdorp1/commands/_scan_engine.py"
      to: "positions.c.delisted"
      via: "_query_open_positions SELECT"
      pattern: "positions.c.delisted"
    - from: "src/bensdorp1/commands/events.py"
      to: "bensdorp1.ui.format_price"
      via: "import"
      pattern: "from bensdorp1.ui import format_price"
---

<objective>
Lay the Phase 11 foundation: add the `positions.delisted` schema column and migration, create the `events.py` module with all 13 catch-up event templates (pure formatters), extend `_OpenPosition` / `_query_open_positions` to carry the fields split math and delisting detection need, and scaffold the full `test_catchup.py` file with all 14 named failing test stubs.

Purpose: Downstream plans (02 engine logic, 03 rendering + gate) build against these contracts — the schema column, the event template strings, and the extended position record — without exploring the codebase.
Output: Modified `schema.py`, `engine.py`, `_scan_engine.py` (struct + query only); new `events.py`; new `test_catchup.py` scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-catch-up-logic/11-CONTEXT.md
@.planning/phases/11-catch-up-logic/11-RESEARCH.md
@.planning/phases/11-catch-up-logic/11-PATTERNS.md

<interfaces>
<!-- Extracted from 11-PATTERNS.md and 11-RESEARCH.md. Executor uses these directly. -->

Current _OpenPosition (src/bensdorp1/commands/_scan_engine.py lines 84-92) fields:
  id: int, symbol: str, entry_date: datetime, initial_stop: float,
  highest_close: float, trailing_stop: float

Phase 11 must ADD three fields: entry_close: float, shares: int, delisted: int

There are TWO sites that construct _OpenPosition and MUST be updated to pass the
three new fields: _query_open_positions (lines 442-451) and the in-memory snapshot
replacement in _update_position_stops (lines 529-536). The snapshot replacement
carries entry_close=pos.entry_close, shares=pos.shares, delisted=pos.delisted
through unchanged.

Existing positions table columns (src/bensdorp1/db/schema.py lines 42-59):
  id, symbol, entry_date, entry_close, shares, initial_stop, highest_close,
  trailing_stop, scan_id, closed_at, exit_price, realized_pnl,
  closed_reason, closed_manual_reason
schema.py sqlalchemy import block (lines 7-18) does NOT currently import `text`.

Existing run_migrations() ALTER TABLE list (src/bensdorp1/db/engine.py lines 81-89):
  "ALTER TABLE positions ADD COLUMN closed_reason TEXT"
  "ALTER TABLE positions ADD COLUMN closed_manual_reason TEXT"
  (wrapped in try/except OperationalError, conn.commit() per stmt)
engine.py already imports `text` (line 11) and `OperationalError` (line 13).

The 13 template function signatures (11-RESEARCH.md "All 13 Template Signatures"):
  Template 1  render_initial_stop_violated(symbol, trigger_date, close, stop)
  Template 2  render_trailing_stop_violated(symbol, trigger_date, close, stop)
  Template 3  render_new_highest_close(symbol, trigger_date, close, old_stop, new_stop)
  Template 4  render_removed_from_sp500(symbol, removal_date)        # removal_date may be None
  Template 5  render_stock_split(symbol, ratio_str, split_date, old_shares, new_shares, old_price, new_price)
  Template 6  render_dividend(symbol, div_date, amount)
  Template 7  render_market_delist(symbol, delist_date)             # delist_date may be None
  Template 8  render_regime_bull_to_bear(change_date)
  Template 9  render_regime_bear_to_bull(change_date)
  Template 10 render_constituents_updated(change_date, n_added, n_removed)
  Template 11 render_data_fetch_failed(n_days, dates_list)
  Template 12 render_trading_holidays(n, dates_list)
  Template 13 render_composite(symbol, events_list)

Spec §8.9 verbatim wording for Templates 1-7 + 13 is in 11-RESEARCH.md "Code
Examples" and quoted in 11-CONTEXT.md §specifics. format_price renders "$X,XXX.XX".
Continuation lines are indented 6 spaces (matches the spec §7.6 example alignment).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add positions.delisted column (schema + migration)</name>
  <files>src/bensdorp1/db/schema.py, src/bensdorp1/db/engine.py</files>
  <read_first>
    - src/bensdorp1/db/schema.py (current positions Table definition lines 42-59 and the sqlalchemy import block lines 7-18)
    - src/bensdorp1/db/engine.py (current run_migrations() ALTER TABLE loop, lines 71-89)
    - .planning/phases/11-catch-up-logic/11-PATTERNS.md (schema.py column-addition pattern; engine.py run_migrations extension — both show exact placement)
    - .planning/phases/11-catch-up-logic/11-RESEARCH.md (Pitfall 4: server_default vs default for Core inserts)
  </read_first>
  <action>
    In src/bensdorp1/db/schema.py: add `Column("delisted", Integer, nullable=False, server_default=text("0"))` as the final column of the `positions` Table (after `closed_manual_reason`, before the closing paren on line 59). Use `server_default=text("0")` NOT `default=0` — per Pitfall 4, Core inserts that omit the column must not violate NOT NULL. Add `text` to the existing `from sqlalchemy import (...)` block (it is not currently imported in schema.py).

    In src/bensdorp1/db/engine.py: append exactly one statement `"ALTER TABLE positions ADD COLUMN delisted INTEGER NOT NULL DEFAULT 0"` to the existing ALTER TABLE list inside run_migrations(), after the `closed_manual_reason` statement. Keep the existing try/except OperationalError + conn.commit() loop unchanged. `text` and `OperationalError` are already imported in engine.py — do not re-import.
  </action>
  <verify>
    <automated>cd "C:/Users/david/Documents/Projetos/bensdorp-1" && uv run python -c "from sqlalchemy import create_engine; from bensdorp1.db.engine import run_migrations; from bensdorp1.db.schema import positions; e=create_engine('sqlite://'); run_migrations(e); run_migrations(e); assert 'delisted' in positions.c; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - schema.py positions Table contains `Column("delisted", Integer, nullable=False, server_default=text("0"))`
    - schema.py sqlalchemy import block includes `text`
    - engine.py run_migrations ALTER TABLE list contains `"ALTER TABLE positions ADD COLUMN delisted INTEGER NOT NULL DEFAULT 0"`
    - The verify command prints `OK` (idempotent double run_migrations succeeds; column present in metadata)
    - `uv run mypy src/bensdorp1/db/schema.py src/bensdorp1/db/engine.py` reports no new errors
  </acceptance_criteria>
  <done>positions.delisted exists on fresh DBs (create_all) and via run_migrations ALTER TABLE; double-run is idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create events.py — 13 catch-up templates + split-notification renderer</name>
  <files>src/bensdorp1/commands/events.py</files>
  <read_first>
    - src/bensdorp1/ui/styles.py (format_price implementation and pure-formatter module style — analog)
    - src/bensdorp1/ui/__init__.py (confirm format_price is exported from bensdorp1.ui)
    - .planning/phases/11-catch-up-logic/11-CONTEXT.md (§specifics — verbatim catch-up entry wording, split notification format, Template 7 best-effort format)
    - .planning/phases/11-catch-up-logic/11-RESEARCH.md (All 13 Template Signatures table; Code Examples)
    - .planning/Bensdorp_1.md §8.9 lines 1570-1665 (the 13 event templates — copy wording verbatim)
    - .planning/Bensdorp_1.md §8.3 lines 1525-1531 (split notification verbatim format)
  </read_first>
  <behavior>
    - render_initial_stop_violated("AAPL", date(2026,5,18), 178.20, 179.50) contains "AAPL", "2026-05-18", "$178.20", "$179.50", "still pending"
    - render_trailing_stop_violated produces the Template 2 wording (trailing-stop variant of Template 1: "Trailing stop violated on ...")
    - render_new_highest_close("MSFT", date(2026,5,17), 325.40, 240.00, 244.05) contains "$325.40" and "Trailing stop updated from $240.00 to $244.05"
    - render_removed_from_sp500("DELI", None) renders without raising when removal_date is None (best-effort date per Open Question 2)
    - render_stock_split("NVDA", "2:1", date(2026,5,19), 23, 46, 432.50, 216.25) contains "2:1", "23", "46", "$432.50", "$216.25"
    - render_dividend("AAPL", date(2026,5,17), 0.24) contains "$0.24" and "No impact on strategy"
    - render_market_delist("SIVB", date(2026,5,18)) contains "Delisted from the market" and the sell --manual hint
    - render_composite("AAPL", ["e1","e2"]) contains "AAPL", "e1", "e2" as a bulleted list (one entry per position, not repeated symbol)
  </behavior>
  <action>
    Create src/bensdorp1/commands/events.py as a pure-formatter module (no I/O, no DB, no Rich markup in return values). Header docstring: "Catch-up event template rendering functions (spec §8.9)." Import `from __future__ import annotations`, `from datetime import date`, and `from bensdorp1.ui import format_price`.

    Implement all 13 functions with the signatures in the <interfaces> block and the verbatim strings from spec §8.9 / CONTEXT.md §specifics. Use format_price(value) for every dollar value (never inline f"${...}"). Use trigger_date.isoformat() (etc.) for dates. For Template 4 (render_removed_from_sp500) accept `removal_date: date | None` — when None, omit the " on {DATE}" clause (best-effort detection date per Open Question 2). For Template 7 (render_market_delist) accept `delist_date: date | None` similarly. Continuation lines are indented 6 spaces to match the spec §7.6 example alignment.

    Also add `render_split_notification(symbol, ratio_str, split_date, old_shares, new_shares, old_price, new_price)` returning the spec §8.3 System-notes format ("Split applied: {ratio_str} (effective {date})" / "Shares: {old} -> {new}" / "Entry price: {old_price} -> {new_price}"). This is the System-notes variant used by _apply_splits in Plan 02; Template 5 (render_stock_split) is the catch-up-summary variant.

    Every function returns a plain `str`. No fenced markup, no Rich tags. Match the spec §8.9 / §8.3 verbatim characters exactly (the spec uses the unicode arrow in the split notification — follow the spec where it is verbatim; otherwise use ASCII per UI-06).
  </action>
  <verify>
    <automated>cd "C:/Users/david/Documents/Projetos/bensdorp-1" && uv run python -c "from bensdorp1.commands import events; import datetime as d; print(events.render_initial_stop_violated('AAPL', d.date(2026,5,18), 178.20, 179.50)); print(events.render_removed_from_sp500('DELI', None)); print(events.render_composite('AAPL',['a','b']))"</automated>
  </verify>
  <acceptance_criteria>
    - events.py defines all 13 render_* functions plus render_split_notification
    - events.py imports format_price from bensdorp1.ui and uses it for every dollar value (no inline `f"${`): `cd "C:/Users/david/Documents/Projetos/bensdorp-1" && grep -c 'f"\${' src/bensdorp1/commands/events.py` returns 0
    - render_removed_from_sp500 and render_market_delist accept a `date | None` second arg and do not raise when None
    - The verify command prints three formatted blocks with no traceback
    - `uv run mypy src/bensdorp1/commands/events.py` strict reports zero errors
  </acceptance_criteria>
  <done>events.py provides 13 verbatim §8.9 templates + the §8.3 split notification as pure str formatters using format_price; None-date paths render cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Extend _OpenPosition + _query_open_positions; scaffold test_catchup.py (14 stubs)</name>
  <files>src/bensdorp1/commands/_scan_engine.py, tests/test_commands/test_catchup.py</files>
  <read_first>
    - src/bensdorp1/commands/_scan_engine.py (_OpenPosition lines 84-92; _query_open_positions lines 427-452; in-memory snapshot replacement lines 529-536)
    - .planning/phases/11-catch-up-logic/11-PATTERNS.md (_OpenPosition extension; _query_open_positions extension; in-memory snapshot replacement — exact field lists)
    - .planning/phases/11-catch-up-logic/11-RESEARCH.md (Pitfall 1 _OpenPosition missing fields; Validation Architecture: the 14 named tests + their requirement IDs)
    - tests/test_commands/test_scan_engine.py (db_engine fixture usage; positions insert pattern; yfinance mock pattern)
    - tests/conftest.py (db_engine fixture)
  </read_first>
  <action>
    In _scan_engine.py: add three fields to the `_OpenPosition` NamedTuple — `entry_close: float`, `shares: int`, `delisted: int` (append after trailing_stop). Extend `_query_open_positions()` SELECT to include `positions.c.entry_close`, `positions.c.shares`, `positions.c.delisted`, and pass them into the `_OpenPosition(...)` construction (entry_close=row.entry_close, shares=row.shares, delisted=row.delisted). Update the in-memory snapshot replacement in `_update_position_stops` (lines 529-536) to carry the three new fields through unchanged (entry_close=pos.entry_close, shares=pos.shares, delisted=pos.delisted) so the NamedTuple construction stays valid. Do NOT add split/delist/render logic here — that is Plans 02 and 03. This task only widens the record and its two construction sites.

    Create tests/test_commands/test_catchup.py with the file header + imports from 11-PATTERNS.md (db_engine fixture, MagicMock/patch, pandas, the events.py imports, the _scan_engine imports). Add all 14 named test functions as stubs that currently fail (use `raise NotImplementedError("filled in Plan 02/03")` — NOT pytest.skip, so they show as failing until implemented):
      Plan 02 (8): test_apply_splits_math, test_split_idempotent, test_split_audit_event, test_split_outside_window_ignored, test_delisted_flag_set, test_delisted_event_not_repeated, test_delisted_excluded_from_candidates, test_delisted_template4
      Plan 03 part A walk (2): test_catchup_stop_reconstruction, test_split_in_catchup_template
      Plan 03 rendering (4): test_catchup_summary_rendering, test_composite_template, test_template3_initial_final_only, test_catch_up_audit_event
    Each stub has a docstring naming the requirement (STATE-05 / STATE-07 / DATA-06) and the behavior it will assert (copy from the Validation Architecture test map). The imports of _apply_splits / _detect_delisted_positions from _scan_engine will not exist until Plan 02 lands — import those names lazily inside the relevant test bodies (not at module top) so the file is importable now. Keep the events.py imports at module top (they exist after Task 2).
  </action>
  <verify>
    <automated>cd "C:/Users/david/Documents/Projetos/bensdorp-1" && uv run python -c "from bensdorp1.commands._scan_engine import _OpenPosition; p=_OpenPosition(id=1,symbol='X',entry_date=__import__('datetime').datetime(2026,1,1),initial_stop=1.0,highest_close=1.0,trailing_stop=1.0,entry_close=1.0,shares=1,delisted=0); print(p.entry_close, p.shares, p.delisted)"</automated>
  </verify>
  <verify>
    <automated>cd "C:/Users/david/Documents/Projetos/bensdorp-1" && uv run pytest tests/test_commands/test_catchup.py --collect-only -q && uv run pytest tests/test_commands/test_scan.py tests/test_commands/test_scan_engine.py -q</automated>
  </verify>
  <acceptance_criteria>
    - _OpenPosition has fields entry_close, shares, delisted (verify command prints "1.0 1 0")
    - _query_open_positions SELECT references positions.c.entry_close, positions.c.shares, positions.c.delisted
    - The in-memory snapshot replacement in _update_position_stops constructs _OpenPosition with all 9 fields (no TypeError)
    - test_catchup.py collects exactly 14 tests (`pytest --collect-only` lists 14)
    - All 14 stubs raise NotImplementedError (not skipped): `cd "C:/Users/david/Documents/Projetos/bensdorp-1" && grep -c "NotImplementedError" tests/test_commands/test_catchup.py` returns 14
    - Existing test_scan.py and test_scan_engine.py still pass (record widened, no behavior change)
    - `uv run mypy src/bensdorp1/commands/_scan_engine.py` strict reports zero errors
  </acceptance_criteria>
  <done>_OpenPosition carries the three new fields at both construction sites; test_catchup.py exists with 14 failing stubs; existing scan tests unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema/migration -> SQLite DDL | ALTER TABLE runs against the user's live DB |
| events.py output -> Rich console (later) | Template strings are eventually printed to the terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | run_migrations ALTER TABLE | mitigate | Idempotent try/except OperationalError; DEFAULT 0 so existing rows are not corrupted; double-run verified by automated check |
| T-11-02 | Information disclosure | events.py template strings | accept | Templates contain only the user's own position data already shown elsewhere; no secrets, single-user local tool |
| T-11-03 | Tampering | events.py Rich markup injection | mitigate | events.py returns plain str with no Rich markup; callers (Plan 03) wrap in Text() so symbol/values cannot inject console markup |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in Phase 11 — no install tasks (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `uv run python -c "...run_migrations twice; assert delisted in positions.c..."` prints OK
- `uv run pytest tests/test_commands/test_catchup.py --collect-only -q` lists 14 tests
- `uv run pytest tests/test_commands/test_scan.py tests/test_commands/test_scan_engine.py -q` — unchanged, green
- `uv run mypy src/bensdorp1` strict — zero errors
- `uv run ruff check src/bensdorp1/commands/events.py src/bensdorp1/db/schema.py src/bensdorp1/db/engine.py`
</verification>

<success_criteria>
- positions.delisted column added (schema + idempotent migration)
- events.py: 13 verbatim §8.9 templates + §8.3 split notification, pure str formatters using format_price
- _OpenPosition + _query_open_positions + snapshot site carry entry_close/shares/delisted
- test_catchup.py scaffolded with 14 failing NotImplementedError stubs
- Existing scan tests green; mypy strict clean
</success_criteria>

<output>
Create `.planning/phases/11-catch-up-logic/11-01-SUMMARY.md` when done
</output>

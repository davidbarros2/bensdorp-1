---
phase: 11-catch-up-logic
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/bensdorp1/commands/_scan_engine.py
  - src/bensdorp1/data/__init__.py
  - tests/test_commands/test_catchup.py
autonomous: true
requirements: [STATE-05]

must_haves:
  truths:
    - "After an absence (missed_days >= 1) the scan output shows a 'Catch-up summary' block with === separators BEFORE the regular scan sections"
    - "The summary shows 'State has been updated for N open positions.' and per-position entries only for positions with notable events (silent otherwise, D-01)"
    - "A position with multiple notable events renders as a single Template 13 composite entry (D-02), with the trailing stop shown initial->final only (D-03)"
    - "The retroactive pending-triggers table appears in the summary and triggers also appear in today's scan output below"
    - "A CATCH_UP_PERFORMED audit event is written when missed_days >= 1"
    - "Template 7 (market delist) renders only when a held symbol has zero price rows across ALL missed days (D-09)"
  artifacts:
    - path: "src/bensdorp1/commands/_scan_engine.py"
      provides: "catch-up summary rendering in _render_output; CATCH_UP_PERFORMED logging; composite collapse; Template 7 threshold"
      contains: "Catch-up summary"
  key_links:
    - from: "src/bensdorp1/commands/_scan_engine.py"
      to: "AuditEventType.CATCH_UP_PERFORMED"
      via: "log_event in run_scan"
      pattern: "CATCH_UP_PERFORMED"
    - from: "src/bensdorp1/commands/_scan_engine.py"
      to: "events.render_composite"
      via: "_render_output composite collapse"
      pattern: "render_composite"
---

<objective>
Complete Phase 11: render the catch-up summary block in `_render_output()` (D-04 before-regular-output placement, D-01 silent positions, D-02 composite, D-03 initial->final stop, retroactive-triggers table), collapse multi-event positions into Template 13, add Template 7 (market delist) with the D-09 zero-rows-across-all-missed-days threshold, log `CATCH_UP_PERFORMED` in `run_scan()` when `missed_days >= 1`, rename `catch_up_notes` -> `catch_up_events` at all call sites, remove the stale "DATA-06 deferred" comment from `data/__init__.py`, fill the remaining test bodies, and run the full verification gate.

Purpose: Delivers the user-visible half of STATE-05 — the explicit catch-up reporting per spec §7.6 — and closes the phase.
Output: Modified `_scan_engine.py` and `data/__init__.py`; all 14 test_catchup.py bodies filled; full suite + mypy strict + ruff green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-catch-up-logic/11-CONTEXT.md
@.planning/phases/11-catch-up-logic/11-RESEARCH.md
@.planning/phases/11-catch-up-logic/11-PATTERNS.md
@.planning/phases/11-catch-up-logic/11-02-SUMMARY.md

<interfaces>
<!-- From Plans 01-02 (landed) + 11-PATTERNS.md _render_output block pattern -->

_render_output current signature accepts catch_up_notes: list[str] (renders in
  System notes). Phase 11 renames to catch_up_events and adds:
    missed_days: list[date]        # for the "absent for N trading days" header line
    open_positions_count: int      # for "State has been updated for N open positions."
  Update ALL call sites in run_scan() and any test helpers.

SEPARATOR = "=" * 64 (module constant in _scan_engine.py) — reuse for the
  "Catch-up summary" header (spec §7.6).

ALL strings to capture.print() MUST be wrapped in Text() (markup safety). Template
  strings from events.py are plain str -> Text(event_str) is safe.

events.py: render_composite(symbol, events_list) for D-02; render_market_delist
  for Template 7; render_new_highest_close for the D-03 collapsed single entry.

Catch-up summary verbatim format: see 11-CONTEXT.md §specifics (spec §7.6 example)
  — header separator, "You were absent for N trading days (START to END).",
  blank line, "State has been updated for N open positions.", per-position entries,
  "The following retroactive exit triggers are still pending..." table, closing
  "Confirm sells with `bensdorp1 sell SYMBOL PRICE`..." line.

CATCH_UP_PERFORMED payload (11-PATTERNS.md): {missed_days: len, start_date, end_date}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Catch-up summary rendering + composite collapse + Template 7 threshold</name>
  <read_first>
    - src/bensdorp1/commands/_scan_engine.py (_render_output full body, especially section 1/header ~line 803 and the System-notes section ~984-985; the catch_up_notes parameter and its call site ~200-212)
    - .planning/phases/11-catch-up-logic/11-CONTEXT.md (§specifics catch-up summary verbatim, split notification, Template 7 best-effort; D-01..D-04, D-09)
    - .planning/phases/11-catch-up-logic/11-PATTERNS.md (_render_output catch-up block pattern; Text() wrapping; render_output call-site threading lines ~542-556)
    - .planning/phases/11-catch-up-logic/11-RESEARCH.md (Pitfall 3 catch_up_notes->catch_up_events rename all callers; Pitfall 5 N>=1 threshold; Pitfall 6 Template 3 collapse; D-09 Template 7 threshold)
    - .planning/Bensdorp_1.md §7.6 (catch-up flow Phase B explicit reporting — verbatim)
  </read_first>
  <behavior>
    - test_catchup_summary_rendering: with two positions (one trailing-stop violated, one new-high), captured output contains "Catch-up summary", "You were absent for", "State has been updated for 2 open positions.", a per-position entry for each, and the retroactive-triggers table header "Symbol  Triggered on   Reason"
    - test_composite_template: a position with >=2 notable events renders a single render_composite entry (symbol appears once, events as bullets) — not repeated symbol lines
    - test_template3_initial_final_only: a position with new highs on multiple missed days shows exactly one "Trailing stop updated from $X to $Y" line using the start-of-absence X and end-of-absence Y
    - (Template 7) a held symbol with zero price rows across ALL missed days renders render_market_delist; a symbol with a partial gap does NOT
  </behavior>
  <action>
    In _render_output(): rename the `catch_up_notes` parameter to `catch_up_events: list[str]` and add `missed_days: list[date]` and `open_positions_count: int` parameters. When `catch_up_events` is non-empty (i.e. missed_days >= 1), render the catch-up summary block BEFORE section 1 (D-04): print SEPARATOR, "Catch-up summary", SEPARATOR, blank, then "You were absent for {N} trading days ({start} to {end})." using missed_days[0]/[-1], blank, "State has been updated for {open_positions_count} open positions.", blank, the per-position entries (each already a formatted string from Plan 02's accumulator), blank, then the retroactive pending-triggers table (Symbol / Triggered on / Reason) built from the still-pending missed-day triggers, then the closing "Confirm sells with `bensdorp1 sell SYMBOL PRICE`..." line. Wrap every printed string in Text(). After this block, the existing regular scan sections (header, regime, exit triggers, buy candidates, System notes) render unchanged.

    Composite collapse (D-02/D-03): when a position has >= 2 accumulated notable events, render them via render_composite(symbol, events) as a single entry. Ensure the trailing-stop line within a composite is the initial->final single line produced in Plan 02 (do not re-expand). Positions with zero notable events are silent (D-01) — counted in the N-positions line but no entry.

    Template 7 (D-09): for each held position, if price rows are completely absent across ALL missed trading days (zero rows in price_daily for the symbol over the full missed-days set), render render_market_delist (Template 7). A partial gap must NOT trigger Template 7 — instead surface available-day events normally and, if desired, a Template 11 data-fetch note for the missing days. Implement the zero-rows-across-all-missed-days check explicitly.

    Update ALL call sites: run_scan()'s _render_output(...) call (pass catch_up_events, [d.date() for d in missed_days], len(open_positions)) and any test helper that calls _render_output. Update the old System-notes "No catch-up actions needed." behavior consistently with the rename.

    Fill test bodies: test_catchup_summary_rendering, test_composite_template, test_template3_initial_final_only, and the Template 7 threshold assertion. Use a Console(record=True, width=120) capture and assert on capture.export_text().
  </action>
  <verify>
    <automated>cd "C:/Users/david/Documents/Projetos/bensdorp-1" && uv run pytest tests/test_commands/test_catchup.py::test_catchup_summary_rendering tests/test_commands/test_catchup.py::test_composite_template tests/test_commands/test_catchup.py::test_template3_initial_final_only -x</automated>
  </verify>
  <acceptance_criteria>
    - _scan_engine.py _render_output prints the literal "Catch-up summary" header guarded by non-empty catch_up_events
    - The string `catch_up_notes` no longer appears as a parameter name in _render_output: `cd "C:/Users/david/Documents/Projetos/bensdorp-1" && grep -c 'catch_up_notes' src/bensdorp1/commands/_scan_engine.py` returns 0
    - _render_output call site in run_scan passes missed_days and open positions count
    - test_catchup_summary_rendering, test_composite_template, test_template3_initial_final_only pass
    - Template 7 renders only when zero price rows exist across all missed days (assert both positive and negative cases)
    - Every new capture.print() argument is wrapped in Text()
  </acceptance_criteria>
  <done>The catch-up summary renders per spec §7.6 before regular output; composites collapse (D-02/D-03); Template 7 uses the D-09 threshold; all callers updated; three rendering tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CATCH_UP_PERFORMED logging + Template 5 in-catchup + cleanup stale comment</name>
  <read_first>
    - src/bensdorp1/commands/_scan_engine.py (run_scan end-of-pipeline where SCAN_PERFORMED is logged and create_backup is called; where missed_days is in scope)
    - src/bensdorp1/data/__init__.py (the docstring line stating "DATA-06 split detection deferred to Phase 11")
    - src/bensdorp1/data/prices.py (any "DATA-06 deferred"/TODO split comment to remove)
    - .planning/phases/11-catch-up-logic/11-RESEARCH.md (State of the Art: deprecated comments to remove after Phase 11; CATCH_UP_PERFORMED payload)
    - .planning/phases/11-catch-up-logic/11-CONTEXT.md (D-04 catch-up summary, audit event)
    - src/bensdorp1/db/audit.py (CATCH_UP_PERFORMED member; log_event)
  </read_first>
  <behavior>
    - test_catch_up_audit_event: after a run_scan with missed_days >= 1, exactly one CATCH_UP_PERFORMED audit row exists with payload missed_days==len, start_date, end_date set; a no-absence scan writes NO CATCH_UP_PERFORMED row
    - test_split_in_catchup_template: (assert finalized) a split inside the catch-up window surfaces a Template 5 entry in the rendered catch-up summary for that position
  </behavior>
  <action>
    In run_scan(): after the catch-up reconstruction completes and before/alongside the existing SCAN_PERFORMED logging, call `log_event(engine, AuditEventType.CATCH_UP_PERFORMED, symbol=None, payload={"missed_days": len(missed_days), "start_date": missed_days[0].date().isoformat(), "end_date": missed_days[-1].date().isoformat()})` ONLY when `len(missed_days) >= 1` (Pitfall 5 threshold — one missed day means two trading days elapsed, which is the spec's >=2 absence). Do not log it on no-absence scans. Keep SCAN_PERFORMED and create_backup unchanged.

    Ensure a split applied during the catch-up window contributes a Template 5 (render_stock_split) entry to that position's accumulated catch-up events so it appears in the summary (wire Plan 02's split-in-window event into the rendered per-position entry). Finalize test_split_in_catchup_template to assert the Template 5 text appears in the captured summary.

    Remove the now-false documentation: in data/__init__.py delete/replace the "DATA-06 split detection deferred to Phase 11" docstring line (DATA-06 is now implemented), and remove any matching TODO/deferred comment in prices.py. Do not change runtime behavior of data/ — comment/docstring edits only.

    Fill test_catch_up_audit_event.
  </action>
  <verify>
    <automated>cd "C:/Users/david/Documents/Projetos/bensdorp-1" && uv run pytest tests/test_commands/test_catchup.py::test_catch_up_audit_event tests/test_commands/test_catchup.py::test_split_in_catchup_template -x</automated>
  </verify>
  <acceptance_criteria>
    - _scan_engine.py logs CATCH_UP_PERFORMED guarded by `len(missed_days) >= 1`
    - test_catch_up_audit_event passes (event present on absence, absent on no-absence scan)
    - test_split_in_catchup_template passes with Template 5 text asserted in rendered output
    - No "DATA-06" deferred/TODO comment remains: `cd "C:/Users/david/Documents/Projetos/bensdorp-1" && grep -rn "DATA-06" src/bensdorp1/data/ | grep -iv "complete" | grep -ic "defer\|todo"` returns 0
    - mypy strict on _scan_engine.py and data/ reports zero errors
  </acceptance_criteria>
  <done>CATCH_UP_PERFORMED logs once per absence (never on no-absence scans); Template 5 surfaces in-summary; stale DATA-06 deferral comments removed.</done>
</task>

<task type="auto">
  <name>Task 3: Full verification gate</name>
  <read_first>
    - .planning/phases/11-catch-up-logic/11-RESEARCH.md (Validation Architecture: full suite command, coverage targets, Phase Requirements -> Test Map)
    - pyproject.toml ([tool.pytest.ini_options], [tool.coverage.*], ruff/mypy config)
    - tests/test_commands/test_catchup.py (confirm all 14 tests now implemented, none skipped)
  </read_first>
  <action>
    Run the full verification gate and fix any failures uncovered (do not weaken tests or thresholds to pass):
    (1) Confirm all 14 test_catchup.py tests are implemented (zero pytest.skip / NotImplementedError remain).
    (2) `uv run pytest --cov=src/bensdorp1 --cov-report=term-missing` — overall coverage >= 90% (TEST-02), all tests green.
    (3) `uv run mypy src/bensdorp1` strict — zero errors.
    (4) `uv run ruff check src/bensdorp1 tests` and `uv run ruff format --check src/bensdorp1 tests` — clean.
    (5) `uv run bensdorp1 scan --help` smoke — no traceback (command still wires).
    If coverage on the new Phase 11 code (_apply_splits, _detect_delisted_positions, events.py, catch-up rendering) is below the line, add targeted tests rather than lowering the threshold.
  </action>
  <verify>
    <automated>cd "C:/Users/david/Documents/Projetos/bensdorp-1" && uv run pytest --cov=src/bensdorp1 --cov-report=term-missing -q && uv run mypy src/bensdorp1 && uv run ruff check src/bensdorp1 tests && uv run ruff format --check src/bensdorp1 tests</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_commands/test_catchup.py -q` reports 14 passed, 0 skipped
    - Overall coverage >= 90% (TEST-02)
    - mypy strict: zero errors across src/bensdorp1
    - ruff check and ruff format --check: clean
    - `uv run bensdorp1 scan --help` exits 0 with no traceback
    - No pytest.skip / NotImplementedError remains in test_catchup.py: `cd "C:/Users/david/Documents/Projetos/bensdorp-1" && grep -c "pytest.skip\|NotImplementedError" tests/test_commands/test_catchup.py` returns 0
  </acceptance_criteria>
  <done>All 14 catch-up tests pass; full suite >=90% coverage; mypy strict, ruff check, ruff format all green; scan command wires cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accumulated event strings -> Rich console | Catch-up event strings printed to the user's terminal |
| price_daily presence -> Template 7 decision | Data-gap drives a "delisted from market" message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Tampering | catch-up event strings printed via Rich | mitigate | All capture.print() args wrapped in Text() (markup=False semantics); events.py returns plain str with no Rich markup |
| T-11-09 | Spoofing | Template 7 false positive from isolated data gap | mitigate | D-09 threshold: Template 7 only when zero price rows across ALL missed days; partial gaps route to normal events / Template 11 |
| T-11-10 | Repudiation | catch-up reconstruction without trail | mitigate | CATCH_UP_PERFORMED audit event with missed_days/start/end payload, logged once per absence |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in Phase 11 — no install tasks |
</threat_model>

<verification>
- `uv run pytest tests/test_commands/test_catchup.py -q` — 14 passed, 0 skipped
- `uv run pytest --cov=src/bensdorp1 --cov-report=term-missing -q` — >= 90% overall
- `uv run mypy src/bensdorp1` — zero errors
- `uv run ruff check src/bensdorp1 tests` + `uv run ruff format --check src/bensdorp1 tests` — clean
- `uv run bensdorp1 scan --help` — exits 0
</verification>

<success_criteria>
- Catch-up summary renders per spec §7.6 before regular output (D-01..D-04); composites collapse; Template 7 uses D-09 threshold
- CATCH_UP_PERFORMED logged once per absence, never on no-absence scans
- Stale DATA-06 deferral comments removed
- All 14 catch-up tests pass; full suite >=90%; mypy strict, ruff, format all green
</success_criteria>

<output>
Create `.planning/phases/11-catch-up-logic/11-03-SUMMARY.md` when done
</output>

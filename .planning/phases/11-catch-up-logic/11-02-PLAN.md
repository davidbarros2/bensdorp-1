---
phase: 11-catch-up-logic
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/bensdorp1/commands/_scan_engine.py
  - tests/test_commands/test_catchup.py
autonomous: true
requirements: [DATA-06, STATE-07, STATE-05]

must_haves:
  truths:
    - "A split detected in the window (max(entry_date,last_scan_date), today] adjusts shares (floor x ratio) and divides entry_close/highest_close/initial_stop/trailing_stop by ratio"
    - "Re-running scan does not re-apply a split already applied (D-05 window self-advances)"
    - "A held position whose symbol left the constituents set gets delisted=1 and one POSITION_DELISTED_FROM_INDEX event, never repeated on later scans"
    - "Delisted symbols are excluded from buy candidates"
    - "_run_preflight returns last_scan_date so split window can be computed"
    - "The missed-days walk accumulates per-position catch-up event strings (stop violations, new highest close, dividends) for Plan 03 to render"
  artifacts:
    - path: "src/bensdorp1/commands/_scan_engine.py"
      provides: "_apply_splits, _detect_delisted_positions, extended _run_preflight, event-accumulating _update_position_stops"
      contains: "def _apply_splits"
  key_links:
    - from: "src/bensdorp1/commands/_scan_engine.py"
      to: "yfinance Ticker.splits"
      via: "_apply_splits"
      pattern: "yf\\.Ticker\\(.*\\)\\.splits"
    - from: "src/bensdorp1/commands/_scan_engine.py"
      to: "AuditEventType.SPLIT_APPLIED"
      via: "log_event"
      pattern: "SPLIT_APPLIED"
    - from: "src/bensdorp1/commands/_scan_engine.py"
      to: "AuditEventType.POSITION_DELISTED_FROM_INDEX"
      via: "log_event"
      pattern: "POSITION_DELISTED_FROM_INDEX"
---

<objective>
Implement the Phase 11 engine logic inside `_scan_engine.py`: `_apply_splits()` (D-05 window + D-06 math + SPLIT_APPLIED audit + System-notes notification), `_detect_delisted_positions()` (delisted flag + one-time POSITION_DELISTED_FROM_INDEX event + buy-candidate exclusion), extend `_run_preflight()` to return `last_scan_date`, and extend the `_update_position_stops()` missed-days walk to accumulate per-position catch-up event strings (using events.py templates) plus dividend (D-08) and regime-change (D-10) detection. Thread `last_scan_date` from `run_scan()`.

Purpose: This is the behavioral core of STATE-07 (delisting) and DATA-06 (splits) and the data half of STATE-05 (catch-up reconstruction). Plan 03 consumes the accumulated event strings for rendering.
Output: Modified `_scan_engine.py`; split/delisted/dividend/regime test bodies filled in `test_catchup.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-catch-up-logic/11-CONTEXT.md
@.planning/phases/11-catch-up-logic/11-RESEARCH.md
@.planning/phases/11-catch-up-logic/11-PATTERNS.md
@.planning/phases/11-catch-up-logic/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (now landed) + 11-RESEARCH.md / 11-PATTERNS.md -->

_OpenPosition now has: id, symbol, entry_date, initial_stop, highest_close,
  trailing_stop, entry_close, shares, delisted

events.py (from Plan 01) exposes render_* templates and
  render_split_notification(symbol, ratio_str, split_date, old_shares, new_shares, old_price, new_price)

Reusable from strategy.positions (unchanged): update_highest_close(),
  compute_trailing_stop(), compute_effective_stop()

Reusable from bensdorp1.data: get_trading_days(start, end)

log_event(engine, AuditEventType.X, symbol=..., payload=<json-dict>)
  Event types already registered: SPLIT_APPLIED, POSITION_DELISTED_FROM_INDEX, CATCH_UP_PERFORMED

Ticker normalization: use the existing period->hyphen helper at the yfinance call
  site (the same conversion prices.py uses; locate the existing _to_yfinance-style
  helper or replicate the single conversion point — do NOT add a second
  normalization location, per DATA-08).

D-05 split window: applicable = splits[(splits.index.date > window_start) &
  (splits.index.date <= today)].sort_index(); window_start = max(entry_date.date(),
  last_scan_date or entry_date.date())
D-06 math: shares = floor(shares * ratio); entry_close /= ratio; highest_close /= ratio;
  initial_stop /= ratio; trailing_stop /= ratio
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _apply_splits() — D-05 window, D-06 math, audit + notification</name>
  <read_first>
    - src/bensdorp1/commands/_scan_engine.py (_update_position_stops DB UPDATE pattern lines ~521-527; in-memory snapshot replacement lines ~529-536; existing yfinance import + ticker normalization usage)
    - src/bensdorp1/data/prices.py (the SINGLE period<->hyphen normalization point — reuse it; DATA-08 forbids a second normalization site)
    - .planning/phases/11-catch-up-logic/11-RESEARCH.md (Pattern 1 _apply_splits full reference; Pitfall 2 UTC-aware DatetimeIndex; Pitfall 7 last_scan_date threading)
    - .planning/phases/11-catch-up-logic/11-CONTEXT.md (D-05, D-06, split notification §specifics format)
    - src/bensdorp1/db/audit.py (log_event signature; SPLIT_APPLIED member)
  </read_first>
  <behavior>
    - test_apply_splits_math: position NVDA shares=23 entry_close=432.50 initial_stop=400.0 highest_close=432.50 trailing_stop=324.37; a 2.0 split in window -> DB row shares==46, entry_close==pytest.approx(216.25), highest_close==216.25, initial_stop==200.0, trailing_stop==162.185
    - test_split_idempotent: after applying a split with last_scan_date < split_date, a second _apply_splits call with last_scan_date >= split_date applies nothing (shares stay 46)
    - test_split_audit_event: one SPLIT_APPLIED audit row with payload ratio==2.0, before.shares==23, after.shares==46
    - test_split_outside_window_ignored: split_date <= last_scan_date -> no change, no audit row
  </behavior>
  <action>
    Add `_apply_splits(engine, open_positions, last_scan_date, today, split_notifications) -> list[_OpenPosition]` to _scan_engine.py. For each open position: compute window_start = max(entry_date.date(), last_scan_date if not None else entry_date.date()). Fetch `yf.Ticker(<hyphen-normalized symbol>).splits` inside try/except (on any exception, skip this position — V5 input-validation guard). If empty, skip. Filter to `(splits.index.date > window_start) & (splits.index.date <= today)`, then `.sort_index()`. Iterate items chronologically; guard `if ratio <= 0: continue` (V5 tamper guard, threat T-11-04). Apply D-06 math with `math.floor` for shares. Run a parameterized `update(positions).where(id==pos.id).values(...)` then `conn.commit()` (existing pattern). Call `log_event(engine, AuditEventType.SPLIT_APPLIED, symbol=pos.symbol, payload={split_date, ratio, before:{...}, after:{...}})`. Build ratio_str (`f"{int(ratio)}:1"` when integral else `f"{ratio}:1"`) and append `render_split_notification(...)` to the passed `split_notifications` list. Replace the in-memory _OpenPosition at its index with the new field values (carry id/symbol/entry_date/delisted unchanged). Return the updated list. Use the existing period->hyphen normalization helper from prices.py — do not introduce a new one.

    Fill the four split test bodies in test_catchup.py (test_apply_splits_math, test_split_idempotent, test_split_audit_event, test_split_outside_window_ignored) using the db_engine fixture and the yfinance mock pattern (`patch("bensdorp1.commands._scan_engine.yf.Ticker")` returning a MagicMock whose `.splits` is a pd.Series with a UTC DatetimeIndex), per the assertion patterns in 11-PATTERNS.md.
  </action>
  <verify>
    <automated>cd "C:/Users/david/Documents/Projetos/bensdorp-1" && uv run pytest tests/test_commands/test_catchup.py::test_apply_splits_math tests/test_commands/test_catchup.py::test_split_idempotent tests/test_commands/test_catchup.py::test_split_audit_event tests/test_commands/test_catchup.py::test_split_outside_window_ignored -x</automated>
  </verify>
  <acceptance_criteria>
    - _scan_engine.py contains `def _apply_splits(`
    - _scan_engine.py contains a `ratio <= 0` guard and a try/except around `yf.Ticker(...).splits`
    - All four named split tests pass
    - mypy strict on _scan_engine.py reports zero errors
    - No new period<->hyphen normalization function introduced (reuses prices.py helper): `cd "C:/Users/david/Documents/Projetos/bensdorp-1" && grep -c 'def _to_yfinance\|def .*normalize' src/bensdorp1/commands/_scan_engine.py` returns 0
  </acceptance_criteria>
  <done>Splits in-window apply D-06 math, log SPLIT_APPLIED, emit a System-notes notification, and are idempotent across scans via the D-05 window; four split tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _detect_delisted_positions() + buy-candidate exclusion + extend _run_preflight</name>
  <read_first>
    - src/bensdorp1/commands/_scan_engine.py (_run_preflight return tuple lines ~262-304; where constituents are loaded; where buy candidates are screened/filtered; run_scan unpacking of _run_preflight at lines ~142-145)
    - .planning/phases/11-catch-up-logic/11-RESEARCH.md (Pattern 4 _detect_delisted_positions full reference; Pitfall 7 last_scan_date; Open Question 2 Template 4 date)
    - .planning/phases/11-catch-up-logic/11-CONTEXT.md (D-11 delisted flag set-once semantics; D-04 catch-up vs scan output)
    - .planning/phases/11-catch-up-logic/11-PATTERNS.md (_run_preflight return-tuple extension; run_scan call-site threading)
    - src/bensdorp1/db/audit.py (POSITION_DELISTED_FROM_INDEX)
  </read_first>
  <behavior>
    - test_delisted_flag_set: open position symbol "SIVB" absent from constituents, delisted==0 -> after detection DB row delisted==1
    - test_delisted_event_not_repeated: position already delisted==1 -> _detect_delisted_positions logs NO new POSITION_DELISTED_FROM_INDEX event (count stays 1 across two calls)
    - test_delisted_excluded_from_candidates: a delisted symbol that would otherwise rank in top candidates does not appear in the returned buy candidates
    - test_delisted_template4: detection returns a Template 4 event string (render_removed_from_sp500) for the newly delisted position
  </behavior>
  <action>
    Add `_detect_delisted_positions(engine, open_positions, constituents) -> list[str]` to _scan_engine.py per Pattern 4: for each pos where `pos.symbol not in set(constituents)` and `pos.delisted == 0`, run parameterized `update(positions).values(delisted=1)`, `conn.commit()`, `log_event(POSITION_DELISTED_FROM_INDEX, symbol=pos.symbol, payload={position_id})`, and append `render_removed_from_sp500(pos.symbol, None)` (Template 4; None removal_date per Open Question 2 — detection date is best-effort). Return the list of event strings. Positions already delisted==1 are skipped (no re-log, no duplicate event) per D-11.

    Call `_detect_delisted_positions` from within `_run_preflight()` after constituents are loaded and after open positions are available (query open positions inside preflight if not already, or thread them). Extend `_run_preflight()` return tuple to add `last_scan_date: date | None` as the final element (Pitfall 7) and the delisted event strings into the catch-up events accumulator. Update the run_scan() unpacking site (lines ~142-145) to receive `last_scan_date`.

    Buy-candidate exclusion: at the point where buy candidates are computed/filtered, exclude any symbol that is currently delisted (either absent from constituents OR positions.delisted==1 for a held symbol). Held delisted positions keep stop monitoring (do not close them) — only exclude from the buy-candidate list (STATE-07).

    Fill the four delisted test bodies in test_catchup.py.
  </action>
  <verify>
    <automated>cd "C:/Users/david/Documents/Projetos/bensdorp-1" && uv run pytest tests/test_commands/test_catchup.py::test_delisted_flag_set tests/test_commands/test_catchup.py::test_delisted_event_not_repeated tests/test_commands/test_catchup.py::test_delisted_excluded_from_candidates tests/test_commands/test_catchup.py::test_delisted_template4 -x</automated>
  </verify>
  <acceptance_criteria>
    - _scan_engine.py contains `def _detect_delisted_positions(`
    - _run_preflight return signature includes `date | None` as the last tuple element (last_scan_date)
    - run_scan unpacking of _run_preflight binds last_scan_date
    - All four delisted tests pass; delisted event is logged exactly once across repeated detection calls
    - mypy strict on _scan_engine.py reports zero errors; existing test_scan_engine.py + test_scan.py still pass
  </acceptance_criteria>
  <done>Delisted positions are flagged once, logged once, excluded from buy candidates, kept under stop monitoring; last_scan_date threaded; four delisted tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Event-accumulating missed-days walk (stop/highest-close, dividend, regime)</name>
  <read_first>
    - src/bensdorp1/commands/_scan_engine.py (_update_position_stops missed-days walk; where update_highest_close/compute_trailing_stop/compute_effective_stop are called; where missed_days and price dfs are available)
    - .planning/phases/11-catch-up-logic/11-RESEARCH.md (Pattern 1 _apply_splits ordering; D-08 dividend window; D-10 regime detection from price_daily SPX SMA200; Pitfall 6 Template 3 initial->final collapse)
    - .planning/phases/11-catch-up-logic/11-CONTEXT.md (D-01 silent positions, D-02 composite, D-03 initial->final, D-07 splits every scan, D-08 dividends catch-up only, D-09 Template 7 threshold, D-10 regime)
    - src/bensdorp1/strategy/positions.py (update_highest_close, compute_trailing_stop, compute_effective_stop)
    - tests/test_commands/test_scan_engine.py (missed-days walk test setup analog)
  </read_first>
  <behavior>
    - test_catchup_stop_reconstruction: across a 3-missed-day walk with rising closes, highest_close and trailing_stop are updated to the final-day values in the DB
    - test_split_in_catchup_template: when _apply_splits applies a split during the catch-up window, a Template 5 (render_stock_split) event string is included in that position's accumulated events
    - (Dividend) a dividend in the catch-up window for a held position produces a render_dividend event string
    - (Regime) an SPX SMA200 regime flip between two consecutive missed days produces a render_regime_bull_to_bear or render_regime_bear_to_bull event string
  </behavior>
  <action>
    Extend `_update_position_stops()` so that, in addition to the existing silent stop updates, it accumulates a per-position list of catch-up event strings:
    - Call `_apply_splits(...)` at the START of the function (before the missed-days walk) so subsequent stop math uses split-adjusted values (D-07). When a split lands inside the catch-up window (missed_days non-empty), append a Template 5 event (render_stock_split) to that position's event list in addition to the System-notes notification.
    - During the walk, when a stop is violated on a missed day, append render_initial_stop_violated or render_trailing_stop_violated (choose based on which stop the close breached — initial vs trailing per effective stop composition). When a new highest close is reached, track it; per D-03/Pitfall 6, do NOT append one Template 3 per day — record only "a new high occurred" plus the initial trailing stop (start of absence) and the final trailing stop (after the walk), and append a single render_new_highest_close using initial->final values.
    - Dividends (D-08): only when missed_days is non-empty, fetch `yf.Ticker(<hyphen symbol>).dividends`, filter to `(index.date > last_scan_date) & (index.date <= today)`, and append render_dividend per dividend in window. Skip dividend fetch entirely on no-absence scans.
    - Regime (D-10): compute SPX SMA200 for each missed trading day from the already-loaded price_daily SPX data; when the regime flips between consecutive missed days, append render_regime_bull_to_bear / render_regime_bear_to_bull. No new data fetch.
    Return the accumulated per-position events (and the still-pending retroactive triggers) to run_scan so Plan 03 can render them. Use a structure that lets Plan 03 collapse multi-event positions into Template 13 (e.g., dict[symbol, list[str]]).

    Fill test_catchup_stop_reconstruction and test_split_in_catchup_template; add dividend and regime assertions to the appropriate stubs.
  </action>
  <verify>
    <automated>cd "C:/Users/david/Documents/Projetos/bensdorp-1" && uv run pytest tests/test_commands/test_catchup.py::test_catchup_stop_reconstruction tests/test_commands/test_catchup.py::test_split_in_catchup_template -x</automated>
  </verify>
  <verify>
    <automated>cd "C:/Users/david/Documents/Projetos/bensdorp-1" && uv run pytest tests/test_commands/test_scan.py tests/test_commands/test_scan_engine.py -q</automated>
  </verify>
  <acceptance_criteria>
    - _update_position_stops calls _apply_splits before the missed-days walk
    - Dividend fetch is guarded so it only runs when missed_days is non-empty (no dividend fetch on no-absence scans) — verify the guard exists in source
    - Template 3 collapse: only one render_new_highest_close per position regardless of number of new-high days (D-03)
    - test_catchup_stop_reconstruction and test_split_in_catchup_template pass
    - Existing test_scan.py and test_scan_engine.py still pass
    - mypy strict on _scan_engine.py reports zero errors
  </acceptance_criteria>
  <done>The missed-days walk applies splits first, then accumulates per-position catch-up event strings (stop violations, single collapsed new-high, dividends in-window, regime flips) for Plan 03; existing scan tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yfinance -> _apply_splits / dividends | Split ratios and dividend amounts are untrusted external numeric input |
| constituents set -> _detect_delisted_positions | Membership drives a persistent flag write |
| symbol string -> SQL | Symbols flow into parameterized queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | _apply_splits ratio handling | mitigate | `ratio <= 0` guard before applying; try/except around yf.Ticker().splits so a malformed/missing response skips the position instead of corrupting state (ASVS V5) |
| T-11-05 | Tampering | symbol in position UPDATE queries | mitigate | SQLAlchemy parameterized update()/where() only — no string interpolation (established project rule) |
| T-11-06 | Spoofing | data-gap false delist (Template 7 path) | mitigate | Template 7 deferred to Plan 03 rendering; D-09 threshold (zero rows across ALL missed days) prevents partial-gap false positives |
| T-11-07 | Repudiation | split/delist actions on state | mitigate | log_event(SPLIT_APPLIED / POSITION_DELISTED_FROM_INDEX) with before/after payload provides audit trail |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in Phase 11 — no install tasks |
</threat_model>

<verification>
- `uv run pytest tests/test_commands/test_catchup.py -q` — the 8 Plan-02 tests pass (4 split + 4 delisted + 2 walk), remaining stubs still skip
- `uv run pytest tests/test_commands/test_scan.py tests/test_commands/test_scan_engine.py -q` — unchanged behavior
- `uv run mypy src/bensdorp1` strict — zero errors
- `uv run ruff check src/bensdorp1/commands/_scan_engine.py`
</verification>

<success_criteria>
- _apply_splits applies D-06 math in the D-05 window, logs SPLIT_APPLIED, emits notification, idempotent across scans
- _detect_delisted_positions flags once, logs once, excludes delisted from buy candidates, keeps monitoring
- _run_preflight returns last_scan_date; run_scan threads it
- missed-days walk accumulates per-position catch-up events (stop/highest-close collapsed, dividends, regime)
- 8 Plan-02 tests green; existing scan tests green; mypy strict clean
</success_criteria>

<output>
Create `.planning/phases/11-catch-up-logic/11-02-SUMMARY.md` when done
</output>

---
phase: 05-ui-components
plan: 05
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
files_modified:
  - src/bensdorp1/ui/__init__.py
  - tests/conftest.py
  - tests/test_ui/test_public_api.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - UI-09
  - UI-10

must_haves:
  truths:
    - "`from bensdorp1.ui import (Severity, print_error, print_warning, print_info, print_success, print_message, render_table, confirm_prompt, text_prompt, number_prompt, print_empty_state, feedback, format_price, format_pct, format_pnl, format_volume, format_days, format_date, format_time, format_timezone_pair, format_relative_duration)` resolves"
    - "`record_console` pytest fixture available in every `tests/test_ui/` test, returning `Console(record=True, width=80)` per call"
    - "`uv run pytest tests/test_ui/ -x -q` exits 0 across the full Phase 5 test suite"
    - "Combined coverage on `src/bensdorp1/ui/` and `src/bensdorp1/config.py` is >= 90%"
    - "`uv run mypy src/bensdorp1/ui/ src/bensdorp1/config.py` exits 0 (mypy strict clean across the entire phase 5 surface)"
    - "`uv run ruff check src/bensdorp1/ui/ src/bensdorp1/config.py tests/test_ui/` exits 0"
    - "`uv run pytest` exits 0 (full repo suite — Phase 5 did not break Phases 1-4)"
    - "No file under `src/bensdorp1/ui/` imports from `bensdorp1.db`, `bensdorp1.data`, `bensdorp1.strategy`, or `bensdorp1.commands` (D-08 / no circular import constraint)"
  artifacts:
    - path: "src/bensdorp1/ui/__init__.py"
      provides: "alphabetised re-exports of the entire public surface of ui/"
      contains: "__all__"
      min_lines: 25
    - path: "tests/conftest.py"
      provides: "record_console fixture added — Console(record=True, width=80) per test"
      contains: "def record_console"
    - path: "tests/test_ui/test_public_api.py"
      provides: "imports each public name from bensdorp1.ui and asserts it is the same object as the source module's export"
  key_links:
    - from: "src/bensdorp1/ui/__init__.py"
      to: "src/bensdorp1/ui/styles.py, messages.py, tables.py, prompts.py, progress.py, empty_states.py"
      via: "from bensdorp1.ui.* import"
      pattern: "from bensdorp1.ui\\."
    - from: "tests/test_ui/test_public_api.py"
      to: "src/bensdorp1/ui/__init__.py"
      via: "import bensdorp1.ui as ui"
      pattern: "from bensdorp1.ui import"
---

<objective>
Finalize the `ui/` subpackage public API and lock the phase gate: alphabetised re-exports
in `ui/__init__.py`, the `record_console` pytest fixture in `tests/conftest.py`, a
public-API smoke test that imports every exposed name, then run the full coverage,
mypy-strict, and ruff verification.

This plan integrates Plans 05-01 through 05-04 into a single importable surface that
Phase 6+ commands can consume via `from bensdorp1.ui import (...)` — no deep imports
required.

Output:
- `src/bensdorp1/ui/__init__.py` — re-exports (replaces the empty package marker from Plan 05-01).
- `tests/conftest.py` — `record_console` fixture added (do not modify the existing `db_engine` fixture).
- `tests/test_ui/test_public_api.py` — public surface smoke + circular-import check.
- Full repo suite green; coverage >= 90% on `src/bensdorp1/ui/` + `src/bensdorp1/config.py`;
  mypy strict and ruff clean across the entire phase 5 surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ui-components/05-CONTEXT.md
@.planning/phases/05-ui-components/05-RESEARCH.md
@.planning/phases/05-ui-components/05-PATTERNS.md
@.planning/phases/05-ui-components/05-VALIDATION.md
@.planning/Bensdorp_1.md
@src/bensdorp1/db/__init__.py
@src/bensdorp1/strategy/__init__.py
@src/bensdorp1/ui/styles.py
@src/bensdorp1/ui/messages.py
@src/bensdorp1/ui/empty_states.py
@src/bensdorp1/ui/tables.py
@src/bensdorp1/ui/prompts.py
@src/bensdorp1/ui/progress.py
@tests/conftest.py
@CLAUDE.md

<interfaces>
From src/bensdorp1/db/__init__.py (re-export template — lines 1-13):
  - module docstring → imports grouped by submodule → alphabetised `__all__` list

From src/bensdorp1/strategy/__init__.py (re-export template — lines 1-34):
  - same structure with scope-scoping docstring (D-02 note)

Public symbols to re-export (sourced from Plans 05-01..04):
  - From `bensdorp1.ui.styles`:
      `format_price`, `format_pct`, `format_pnl`, `format_volume`, `format_days`,
      `format_date`, `format_time`, `format_timezone_pair`, `format_relative_duration`,
      `ERROR_STYLE`, `WARNING_STYLE`, `INFO_STYLE`, `SUCCESS_STYLE`, `MUTED_STYLE`
      (NOT `_console` — leading underscore is private)
  - From `bensdorp1.ui.messages`:
      `Severity`, `print_message`, `print_error`, `print_warning`, `print_info`, `print_success`
  - From `bensdorp1.ui.tables`:
      `render_table`
  - From `bensdorp1.ui.prompts`:
      `confirm_prompt`, `text_prompt`, `number_prompt`
  - From `bensdorp1.ui.progress`:
      `feedback` (the namespace), plus `SpinnerContext`, `TrackContext`, `MultiStepContext`,
      `BlockBarColumn`, `THRESHOLD_SPINNER`, `THRESHOLD_PROGRESS`, `THRESHOLD_ETA`
  - From `bensdorp1.ui.empty_states`:
      `print_empty_state`

Existing tests/conftest.py (Plan 02-02):
  - `db_engine` fixture exists — DO NOT MODIFY
  - Add `record_console` fixture alongside it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize ui/__init__.py with alphabetised re-exports and update tests/conftest.py with record_console fixture</name>
  <files>src/bensdorp1/ui/__init__.py, tests/conftest.py</files>
  <read_first>
    - .planning/phases/05-ui-components/05-CONTEXT.md (§domain — list of modules; §existing code insights — re-export convention)
    - .planning/phases/05-ui-components/05-PATTERNS.md (§ui/__init__.py section — analog of db/__init__.py; §conftest.py addition for record_console)
    - .planning/phases/05-ui-components/05-RESEARCH.md (Finding #7 — record_console fixture template; §Validation Architecture — coverage commands)
    - src/bensdorp1/db/__init__.py (re-export template — alphabetised __all__)
    - src/bensdorp1/strategy/__init__.py (re-export template with module docstring)
    - tests/conftest.py (current state — db_engine fixture; do NOT remove it)
    - src/bensdorp1/ui/styles.py, messages.py, tables.py, prompts.py, progress.py, empty_states.py (created in Plans 05-01..04 — read these to confirm the actual exported names match the interfaces block above)
  </read_first>
  <action>
    Replace `src/bensdorp1/ui/__init__.py` (currently the empty package marker from
    Plan 05-01) with the full re-export module:
    - Module docstring: "Public surface of the bensdorp1.ui subpackage. All commands
      (Phases 6-14) consume only the names exported here — never deep-import from
      ui.styles/messages/etc."
    - Group imports by source submodule, alphabetical within each block:
      `from bensdorp1.ui.empty_states import print_empty_state`
      `from bensdorp1.ui.messages import (Severity, print_error, print_info, print_message, print_success, print_warning)`
      `from bensdorp1.ui.progress import (BlockBarColumn, MultiStepContext, SpinnerContext, THRESHOLD_ETA, THRESHOLD_PROGRESS, THRESHOLD_SPINNER, TrackContext, feedback)`
      `from bensdorp1.ui.prompts import (confirm_prompt, number_prompt, text_prompt)`
      `from bensdorp1.ui.styles import (ERROR_STYLE, INFO_STYLE, MUTED_STYLE, SUCCESS_STYLE, WARNING_STYLE, format_date, format_days, format_pct, format_pnl, format_price, format_relative_duration, format_time, format_timezone_pair, format_volume)`
      `from bensdorp1.ui.tables import render_table`
    - `__all__` list: alphabetically sorted union of every imported name above. Do NOT
      include `_console`, `_render_kv_block`, `_SEVERITY_COLORS`, `_default_console` — those
      are module-private.

    Update `tests/conftest.py` — APPEND (do NOT replace) the `record_console` fixture:
    - Add `from rich.console import Console` to the imports if not present.
    - Add:
      `@pytest.fixture`
      `def record_console() -> Console:`
      `    """Fresh Console(record=True, width=80) per test. UI suite uses this for export_text() assertions."""`
      `    return Console(record=True, width=80)`
    - Keep the existing `db_engine` fixture intact.
    - Keep the existing module docstring; optionally extend it with a one-line note that `record_console` is provided for the UI test suite.

    Do NOT remove or modify `db_engine`. Do NOT modify the existing `_reset_engine_for_testing` calls.
    Do NOT add any other fixtures.
    Do NOT import from `bensdorp1.db`, `bensdorp1.data`, `bensdorp1.strategy`, or `bensdorp1.commands` in `ui/__init__.py`.
  </action>
  <verify>
    <automated>uv run python -c "from bensdorp1.ui import (Severity, print_error, print_warning, print_info, print_success, print_message, render_table, confirm_prompt, text_prompt, number_prompt, print_empty_state, feedback, format_price, format_pct, format_pnl, format_volume, format_days, format_date, format_time, format_timezone_pair, format_relative_duration, ERROR_STYLE, WARNING_STYLE, INFO_STYLE, SUCCESS_STYLE, MUTED_STYLE, BlockBarColumn, SpinnerContext, TrackContext, MultiStepContext, THRESHOLD_SPINNER, THRESHOLD_PROGRESS, THRESHOLD_ETA); print('OK')"</automated>
    <automated>uv run mypy src/bensdorp1/ui/__init__.py tests/conftest.py</automated>
    <automated>uv run ruff check src/bensdorp1/ui/__init__.py tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/bensdorp1/ui/__init__.py` contains an `__all__` list with at least 25 entries (every public name from the 6 submodules).
    - Source: `src/bensdorp1/ui/__init__.py` does NOT contain `_console`, `_render_kv_block`, or `_SEVERITY_COLORS` (no private names re-exported).
    - Source: `src/bensdorp1/ui/__init__.py` does NOT contain `import bensdorp1.db`, `import bensdorp1.data`, `import bensdorp1.strategy`, or `import bensdorp1.commands`.
    - Source: `tests/conftest.py` contains `def record_console` and `db_engine` (both fixtures present).
    - Output: the smoke-import `python -c "from bensdorp1.ui import (...)"` command listed in `<verify>` exits 0.
    - Type command: `uv run mypy src/bensdorp1/ui/__init__.py tests/conftest.py` exits 0.
    - Lint command: `uv run ruff check src/bensdorp1/ui/__init__.py tests/conftest.py` reports 0 errors.
  </acceptance_criteria>
  <done>Public surface complete; conftest fixture added; mypy/ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Public API smoke test + coverage gate + full repo verification</name>
  <files>tests/test_ui/test_public_api.py</files>
  <read_first>
    - .planning/phases/05-ui-components/05-VALIDATION.md (Per-Task Verification Map rows for Plan E — ui/__init__.py re-exports, ui/ coverage gate, mypy strict clean, ruff clean)
    - .planning/phases/05-ui-components/05-RESEARCH.md (§Validation Architecture — full suite + coverage commands)
    - src/bensdorp1/ui/__init__.py (created in Task 1 — read the `__all__` list to write identity assertions)
    - src/bensdorp1/db/__init__.py (analog: simple re-export module — pattern for smoke tests, no dedicated test file exists for re-exports but Plan 05-05 adds one for ui to lock the public surface)
  </read_first>
  <behavior>
    `tests/test_ui/test_public_api.py` contains:

    - `test_public_api_complete`: import `bensdorp1.ui`; assert every name in the
      module's `__all__` list resolves via `getattr(bensdorp1.ui, name)` and is not None.

    - `test_public_api_identity_with_source_modules`: for each (public_name, source_module) pair, assert `getattr(bensdorp1.ui, public_name) is getattr(<source_module>, public_name)`. Verifies the re-export is by-reference (not a copy), so monkeypatch in tests works through either path. Example pairs:
        (`Severity`, `bensdorp1.ui.messages`)
        (`render_table`, `bensdorp1.ui.tables`)
        (`confirm_prompt`, `bensdorp1.ui.prompts`)
        (`feedback`, `bensdorp1.ui.progress`)
        (`format_price`, `bensdorp1.ui.styles`)
        (`print_empty_state`, `bensdorp1.ui.empty_states`)

    - `test_no_disallowed_imports_in_ui`: walk every `.py` file under `src/bensdorp1/ui/` and assert NONE of them contain a top-level import from `bensdorp1.db`, `bensdorp1.data`, `bensdorp1.strategy`, or `bensdorp1.commands`. Use `ast.parse` and walk `ast.ImportFrom` nodes. This locks the architectural invariant from D-08.

    - `test_record_console_fixture_works(record_console)`: receive the fixture, call `record_console.print("hello")`, assert `"hello"` in `record_console.export_text()`.

    Each test annotated `-> None`. Imports: stdlib `ast`, `pathlib.Path`; third-party `pytest`, `rich.console.Console`; first-party `import bensdorp1.ui` and the individual submodules for identity checks.
  </behavior>
  <action>
    Create `tests/test_ui/test_public_api.py` per `<behavior>`.

    Walk-imports test implementation:
    - `UI_DIR = Path("src/bensdorp1/ui")`.
    - For each `.py` file in `UI_DIR.rglob("*.py")`:
      - `tree = ast.parse(file.read_text(encoding="utf-8"))`.
      - For each `node in ast.walk(tree)`:
        - if `isinstance(node, ast.ImportFrom) and node.module is not None`:
          - assert `not node.module.startswith("bensdorp1.db")`, ditto data/strategy/commands.

    Then run the phase gate from `<verify>` below to confirm:
    - Full phase 5 test suite green.
    - Coverage >= 90% on `src/bensdorp1/ui/` + `src/bensdorp1/config.py`.
    - mypy strict clean across the phase 5 source surface.
    - ruff clean across the phase 5 source and test surface.
    - Full repo suite green (Phase 5 did not break Phases 1-4).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ui/test_public_api.py -x -q</automated>
    <automated>uv run pytest tests/test_ui/ -x -q</automated>
    <automated>uv run pytest tests/test_ui/ --cov=src/bensdorp1/ui --cov=src/bensdorp1/config --cov-report=term-missing --cov-fail-under=90</automated>
    <automated>uv run mypy src/bensdorp1/ui/ src/bensdorp1/config.py tests/test_ui/</automated>
    <automated>uv run ruff check src/bensdorp1/ui/ src/bensdorp1/config.py tests/test_ui/ tests/conftest.py</automated>
    <automated>uv run pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File: `tests/test_ui/test_public_api.py` defines `test_public_api_complete`, `test_public_api_identity_with_source_modules`, `test_no_disallowed_imports_in_ui`, `test_record_console_fixture_works`.
    - Test command: `uv run pytest tests/test_ui/test_public_api.py -x -q` exits 0.
    - Test command: `uv run pytest tests/test_ui/ -x -q` exits 0 (full phase 5 suite green).
    - Coverage gate: `uv run pytest tests/test_ui/ --cov=src/bensdorp1/ui --cov=src/bensdorp1/config --cov-fail-under=90` exits 0.
    - Type command: `uv run mypy src/bensdorp1/ui/ src/bensdorp1/config.py tests/test_ui/` exits 0.
    - Lint command: `uv run ruff check src/bensdorp1/ui/ src/bensdorp1/config.py tests/test_ui/ tests/conftest.py` reports 0 errors.
    - Full repo gate: `uv run pytest -x -q` exits 0 (Phase 5 changes do not break Phases 1-4).
    - Architectural invariant: `test_no_disallowed_imports_in_ui` passes — no `ui/*.py` file imports from db/data/strategy/commands.
  </acceptance_criteria>
  <done>Public API smoke + identity + import-policy tests green; coverage ≥ 90%; mypy strict + ruff clean; full repo suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ui/__init__.py` → submodules | All public surface flows through here; downstream commands rely on identity |
| test file walking source tree | `ast.parse` over source files; pure-read |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering | Future ui/*.py adds import from db/data/strategy/commands | mitigate | `test_no_disallowed_imports_in_ui` uses `ast.parse` to walk every `.py` in `src/bensdorp1/ui/`. Any disallowed `ImportFrom` causes test failure. CI gate enforces the architectural invariant on every PR. |
| T-05-15 | Tampering | Re-export changes break downstream commands silently | mitigate | `test_public_api_identity_with_source_modules` asserts each re-exported symbol IS the same object as the source module's export (`is` identity, not `==` equality). Any aliasing/wrapping that breaks monkeypatch flow is caught. |
| T-05-16 | Information Disclosure | Coverage report leaks source paths | accept | Coverage runs locally and in CI; paths are project-relative. No secrets traversed. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. RESEARCH.md confirms zero new dependencies. |
</threat_model>

<verification>
- Every public name from Plans 05-01..04 is importable from `bensdorp1.ui`.
- Re-exports are by-reference (identity preserved).
- No `ui/*.py` imports from disallowed subpackages (architectural invariant locked by test).
- `record_console` fixture works in the UI test suite.
- Coverage gate >= 90% on `src/bensdorp1/ui/` and `src/bensdorp1/config.py`.
- mypy strict clean across all Phase 5 source files.
- ruff clean across all Phase 5 source and test files.
- Full repo suite green — Phases 1-4 unaffected.
</verification>

<success_criteria>
- All six `<verify>` commands in Task 2 exit 0.
- `bensdorp1.ui.__all__` is the union of every public symbol from the 6 submodules, alphabetised, with no leading-underscore names.
- Phase 5 success criteria from ROADMAP §Phase 5 are demonstrably met:
  1. Severity prefix functions emit colored ANSI in color terminals and plain text in NO_COLOR (Plan 05-02).
  2. Table renderer produces minimalist output (Plan 05-03).
  3. Feedback thresholds trigger correctly at silent/spinner/bar/bar+ETA boundaries (Plan 05-04).
  4. Every timestamp via `format_timezone_pair` shows ET + user-local city (Plan 05-01).
  5. Numerical formatters produce spec strings for prices, percentages, P&L, volumes, days (Plan 05-01).
</success_criteria>

<output>
Create `.planning/phases/05-ui-components/05-05-SUMMARY.md` when done. Include:
- Final coverage percentage achieved.
- Confirmation that the architectural invariant test (`test_no_disallowed_imports_in_ui`) passes.
- Any deviations from the planned interfaces (should be none).
</output>

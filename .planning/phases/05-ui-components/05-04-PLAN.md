---
phase: 05-ui-components
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/bensdorp1/ui/progress.py
  - tests/test_ui/test_progress.py
autonomous: true
requirements:
  - UI-04
  - UI-06

must_haves:
  truths:
    - "`from bensdorp1.ui.progress import SpinnerContext, TrackContext, MultiStepContext, BlockBarColumn, THRESHOLD_SPINNER, THRESHOLD_PROGRESS, THRESHOLD_ETA` resolves"
    - "`THRESHOLD_SPINNER == 1.0`, `THRESHOLD_PROGRESS == 6.0`, `THRESHOLD_ETA == 30.0`"
    - "`TrackContext._build_renderable(elapsed=0.5)` returns a `rich.text.Text` whose plain string is `\"\"` (silent tier, <1s)"
    - "`TrackContext._build_renderable(elapsed=3.0)` returns a `rich.console.RenderableType` representing a spinner (NOT a Group with key/value lines)"
    - "`TrackContext._build_renderable(elapsed=10.0)` returns a `rich.console.Group` containing key/value lines `Progress`, `Current`, `Elapsed` (progress-bar tier, no Remaining)"
    - "`TrackContext._build_renderable(elapsed=45.0)` returns a `rich.console.Group` containing `Progress`, `Current`, `Elapsed`, `Remaining` (progress+ETA tier)"
    - "`BlockBarColumn(bar_width=20).render(task)` where task.completed=10 task.total=20 returns Text whose string contains exactly 10 `█` and 10 `░` chars (NO `━`, NO `╸`, NO `╺`)"
    - "`BlockBarColumn(bar_width=20).render(task)` where task.total is None or 0 returns Text containing 0 `█` and 20 `░`"
    - "`MultiStepContext(total=2).step(\"Fetch data\")` followed by exit prints `[1/2] Fetch data... done.` on a separate line AFTER the Live context closes (rule 6.23)"
    - "`SpinnerContext(\"Loading...\")` enters Live; when `time.monotonic` is mocked to return <1s on exit, no spinner text is rendered to the console; when mocked to ≥1s during the context, the spinner glyphs appear"
    - "`list(rich.spinner.SPINNERS[\"dots\"][\"frames\"]) == list(\"⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏\")` — guards against Rich version drift (rule 6.22)"
    - "`__exit__` on every context manager uses explicit `types.TracebackType | None` typing (NOT `*args: object`)"
  artifacts:
    - path: "src/bensdorp1/ui/progress.py"
      provides: "THRESHOLD_* constants, BlockBarColumn, SpinnerContext, TrackContext, MultiStepContext"
      contains: "class TrackContext"
    - path: "tests/test_ui/test_progress.py"
      provides: "spinner frames assertion, all 4 tiers, BlockBarColumn chars, multi_step done line, mypy-strict-compatible __exit__"
  key_links:
    - from: "src/bensdorp1/ui/progress.py"
      to: "src/bensdorp1/ui/styles.py"
      via: "import _console as _default_console"
      pattern: "from bensdorp1.ui.styles import"
    - from: "tests/test_ui/test_progress.py"
      to: "src/bensdorp1/ui/progress.py"
      via: "monkeypatch time.monotonic on module"
      pattern: "monkeypatch.setattr.*bensdorp1\\.ui\\.progress"
---

<objective>
Build the feedback-threshold context managers and the spec-compliant block-character
progress bar. This is the most complex Phase 5 module: 3 context managers, 4 time tiers,
and a custom `BlockBarColumn` because Rich's built-in `BarColumn` renders `━` instead of
spec-required `█`/`░`.

Per D-03 the three primitives are `feedback.spinner()`, `feedback.track()`, and
`feedback.multi_step()`. This plan exposes the underlying class implementations
(`SpinnerContext`, `TrackContext`, `MultiStepContext`); the public `feedback` namespace
is assembled in Plan 05-05 via `ui/__init__.py` re-exports.

Output:
- `src/bensdorp1/ui/progress.py` — `THRESHOLD_*` constants, `BlockBarColumn`,
  `SpinnerContext`, `TrackContext`, `MultiStepContext`.
- `tests/test_ui/test_progress.py` — spinner frame assertion, all 4 tiers via mocked
  `time.monotonic`, `BlockBarColumn` character assertions, multi-step `done.` line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ui-components/05-CONTEXT.md
@.planning/phases/05-ui-components/05-RESEARCH.md
@.planning/phases/05-ui-components/05-PATTERNS.md
@.planning/phases/05-ui-components/05-VALIDATION.md
@.planning/Bensdorp_1.md
@src/bensdorp1/ui/styles.py
@CLAUDE.md

<interfaces>
From src/bensdorp1/ui/styles.py (Plan 05-01):
  - `_console: Console`  — module singleton; progress.py imports as `_default_console`

From rich (verified in RESEARCH.md):
  - `Console`, `Group` from `rich.console`
  - `Live(console, refresh_per_second, transient)` from `rich.live` — context manager
  - `Spinner("dots", description)` from `rich.spinner` — frames match spec exactly
  - `SPINNERS` dict from `rich.spinner` — used for frame-assertion test
  - `ProgressColumn`, `Task` from `rich.progress` — base class for BlockBarColumn
  - `Text` from `rich.text`

Mypy-strict `__exit__` signature (REQUIRED — RESEARCH Finding #4, Pitfall #3):
  def __exit__(
      self,
      exc_type: type[BaseException] | None,
      exc_val: BaseException | None,
      exc_tb: types.TracebackType | None,
  ) -> None:
  # `*args: object` is FORBIDDEN — mypy strict fails.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ui/progress.py with thresholds, BlockBarColumn, SpinnerContext, TrackContext, MultiStepContext</name>
  <files>src/bensdorp1/ui/progress.py</files>
  <read_first>
    - .planning/phases/05-ui-components/05-CONTEXT.md (D-03 — three context managers, four tiers, `done.` line behavior; §specifics — spinner frames, progress bar chars, progress bar layout, screen-title separators)
    - .planning/phases/05-ui-components/05-RESEARCH.md (Findings #1, #2, #3 — verified spinner match, TrackContext pattern, BlockBarColumn; Open Question #2 — multi_step done line outside Live; §Common Pitfalls 1, 3, 5)
    - .planning/phases/05-ui-components/05-PATTERNS.md (§progress.py section — class-based context manager pattern; multi-step done-line pattern; critical __exit__ typing note)
    - .planning/Bensdorp_1.md §6.20 (feedback thresholds: silent<1s, spinner 1-6s, bar 6-30s, bar+ETA >30s), §6.21 (progress display layout — Progress/Current/Elapsed/Remaining keys), §6.22 (braille spinner frames), §6.23 (multi-step: phase header `[N/TOTAL]`, completed phases get `done.` appended), §6.30 (allowed exceptions: `█` and `░`)
    - src/bensdorp1/ui/styles.py (Plan 05-01 — import _console)
    - tests/test_db_engine.py (monkeypatch pattern analog)
    - CLAUDE.md (mypy strict; no `# type: ignore` without doc; ruff TC rules around Live/Group types)
  </read_first>
  <action>
    Create `src/bensdorp1/ui/progress.py`:

    Module docstring: "Feedback-threshold context managers per rule 6.20 and the
    block-character progress bar per rule 6.30 (D-03). Live rendering uses Rich.Live with
    a dynamically-updated Group. time.monotonic() is the time source; tests mock it."

    Imports (TC-aware — keep runtime types at runtime):
    - stdlib: `import time`, `import types`, `from collections.abc import Iterator`, `from contextlib import contextmanager`
    - third-party (runtime): `from rich.console import Console, Group, RenderableType`,
      `from rich.live import Live`, `from rich.progress import ProgressColumn, Task`,
      `from rich.spinner import Spinner`, `from rich.text import Text`
    - first-party: `from bensdorp1.ui.styles import _console as _default_console`

    Threshold constants (module-level):
    - `THRESHOLD_SPINNER: float = 1.0`
    - `THRESHOLD_PROGRESS: float = 6.0`
    - `THRESHOLD_ETA: float = 30.0`

    `class BlockBarColumn(ProgressColumn):` (RESEARCH Finding #3, rule 6.30):
    - `def __init__(self, bar_width: int = 20) -> None:` sets `self.bar_width = bar_width` then calls `super().__init__()`.
    - `def render(self, task: Task) -> Text:` body:
      `pct = (task.completed / task.total) if task.total else 0.0`;
      `filled = int(self.bar_width * pct)`;
      `empty = self.bar_width - filled`;
      `return Text("█" * filled + "░" * empty)`.

    `class SpinnerContext:` (D-03 spinner primitive, rule 6.22):
    - `__init__(self, description: str, *, console: Console | None = None) -> None`: store `description`, `_console`, `_live: Live | None = None`, `_start: float = 0.0`.
    - `__enter__(self) -> "SpinnerContext"`: set `self._start = time.monotonic()`;
      build `Live(self._console, refresh_per_second=10, transient=True)`; call `self._live.__enter__()`.
      Initial renderable is `Text("")` (silent until threshold crosses — Pitfall #5).
      Schedule a check: since SpinnerContext doesn't have an `advance` callback, register no callback — the Live context renders the current `_live` payload on its refresh tick. To swap from silent → spinner at the 1s threshold, the `SpinnerContext` exposes a public helper `tick()` (called by the caller in tight loops) OR uses `Live` auto-refresh. To keep the API simple per D-03 ("unknown-duration operations"), implement `tick()` that callers may invoke from inside the context to refresh the renderable based on elapsed time. If the caller does not call `tick()`, the spinner appears only after Live's internal refresh evaluates `_build_renderable`. To keep both paths working, store the renderable as an attribute `self._renderable` and override Live behavior by updating before `_live.refresh()` on every `tick()`. Simplest path: implement `tick()` that recomputes the renderable from elapsed time and calls `self._live.update(...)`.
    - `tick(self) -> None`: compute `elapsed = time.monotonic() - self._start`; if `elapsed < THRESHOLD_SPINNER` use `Text("")`, else use `Spinner("dots", self._description)`; call `self._live.update(<chosen renderable>)`.
    - `__exit__(self, exc_type: type[BaseException] | None, exc_val: BaseException | None, exc_tb: types.TracebackType | None) -> None`: if `self._live is not None: self._live.__exit__(exc_type, exc_val, exc_tb)`.

    `class TrackContext:` (D-03 track primitive, rules 6.20/6.21):
    - `__init__(self, description: str, total: int, *, console: Console | None = None) -> None`: store all four (`description`, `total`, `_console`, `_live: Live | None`, `_start: float = 0.0`, `_completed: int = 0`, `_current_label: str = ""`).
    - `advance(self, current_label: str = "") -> None`: `self._completed += 1`; `self._current_label = current_label`; `elapsed = time.monotonic() - self._start`; if `self._live is not None: self._live.update(self._build_renderable(elapsed))`.
    - `_build_renderable(self, elapsed: float) -> RenderableType`:
      - if `elapsed < THRESHOLD_SPINNER`: return `Text("")`  # silent
      - elif `elapsed < THRESHOLD_PROGRESS`: return `Spinner("dots", self._description)`
      - elif `elapsed < THRESHOLD_ETA`: return `self._build_progress_block(elapsed, with_eta=False)`
      - else: return `self._build_progress_block(elapsed, with_eta=True)`
    - `_build_progress_block(self, elapsed: float, with_eta: bool) -> Group`: build the spec 6.21 layout. Use `BlockBarColumn(bar_width=20)`-style logic inline (compute `pct = self._completed / self._total if self._total else 0.0`, `filled = int(20 * pct)`, `empty = 20 - filled`, `bar = "█" * filled + "░" * empty`). Compose 4 (or 3 without ETA) `Text` lines:
      - `Text(f"Progress:   {bar}  {self._completed}/{self._total}  ({int(pct*100)}%)")`
      - `Text(f"Current:    {self._current_label}")`
      - `Text(f"Elapsed:    {self._format_duration(elapsed)}")`
      - if `with_eta`: `Text(f"Remaining:  ~{self._format_duration(self._estimate_remaining(elapsed))}")`
      Return `Group(*lines)`.
    - `_format_duration(self, seconds: float) -> str`: helper that returns `f"{int(seconds // 60)}m {int(seconds) % 60}s"` (e.g., `"1m 42s"`). For <60s: `f"{int(seconds)}s"`.
    - `_estimate_remaining(self, elapsed: float) -> float`: `if self._completed == 0: return 0.0`; `rate = self._completed / elapsed`; `remaining_items = self._total - self._completed`; `return remaining_items / rate if rate > 0 else 0.0`.
    - `__enter__(self) -> "TrackContext"`: `self._start = time.monotonic()`; `con = self._console`; `self._live = Live(con, refresh_per_second=10, transient=True)`; `self._live.__enter__()`; return `self`.
    - `__exit__`: same typed signature as SpinnerContext; delegate to `self._live.__exit__(exc_type, exc_val, exc_tb)`.

    `class MultiStepContext:` (D-03 multi_step primitive, rule 6.23):
    - `__init__(self, total: int, *, console: Console | None = None) -> None`: store `total`, `_console = console if console is not None else _default_console`, `_current: int = 0`.
    - `__enter__` and `__exit__` (typed) — no Live owned at this level; each `step()` owns its own context.
    - `@contextmanager` `def step(self, description: str, total: int | None = None) -> Iterator[SpinnerContext | TrackContext]:` body:
      - `self._current += 1`
      - `con.print(Text(f"[{self._current}/{self._total}] {description}"))` — phase header
      - if `total is None`: build `inner = SpinnerContext(description, console=con)` else `inner = TrackContext(description, total, console=con)`
      - with `inner: yield inner`
      - AFTER inner exits (Live is transient and disappears — Open Question #2), `con.print(Text(f"[{self._current}/{self._total}] {description}... done."))` — this is the persistent done line per rule 6.23.

    Optional convenience: define a module-level `class _FeedbackNamespace:` providing
    `spinner`, `track`, `multi_step` as instance methods that construct the contexts.
    Then `feedback = _FeedbackNamespace()`. Plan 05-05 re-exports `feedback` from
    `ui/__init__.py`. This matches the D-03 description `feedback.spinner(...)`.

    Do NOT use `[bold]` or any decorative icons.
    Do NOT use `*args: object` on `__exit__` — RESEARCH Pitfall #3 and Finding #4.
    Do NOT use Rich's built-in `BarColumn()` for the visual — only `BlockBarColumn`.
    Do NOT import from `bensdorp1.db`, `bensdorp1.data`, `bensdorp1.strategy`, `bensdorp1.commands`.
  </action>
  <verify>
    <automated>uv run python -c "from bensdorp1.ui.progress import SpinnerContext, TrackContext, MultiStepContext, BlockBarColumn, THRESHOLD_SPINNER, THRESHOLD_PROGRESS, THRESHOLD_ETA, feedback; assert THRESHOLD_SPINNER == 1.0 and THRESHOLD_PROGRESS == 6.0 and THRESHOLD_ETA == 30.0; print('OK')"</automated>
    <automated>uv run mypy src/bensdorp1/ui/progress.py</automated>
    <automated>uv run ruff check src/bensdorp1/ui/progress.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/bensdorp1/ui/progress.py` contains `class BlockBarColumn(ProgressColumn):`, `class SpinnerContext:`, `class TrackContext:`, `class MultiStepContext:` (4 class definitions).
    - Source: `src/bensdorp1/ui/progress.py` contains `THRESHOLD_SPINNER: float = 1.0`, `THRESHOLD_PROGRESS: float = 6.0`, `THRESHOLD_ETA: float = 30.0`.
    - Source: `src/bensdorp1/ui/progress.py` contains `types.TracebackType | None` substring (correct __exit__ typing).
    - Source: `grep -v '^#' src/bensdorp1/ui/progress.py | grep -c '\*args: object'` equals `0` (Pitfall #3).
    - Source: `grep -v '^#' src/bensdorp1/ui/progress.py | grep -c 'BarColumn\\|━\\|╸\\|╺'` equals `0` (no Rich built-in bar; no box-drawing bar chars).
    - Source: `src/bensdorp1/ui/progress.py` contains literal characters `█` and `░`.
    - Source: `src/bensdorp1/ui/progress.py` contains `feedback` module-level binding (instance of namespace class).
    - Import smoke: `python -c "from bensdorp1.ui.progress import feedback; ctx = feedback.spinner('test'); assert ctx is not None"` succeeds.
    - Type command: `uv run mypy src/bensdorp1/ui/progress.py` exits 0.
    - Lint command: `uv run ruff check src/bensdorp1/ui/progress.py` reports 0 errors.
  </acceptance_criteria>
  <done>All 4 classes + 3 constants + `feedback` namespace defined; `__exit__` typed correctly; uses `█`/`░` only; mypy/ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test progress.py — spinner frames, 4 tiers via mocked time, BlockBarColumn, multi_step done line</name>
  <files>tests/test_ui/test_progress.py</files>
  <read_first>
    - .planning/phases/05-ui-components/05-RESEARCH.md (Finding #1 spinner frame test snippet; Finding #2 mock time.monotonic pattern; Finding #7 export_text gotcha)
    - .planning/phases/05-ui-components/05-PATTERNS.md (§monkeypatch test pattern; §Test Patterns section)
    - .planning/phases/05-ui-components/05-VALIDATION.md (Per-Task Verification Map — Plan D rows for exact test command names to mirror)
    - .planning/Bensdorp_1.md §6.20 (4 tiers), §6.22 (frames), §6.23 (done line)
    - src/bensdorp1/ui/progress.py (created in Task 1)
    - tests/test_db_engine.py lines 23-29 (monkeypatch.setattr pattern)
  </read_first>
  <behavior>
    Tests (mirror exact names from 05-VALIDATION.md per-task verification map):

    - `test_dots_spinner_frames_match_spec` — assert `list(rich.spinner.SPINNERS["dots"]["frames"]) == list("⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏")` (10 frames). Protects against Rich version drift.

    - `test_block_bar_chars` — instantiate `BlockBarColumn(bar_width=20)`; construct a mock-like `Task` via `unittest.mock.MagicMock` setting `task.completed = 10`, `task.total = 20`; call `column.render(task)`; assert resulting `Text.plain.count("█") == 10 and Text.plain.count("░") == 10` and `"━" not in Text.plain`.

    - `test_block_bar_chars_zero_total` — set `task.total = 0`; assert all `░`, no `█`.

    - `test_block_bar_chars_full` — set `task.completed = 20, task.total = 20`; assert all `█`, no `░`.

    - `test_track_silent_fast(monkeypatch)` — patch `time.monotonic` on the progress module so `elapsed = 0.5` after one advance. Build `TrackContext("Fetching", 100, console=Console(record=True, width=80))`; call `_build_renderable(0.5)` directly; assert the result is a `Text` with `plain == ""`.

    - `test_track_spinner_tier` — call `_build_renderable(3.0)`; assert result is a `Spinner` instance (NOT a `Group`).

    - `test_track_progress_tier` — call `_build_renderable(10.0)`; assert result is a `Group`; inspect its `renderables` and assert at least 3 `Text` items whose plain text starts with `"Progress:"`, `"Current:"`, `"Elapsed:"`. Assert NO `"Remaining:"` item present.

    - `test_track_eta_tier` — call `_build_renderable(45.0)`; assert result is a `Group`; assert `"Remaining:"` IS present.

    - `test_spinner_silent_fast(monkeypatch)` — monkeypatch `time.monotonic` so elapsed < 1s; build `SpinnerContext("test", console=record_console)`; enter; call `tick()`; assert console output contains no `"⠋"` or other braille frame characters (i.e., empty Text rendered).

    - `test_spinner_shows_braille(monkeypatch)` — monkeypatch `time.monotonic` so elapsed >= 1s on tick; build context; enter; tick; exit; assert `"test"` (description) substring in `export_text()` (Spinner description rendered). Actual braille frame depends on tick timing; assert description suffices for tier-activation check.

    - `test_multi_step_done_line(monkeypatch)` — build `MultiStepContext(total=2, console=record_console)`; use `with ctx: ` then `with ctx.step("Fetch data") as inner: pass` then `with ctx.step("Save data") as inner: pass`. Assert `export_text()` contains the EXACT substrings `"[1/2] Fetch data... done."` and `"[2/2] Save data... done."` (rule 6.23). Live is transient so intermediate spinner lines should not persist.

    - `test_track_advance_increments(monkeypatch)` — build TrackContext; enter; advance("AAPL"); advance("GOOGL"); assert `_completed == 2` and `_current_label == "GOOGL"`.

    - `test_feedback_namespace_factories()` — assert `feedback.spinner("x")` returns a `SpinnerContext`; `feedback.track("x", 10)` returns a `TrackContext`; `feedback.multi_step(2)` returns a `MultiStepContext`.

    Monkeypatch pattern for `time.monotonic`:
      `monkeypatch.setattr("bensdorp1.ui.progress.time.monotonic", lambda: 5.0)`
      OR import the `time` symbol from the module and patch its `monotonic` attribute. Use whichever cleanly intercepts the module's `time.monotonic()` calls.

    For tests requiring multiple distinct `time.monotonic` values (start vs check):
      Use a generator: `times = iter([0.0, 0.5])`; `monkeypatch.setattr("bensdorp1.ui.progress.time.monotonic", lambda: next(times))`.
  </behavior>
  <action>
    Create `tests/test_ui/test_progress.py` with the test functions enumerated in
    `<behavior>`. Imports: stdlib `import time`, `from unittest.mock import MagicMock`;
    third-party `import pytest`, `from rich.console import Console, Group`,
    `from rich.text import Text`, `from rich.spinner import Spinner, SPINNERS`;
    first-party `from bensdorp1.ui.progress import (SpinnerContext, TrackContext, MultiStepContext, BlockBarColumn, THRESHOLD_SPINNER, THRESHOLD_PROGRESS, THRESHOLD_ETA, feedback)`.

    Each test annotated `-> None`. Construct `Console(record=True, width=80)` inline (the
    `record_console` fixture lands in Plan 05-05). Where tier behavior depends on absolute
    time, prefer calling `_build_renderable(elapsed)` directly (deterministic, no Live
    refresh dependence) rather than driving the Live context end-to-end.

    Do NOT poll real time. All tests must run in <1 second total.
    Do NOT assert specific braille characters in spinner output beyond the frames-match-spec test (Rich animation timing is non-deterministic).
    Do NOT use sleep().
  </action>
  <verify>
    <automated>uv run pytest tests/test_ui/test_progress.py -x -q</automated>
    <automated>uv run mypy tests/test_ui/test_progress.py</automated>
    <automated>uv run ruff check tests/test_ui/test_progress.py</automated>
  </verify>
  <acceptance_criteria>
    - File: `tests/test_ui/test_progress.py` exists.
    - File contains the function names listed in 05-VALIDATION.md: `test_dots_spinner_frames_match_spec`, `test_spinner_silent_fast`, `test_spinner_shows_braille`, `test_track_silent_fast`, `test_track_spinner_tier`, `test_track_progress_tier`, `test_track_eta_tier`, `test_block_bar_chars`, `test_multi_step_done_line`.
    - Test command: `uv run pytest tests/test_ui/test_progress.py -x -q` exits 0.
    - Test command: `uv run pytest tests/test_ui/test_progress.py::test_dots_spinner_frames_match_spec -x` exits 0.
    - Test command: `uv run pytest tests/test_ui/test_progress.py::test_block_bar_chars -x` exits 0.
    - Test command: `uv run pytest tests/test_ui/test_progress.py::test_track_silent_fast tests/test_ui/test_progress.py::test_track_spinner_tier tests/test_ui/test_progress.py::test_track_progress_tier tests/test_ui/test_progress.py::test_track_eta_tier -x` exits 0.
    - Test command: `uv run pytest tests/test_ui/test_progress.py::test_multi_step_done_line -x` exits 0.
    - Runtime: full `tests/test_ui/test_progress.py` suite finishes in under 3 seconds (no real sleep).
    - Type command: `uv run mypy tests/test_ui/test_progress.py` exits 0.
    - Lint command: `uv run ruff check tests/test_ui/test_progress.py` reports 0 errors.
  </acceptance_criteria>
  <done>All 11 tests green; runtime <3s; mypy/ruff clean; covers all 4 tiers + BlockBarColumn + multi_step done line + spinner frame guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `time.monotonic()` → threshold logic | Wall-clock independent; safe but monkeypatched in tests |
| caller `description`/`current_label` strings → Rich Text | Treat as plain text; no markup interpretation |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | `current_label` from yfinance/symbol input rendered in progress block | mitigate | `_build_progress_block` constructs `Text(f"Current:    {self._current_label}")` — Rich `Text(...)` constructor disables markup parsing by default. No `console.print(self._current_label, markup=True)` anywhere. |
| T-05-12 | DoS | TrackContext misuse (advance never called) | accept | The progress widget refreshes via Live but only renders the current renderable; missing `advance()` keeps it silent. v1 is single-user CLI; misuse is the caller's bug, not a security boundary. |
| T-05-13 | Information Disclosure | Live output blanked on transient exit | accept | `transient=True` is deliberate per spec — the spinner/bar disappears on completion, replaced by a persistent `done.` line. This is the spec requirement, not a vulnerability. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. |
</threat_model>

<verification>
- All 4 feedback tiers activate at the correct elapsed-time boundary (verified via `_build_renderable(elapsed)` direct calls with mocked time).
- `BlockBarColumn.render()` uses ONLY `█` and `░` (rule 6.30); never `━`, `╸`, `╺`.
- `dots` spinner frames match the exact 10-character braille sequence (rule 6.22).
- `MultiStepContext` prints the persistent `done.` line per rule 6.23 after each Live context exits.
- `__exit__` typing uses explicit `types.TracebackType | None` — mypy strict passes.
- Tests run deterministically (no real sleep); full suite under 3 seconds.
</verification>

<success_criteria>
- `uv run pytest tests/test_ui/test_progress.py -x -q` exits 0.
- `uv run mypy src/bensdorp1/ui/progress.py tests/test_ui/test_progress.py` exits 0.
- `uv run ruff check src/bensdorp1/ui/progress.py tests/test_ui/test_progress.py` reports 0 errors.
- No `BarColumn`, `━`, `╸`, `╺`, `*args: object`, `time.sleep(` in either file.
</success_criteria>

<output>
Create `.planning/phases/05-ui-components/05-04-SUMMARY.md` when done.
</output>

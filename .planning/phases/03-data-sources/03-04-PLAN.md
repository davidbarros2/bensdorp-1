---
phase: 03-data-sources
plan: 04
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - src/bensdorp1/data/__init__.py
autonomous: true
requirements:
  - DATA-06
tags:
  - data-layer
  - public-api
  - phase-gate

must_haves:
  truths:
    - "src/bensdorp1/data/__init__.py re-exports the canonical Phase 3 public API: get_constituents, refresh_constituents, get_trading_days, is_trading_day, n_trading_days_ago, update_price_data, check_price_coverage"
    - "__all__ is a sorted alphabetic list of every public symbol (matching the db/__init__.py convention)"
    - "from bensdorp1.data import get_constituents, update_price_data, get_trading_days, check_price_coverage executes without ImportError or AttributeError"
    - "DATA-06 deferral is documented in __init__.py module docstring AND in this plan's must_haves — split detection is Phase 11 (Catch-Up Logic) responsibility"
    - "uv run pytest tests/ exits 0 (full suite green — all Phase 1/2/3 tests pass)"
    - "uv run mypy src/ exits 0 (mypy strict on entire src/ tree)"
    - "uv run ruff check src/ tests/ exits 0 (ruff clean across src/ and tests/)"
  artifacts:
    - path: "src/bensdorp1/data/__init__.py"
      provides: "Public surface for bensdorp1.data subpackage — 7 re-exported public symbols + __all__"
      contains: "__all__"
      min_lines: 13
  key_links:
    - from: "src/bensdorp1/data/__init__.py"
      to: "src/bensdorp1/data/calendar.py"
      via: "from bensdorp1.data.calendar import get_trading_days, is_trading_day, n_trading_days_ago"
      pattern: "from bensdorp1\\.data\\.calendar import"
    - from: "src/bensdorp1/data/__init__.py"
      to: "src/bensdorp1/data/constituents.py"
      via: "from bensdorp1.data.constituents import get_constituents, refresh_constituents"
      pattern: "from bensdorp1\\.data\\.constituents import"
    - from: "src/bensdorp1/data/__init__.py"
      to: "src/bensdorp1/data/prices.py"
      via: "from bensdorp1.data.prices import check_price_coverage, update_price_data"
      pattern: "from bensdorp1\\.data\\.prices import"
---

<objective>
Finalize the Phase 3 deliverable by replacing the placeholder `src/bensdorp1/data/__init__.py` from Plan 01 with the canonical public-API surface for the `bensdorp1.data` subpackage, then run a final integration verification across the entire src/ and tests/ tree. This plan closes Phase 3.

Purpose: Phase 6 (init command), Phase 7 (scan command), and Phase 10 (refresh command) will import their data-layer needs from `bensdorp1.data` directly — they should NOT need to know which submodule a function lives in. The `__init__.py` re-exports give consumers a stable single import surface. The final integration gate (full pytest + mypy + ruff on the whole repo) verifies that all four plans have produced a coherent Phase 3 with no cross-module type errors, no missing exports, and no regression in Phase 1/2 tests.

This plan also formally records the DATA-06 deferral. DATA-06 (split detection and automatic position adjustment) is assigned to Phase 11 (Catch-Up Logic) — see ROADMAP.md §Phase 11. Phase 3 covers DATA-01 through DATA-10 EXCEPT DATA-06; the deferral is documented in the new __init__.py module docstring and surfaced in this plan's metadata.

Output: 1 rewritten `src/bensdorp1/data/__init__.py` (replaces the Plan 01 placeholder) and a verification report confirming the full repo is green. No new modules; no new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-data-sources/03-CONTEXT.md
@.planning/phases/03-data-sources/03-RESEARCH.md
@.planning/phases/03-data-sources/03-PATTERNS.md
@src/bensdorp1/db/__init__.py
@src/bensdorp1/data/calendar.py
@src/bensdorp1/data/constituents.py
@src/bensdorp1/data/prices.py

<interfaces>
<!-- Key types and contracts: the canonical public Phase 3 API surface. -->
<!-- Final __init__.py must re-export these exact 7 symbols. -->

```python
# from bensdorp1.data.calendar
def get_trading_days(start: date, end: date) -> pd.DatetimeIndex: ...
def is_trading_day(dt: date) -> bool: ...
def n_trading_days_ago(n: int, reference: date | None = None) -> date: ...

# from bensdorp1.data.constituents
def get_constituents(engine: Engine) -> dict[str, str]: ...
def refresh_constituents(engine: Engine) -> None: ...

# from bensdorp1.data.prices
def update_price_data(
    engine: Engine,
    symbols: list[str],
    start: date | None = None,
    end: date | None = None,
) -> None: ...
def check_price_coverage(
    engine: Engine,
    required_days: int = 220,
) -> tuple[int, int]: ...
```

Reference template — from src/bensdorp1/db/__init__.py (existing, do not modify):

```python
"""Public surface of the bensdorp1.db subpackage."""

from bensdorp1.db.audit import AuditEventType, log_event
from bensdorp1.db.backup import create_backup
from bensdorp1.db.engine import get_engine, run_migrations

__all__ = [
    "AuditEventType",
    "create_backup",
    "get_engine",
    "log_event",
    "run_migrations",
]
```

Mirror this structure exactly — same import-grouping (one line per submodule, alphabetic), same sorted-alphabetic `__all__`, same one-line docstring style.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize data/__init__.py public API surface</name>
  <files>src/bensdorp1/data/__init__.py</files>
  <read_first>
    - src/bensdorp1/data/__init__.py (current state — Plan 01 placeholder; this task overwrites it)
    - src/bensdorp1/db/__init__.py (canonical template — mirror its structure exactly)
    - src/bensdorp1/data/calendar.py (verify the 3 public functions exist: get_trading_days, is_trading_day, n_trading_days_ago)
    - src/bensdorp1/data/constituents.py (verify the 2 public functions exist: get_constituents, refresh_constituents)
    - src/bensdorp1/data/prices.py (verify the 2 public functions exist: update_price_data, check_price_coverage)
    - .planning/phases/03-data-sources/03-PATTERNS.md (§`src/bensdorp1/data/__init__.py` section, lines 26-50 — exact template)
    - .planning/REQUIREMENTS.md (DATA-06 traceability row — confirms Phase 11 owns split detection)
    - .planning/ROADMAP.md (§Phase 11 Catch-Up Logic — confirms DATA-06 is in scope there)
  </read_first>
  <behavior>
    - `from bensdorp1.data import get_constituents, refresh_constituents, get_trading_days, is_trading_day, n_trading_days_ago, update_price_data, check_price_coverage` — all 7 imports succeed.
    - `bensdorp1.data.__all__` is a list of exactly 7 string elements in alphabetic order.
    - `dir(bensdorp1.data)` contains every name in `__all__` plus the submodule names (calendar, constituents, prices).
    - No private helpers (prefix `_`) are exported.
    - Module docstring documents that DATA-06 (split detection) is deferred to Phase 11.
  </behavior>
  <action>
    Overwrite `src/bensdorp1/data/__init__.py` with the final public API surface. The Plan 01 placeholder ("""Public surface of the bensdorp1.data subpackage. Final re-exports added in Plan 04.""") is replaced wholesale.

    File contents (mirror db/__init__.py structure exactly):

    1. Module docstring (lines 1-5 approximately):
       - Triple-quoted multi-line docstring.
       - Line 1: `"""Public surface of the bensdorp1.data subpackage."""` (one-line summary).
       - Optional second paragraph (recommended): a 1-2 line note that DATA-06 split detection is OUT OF SCOPE and is owned by Phase 11 (Catch-Up Logic). Use a triple-quoted block ending with `"""`.

    2. Three import lines (alphabetic by submodule name), one per submodule, each line listing the public symbols in alphabetic order:
       - `from bensdorp1.data.calendar import get_trading_days, is_trading_day, n_trading_days_ago`
       - `from bensdorp1.data.constituents import get_constituents, refresh_constituents`
       - `from bensdorp1.data.prices import check_price_coverage, update_price_data`

    3. `__all__` list — alphabetic, every string a public symbol from the imports above:
       - `check_price_coverage`
       - `get_constituents`
       - `get_trading_days`
       - `is_trading_day`
       - `n_trading_days_ago`
       - `refresh_constituents`
       - `update_price_data`

    Do NOT use `__all__` to re-export the submodules themselves (calendar, constituents, prices) — only the public callable symbols. Submodules are accessible via the dotted path (`bensdorp1.data.calendar`) without needing to be in `__all__`.

    Do NOT import any private helpers (anything starting with `_`). Do NOT re-export `AuditEventType` or anything from `bensdorp1.db` — that is `bensdorp1.db.__init__.py`'s responsibility; cross-package re-exports create confusion.

    Final length: approximately 13 lines (matching db/__init__.py — see line count of that file).

    After writing, run the verification commands in the next task. Do NOT proceed to run integration verification yet — that is Task 2.
  </action>
  <verify>
    <automated>python -c "from bensdorp1.data import check_price_coverage, get_constituents, get_trading_days, is_trading_day, n_trading_days_ago, refresh_constituents, update_price_data; import bensdorp1.data; assert bensdorp1.data.__all__ == sorted(bensdorp1.data.__all__), 'not sorted'; assert len(bensdorp1.data.__all__) == 7, f'expected 7, got {len(bensdorp1.data.__all__)}'; print('public API ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bensdorp1/data/__init__.py` exists with at least 13 lines.
    - File contains exact substring `from bensdorp1.data.calendar import get_trading_days, is_trading_day, n_trading_days_ago`.
    - File contains exact substring `from bensdorp1.data.constituents import get_constituents, refresh_constituents`.
    - File contains exact substring `from bensdorp1.data.prices import check_price_coverage, update_price_data`.
    - File contains the literal `__all__ = [`.
    - Python evaluation: `python -c "import bensdorp1.data as d; print(d.__all__)"` outputs a list containing exactly these 7 strings in sorted order: `['check_price_coverage', 'get_constituents', 'get_trading_days', 'is_trading_day', 'n_trading_days_ago', 'refresh_constituents', 'update_price_data']`.
    - `python -c "from bensdorp1.data import check_price_coverage, get_constituents, get_trading_days, is_trading_day, n_trading_days_ago, refresh_constituents, update_price_data; print('ok')"` prints `ok` and exits 0.
    - `grep -v '^#' src/bensdorp1/data/__init__.py | grep -E "import _[a-z]"` returns 0 matches (no private helpers re-exported).
    - File still contains a triple-quoted docstring referencing "DATA-06" and "Phase 11" (documenting the deferral) — verify via `grep -E "(DATA-06|Phase 11)" src/bensdorp1/data/__init__.py` returning >= 1 match.
    - `uv run mypy src/bensdorp1/data/__init__.py` exits 0.
    - `uv run ruff check src/bensdorp1/data/__init__.py` exits 0.
  </acceptance_criteria>
  <done>data/__init__.py re-exports the 7 canonical Phase 3 public symbols; __all__ is sorted-alphabetic; submodule imports group exactly as db/__init__.py does; DATA-06 deferral is recorded in the module docstring; mypy + ruff clean on the new file.</done>
</task>

<task type="auto">
  <name>Task 2: Full-repo integration verification (Phase 3 gate)</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md (§Phase 3 — confirm the 4 acceptance criteria for the phase as a whole: constituents fetched + cross-checked with discrepancy rules; 220-day yfinance history; period↔hyphen normalization in one place; retry with backoff + 95% threshold)
    - .planning/REQUIREMENTS.md (DATA-01 through DATA-10 — verify which are implemented in Phase 3 vs deferred to Phase 11)
    - .planning/phases/03-data-sources/03-01-PLAN.md (Plan 01 must_haves and acceptance — verify all met)
    - .planning/phases/03-data-sources/03-02-PLAN.md (Plan 02 must_haves and acceptance — verify all met)
    - .planning/phases/03-data-sources/03-03-PLAN.md (Plan 03 must_haves and acceptance — verify all met)
  </read_first>
  <behavior>
    - `uv run pytest tests/ -q` exits 0 with at least the following test counts: existing Phase 1/2 tests still passing (no regression), 9+ calendar tests passing, 14+ constituents tests passing, 17+ prices tests passing. Total ≥ existing baseline + 40 new tests.
    - `uv run mypy src/` exits 0 — mypy strict across the entire src/ tree, including data/ and db/ subpackages.
    - `uv run ruff check src/ tests/` exits 0 — ruff lint across all source and test files.
    - No test is skipped except the two Plan 01 scaffold placeholders (NOTE: those scaffolds were OVERWRITTEN in Plans 02 and 03; if any test is now `s` (skipped) in test_data_constituents.py or test_data_prices.py, that is a Plan 02/03 failure — investigate). The only legitimate skips would be in test files outside Phase 3 scope.
    - `python -c "from bensdorp1.data import *"` does not raise.
  </behavior>
  <action>
    Run the Phase 3 integration verification gate. This is a non-modifying task — it executes commands and confirms the entire phase is green before producing a SUMMARY. If any command fails, do NOT write a "success" SUMMARY — instead, record the failure in the SUMMARY for the developer to triage.

    Step 1 — Full test suite:
    - Run `uv run pytest tests/ -v --tb=short 2>&1 | tee .planning/phases/03-data-sources/03-04-pytest-output.log`.
    - Confirm exit code is 0.
    - Confirm the output line `N passed` shows a count ≥ (Phase-1+2-baseline) + 40 (new from Phase 3: ~9 calendar + ~14 constituents + ~17 prices).
    - Confirm no `F` (failures), no `E` (errors), and skipped count is 0 (or note any legitimate non-Phase-3 skips).
    - Confirm tests/test_data_prices.py runtime is under 5 seconds (sleep is properly mocked).
    - Confirm tests/test_data_constituents.py contains NO real network calls (no test takes longer than the local DB write would take — verify by inspecting per-test timings).

    Step 2 — mypy strict full src/:
    - Run `uv run mypy src/`.
    - Confirm exit code is 0.
    - Confirm output is `Success: no issues found in N source files` where N covers all .py files under src/bensdorp1.

    Step 3 — ruff lint full src/ + tests/:
    - Run `uv run ruff check src/ tests/`.
    - Confirm exit code is 0.
    - Confirm output is `All checks passed!` or equivalent zero-error result.

    Step 4 — Public API smoke test:
    - Run `python -c "from bensdorp1.data import get_constituents, refresh_constituents, get_trading_days, is_trading_day, n_trading_days_ago, update_price_data, check_price_coverage; print('all 7 imports ok')"`.
    - Confirm exit code is 0 and output contains `all 7 imports ok`.

    Step 5 — Requirement coverage audit:
    - Confirm DATA-01 is implemented (covered by Plan 02 — Wikipedia + Slickcharts fetch in constituents.py).
    - Confirm DATA-02 is implemented (covered by Plan 02 — discrepancy classification 0-3/4-10/11+).
    - Confirm DATA-03 is implemented (covered by Plan 03 — auto_adjust=True in every yf.download call).
    - Confirm DATA-04 is implemented (covered by Plan 03 — 220-day default range via DEFAULT_LOOKBACK_DAYS=350).
    - Confirm DATA-05 is implemented (covered by Plan 02 — 7-day TTL via CACHE_TTL_DAYS=7).
    - Confirm DATA-06 is DEFERRED to Phase 11 (verified by `grep -E "DATA-06|Phase 11" src/bensdorp1/data/__init__.py src/bensdorp1/data/prices.py` returning >= 2 matches — one in __init__.py docstring, one in prices.py docstring).
    - Confirm DATA-07 is implemented (covered by Plan 01 — calendar.py NYSE wrappers).
    - Confirm DATA-08 is implemented (covered by Plan 03 — _to_yfinance and _to_db helpers; period in DB, hyphen at yfinance call site).
    - Confirm DATA-09 is implemented (covered by Plan 03 — BACKOFF_DELAYS = [1.0, 2.0, 4.0]).
    - Confirm DATA-10 is implemented (covered by Plan 03 — check_price_coverage returns (covered, total)).

    Step 6 — Update REQUIREMENTS.md traceability:
    - Optional but recommended: append to the SUMMARY a checklist of DATA-01..DATA-10 with checkmarks for Plan 01-03 deliverables and an explicit note that DATA-06 is deferred to Phase 11. Do NOT modify REQUIREMENTS.md or ROADMAP.md in this task — STATE-tracker updates happen at phase completion via the GSD workflow.

    If ALL six steps succeed, this Phase 3 is GREEN and ready for `/gsd-verify-work`. Record the result in the SUMMARY (next).

    If ANY step fails, record the exact failure (command, exit code, last 20 lines of output) in the SUMMARY and stop. Do NOT proceed to GSD verify-work with red tests.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q &amp;&amp; uv run mypy src/ &amp;&amp; uv run ruff check src/ tests/ &amp;&amp; python -c "from bensdorp1.data import check_price_coverage, get_constituents, get_trading_days, is_trading_day, n_trading_days_ago, refresh_constituents, update_price_data; print('phase 3 green')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -q` exits 0; output contains `passed` and not `failed` and not `errored`.
    - `uv run mypy src/` exits 0; output contains `Success: no issues found`.
    - `uv run ruff check src/ tests/` exits 0; output contains `All checks passed` or is empty.
    - `python -c "from bensdorp1.data import check_price_coverage, get_constituents, get_trading_days, is_trading_day, n_trading_days_ago, refresh_constituents, update_price_data"` exits 0.
    - File `.planning/phases/03-data-sources/03-04-pytest-output.log` exists with at least 50 lines of test output (verifies the full suite ran, not just a subset).
    - `grep -E "(DATA-06|Phase 11)" src/bensdorp1/data/__init__.py src/bensdorp1/data/prices.py` returns at least 2 matches (deferral documented in both files).
    - SUMMARY file `.planning/phases/03-data-sources/03-04-SUMMARY.md` exists with a section "Phase 3 Requirement Coverage" listing DATA-01..DATA-10 statuses.
  </acceptance_criteria>
  <done>Full repo green: pytest exit 0, mypy strict exit 0, ruff exit 0, all 7 public API imports work, DATA-06 deferral confirmed in 2+ source files, Phase 3 requirement coverage audited (9 implemented + 1 deferred). Phase 3 is ready for `/gsd-verify-work`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repository | This plan only modifies `src/bensdorp1/data/__init__.py` (re-export shim) and runs verification commands. No external input is ingested. |
| previous plans → this plan | Plans 01/02/03 produced source and test files; this plan trusts their content but enforces the integration gate (mypy + ruff + pytest across the full src/) — any cross-module type incoherence surfaces here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-API-01 | Tampering | data/__init__.py re-exports a private helper by mistake | mitigate | `__all__` is the source of truth; verified by `grep -v '^#' src/bensdorp1/data/__init__.py | grep -E "import _[a-z]"` returning 0 in acceptance criteria. Python's `from X import *` would only honor `__all__` even if a private was imported. |
| T-03-API-02 | Information Disclosure | Test output log `03-04-pytest-output.log` contains full test names and assertion text | accept | Log is local to .planning/phases/; not pushed to a remote without explicit user action. Contains no secrets — only Python test output strings. The log can be committed to git for audit trail per project commit_docs convention. |
| T-03-API-03 | Denial of Service | Full test suite + mypy + ruff takes >10 minutes in CI | accept | Phase 3 tests use mocked yfinance and time.sleep (verified in Plan 03 acceptance); total verification under 30 seconds on any modern machine. mypy strict on a ~200-file repo completes in under 10 seconds. ruff is sub-second. |
| T-03-API-04 | Repudiation | DATA-06 deferral is recorded only in two docstrings — could be overlooked during Phase 11 planning | mitigate | This plan's must_haves explicitly state DATA-06 is deferred and lists Phase 11 as the owner. ROADMAP.md §Phase 11 already lists DATA-06 in its Requirements line. REQUIREMENTS.md Traceability table maps DATA-06 → Phase 11. Three independent records of the deferral. |
| T-03-API-SC | Tampering | No new package installs in this plan | mitigate (n/a) | No `uv add`, no `pip install`. All deps were already installed by Plan 01 or earlier phases. |
</threat_model>

<verification>
This plan IS the Phase 3 verification gate. Task 2 explicitly runs the full integration suite. If any of `uv run pytest tests/`, `uv run mypy src/`, or `uv run ruff check src/ tests/` returns non-zero, Phase 3 is RED and must be triaged before invoking `/gsd-verify-work`.

After this plan completes successfully, the developer should run:

```
/gsd-verify-work 03
```

to invoke the checker agent against the phase deliverables.
</verification>

<success_criteria>
- `src/bensdorp1/data/__init__.py` exports exactly the 7 canonical Phase 3 public symbols via a sorted-alphabetic `__all__`.
- Phase 1 + Phase 2 + Phase 3 tests all pass together (no regressions).
- mypy strict on the entire src/ tree is clean.
- ruff is clean across src/ and tests/.
- DATA-06 (split detection) is documented as deferred to Phase 11 in at least two source files (__init__.py docstring + prices.py docstring) AND in this plan's must_haves and threat model — three independent records.
- DATA-01..DATA-05, DATA-07..DATA-10 are all implemented and tested (9 of 10 Phase 3 requirements green; DATA-06 deferred).
- Test runtime for the full Phase 3 test files (calendar + constituents + prices) under 10 seconds combined (all yfinance and time.sleep mocked).
- Phase 3 is ready for `/gsd-verify-work` checker invocation.
</success_criteria>

<output>
Create `.planning/phases/03-data-sources/03-04-SUMMARY.md` documenting:

### Phase 3 Requirement Coverage

| Req | Status | Plan | Module |
|-----|--------|------|--------|
| DATA-01 | Implemented | 03-02 | constituents.py |
| DATA-02 | Implemented | 03-02 | constituents.py |
| DATA-03 | Implemented | 03-03 | prices.py |
| DATA-04 | Implemented | 03-03 | prices.py |
| DATA-05 | Implemented | 03-02 | constituents.py |
| DATA-06 | DEFERRED | Phase 11 | (Catch-Up Logic) |
| DATA-07 | Implemented | 03-01 | calendar.py |
| DATA-08 | Implemented | 03-03 | prices.py |
| DATA-09 | Implemented | 03-03 | prices.py |
| DATA-10 | Implemented | 03-03 | prices.py |

### Integration Verification Results
- pytest: PASS / FAIL with counts (collected, passed, skipped, failed, errored, runtime in seconds)
- mypy strict: PASS / FAIL with file count
- ruff: PASS / FAIL with error count
- Public API import smoke test: PASS / FAIL

### Phase 3 Acceptance (from ROADMAP.md §Phase 3)
1. "Constituents are fetched from Wikipedia and cross-checked against Slickcharts; discrepancy rules (0-3 silent, 4-10 warn, 11+ abort buy candidates) are enforced and logged" — IMPLEMENTED via Plan 02; verified by tests `test_refresh_silent_when_discrepancy_le_3`, `test_refresh_warn_when_discrepancy_4_to_10`, `test_refresh_abort_when_discrepancy_ge_11`.
2. "yfinance returns 220 trading days of adjusted-close price history for a constituent symbol; all arithmetic uses NYSE trading days exclusively" — IMPLEMENTED via Plans 01 and 03; verified by `test_220_days_range_default` and `test_n_trading_days_ago_220_does_not_raise`.
3. "Tickers are stored in period form (BRK.B) and converted to hyphen form (BRK-B) only at the yfinance call site" — IMPLEMENTED via Plan 03; verified by `test_ticker_normalization_period_stored_in_db` and `test_yfinance_called_with_hyphen_form`.
4. "A failed download retries with exponential backoff (1s, 2s, 4s); if fewer than 95% of constituents have price data, the scan is aborted" — IMPLEMENTED via Plan 03; verified by `test_failed_ticker_retried_with_backoff`, `test_failed_ticker_exhausts_retries_emits_audit`, `test_check_price_coverage_pass`, `test_check_price_coverage_fail`.

### Next Steps
- Run `/gsd-verify-work 03` to invoke the checker.
- Phase 4 (Strategy Logic) is unblocked and depends on Phase 3 outputs (price_daily + constituents_cache populated by Phase 6 init).
</output>

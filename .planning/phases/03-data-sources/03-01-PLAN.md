---
phase: 03-data-sources
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/bensdorp1/data/__init__.py
  - src/bensdorp1/data/calendar.py
  - tests/test_data_calendar.py
  - tests/test_data_constituents.py
  - tests/test_data_prices.py
autonomous: true
requirements:
  - DATA-07
tags:
  - data-layer
  - calendar
  - nyse
  - pyproject

must_haves:
  truths:
    - "pyproject.toml [dependency-groups] dev list contains lxml-stubs>=0.5.1"
    - "pyproject.toml has [[tool.mypy.overrides]] block for module = \"yfinance\" with ignore_missing_imports = true"
    - "pyproject.toml has [[tool.mypy.overrides]] block for module = \"pandas_market_calendars\" with ignore_missing_imports = true"
    - "src/bensdorp1/data/__init__.py exists (placeholder, no re-exports yet) — Plan 04 finalizes the public surface"
    - "src/bensdorp1/data/calendar.py exists with get_trading_days, is_trading_day, n_trading_days_ago functions"
    - "tests/test_data_calendar.py covers excludes_holidays, excludes_weekends, n_trading_days_ago, and is_trading_day branches"
    - "tests/test_data_constituents.py exists as an importable skeleton with stub-marker test stubs that Plan 02 fills in"
    - "tests/test_data_prices.py exists as an importable skeleton with stub-marker test stubs that Plan 03 fills in"
    - "uv run pytest tests/test_data_calendar.py exits 0"
    - "uv run mypy src/bensdorp1/data/calendar.py exits 0 (mypy strict)"
    - "uv run ruff check src/bensdorp1/data/ tests/test_data_calendar.py exits 0"
  artifacts:
    - path: "pyproject.toml"
      provides: "lxml-stubs dev dep + mypy overrides for yfinance and pandas_market_calendars"
      contains: "lxml-stubs"
    - path: "src/bensdorp1/data/__init__.py"
      provides: "Placeholder subpackage marker (final re-exports added in Plan 04)"
      min_lines: 1
    - path: "src/bensdorp1/data/calendar.py"
      provides: "NYSE calendar wrappers: get_trading_days, is_trading_day, n_trading_days_ago, _NYSE singleton"
      exports:
        - get_trading_days
        - is_trading_day
        - n_trading_days_ago
      contains: "import pandas_market_calendars as mcal"
    - path: "tests/test_data_calendar.py"
      provides: "Unit tests for DATA-07 — at least 4 tests covering holiday exclusion, weekend exclusion, n_trading_days_ago arithmetic, is_trading_day"
      min_lines: 30
    - path: "tests/test_data_constituents.py"
      provides: "Importable test scaffold for Plan 02 (DATA-01, DATA-02, DATA-05)"
      min_lines: 5
    - path: "tests/test_data_prices.py"
      provides: "Importable test scaffold for Plan 03 (DATA-03, DATA-04, DATA-08, DATA-09, DATA-10)"
      min_lines: 5
  key_links:
    - from: "src/bensdorp1/data/calendar.py"
      to: "pandas_market_calendars"
      via: "module-level _NYSE = mcal.get_calendar(\"NYSE\")"
      pattern: "_NYSE\\s*=\\s*mcal\\.get_calendar"
    - from: "pyproject.toml"
      to: "mypy strict on yfinance and pandas_market_calendars imports"
      via: "[[tool.mypy.overrides]] with ignore_missing_imports"
      pattern: "module = \"(yfinance|pandas_market_calendars)\""
---

<objective>
Establish Phase 3 foundations with three deliverables: (1) update pyproject.toml so the data layer can use yfinance, pandas_market_calendars, and lxml under mypy strict; (2) create the `bensdorp1.data` subpackage with a working `calendar.py` (NYSE trading-day wrappers — pure computation, no I/O); (3) lay down test-file scaffolds for the constituents and prices modules so Plans 02 and 03 can drop tests into stable file paths without renaming.

Purpose: `calendar.py` is the only Phase 3 module with no external I/O, so it ships first as the foundation. The pyproject.toml updates unblock mypy strict on the rest of the phase. The test scaffolds prevent two parallel plans from racing on file creation order and let the executor verify "the file exists" assertions before any test logic is written.

Output: 1 modified config file (`pyproject.toml`), 1 new module (`src/bensdorp1/data/calendar.py`), 1 new subpackage marker (`src/bensdorp1/data/__init__.py`), 3 new test files (`tests/test_data_calendar.py`, `tests/test_data_constituents.py`, `tests/test_data_prices.py`). DATA-07 is fully implemented and tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-data-sources/03-CONTEXT.md
@.planning/phases/03-data-sources/03-RESEARCH.md
@.planning/phases/03-data-sources/03-PATTERNS.md
@src/bensdorp1/db/__init__.py
@src/bensdorp1/db/engine.py
@src/bensdorp1/db/schema.py
@tests/conftest.py
@tests/test_db_engine.py
@pyproject.toml

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from pandas_market_calendars 5.3.2. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From pandas_market_calendars (already installed, verified in RESEARCH.md):

```python
import pandas_market_calendars as mcal  # untyped — covered by mypy override added this plan
nyse = mcal.get_calendar("NYSE")
days: pd.DatetimeIndex = nyse.valid_days(start_date="2025-01-01", end_date="2025-12-31")
# days.dtype == datetime64[us, UTC]
# valid_days() REQUIRES ISO date strings (use date.isoformat()), not date objects
```

Function signatures (final shapes for `src/bensdorp1/data/calendar.py`):

```python
def get_trading_days(start: date, end: date) -> pd.DatetimeIndex: ...
def is_trading_day(dt: date) -> bool: ...
def n_trading_days_ago(n: int, reference: date | None = None) -> date: ...
```

DB schema columns this phase touches later (for reference; not used in calendar.py):

```python
# From src/bensdorp1/db/schema.py
constituents_cache: Table  # columns: id, symbol (unique), company_name, fetched_at (DateTime tz=True)
price_daily: Table         # columns: id, symbol, trade_date (DateTime tz=True), close, volume
                           # unique index: ix_price_daily_symbol_date on (symbol, trade_date)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update pyproject.toml — lxml-stubs dev dep and mypy overrides</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current state — see lines 28-35 for [dependency-groups] dev list and lines 53-55 for the existing [[tool.mypy.overrides]] block)
    - CLAUDE.md (§Verified Library Versions, §mypy Strict Mode Configuration, §Ruff Configuration — confirms PEP 735 [dependency-groups] is the standard, not [tool.uv.dev-dependencies])
    - .planning/phases/03-data-sources/03-RESEARCH.md (§pyproject.toml Changes Required, lines 674-708 — exact target text for both edits)
    - .planning/phases/03-data-sources/03-PATTERNS.md (§pyproject.toml section — additive changes only; do NOT touch other tool config blocks)
  </read_first>
  <behavior>
    - mypy strict on a file importing `yfinance` produces zero errors (with new override).
    - mypy strict on a file importing `pandas_market_calendars` produces zero errors (with new override).
    - `uv sync` completes without errors after edit; `lxml-stubs` shows in `uv pip list` output.
    - The existing override `module = "bensdorp1.commands.*"` with `disallow_untyped_decorators = false` is preserved unchanged.
    - The existing `[project.dependencies]` list is preserved unchanged (lxml is already present at >=6.1.1).
  </behavior>
  <action>
    Make two additive edits to pyproject.toml (DATA-07 plumbing — no functional code yet).

    Edit 1 — extend `[dependency-groups]` dev list:
    - Locate the existing `[dependency-groups]` table (currently lines 28-35) containing pytest, pytest-cov, hypothesis, ruff, mypy.
    - Append a single new entry: `"lxml-stubs>=0.5.1",` as a new list element (keep existing entries unchanged).
    - Use PEP 735 syntax — do NOT add or rename to `[tool.uv.dev-dependencies]`.

    Edit 2 — append two new `[[tool.mypy.overrides]]` blocks AFTER the existing `bensdorp1.commands.*` block (currently lines 53-55):
    - First new block: `module = "yfinance"` with `ignore_missing_imports = true`.
    - Second new block: `module = "pandas_market_calendars"` with `ignore_missing_imports = true`.
    - Do NOT add overrides for `bs4`, `httpx`, or `lxml` — bs4 ships py.typed, httpx ships inline types, and lxml is covered by `lxml-stubs` in the dev group.
    - Do NOT modify `[tool.mypy]`, `[tool.ruff]`, `[tool.ruff.lint]`, `[tool.pytest.ini_options]`, `[build-system]`, or `[project.scripts]`.

    After saving, run `uv sync --group dev` to install `lxml-stubs` into the venv. If `uv sync` fails for unrelated reasons (CI auth, etc.), record the error but do not roll back the file edits — the file changes are correct.
  </action>
  <verify>
    <automated>uv run mypy --strict -c "import yfinance; import pandas_market_calendars; import lxml.etree" 2>&amp;1 | grep -v '^Success' ; uv pip list 2>&amp;1 | grep -i lxml-stubs</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains exact substring `"lxml-stubs>=0.5.1"` inside the existing `[dependency-groups].dev` list.
    - `pyproject.toml` contains the literal lines `[[tool.mypy.overrides]]` followed by `module = "yfinance"` followed by `ignore_missing_imports = true` (in that order, contiguous).
    - `pyproject.toml` contains the literal lines `[[tool.mypy.overrides]]` followed by `module = "pandas_market_calendars"` followed by `ignore_missing_imports = true` (in that order, contiguous).
    - `pyproject.toml` STILL contains the existing block `module = "bensdorp1.commands.*"` with `disallow_untyped_decorators = false` (preserved).
    - `pyproject.toml` does NOT contain a `[tool.uv.dev-dependencies]` table (PEP 735 only).
    - `uv pip list` output contains a line starting with `lxml-stubs`.
    - `uv run mypy --strict -c "import yfinance"` exits 0 (no missing-stub errors).
    - `uv run mypy --strict -c "import pandas_market_calendars"` exits 0.
  </acceptance_criteria>
  <done>pyproject.toml has lxml-stubs in dev group and mypy overrides for yfinance and pandas_market_calendars; uv sync succeeded; existing dependencies and override blocks untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create data subpackage marker and calendar.py + tests</name>
  <files>src/bensdorp1/data/__init__.py, src/bensdorp1/data/calendar.py, tests/test_data_calendar.py</files>
  <read_first>
    - src/bensdorp1/db/__init__.py (mirror its `__all__` sorted-list convention — Plan 04 will replicate this for the data package; this plan creates a minimal placeholder)
    - src/bensdorp1/db/engine.py (mirror its module-docstring style and `-> None` discipline)
    - tests/test_db_engine.py (mirror test file structure: docstring, `-> None` on every test, descriptive names)
    - tests/conftest.py (do NOT import db_engine fixture — calendar.py has no I/O)
    - .planning/phases/03-data-sources/03-RESEARCH.md (§Pattern 6 NYSE Calendar Wrappers, lines 322-362 — verified pmc 5.3.2 API; §Pitfall 4 confirms no pytz)
    - .planning/phases/03-data-sources/03-PATTERNS.md (§`src/bensdorp1/data/calendar.py` section, lines 54-129 — exact function bodies to use)
    - CLAUDE.md (§pandas-market-calendars v5 and pytz removal — affirms zoneinfo-only)
  </read_first>
  <behavior>
    - `get_trading_days(date(2025, 1, 1), date(2025, 1, 31))` returns a `pd.DatetimeIndex` with `dtype=datetime64[us, UTC]` and length 21 (Jan 2025 has 21 trading days; New Year's Day excluded).
    - `get_trading_days(date(2025, 1, 1), date(2025, 1, 1))` returns an empty or single-element DatetimeIndex (New Year's Day is a holiday, so empty).
    - `is_trading_day(date(2025, 1, 2))` returns `True` (Thursday, regular session).
    - `is_trading_day(date(2025, 1, 4))` returns `False` (Saturday).
    - `is_trading_day(date(2025, 1, 1))` returns `False` (New Year's Day holiday).
    - `n_trading_days_ago(1, reference=date(2025, 1, 3))` returns `date(2025, 1, 2)` (one trading day before Friday 1/3 is Thursday 1/2).
    - `n_trading_days_ago(5, reference=date(2025, 1, 10))` returns `date(2025, 1, 3)` (Friday Jan 10 minus 5 trading days back is Friday Jan 3, skipping weekend).
    - `n_trading_days_ago(220, reference=date(2025, 12, 31))` returns a date in mid-February 2025 (no exception raised; verifies the buffer `int(n * 1.5) + 30` is sufficient for the production use case in DATA-04).
    - `n_trading_days_ago(10000)` raises `ValueError("Not enough trading days in range for n=10000")`.
    - Calling `from bensdorp1.data import calendar` does not raise.
    - Calling `from bensdorp1.data.calendar import get_trading_days, is_trading_day, n_trading_days_ago` succeeds.
  </behavior>
  <action>
    Create three files. Code excerpts and full bodies are specified in PATTERNS.md §calendar.py and RESEARCH.md §Pattern 6 — copy those verbatim with the adjustments below.

    File 1: `src/bensdorp1/data/__init__.py` — placeholder subpackage marker only.
    - Single triple-quoted docstring: `"""Public surface of the bensdorp1.data subpackage. Final re-exports added in Plan 04."""`
    - No imports, no `__all__`. Plan 04 will overwrite this file with the full public API.
    - This minimal stub exists only so `from bensdorp1.data import calendar` resolves as a subpackage import in Plan 02/03 tests.

    File 2: `src/bensdorp1/data/calendar.py` — full implementation.
    - Module docstring per PATTERNS.md §calendar.py "Docstring pattern" (mention DATA-07, "Depends on: pandas_market_calendars>=5.3.2 (zoneinfo-based, no pytz)", "Used by: data/prices.py, commands/scan.py (Phase 7), commands/init.py (Phase 6)").
    - Imports: `from datetime import date, timedelta`, `import pandas as pd`, `import pandas_market_calendars as mcal` (no `# type: ignore` needed — mypy override added in Task 1 covers it).
    - Module-level singleton: `_NYSE = mcal.get_calendar("NYSE")` — no lock (read-only, no mutation).
    - Three public functions with exact signatures from `<interfaces>` block above and bodies from PATTERNS.md lines 105-129:
      - `get_trading_days(start: date, end: date) -> pd.DatetimeIndex` — calls `_NYSE.valid_days(start_date=start.isoformat(), end_date=end.isoformat())`.
      - `is_trading_day(dt: date) -> bool` — wraps `pd.Timestamp(dt).normalize().tz_localize("UTC")` and checks membership in `get_trading_days(dt, dt)`.
      - `n_trading_days_ago(n: int, reference: date | None = None) -> date` — uses `int(n * 1.5) + 30` day buffer, raises `ValueError(f"Not enough trading days in range for n={n}")` when buffer is insufficient.
    - Every function annotated `-> None` or with explicit return type (mypy strict requirement).
    - NO pytz imports anywhere (pmc v5 removed pytz; use zoneinfo or rely on UTC-by-default behavior of valid_days).

    File 3: `tests/test_data_calendar.py` — 6 minimum test functions covering DATA-07.
    - Module docstring naming DATA-07 and the analog `tests/test_db_engine.py`.
    - Imports: `from datetime import date`, `import pytest`, `from bensdorp1.data.calendar import get_trading_days, is_trading_day, n_trading_days_ago`.
    - Every test function annotated `-> None` and with a one-line docstring.
    - Required tests (function names exact):
      - `test_get_trading_days_excludes_new_years_day` — asserts `date(2025, 1, 1)` is NOT in `get_trading_days(date(2025, 1, 1), date(2025, 1, 5))` results.
      - `test_get_trading_days_excludes_weekends` — asserts `get_trading_days(date(2025, 1, 4), date(2025, 1, 5))` is empty (Saturday + Sunday).
      - `test_is_trading_day_true_for_thursday` — `is_trading_day(date(2025, 1, 2))` is True.
      - `test_is_trading_day_false_for_saturday` — `is_trading_day(date(2025, 1, 4))` is False.
      - `test_is_trading_day_false_for_holiday` — `is_trading_day(date(2025, 1, 1))` is False (New Year's Day).
      - `test_n_trading_days_ago_one_step` — `n_trading_days_ago(1, reference=date(2025, 1, 3))` equals `date(2025, 1, 2)`.
      - `test_n_trading_days_ago_skips_weekend` — `n_trading_days_ago(1, reference=date(2025, 1, 6))` equals `date(2025, 1, 3)` (Monday Jan 6 → Friday Jan 3, skipping weekend).
      - `test_n_trading_days_ago_220_does_not_raise` — `n_trading_days_ago(220, reference=date(2025, 12, 31))` returns a `date` instance (validates DATA-04 use case).
      - `test_n_trading_days_ago_raises_when_buffer_insufficient` — `pytest.raises(ValueError, match=r"Not enough trading days")` for `n_trading_days_ago(10000)`.
    - Tests are pure (no `db_engine` fixture, no mocks) — calendar.py has zero I/O.

    Run `uv run pytest tests/test_data_calendar.py -x -q` and confirm all 9 tests pass. Run `uv run mypy src/bensdorp1/data/calendar.py` and confirm zero errors. Run `uv run ruff check src/bensdorp1/data/calendar.py tests/test_data_calendar.py` and confirm zero errors.
  </action>
  <verify>
    <automated>uv run pytest tests/test_data_calendar.py -x -q &amp;&amp; uv run mypy src/bensdorp1/data/calendar.py &amp;&amp; uv run ruff check src/bensdorp1/data/calendar.py tests/test_data_calendar.py</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bensdorp1/data/__init__.py` exists and is a valid Python module (`python -c "import bensdorp1.data"` exits 0).
    - File `src/bensdorp1/data/calendar.py` exists; `grep -E "^def (get_trading_days|is_trading_day|n_trading_days_ago)" src/bensdorp1/data/calendar.py` matches all three function names.
    - `src/bensdorp1/data/calendar.py` contains the literal substring `_NYSE = mcal.get_calendar("NYSE")` exactly once at module scope.
    - `src/bensdorp1/data/calendar.py` contains NO occurrence of `import pytz` or `from pytz` (pmc v5 compatibility).
    - File `tests/test_data_calendar.py` exists with at least 9 `def test_` functions matching the names listed in the action block.
    - `uv run pytest tests/test_data_calendar.py -x -q` exits 0 with 9 passed.
    - `uv run mypy src/bensdorp1/data/calendar.py` exits 0 under mypy strict.
    - `uv run ruff check src/bensdorp1/data/calendar.py tests/test_data_calendar.py` exits 0.
    - `python -c "from bensdorp1.data.calendar import get_trading_days, is_trading_day, n_trading_days_ago; print('ok')"` prints `ok` and exits 0.
  </acceptance_criteria>
  <done>calendar.py exports get_trading_days, is_trading_day, n_trading_days_ago — all verified against pmc 5.3.2; 9 unit tests pass; mypy strict clean; ruff clean; DATA-07 fully satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Create test scaffolds for constituents and prices modules</name>
  <files>tests/test_data_constituents.py, tests/test_data_prices.py</files>
  <read_first>
    - tests/test_db_audit.py (mirror file header docstring style, imports order, and per-test docstring convention — this is the analog for test_data_constituents.py per PATTERNS.md)
    - tests/test_db_backup.py (mirror multi-assertion test structure — this is the analog for test_data_prices.py per PATTERNS.md)
    - .planning/phases/03-data-sources/03-PATTERNS.md (§`tests/test_data_constituents.py` and §`tests/test_data_prices.py` sections — exact import patterns)
    - .planning/phases/03-data-sources/03-RESEARCH.md (§Validation Architecture → Phase Requirements → Test Map — full list of test names Plan 02 and Plan 03 will use)
  </read_first>
  <behavior>
    - `uv run pytest tests/test_data_constituents.py -x -q` exits 0 with all tests marked as skipped (placeholder for Plan 02).
    - `uv run pytest tests/test_data_prices.py -x -q` exits 0 with all tests marked as skipped (placeholder for Plan 03).
    - Both files import cleanly without `ImportError` (do NOT import `bensdorp1.data.constituents` or `bensdorp1.data.prices` yet — those modules do not exist; the scaffold uses lazy imports inside test bodies or pytest.importorskip).
    - Plans 02 and 03 can replace the scaffolds in-place by overwriting these files; the filenames are stable from this point.
  </behavior>
  <action>
    Create two test-file scaffolds that exist only to claim filenames and provide module-level docstrings + import skeletons. Each test inside is marked with `pytest.skip(reason="Implemented in Plan NN")` so they show as SKIPPED, not as failures or empty files.

    File 1: `tests/test_data_constituents.py`
    - Module docstring: `"""Tests for DATA-01, DATA-02, DATA-05: constituent fetching, discrepancy check, 7-day cache. Implemented in Plan 02. This file is a scaffold created in Plan 01 to stabilize the test filename."""`.
    - Imports: `import pytest`. Do NOT import from `bensdorp1.data.constituents` yet (module does not exist).
    - Add a single placeholder test:
      - `def test_constituents_scaffold_implemented_in_plan_02() -> None:` with body `pytest.skip("Implemented in Plan 02 (DATA-01/02/05)")`.
    - One-line docstring on the test: `"""Placeholder — Plan 02 replaces this scaffold with the full DATA-01/02/05 test suite."""`.

    File 2: `tests/test_data_prices.py`
    - Module docstring: `"""Tests for DATA-03, DATA-04, DATA-08, DATA-09, DATA-10: price download, normalization, retry, coverage. Implemented in Plan 03. This file is a scaffold created in Plan 01 to stabilize the test filename."""`.
    - Imports: `import pytest`. Do NOT import from `bensdorp1.data.prices` yet.
    - Add a single placeholder test:
      - `def test_prices_scaffold_implemented_in_plan_03() -> None:` with body `pytest.skip("Implemented in Plan 03 (DATA-03/04/08/09/10)")`.
    - One-line docstring on the test: `"""Placeholder — Plan 03 replaces this scaffold with the full DATA-03/04/08/09/10 test suite."""`.

    Both files must pass `ruff check` and `mypy --strict` with zero errors. Both placeholder tests must show as `s` (skipped) in pytest output, not `F` (failed) or `E` (errored).
  </action>
  <verify>
    <automated>uv run pytest tests/test_data_constituents.py tests/test_data_prices.py -q 2>&amp;1 | grep -E "2 skipped|0 failed" &amp;&amp; uv run mypy tests/test_data_constituents.py tests/test_data_prices.py &amp;&amp; uv run ruff check tests/test_data_constituents.py tests/test_data_prices.py</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_data_constituents.py` exists and contains exactly one `def test_` function named `test_constituents_scaffold_implemented_in_plan_02`.
    - File `tests/test_data_prices.py` exists and contains exactly one `def test_` function named `test_prices_scaffold_implemented_in_plan_03`.
    - Neither scaffold file contains `from bensdorp1.data.constituents` or `from bensdorp1.data.prices` (the modules do not exist yet — would cause collection errors).
    - `uv run pytest tests/test_data_constituents.py tests/test_data_prices.py -q` output contains `2 skipped` (not failed, not errored).
    - `uv run mypy tests/test_data_constituents.py tests/test_data_prices.py` exits 0.
    - `uv run ruff check tests/test_data_constituents.py tests/test_data_prices.py` exits 0.
  </acceptance_criteria>
  <done>Both test scaffolds exist as placeholders showing 1 SKIP each; Plans 02 and 03 can overwrite them without renaming; ruff and mypy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → process | pyproject.toml read by uv/mypy/ruff at install and build time |
| process → process | mypy override applies to `import yfinance` and `import pandas_market_calendars` — must NOT relax type checking elsewhere |
| disk → process | test scaffolds are collected by pytest; placeholder skip prevents accidental execution of unfinished test bodies |

No untrusted input crosses any boundary in this plan: no HTTP calls, no DB writes, no user input. `calendar.py` consumes only `date` objects from Python's stdlib `datetime` module (trusted source).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | pyproject.toml mypy overrides scope | mitigate | Override `module = "yfinance"` and `module = "pandas_market_calendars"` use exact-module form (no wildcards); cannot accidentally relax checks on `bensdorp1.*` or other third-party packages. Verified by reading `[[tool.mypy.overrides]]` blocks after edit. |
| T-03-02 | Tampering | pyproject.toml [dependency-groups] | mitigate | Only an additive list append (`"lxml-stubs>=0.5.1"`); the existing 5 entries are preserved unchanged. Verified by `grep -c '">=' pyproject.toml` matching expected count. |
| T-03-03 | Denial of Service | `n_trading_days_ago(n)` with adversarially large `n` | mitigate | Function raises `ValueError` when `n` exceeds the buffer rather than fetching unbounded data from pmc; `pmc.valid_days` only reads a static internal holiday list, no network. Test `test_n_trading_days_ago_raises_when_buffer_insufficient` covers this. |
| T-03-04 | Information Disclosure | calendar.py log output | accept | calendar.py emits no logs and no audit events; the only output is `ValueError.args`, which contains the integer `n` (not sensitive). |
| T-03-SC | Tampering | npm/pip installs of `lxml-stubs` | mitigate | Package legitimacy verified in 03-RESEARCH.md §Package Legitimacy Audit (lxml-stubs: [OK] via slopcheck 0.6.1, 2026-05-23). No blocking checkpoint required (slopcheck verdict was OK). All other Phase 3 packages (yfinance, pandas-market-calendars, etc.) are already installed from Phase 1/2. |
</threat_model>

<verification>
After Tasks 1-3 complete, run the full Phase 1+2 test suite plus the new calendar tests to confirm nothing regressed:

```
uv run pytest tests/ -q
uv run mypy src/
uv run ruff check src/ tests/
```

All three must exit 0. Plan 01 is the only plan in Wave 1; Plans 02-04 in later waves depend on its outputs (pyproject.toml mypy config, data subpackage marker, calendar.py module, and the two test scaffolds).
</verification>

<success_criteria>
- pyproject.toml has lxml-stubs in `[dependency-groups]` dev list and `[[tool.mypy.overrides]]` blocks for yfinance and pandas_market_calendars.
- `src/bensdorp1/data/__init__.py` exists as a placeholder subpackage marker.
- `src/bensdorp1/data/calendar.py` implements `get_trading_days`, `is_trading_day`, `n_trading_days_ago` per RESEARCH.md Pattern 6.
- `tests/test_data_calendar.py` has at least 9 passing tests covering DATA-07 (holiday exclusion, weekend exclusion, n-trading-days arithmetic, value-error path).
- `tests/test_data_constituents.py` and `tests/test_data_prices.py` exist as scaffolds with 1 SKIP each (Plans 02/03 will overwrite).
- Full suite (`uv run pytest tests/ -q`, mypy strict, ruff) exits 0.
- DATA-07 is the only requirement fully completed in this plan; DATA-01/02/03/04/05/08/09/10 are deferred to Plans 02 and 03; DATA-06 is deferred to Phase 11 (catch-up logic) — see ROADMAP.md.
</success_criteria>

<output>
Create `.planning/phases/03-data-sources/03-01-SUMMARY.md` summarizing:
- pyproject.toml lines changed (line numbers before/after).
- Files created with line counts.
- Test pass count (expected: 9 passed for calendar, 2 skipped for scaffolds, all existing Phase 1/2 tests still passing).
- Any deviations from PATTERNS.md or RESEARCH.md (expected: none).
- Note that Plans 02-04 are unblocked and may run in waves 2, 3, 4 sequentially.
</output>

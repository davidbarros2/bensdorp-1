---
phase: 1
slug: project-skeleton-and-tooling
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-23
---

# Phase 1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.0.3 |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` — Wave 0 creates it |
| **Quick run command** | `uv run pytest tests/ -x -q` |
| **Full suite command** | `uv run pytest tests/ -v && uv run ruff check . && uv run ruff format --check . && uv run mypy src/` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/ -x -q`
- **After every plan wave:** Run `uv run pytest tests/ -v && uv run ruff check . && uv run ruff format --check . && uv run mypy src/`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 1-xx-01 | scaffold | 1 | CMD-17 | — | N/A | unit (CliRunner) | `uv run pytest tests/test_cli.py::test_help_root_shows_all_panels -x` | ❌ W0 | ⬜ pending |
| 1-xx-02 | scaffold | 1 | CMD-17 | — | N/A | unit (CliRunner) | `uv run pytest tests/test_cli.py::test_help_subcommand_shows_help -x` | ❌ W0 | ⬜ pending |
| 1-xx-03 | scaffold | 1 | CMD-17 | — | Command name validated before subprocess | unit (CliRunner) | `uv run pytest tests/test_cli.py::test_help_unknown_command_exits_nonzero -x` | ❌ W0 | ⬜ pending |
| 1-xx-04 | scaffold | 1 | D-05 | — | N/A | unit (CliRunner) | `uv run pytest tests/test_cli.py -k "test_stub" -x` | ❌ W0 | ⬜ pending |
| 1-xx-05 | scaffold | 1 | REPO-01 | — | N/A | existence | `uv run pytest tests/test_repo.py::test_license_exists -x` | ❌ W0 | ⬜ pending |
| 1-xx-06 | ci | 2 | REPO-07/TEST-06 | T-1-01 | pull_request_target used (not pull_request) | smoke (CI) | GitHub Actions green on push | ❌ W0 | ⬜ pending |
| 1-xx-07 | ci | 2 | REPO-03 | T-1-01 | pull_request_target prevents fork secret exfil | smoke (CI) | close-pr.yml triggers on PR open | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/__init__.py` — package marker
- [ ] `tests/test_cli.py` — CliRunner tests for all 17 stubs and help command (CMD-17)
- [ ] `tests/test_repo.py` — structural assertions (LICENSE exists, pyproject.toml has entry point)
- [ ] `pyproject.toml` — pytest + coverage + mypy + ruff configuration (Wave 0 creates this)
- [ ] `uv.lock` — generated by `uv lock` after pyproject.toml is written

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Branch protection on main (CI must pass for merge) | REPO-06 | GitHub repository settings — not configurable via committed files | After push: Settings → Branches → Add rule → require status checks: `test (ubuntu-latest)`, `test (windows-latest)` |
| Issues disabled; Discussions disabled; Wiki disabled | REPO-02 | GitHub repository settings — not configurable via committed files | After push: Settings → General → Features: uncheck Issues, Discussions, Wiki |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending

---
phase: 01-project-skeleton-and-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/bensdorp1/__init__.py
  - src/bensdorp1/_app.py
  - src/bensdorp1/cli.py
  - src/bensdorp1/commands/__init__.py
autonomous: true
requirements:
  - REPO-01
  - REPO-07
  - TEST-06

must_haves:
  truths:
    - "uv sync --locked --dev completes without errors after pyproject.toml is committed"
    - "bensdorp1.cli:app is importable as a Python entry point"
    - "The Typer app singleton exists at src/bensdorp1/_app.py with no circular import risk"
    - "Package version 0.1.0 is declared in both pyproject.toml and src/bensdorp1/__init__.py"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package manifest, build system, entry point, all dev tool configs"
      contains: "[build-system]"
    - path: "src/bensdorp1/__init__.py"
      provides: "Package marker and version string"
      contains: "__version__ = \"0.1.0\""
    - path: "src/bensdorp1/_app.py"
      provides: "Typer app singleton — imported by all command modules to avoid circular imports"
      exports: ["app"]
    - path: "src/bensdorp1/cli.py"
      provides: "Registration hub — imports all command modules for side-effect decoration"
      exports: ["app"]
    - path: "src/bensdorp1/commands/__init__.py"
      provides: "Package marker enabling bensdorp1.commands.X imports"
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "src/bensdorp1/cli.py"
      via: "entry point bensdorp1 = \"bensdorp1.cli:app\""
      pattern: "bensdorp1\\.cli:app"
    - from: "src/bensdorp1/commands/*.py (future)"
      to: "src/bensdorp1/_app.py"
      via: "from bensdorp1._app import app"
      pattern: "from bensdorp1\\._app import app"
---

<objective>
Create the package skeleton that makes bensdorp1 installable with uv: pyproject.toml (build system, dependencies, tool configs), the Typer app singleton in _app.py, the registration hub cli.py, and all __init__.py package markers.

Purpose: Every subsequent plan in this phase builds on this skeleton. Command modules import from _app.py; the entry point resolves through cli.py. Without this wave, nothing is importable.

Output: A pyproject.toml with correct uv_build config, PEP 735 dev deps, and all tool sections; src/bensdorp1/ package with _app.py (Typer singleton) and cli.py (registration stub awaiting Wave 2 imports); empty commands/__init__.py.
</objective>

<execution_context>
@C:/Users/david/AppData/Local/get-shit-done/workflows/execute-plan.md
@C:/Users/david/AppData/Local/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-skeleton-and-tooling/01-CONTEXT.md
@.planning/phases/01-project-skeleton-and-tooling/01-RESEARCH.md
@.planning/phases/01-project-skeleton-and-tooling/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pyproject.toml with uv_build, all dependencies, and tool configs</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/01-project-skeleton-and-tooling/01-PATTERNS.md — §pyproject.toml section: full canonical TOML including [build-system], [project], [project.scripts], [dependency-groups], [tool.ruff], [tool.ruff.lint], [tool.mypy], [[tool.mypy.overrides]], [tool.pytest.ini_options]
    - .planning/phases/01-project-skeleton-and-tooling/01-RESEARCH.md — §Pattern 1, §Pattern 5, §Pattern 6, §Common Pitfalls (uv.lock pitfall)
    - CLAUDE.md — §Verified Library Versions table, §pyproject.toml Structure for uv, §mypy Strict Mode Configuration, §Ruff Configuration
  </read_first>
  <action>
    Create pyproject.toml at the repo root with all required sections. Key values to use exactly:

    [build-system]: requires = ["uv_build>=0.7,<1.0"], build-backend = "uv_build"

    [project]: name = "bensdorp1", version = "0.1.0", description = "CLI for Bensdorp System #1 trend-following strategy", requires-python = ">=3.11"

    [project.dependencies]: typer>=0.21.1, rich>=14.0, sqlalchemy>=2.0.49,<2.1, pydantic>=2.13.4, yfinance>=1.3.0, pandas-market-calendars>=5.3.2, httpx>=0.28.1, beautifulsoup4>=4.14.3, pandas>=3.0.3, numpy>=2.0

    [project.scripts]: bensdorp1 = "bensdorp1.cli:app"

    [dependency-groups] dev group (NOT [tool.uv.dev-dependencies]): pytest>=8.3, pytest-cov>=7.1.0, hypothesis>=6.130, ruff>=0.15, mypy>=2.1.0

    [tool.ruff]: line-length = 88, src = ["src"]
    [tool.ruff.lint]: select = ["E", "F", "I", "UP", "B", "C4", "PT"]

    [tool.mypy]: python_version = "3.11", strict = true, show_error_codes = true
    [[tool.mypy.overrides]]: module = "bensdorp1.commands.*", disallow_untyped_decorators = false

    [tool.pytest.ini_options]: testpaths = ["tests"]

    After writing the file, run: uv lock
    This generates uv.lock and must complete without errors before proceeding.
  </action>
  <verify>
    <automated>uv lock --check 2>&1 || uv lock</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml exists at repo root
    - Contains [build-system] with requires = ["uv_build>=0.7,<1.0"] and build-backend = "uv_build"
    - Contains [project.scripts] with bensdorp1 = "bensdorp1.cli:app"
    - Contains [dependency-groups] section (NOT [tool.uv.dev-dependencies])
    - [[tool.mypy.overrides]] module = "bensdorp1.commands.*" with disallow_untyped_decorators = false
    - uv.lock file exists after running uv lock
    - uv lock --check exits 0 (lockfile is up to date)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create package skeleton — __init__.py files, _app.py singleton, cli.py stub</name>
  <files>
    src/bensdorp1/__init__.py,
    src/bensdorp1/_app.py,
    src/bensdorp1/cli.py,
    src/bensdorp1/commands/__init__.py
  </files>
  <read_first>
    - .planning/phases/01-project-skeleton-and-tooling/01-PATTERNS.md — §src/bensdorp1/__init__.py, §src/bensdorp1/_app.py, §src/bensdorp1/cli.py, §src/bensdorp1/commands/__init__.py sections
    - .planning/phases/01-project-skeleton-and-tooling/01-RESEARCH.md — §Common Pitfalls §Pitfall 1 (circular imports), §Pitfall 7 (missing __init__.py)
  </read_first>
  <action>
    Create four files. All four files in src/bensdorp1/ and src/bensdorp1/commands/ — create directories as needed.

    src/bensdorp1/__init__.py:
      Module docstring: 'bensdorp1 — Bensdorp System #1 trend-following CLI.'
      __version__ = "0.1.0"

    src/bensdorp1/_app.py:
      Import typer. Create app = typer.Typer(...) with: name="bensdorp1", help="Bensdorp System #1 — daily S&P 500 trend-following CLI.", rich_markup_mode="rich", no_args_is_help=True, pretty_exceptions_enable=False.
      This file has NO imports from the bensdorp1 package — only import typer.

    src/bensdorp1/cli.py:
      Import app from bensdorp1._app: from bensdorp1._app import app
      Add side-effect import stubs as comments showing the 17 command modules that Wave 2 will populate. The actual import lines will be added in Plan 02 when command modules exist. For now, cli.py contains only: the _app import and __all__ = ["app"].
      Do NOT add the 17 import lines yet — they would cause ModuleNotFoundError because the command modules do not exist until Plan 02.

    src/bensdorp1/commands/__init__.py:
      Empty file (just a newline). Must exist to make commands/ a Python package.

    After creating files, run: uv run python -c "from bensdorp1._app import app; print(app.info.name)"
    This must print "bensdorp1" and exit 0.
  </action>
  <verify>
    <automated>uv run python -c "from bensdorp1._app import app; print(app.info.name)"</automated>
  </verify>
  <acceptance_criteria>
    - src/bensdorp1/__init__.py contains __version__ = "0.1.0" and module docstring
    - src/bensdorp1/_app.py contains app = typer.Typer( with no_args_is_help=True and pretty_exceptions_enable=False
    - src/bensdorp1/_app.py imports only typer — no bensdorp1 package imports
    - src/bensdorp1/cli.py imports app from bensdorp1._app and declares __all__ = ["app"]
    - src/bensdorp1/commands/__init__.py exists (empty)
    - uv run python -c "from bensdorp1._app import app; print(app.info.name)" prints "bensdorp1" and exits 0
    - uv run python -c "from bensdorp1.cli import app" exits 0 (no ImportError)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → pyproject.toml | Dependencies declared here are fetched from PyPI; a tampered dependency could execute code at install time |
| uv.lock → CI | The lockfile pins exact hashes; if the lockfile is missing or modified, CI could pull different versions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | pyproject.toml dependencies | mitigate | All 11 packages verified via slopcheck 0.6.1 (RESEARCH.md Package Legitimacy Audit); all [OK] |
| T-01-02 | Tampering | uv.lock | mitigate | uv lock generates a hash-pinned lockfile; CI uses --locked flag (Plan 04) to enforce exact versions |
| T-01-03 | Information Disclosure | pretty_exceptions_enable=False | mitigate | Set False on Typer app — suppresses Python tracebacks visible to users; per _app.py pattern |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + Package Legitimacy Audit completed in RESEARCH.md — all packages [OK]; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
After both tasks complete:

1. uv lock --check exits 0
2. uv run python -c "from bensdorp1.cli import app; print('ok')" prints "ok" and exits 0
3. src/bensdorp1/_app.py contains no import from the bensdorp1 package (grep -c "from bensdorp1" src/bensdorp1/_app.py should return 0)
4. All four files exist: src/bensdorp1/__init__.py, src/bensdorp1/_app.py, src/bensdorp1/cli.py, src/bensdorp1/commands/__init__.py
</verification>

<success_criteria>
- pyproject.toml is valid TOML with correct build-system, entry point, and all tool configs
- uv.lock is generated and up to date
- bensdorp1._app.app is importable without error
- bensdorp1.cli.app is importable without error
- No circular import exists between _app.py and any other module
</success_criteria>

<output>
Create .planning/phases/01-project-skeleton-and-tooling/01-01-SUMMARY.md when done
</output>

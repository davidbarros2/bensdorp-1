---
phase: 10-system-commands
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bensdorp1/commands/restore.py
  - tests/test_commands/test_restore.py
autonomous: true
requirements:
  - CMD-02
tags:
  - cli
  - sqlite
  - backup-restore

must_haves:
  truths:
    - "Running `bensdorp1 restore <missing-path>` exits with code 1 and prints an error containing the resolved path; no DB writes occur."
    - "Running `bensdorp1 restore <path-to-non-sqlite-or-incomplete-schema-file>` exits with code 1 after the validation step and prints an error; no confirmation prompts are shown."
    - "Validation passes when PRAGMA integrity_check returns single row 'ok' AND all 8 expected tables (config, scans, positions, audit_log, scan_candidates, scan_exit_triggers, constituents_cache, price_daily) are present."
    - "Two confirmation prompts are shown in order: first 'Replace active database with this file?' then 'A pre-restore backup will be created first. This will overwrite your current database. Are you sure?'. Replying 'n' to either prompt exits 0 without creating a pre-restore backup or copying the file."
    - "Happy path: validates → shows file info → first confirm y → second confirm y → creates `bensdorp1-pre-restore-{timestamp}.db` in backups dir → copies PATH over active DB → logs RESTORE_PERFORMED with payload {restored_from, pre_restore_backup} → prints success message referencing `bensdorp1 status`."
    - "Secondary SQLAlchemy engine used for validation is always disposed before the file copy (Windows file-locking safety)."
  artifacts:
    - path: "src/bensdorp1/commands/restore.py"
      provides: "Real restore command implementation (replaces stub)"
      contains: "def restore("
    - path: "tests/test_commands/test_restore.py"
      provides: "CliRunner integration tests for restore command (5 scenarios)"
      contains: "def test_restore_"
  key_links:
    - from: "src/bensdorp1/commands/restore.py"
      to: "src/bensdorp1/db/backup.py + shutil"
      via: "inline shutil.copy2 for pre-restore backup with exact filename"
      pattern: "shutil\\.copy2"
    - from: "src/bensdorp1/commands/restore.py"
      to: "src/bensdorp1/db/audit.py"
      via: "log_event(engine, AuditEventType.RESTORE_PERFORMED, payload=...)"
      pattern: "AuditEventType.RESTORE_PERFORMED"
    - from: "src/bensdorp1/commands/restore.py"
      to: "secondary SQLAlchemy Engine"
      via: "create_engine(URL.create('sqlite+pysqlite', database=...)) + dispose"
      pattern: "create_engine|val_engine.dispose"
---

<objective>
Replace the `restore` command stub with a guarded DB-replacement implementation that validates the target backup file, requires two confirmation prompts, creates a pre-restore backup, copies the file, and logs `RESTORE_PERFORMED` to the new (restored) database per CONTEXT.md D-07 through D-12. Add CliRunner integration tests covering invalid path, schema-invalid, both first/second-confirm-no cases, and the full happy path.

Purpose: Closes CMD-02 — gives the user a safe path to recover from a corrupted or unwanted-state DB by restoring any prior backup, with two human-in-the-loop confirmations and an automatic pre-restore snapshot for "undo".

Output: Working `bensdorp1 restore PATH` command and test_restore.py covering the 5 named test cases in 10-RESEARCH.md "Phase Requirements → Test Map".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-system-commands/10-CONTEXT.md
@.planning/phases/10-system-commands/10-RESEARCH.md
@.planning/phases/10-system-commands/10-PATTERNS.md

<!-- Source-of-truth files (read these — analogs and dependencies) -->
@src/bensdorp1/commands/cash.py
@src/bensdorp1/commands/restore.py
@src/bensdorp1/db/__init__.py
@src/bensdorp1/db/audit.py
@src/bensdorp1/db/backup.py
@src/bensdorp1/db/engine.py
@src/bensdorp1/db/schema.py
@src/bensdorp1/ui/__init__.py
@src/bensdorp1/ui/prompts.py
@src/bensdorp1/config.py
@tests/test_commands/test_cash.py
@tests/conftest.py

<interfaces>
<!-- Key signatures the executor needs. Extracted from codebase. -->

From src/bensdorp1/db/__init__.py:
- AuditEventType (StrEnum) — includes RESTORE_PERFORMED = "restore_performed"
- log_event(engine, event_type, symbol=None, payload=None) -> None
- get_engine(path: Path | None = None) -> Engine
- run_migrations(engine) -> None
- create_backup(engine: Engine, backups_dir: Path) -> Path   # NOT used here — D-10 + Open Question 1 → use inline shutil.copy2 with exact pre-restore filename

From src/bensdorp1/db/engine.py (private build pattern for the secondary validation engine):
- URL.create("sqlite+pysqlite", database=str(path))
- create_engine(url)

From src/bensdorp1/ui/__init__.py:
- confirm_prompt(message: str, *, console: Console | None = None) -> bool   # raises KeyboardInterrupt on Ctrl+C
- print_error / print_info / print_success(title, *, data=None, body=None, impact=None, actions=None, console=None)
- render_kv_block(data, console)

From src/bensdorp1/config.py:
- DATA_DIR: Path  # base directory, default ~/bensdorp1

EXPECTED_TABLES (frozenset) — exact 8 names per D-07:
{"config", "scans", "positions", "audit_log", "scan_candidates", "scan_exit_triggers", "constituents_cache", "price_daily"}

App registration is already in cli.py (line 16); decorator `@app.command(rich_help_panel="Setup")` on `def restore() -> None` in the current stub. Add the PATH argument in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement restore command with validation, two confirmations, pre-restore backup, file copy, and audit log</name>
  <files>src/bensdorp1/commands/restore.py</files>
  <read_first>
    - src/bensdorp1/commands/restore.py (current stub — confirm `rich_help_panel="Setup"` and existing signature)
    - src/bensdorp1/commands/cash.py (canonical confirm-then-act + log_event pattern; KeyboardInterrupt handler lines 107-113)
    - src/bensdorp1/db/backup.py (filename convention `bensdorp1-{ts}.db`; the pre-restore backup uses `bensdorp1-pre-restore-{ts}.db` per spec §10 + D-10)
    - src/bensdorp1/db/engine.py (URL.create + create_engine pattern for secondary validation engine)
    - src/bensdorp1/db/audit.py (log_event signature; RESTORE_PERFORMED event type)
    - src/bensdorp1/db/schema.py (8 table names enumerated in EXPECTED_TABLES)
    - src/bensdorp1/ui/prompts.py (confirm_prompt re-raises KeyboardInterrupt; caller must wrap)
    - src/bensdorp1/config.py (DATA_DIR resolution)
    - .planning/phases/10-system-commands/10-CONTEXT.md (D-07 through D-12)
    - .planning/phases/10-system-commands/10-RESEARCH.md (Pitfalls 2, 4, 6, 7; Pattern 4 & 5; Open Question 1)
  </read_first>
  <behavior>
    - Path resolution: `path.resolve()`; if not exists → print_error + raise typer.Exit(code=1).
    - Validation (before any prompt): open secondary engine to the resolved path, run PRAGMA integrity_check (use fetchall, expect single row 'ok'), get table names via SQLAlchemy inspect, require EXPECTED_TABLES is subset of existing tables. Wrap in try/except/finally with `val_engine.dispose()` in finally. On any failure (including exception) → print_error + raise typer.Exit(code=1). No confirmation prompts shown.
    - File info kv block: resolved absolute path, file size (KB or MB depending on size), last modified (format_timezone_pair on mtime). Printed before first prompt.
    - First confirm: `confirm_prompt("Replace active database with this file?", console=console)`. Wrap in try/except KeyboardInterrupt to raise typer.Exit() from None. Reply 'n' → typer.Exit() (code 0). No pre-restore backup created, no copy.
    - Second confirm: `confirm_prompt("A pre-restore backup will be created first. This will overwrite your current database. Are you sure?", console=console)`. Same wrapping; reply 'n' → typer.Exit() (code 0). No pre-restore backup created, no copy.
    - Pre-restore backup: build timestamp `datetime.now(UTC).strftime("%Y%m%dT%H%M%S_%fZ")` to match backup.py's format; build `pre_restore_path = backups_dir / f"bensdorp1-pre-restore-{ts}.db"`. Ensure `backups_dir.mkdir(parents=True, exist_ok=True)`. Use `shutil.copy2(db_path, pre_restore_path)`. (Per RESEARCH.md Pattern 5 / Open Question 1 — exact spec naming over create_backup naming; safe in single-user CLI per A1.)
    - File copy: `shutil.copy2(resolved, db_path)` — overwrites the active DB.
    - Log RESTORE_PERFORMED: payload exactly `{"restored_from": str(resolved), "pre_restore_backup": str(pre_restore_path)}`. Logged to the cached engine (same file path — post-copy reads see the new content).
    - Success message: `print_success("Database restored. Run \`bensdorp1 status\` to verify.", console=console)`.
    - Error recovery: if the file copy raises an exception after the pre-restore backup is created, print an error including the pre-restore backup path so the user can recover, then re-raise (or raise typer.Exit(code=1)).
  </behavior>
  <action>
    Replace the current stub in `src/bensdorp1/commands/restore.py` with a real implementation.

    Imports (top of file):
    - stdlib: `import shutil`, `from datetime import UTC, datetime`, `from pathlib import Path`, `from typing import Any`.
    - Third-party: `import typer`, `from rich.console import Console`, `from sqlalchemy import inspect as sa_inspect`, `from sqlalchemy import text as sa_text`, `from sqlalchemy.engine import URL, create_engine`.
    - First-party: `from bensdorp1._app import app`, `from bensdorp1.config import DATA_DIR`, `from bensdorp1.db import AuditEventType, get_engine, log_event, run_migrations`, `from bensdorp1.ui import confirm_prompt, format_timezone_pair, print_error, print_success, render_kv_block`.

    Module-level constant:
    `EXPECTED_TABLES: frozenset[str] = frozenset({"config", "scans", "positions", "audit_log", "scan_candidates", "scan_exit_triggers", "constituents_cache", "price_daily"})`

    Keep decorator `@app.command(rich_help_panel="Setup")` and update the signature to take a Path argument:
    `def restore(path: Path = typer.Argument(..., help="Path to the backup .db file to restore."),) -> None:`

    Docstring on the function: `"""Replace the active database with a backup file."""`.

    Body sequence:

    1. **DB entry triad** (cash.py lines 50-53):
       `db_path = DATA_DIR / "data" / "bensdorp1.db"`, `engine = get_engine(db_path)`, `run_migrations(engine)`, `console = Console()`.

    2. **PATH resolution + existence**:
       `resolved = path.resolve()`. If `not resolved.exists()` or `not resolved.is_file()`: `print_error(f"File not found: {resolved}", console=console)` then `raise typer.Exit(code=1)`.

    3. **Validation via secondary engine** (Pattern 4):
       Build URL: `val_url = URL.create("sqlite+pysqlite", database=str(resolved))`. Create engine: `val_engine = create_engine(val_url)`. Wrap in `try/except Exception / finally`:
       - In try: `with val_engine.connect() as conn:` run `rows = conn.execute(sa_text("PRAGMA integrity_check")).fetchall()` then `integrity_ok = len(rows) == 1 and rows[0][0] == "ok"`. Then outside the conn context (or inside — either works): `inspector = sa_inspect(val_engine); existing_tables = frozenset(inspector.get_table_names()); schema_valid = EXPECTED_TABLES.issubset(existing_tables)`.
       - In except `Exception`: set `integrity_ok = False; schema_valid = False`.
       - In finally: `val_engine.dispose()` — CRITICAL on Windows (Pitfall 2).
       If `not integrity_ok` or `not schema_valid`: build a missing-tables list `sorted(EXPECTED_TABLES - existing_tables)` if available else empty; `print_error("Backup file failed schema validation.", data={"Path": str(resolved), "Integrity OK": str(integrity_ok), "Missing tables": ", ".join(missing) or "(could not inspect)"}, console=console)`. Raise `typer.Exit(code=1)`.

    4. **File info block** (D-08):
       Compute size: `size_bytes = resolved.stat().st_size`; if `size_bytes >= 1024 * 1024`: `size_str = f"{size_bytes / (1024*1024):.1f} MB"` else `f"{size_bytes / 1024:.1f} KB"`.
       Compute mtime as UTC datetime: `mtime = datetime.fromtimestamp(resolved.stat().st_mtime, tz=UTC)`.
       Print blank line, then `console.print(Text("Validating backup file..."), markup=False, highlight=False)` then blank line, then `render_kv_block({"Path": str(resolved), "Size": size_str, "Modified": format_timezone_pair(mtime)}, console)`. Print blank line for spacing before first prompt. (Use `from rich.text import Text` for the header line.)

    5. **First confirmation** (cash.py lines 107-113 pattern):
       ```
       try:
           confirmed = confirm_prompt("Replace active database with this file?", console=console)
       except KeyboardInterrupt:
           raise typer.Exit() from None
       if not confirmed:
           raise typer.Exit()
       ```

    6. **Second confirmation** — same pattern, different prompt text:
       `confirm_prompt("A pre-restore backup will be created first. This will overwrite your current database. Are you sure?", console=console)`. Same try/except/if-not pattern.

    7. **Pre-restore backup**:
       `backups_dir = DATA_DIR / "backups"`; `backups_dir.mkdir(parents=True, exist_ok=True)`. `ts = datetime.now(UTC).strftime("%Y%m%dT%H%M%S_%fZ")` (matches backup.py timestamp format). `pre_restore_path = backups_dir / f"bensdorp1-pre-restore-{ts}.db"`. `shutil.copy2(db_path, pre_restore_path)`.

    8. **File copy + error recovery**:
       Wrap in try/except: `shutil.copy2(resolved, db_path)`. On Exception: print_error including pre_restore_path location, then `raise typer.Exit(code=1) from <exc>`.

    9. **Log RESTORE_PERFORMED**:
       `payload: dict[str, Any] = {"restored_from": str(resolved), "pre_restore_backup": str(pre_restore_path)}`. `log_event(engine, AuditEventType.RESTORE_PERFORMED, payload=payload)`. The cached engine still points to the same file path; post-copy reads see the new content (no engine reset needed per RESEARCH.md anti-pattern note).

    10. **Success message**: `print_success("Database restored. Run \`bensdorp1 status\` to verify.", console=console)`.

    Type-hint correctness for mypy strict: `restore(path: Path = typer.Argument(...)) -> None`; `EXPECTED_TABLES: frozenset[str]` annotated explicitly. All DB queries parameterized via SQLAlchemy text or expression language.
  </action>
  <verify>
    <automated>cd C:/Users/david/Documents/Projetos/bensdorp-1 && uv run ruff check src/bensdorp1/commands/restore.py && uv run ruff format --check src/bensdorp1/commands/restore.py && uv run mypy --strict src/bensdorp1/commands/restore.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/bensdorp1/commands/restore.py` no longer contains the literal string "Not yet implemented.".
    - File contains `def restore(` with a `path: Path = typer.Argument(` parameter — verifiable via grep.
    - File contains `EXPECTED_TABLES` constant containing all 8 table name string literals: `"config"`, `"scans"`, `"positions"`, `"audit_log"`, `"scan_candidates"`, `"scan_exit_triggers"`, `"constituents_cache"`, `"price_daily"` — verifiable via `grep -c '"config"\\|"scans"\\|"positions"\\|"audit_log"\\|"scan_candidates"\\|"scan_exit_triggers"\\|"constituents_cache"\\|"price_daily"' src/bensdorp1/commands/restore.py` returns at least 8.
    - File contains `PRAGMA integrity_check` and uses `.fetchall()` on its result (Pitfall 4).
    - File contains `val_engine.dispose()` inside a `finally:` block — verifiable by reading the file for the literal sequence `finally:` followed by `val_engine.dispose()` within the same code block (Pitfall 2).
    - File contains TWO distinct `confirm_prompt(` calls with the literal substrings `"Replace active database"` and `"pre-restore backup will be created"` — verifiable via `grep -c 'confirm_prompt(' src/bensdorp1/commands/restore.py` returns 2.
    - File contains `shutil.copy2(` AT LEAST TWICE (once for pre-restore backup, once for the file copy) — verifiable via `grep -c 'shutil.copy2(' src/bensdorp1/commands/restore.py` returns at least 2.
    - File contains the literal string `bensdorp1-pre-restore-` (the filename prefix) — verifiable via grep.
    - File contains `AuditEventType.RESTORE_PERFORMED` and `log_event(` exactly once (single audit log to restored DB).
    - File contains `print_success(` with the literal substring `"Database restored."`.
    - `uv run ruff check src/bensdorp1/commands/restore.py` exits 0.
    - `uv run mypy --strict src/bensdorp1/commands/restore.py` exits 0.
  </acceptance_criteria>
  <done>
    restore.py replaces the stub with the full guarded flow; mypy strict + ruff clean; secondary engine disposal pattern in place for Windows safety.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CliRunner integration tests for restore (invalid path, schema-invalid, two confirm-no scenarios, happy path)</name>
  <files>tests/test_commands/test_restore.py</files>
  <read_first>
    - src/bensdorp1/commands/restore.py (the just-implemented file — confirm patch targets)
    - tests/test_commands/test_cash.py (analog — confirm flow with input="y\n" / "n\n"; MagicMock for log_event)
    - tests/conftest.py (db_engine fixture pattern; _build_engine + metadata.create_all reference for creating a valid backup test file)
    - src/bensdorp1/db/schema.py (metadata import path for creating a real valid SQLite backup file in a fixture)
    - src/bensdorp1/db/engine.py (`_build_engine` private helper — analog for the test fixture only)
    - .planning/phases/10-system-commands/10-RESEARCH.md (test map — 5 named test cases for CMD-02)
  </read_first>
  <behavior>
    - `test_restore_invalid_path`: pass a non-existent path; assert exit_code == 1, error contains "not found", no backup created, no log_event call.
    - `test_restore_schema_invalid`: pass a path to an empty file (no tables) OR a file that is not even SQLite; assert exit_code == 1, error mentions schema validation, no prompts in output, no backup created, no log_event call.
    - `test_restore_first_confirm_no`: pass a valid backup file (fixture creates one with all 8 tables). Input "n\n". Assert exit_code == 0, no shutil.copy2 happened to db_path, no log_event call. Use `mock_log = MagicMock()` and assert `mock_log.assert_not_called()`. The pre-restore backup must NOT exist after the test.
    - `test_restore_second_confirm_no`: same valid backup. Input "y\nn\n". Assert exit_code == 0, no log_event call, no pre-restore backup file.
    - `test_restore_full_flow`: same valid backup. Input "y\ny\n". Assert exit_code == 0, output contains "Database restored.", `mock_log` called once with `AuditEventType.RESTORE_PERFORMED` and a payload dict containing both `restored_from` and `pre_restore_backup` keys. Verify the pre-restore backup file exists in `tmp_path / "backups"` with name matching `bensdorp1-pre-restore-*.db`.
  </behavior>
  <action>
    Create `tests/test_commands/test_restore.py` modeled on `tests/test_commands/test_cash.py`.

    File header: module docstring `"""Tests for commands/restore.py — CMD-02 scenarios."""`.

    Imports (PEP 8 order):
    - stdlib: `from pathlib import Path`, `from unittest.mock import MagicMock, patch`.
    - Third-party: `import pytest`, `from sqlalchemy.engine import Engine`, `from typer.testing import CliRunner`.
    - First-party: `from bensdorp1.cli import app`, `from bensdorp1.db.audit import AuditEventType`, `from bensdorp1.db.schema import metadata`.

    Module-level: `runner = CliRunner()`.

    **Fixture — valid_backup_db**:
    ```
    @pytest.fixture
    def valid_backup_db(tmp_path: Path) -> Path:
        """Create a SQLite file with all 8 expected tables for restore happy-path tests."""
        from sqlalchemy.engine import URL, create_engine
        backup_path = tmp_path / "bensdorp1-test-backup.db"
        url = URL.create("sqlite+pysqlite", database=str(backup_path))
        engine = create_engine(url)
        try:
            metadata.create_all(engine)
        finally:
            engine.dispose()  # CRITICAL on Windows
        return backup_path
    ```

    **Helper — seed active DB file in tmp_path** (so shutil.copy2(db_path, pre_restore_path) has a source):
    Either rely on the `db_engine` fixture (which creates `tmp_path / "test.db"`) and patch `DATA_DIR` so that `DATA_DIR / "data" / "bensdorp1.db"` resolves into tmp_path — but the cash.py test pattern just patches `bensdorp1.commands.cash.get_engine` to return `db_engine` directly, bypassing the path computation. For restore.py the COPY uses `db_path = DATA_DIR / "data" / "bensdorp1.db"` as the *source* (pre-restore backup) and as the *destination* (final copy from PATH). We need this file to actually exist. Approach: patch DATA_DIR to `tmp_path`, then in each happy-path test create `(tmp_path / "data").mkdir(parents=True, exist_ok=True)` and `(tmp_path / "data" / "bensdorp1.db").write_bytes(b"placeholder active db content")`. This is sufficient because shutil.copy2 only needs a source file to read.
    Also patch `get_engine` to return `db_engine` so the audit log goes to the in-memory test DB.

    Common patch block for ALL tests:
    ```
    patch("bensdorp1.commands.restore.DATA_DIR", tmp_path),
    patch("bensdorp1.commands.restore.get_engine", return_value=db_engine),
    patch("bensdorp1.commands.restore.run_migrations"),
    patch("bensdorp1.commands.restore.log_event", mock_log),
    ```

    **Test 1 — test_restore_invalid_path(tmp_path: Path, db_engine: Engine) -> None**:
    - Build a path to a non-existent file: `missing = tmp_path / "does_not_exist.db"`.
    - `mock_log = MagicMock()`.
    - Run in standard patch block: `result = runner.invoke(app, ["restore", str(missing)])`.
    - Assert `result.exit_code == 1`.
    - Assert `"not found" in result.output.lower()` OR `"File not found" in result.output`.
    - Assert `mock_log.assert_not_called()`.

    **Test 2 — test_restore_schema_invalid(tmp_path: Path, db_engine: Engine) -> None**:
    - Create an empty file: `bogus = tmp_path / "empty.db"; bogus.write_bytes(b"")`.
    - `mock_log = MagicMock()`.
    - Run in standard patch block: `result = runner.invoke(app, ["restore", str(bogus)])`.
    - Assert `result.exit_code == 1`.
    - Assert `"schema validation" in result.output.lower()` OR a substring of the print_error title.
    - Assert `"Replace active database" NOT in result.output` (no prompt shown — schema validation precedes prompts).
    - Assert `mock_log.assert_not_called()`.

    **Test 3 — test_restore_first_confirm_no(valid_backup_db: Path, db_engine: Engine, tmp_path: Path) -> None**:
    - Seed active DB placeholder: `(tmp_path / "data").mkdir(parents=True, exist_ok=True); (tmp_path / "data" / "bensdorp1.db").write_bytes(b"placeholder")`.
    - `mock_log = MagicMock()`.
    - Run in standard patch block: `result = runner.invoke(app, ["restore", str(valid_backup_db)], input="n\n")`.
    - Assert `result.exit_code == 0`.
    - Assert `mock_log.assert_not_called()`.
    - Assert `not (tmp_path / "backups").exists() or len(list((tmp_path / "backups").glob("bensdorp1-pre-restore-*.db"))) == 0`.
    - Assert that the active DB content is unchanged: `(tmp_path / "data" / "bensdorp1.db").read_bytes() == b"placeholder"`.

    **Test 4 — test_restore_second_confirm_no(valid_backup_db: Path, db_engine: Engine, tmp_path: Path) -> None**:
    - Same setup as Test 3.
    - Input: `"y\nn\n"`.
    - Assert `result.exit_code == 0`.
    - Assert `mock_log.assert_not_called()`.
    - Assert no `bensdorp1-pre-restore-*.db` file exists in `tmp_path / "backups"`.
    - Assert active DB content unchanged.

    **Test 5 — test_restore_full_flow(valid_backup_db: Path, db_engine: Engine, tmp_path: Path) -> None**:
    - Seed active DB placeholder.
    - Input: `"y\ny\n"`.
    - `mock_log = MagicMock()`.
    - Run in standard patch block.
    - Assert `result.exit_code == 0`.
    - Assert `"Database restored." in result.output`.
    - Assert pre-restore backup was created: `pre_restore_files = list((tmp_path / "backups").glob("bensdorp1-pre-restore-*.db"))` and `len(pre_restore_files) == 1`.
    - Assert active DB was overwritten with valid_backup_db content (sizes equal or bytes equal): `(tmp_path / "data" / "bensdorp1.db").read_bytes() == valid_backup_db.read_bytes()`.
    - Assert `mock_log.assert_called_once()` and that its call args include `AuditEventType.RESTORE_PERFORMED` and a payload kwarg whose value is a dict with both `"restored_from"` and `"pre_restore_backup"` keys.

    All tests use `db_engine` from conftest.py for the log target engine. All tests use absolute paths via `str(path)` when passing to CliRunner.

    On Windows, ensure all engines are disposed before tmp_path cleanup (db_engine fixture handles its engine; valid_backup_db fixture disposes inside `finally`).
  </action>
  <verify>
    <automated>cd C:/Users/david/Documents/Projetos/bensdorp-1 && uv run pytest tests/test_commands/test_restore.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_commands/test_restore.py` exists and contains exactly these 5 test function names: `test_restore_invalid_path`, `test_restore_schema_invalid`, `test_restore_first_confirm_no`, `test_restore_second_confirm_no`, `test_restore_full_flow` — verifiable via `grep -E "^def test_restore_" tests/test_commands/test_restore.py | wc -l` returns 5.
    - File contains a `valid_backup_db` pytest fixture (`@pytest.fixture` decorator on a function named `valid_backup_db`) that uses `metadata.create_all` and disposes the engine in a `finally:` block.
    - File contains `runner = CliRunner()` at module level.
    - File patches `bensdorp1.commands.restore.DATA_DIR`, `bensdorp1.commands.restore.get_engine`, `bensdorp1.commands.restore.run_migrations`, `bensdorp1.commands.restore.log_event` (the last via MagicMock).
    - File contains `input="y\ny\n"` (happy path), `input="n\n"` (first-confirm-no), and `input="y\nn\n"` (second-confirm-no) — verifiable via grep for each literal string.
    - `uv run pytest tests/test_commands/test_restore.py -x` exits 0 with all 5 tests passing.
    - `uv run ruff check tests/test_commands/test_restore.py` exits 0.
    - `uv run mypy --strict tests/test_commands/test_restore.py` exits 0.
  </acceptance_criteria>
  <done>
    All 5 named restore tests pass; mypy strict + ruff clean; tests run cleanly on Windows (no [WinError 32] file-locking failures in tmp_path cleanup).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied PATH argument | Untrusted filesystem path; could be traversal target, missing file, non-SQLite file, or crafted SQLite |
| Backup file content (SQLite) | Untrusted DB content; replaces the active state |
| Filesystem write to active DB path | Single point of irreversible state replacement (with pre-restore undo) |
| Secondary SQLAlchemy engine open on PATH | Holds Windows file lock until disposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | Crafted SQLite file passes integrity_check + has all 8 tables but contains malicious row data | accept | Single-user local CLI; user owns their backups. Schema validation is structural only. Acceptable per ASVS L1 for personal tool (Research §Security Domain). |
| T-10-03-02 | Tampering | Path traversal via PATH (e.g. `../../../etc/passwd`) used to read a non-SQLite file | mitigate | `Path(PATH).resolve()` followed by `is_file()` check, then SQLAlchemy URL.create (not string interpolation into SQL) prevents injection. Integrity check on a non-SQLite file raises an exception → caught in `except Exception:` → schema_valid=False → typer.Exit(code=1). |
| T-10-03-03 | Denial of Service | Windows file-locking: shutil.copy2 over active DB raises [WinError 32] if secondary engine not disposed | mitigate | `val_engine.dispose()` in `finally:` block before any file copy (Pitfall 2). Acceptance criterion verifies the `finally:` + `dispose` pattern in the source. |
| T-10-03-04 | Denial of Service | File copy fails after pre-restore backup created → user left in unknown state | mitigate | Try/except around the file copy: on failure, print_error including the pre_restore_path so user can manually recover by copying it back. Raise typer.Exit(code=1). |
| T-10-03-05 | Repudiation | Restore action not logged | mitigate | log_event(engine, AuditEventType.RESTORE_PERFORMED, payload={restored_from, pre_restore_backup}) written to the restored DB (D-11). Acceptance criterion verifies the audit call in tests. |
| T-10-03-06 | Information Disclosure | Pre-restore backup file may contain sensitive trade history readable by anyone with filesystem access | accept | Single-user local CLI; same trust model as the active DB. No new disclosure. |
| T-10-03-07 | Elevation of Privilege | Rich markup injection via error message containing backup file path with `[` chars in user directory name | mitigate | print_error builds Text internally (messages.py line 55) and renders with `highlight=False`. render_kv_block uses `markup=False` (styles.py line 166). Section headers use `Text(...)` + `markup=False, highlight=False`. |
</threat_model>

<verification>
- `uv run ruff check src/bensdorp1/commands/restore.py tests/test_commands/test_restore.py` exits 0
- `uv run ruff format --check src/bensdorp1/commands/restore.py tests/test_commands/test_restore.py` exits 0
- `uv run mypy --strict src/bensdorp1/commands/restore.py tests/test_commands/test_restore.py` exits 0
- `uv run pytest tests/test_commands/test_restore.py -x -v` — all 5 tests pass
- Negative grep: no occurrence of `create_backup` in restore.py (we use inline shutil.copy2 with exact pre-restore filename per spec §10 + Open Question 1)
</verification>

<success_criteria>
- CMD-02 truth: `bensdorp1 restore PATH` validates schema, requires two confirmations, creates a pre-restore backup, copies the file, logs RESTORE_PERFORMED to the restored DB.
- All 5 named test cases pass.
- mypy strict + ruff clean.
- Stub string "Not yet implemented." no longer present in restore.py.
- Secondary engine disposed before file copy (Windows safety verified by test pass on this OS).
</success_criteria>

<output>
Create `.planning/phases/10-system-commands/10-03-SUMMARY.md` when done, listing: files modified, test count + pass status, mypy/ruff status, any deviations from this plan (with rationale).
</output>

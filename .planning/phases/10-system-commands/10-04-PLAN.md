---
phase: 10-system-commands
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified: []
autonomous: true
requirements:
  - CMD-02
  - CMD-14
  - CMD-15
tags:
  - verification
  - quality-gate

must_haves:
  truths:
    - "Full pytest suite passes (no skips on any Phase 10 test file)."
    - "Coverage on src/bensdorp1 is ≥ 90% (TEST-02 invariant preserved)."
    - "mypy --strict reports zero errors on the entire src/ tree and tests/ tree."
    - "ruff check src/ tests/ reports zero issues."
    - "ruff format --check src/ tests/ reports zero diffs."
    - "All 3 Phase 10 commands respond to `--help` and produce non-empty output (status, refresh, restore)."
  artifacts:
    - path: ".planning/phases/10-system-commands/10-04-SUMMARY.md"
      provides: "Verification gate summary with coverage delta and test counts"
      contains: "coverage"
  key_links:
    - from: "verification gate"
      to: "all 3 implementation plans"
      via: "no production source modifications; pure gate"
      pattern: "files_modified: \\[\\]"
---

<objective>
Run the full Phase 10 verification gate AFTER all implementation plans (10-01, 10-02, 10-03) have completed: pytest --cov, mypy --strict, ruff check, ruff format --check. Validate that each of the 3 Phase 10 commands responds to `--help` non-trivially. Surface any regressions, coverage gaps, or lint/type errors so they can be remediated inside this same phase before declaring Phase 10 complete. No production source files are modified by this plan — it is a pure quality gate.

Purpose: Single point of pass/fail for Phase 10. Mirrors the gate plans used by Phases 4 (04-03), 6 (06-02), 7 (07-04), 8 (08-05), 9 (09-09).

Output: SUMMARY.md with measured coverage %, total test count, and a verdict line confirming the gate passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-system-commands/10-CONTEXT.md
@.planning/phases/10-system-commands/10-RESEARCH.md
@.planning/phases/10-system-commands/10-01-PLAN.md
@.planning/phases/10-system-commands/10-02-PLAN.md
@.planning/phases/10-system-commands/10-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full pytest with coverage + capture results</name>
  <files></files>
  <read_first>
    - pyproject.toml (confirms `[tool.coverage.*]` and pytest configuration; identifies the coverage threshold and reporting flags)
    - .planning/phases/10-system-commands/10-RESEARCH.md (section "Validation Architecture" — quick-run and full-suite commands)
    - .planning/phases/09-consultation-commands/09-09-PLAN.md (canonical Phase 9 verification gate plan to mirror)
  </read_first>
  <action>
    Run `uv run pytest --cov=bensdorp1 --cov-report=term-missing -q` from the repo root. Capture exit code, total test count (passed/failed/skipped), and coverage percentage from the term-missing report.

    If pytest exits non-zero, surface the failing test names in this task's output — DO NOT attempt to fix in this plan; raise back to the user so the underlying implementation plan (10-01/02/03) can be revised.

    If aggregate coverage on `src/bensdorp1/` is below 90%, surface the per-file coverage table from `term-missing` so the gap is visible.

    If coverage on any individual Phase 10 command file (`commands/status.py`, `commands/refresh.py`, `commands/restore.py`) is below 85%, surface that file's missing-line ranges.

    Phase 10 added test counts (target): 5 (status) + 2 (refresh) + 5 (restore) = 12 new tests. Confirm at least 12 new passing tests vs. the pre-Phase-10 baseline.
  </action>
  <verify>
    <automated>cd C:/Users/david/Documents/Projetos/bensdorp-1 && uv run pytest --cov=bensdorp1 --cov-report=term-missing -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest --cov=bensdorp1 --cov-report=term-missing -q` exits 0.
    - Total test count is ≥ pre-Phase-10 baseline + 12 (5 status + 2 refresh + 5 restore).
    - Aggregated coverage on `src/bensdorp1/` is ≥ 90% (TEST-02 invariant).
    - Each of the 3 Phase 10 command files has individual coverage ≥ 85% (verifiable from term-missing report).
    - Zero tests are reported as SKIPPED for any of: `tests/test_commands/test_status.py`, `tests/test_commands/test_refresh.py`, `tests/test_commands/test_restore.py`.
  </acceptance_criteria>
  <done>Pytest passes; coverage thresholds met for Phase 10 files and aggregate.</done>
</task>

<task type="auto">
  <name>Task 2: Run mypy strict + ruff check + ruff format check</name>
  <files></files>
  <read_first>
    - pyproject.toml (confirms `[tool.mypy]` strict settings and `[tool.ruff]` configuration; identifies any per-file ignores already in place)
    - .planning/phases/10-system-commands/10-01-PLAN.md, 10-02-PLAN.md, 10-03-PLAN.md (acceptance criteria already require per-file ruff + mypy clean; this task confirms repo-wide clean)
  </read_first>
  <action>
    Run three commands sequentially and capture all output:

    1. `uv run mypy --strict src/ tests/` — repo-wide strict type check.
    2. `uv run ruff check src/ tests/` — repo-wide lint.
    3. `uv run ruff format --check src/ tests/` — repo-wide format diff check.

    If any of the three exits non-zero, surface the full error output. DO NOT attempt fixes inside this plan; raise back to the user. Common Phase 10 risks to expect (per CLAUDE.md mypy section and RESEARCH.md):
    - `disallow_untyped_decorators` firing on `@app.command` decorators — should already be configured via per-file ignores or a typer plugin; no action needed unless newly broken.
    - `warn_unused_ignores` triggering if a previously-needed `# type: ignore` is now unnecessary.
    - Ruff `TC` (typing-imports) rules moving SQLAlchemy imports into `TYPE_CHECKING` blocks — CLAUDE.md notes the runtime-evaluated exclusion list; confirm no new SA imports leaked into TYPE_CHECKING.
  </action>
  <verify>
    <automated>cd C:/Users/david/Documents/Projetos/bensdorp-1 && uv run mypy --strict src/ tests/ && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run mypy --strict src/ tests/` exits 0.
    - `uv run ruff check src/ tests/` exits 0.
    - `uv run ruff format --check src/ tests/` exits 0.
  </acceptance_criteria>
  <done>mypy strict, ruff check, and ruff format all report clean.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-check that all 3 Phase 10 commands respond to --help</name>
  <files></files>
  <read_first>
    - src/bensdorp1/cli.py (confirms which commands are registered — status/refresh/restore are at lines 15-16, 19)
    - src/bensdorp1/commands/status.py, refresh.py, restore.py (the implementations from Wave 1)
  </read_first>
  <action>
    Run `uv run bensdorp1 <command> --help` for each of the 3 Phase 10 commands and confirm:
    (a) exit code is 0,
    (b) stdout includes the command's docstring summary line,
    (c) stdout does NOT contain `"Not yet implemented."`,
    (d) for `restore`: stdout includes the literal substring `PATH` (the Argument name from typer.Argument).

    Commands to check:
    - `uv run bensdorp1 status --help`
    - `uv run bensdorp1 refresh --help`
    - `uv run bensdorp1 restore --help`

    Also run `uv run bensdorp1 --help` and confirm all three commands appear under the appropriate Rich help panels:
    - `status` under "System"
    - `refresh` under "System"
    - `restore` under "Setup"
  </action>
  <verify>
    <automated>cd C:/Users/david/Documents/Projetos/bensdorp-1 && uv run bensdorp1 status --help && uv run bensdorp1 refresh --help && uv run bensdorp1 restore --help && uv run bensdorp1 --help</automated>
  </verify>
  <acceptance_criteria>
    - All four invocations exit 0.
    - `restore --help` output contains the literal substring `PATH`.
    - No `--help` output contains the literal substring `Not yet implemented.`.
    - `bensdorp1 --help` output contains all three command names (`status`, `refresh`, `restore`) on visible help lines.
  </acceptance_criteria>
  <done>All 3 Phase 10 commands respond to --help with real documentation, not stub messages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / local test invocation | No external input; pure verification gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | Stale `.coverage` file or cached pytest results mask new failures | mitigate | Task 1's `-q` runs fresh by default; project pyproject coverage config does not preserve `.coverage` between runs. If suspect, clear via `rm .coverage` (no automated step needed; user verifies). |
| T-10-04-02 | Repudiation | Gate passes silently with hidden warnings | mitigate | Acceptance criteria require explicit exit-0 from each command and explicit assertions on test counts and coverage thresholds; SUMMARY.md records measured values. |
</threat_model>

<verification>
- All three task commands above exit 0
- SUMMARY.md created with: measured aggregate coverage %, per-Phase-10-file coverage %, total test count, Phase 10 added test count (≥ 12), mypy/ruff/ruff-format verdicts, --help smoke verdict
</verification>

<success_criteria>
- pytest --cov, mypy strict, ruff check, ruff format --check all clean
- All 3 Phase 10 commands respond to --help with non-stub content
- Coverage thresholds met (90% aggregate, 85% per Phase 10 command file)
- No Phase 10 test skipped
</success_criteria>

<output>
Create `.planning/phases/10-system-commands/10-04-SUMMARY.md` summarizing: coverage % (aggregate + per-file for Phase 10 commands), total test count, Phase 10 added test count, mypy/ruff/ruff-format verdicts, smoke-test verdict, and a final PASS / FAIL gate line.
</output>

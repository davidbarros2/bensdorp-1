---
phase: 04-strategy-logic
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified: []
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "uv run pytest tests/test_strategy/ --cov=bensdorp1.strategy --cov-fail-under=95 exits 0"
    - "uv run pytest --cov=bensdorp1 --cov-fail-under=90 exits 0"
    - "uv run mypy src/ exits 0 with no errors across all strategy/ files"
    - "uv run ruff check src/ exits 0 with no errors across all strategy/ files"
    - "All 4 Hypothesis property tests pass (invariants 1-4) with configured max_examples"
  artifacts:
    - path: "src/bensdorp1/strategy/screening.py"
      provides: "4 screening functions + Candidate TypedDict (created in Plan 04-01)"
      contains: "def regime_filter"
    - path: "src/bensdorp1/strategy/positions.py"
      provides: "6 position functions (created in Plan 04-02)"
      contains: "def compute_position_size"
    - path: "src/bensdorp1/strategy/__init__.py"
      provides: "Re-exports all 11 public names (finalized in Plan 04-02)"
      contains: "__all__"
  key_links:
    - from: "tests/test_strategy/test_screening.py"
      to: "src/bensdorp1/strategy/screening.py"
      via: "named imports"
      pattern: "from bensdorp1.strategy.screening import"
    - from: "tests/test_strategy/test_positions.py"
      to: "src/bensdorp1/strategy/positions.py"
      via: "named imports"
      pattern: "from bensdorp1.strategy.positions import"
---

<objective>
Run the full phase verification gate: confirm that strategy/ module coverage exceeds 95% (TEST-01), all-module coverage exceeds 90% (TEST-02), all 4 Hypothesis invariants are green (TEST-03), mypy strict passes with zero errors, and ruff passes with zero errors. Fix any remaining gaps before declaring the phase complete.

Purpose: This plan is the quality gate for Phase 4. Plans 04-01 and 04-02 build the code; this plan verifies the complete system meets all coverage and type-safety targets before the phase is marked done.

Output: No new files. Verification commands run clean. 04-03-SUMMARY.md records evidence of coverage gates met.
</objective>

<execution_context>
@C:\Users\david\Documents\Projetos\bensdorp-1\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\ROADMAP.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\04-strategy-logic\04-CONTEXT.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\04-strategy-logic\04-VALIDATION.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\04-strategy-logic\04-01-SUMMARY.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\04-strategy-logic\04-02-SUMMARY.md

<interfaces>
<!-- Prior plan outputs consumed by this plan. All strategy/ files exist from Plans 04-01 and 04-02. -->

Coverage commands:
  Strategy-only (TEST-01, >= 95%):
    uv run pytest tests/test_strategy/ --cov=bensdorp1.strategy --cov-report=term-missing --cov-fail-under=95 -v

  All-modules (TEST-02, >= 90%):
    uv run pytest --cov=bensdorp1 --cov-report=term-missing --cov-fail-under=90 -v

  Hypothesis only (TEST-03):
    uv run pytest tests/test_strategy/ -k "test_effective_stop_ge_initial or test_trailing_stop_monotonic or test_rank_candidates_max_ten or test_regime_off_when_close_le_sma200" -v

  Type checking:
    uv run mypy src/

  Lint:
    uv run ruff check src/

pyproject.toml does not have a [tool.coverage.run] section.
--cov-fail-under flag is the correct gate mechanism (per RESEARCH.md Validation Architecture).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full verification suite and fix any remaining gaps</name>
  <files></files>
  <read_first>
    - .planning/phases/04-strategy-logic/04-01-SUMMARY.md — which tests were written, any coverage gaps noted
    - .planning/phases/04-strategy-logic/04-02-SUMMARY.md — positions test status, any mypy warnings noted
    - .planning/phases/04-strategy-logic/04-VALIDATION.md — branch coverage map (full list of expected test names per function)
    - .planning/phases/04-strategy-logic/04-RESEARCH.md — Common Pitfalls section (if any failures arise, check against Pitfalls 1-6 first)
  </read_first>
  <action>
    Run the following commands in sequence and record actual output:

    Step 1 — Strategy-only coverage gate (TEST-01):
      uv run pytest tests/test_strategy/ --cov=bensdorp1.strategy --cov-report=term-missing --cov-fail-under=95 -v
      Expected: exits 0. If it fails with coverage < 95%, read the "Missing" column in the term-missing report to identify uncovered lines.

    Step 2 — All-modules coverage gate (TEST-02):
      uv run pytest --cov=bensdorp1 --cov-report=term-missing --cov-fail-under=90 -v
      Expected: exits 0. Note: db/ and data/ coverage from Phases 2 and 3 must remain green; no Phase 4 changes should regress them.

    Step 3 — Hypothesis property tests (TEST-03):
      uv run pytest tests/test_strategy/ -k "test_effective_stop_ge_initial or test_trailing_stop_monotonic or test_rank_candidates_max_ten or test_regime_off_when_close_le_sma200" -v
      Expected: exits 0 for all 4 invariant tests.

    Step 4 — mypy strict (all source):
      uv run mypy src/
      Expected: exits 0. If errors appear in strategy/ files, consult RESEARCH.md Pitfalls section (most likely Pitfall 2: bare pd.Series, Pitfall 3: dict[str, object], or Pitfall 6: missing re-export in __init__.py).

    Step 5 — ruff lint:
      uv run ruff check src/
      Expected: exits 0.

    If any step fails:
      - Coverage < 95%: add targeted tests for the specific uncovered branch (consult branch coverage map in VALIDATION.md); do NOT weaken the --cov-fail-under threshold.
      - mypy error: fix the type annotation in the flagged file; consult RESEARCH.md Pitfalls before attempting workarounds; no # type: ignore unless the error is a known stubs limitation documented in CLAUDE.md.
      - ruff error: fix the lint issue in place; do not add per-file-ignores unless the error is a known intentional exception (only cli.py has one, per pyproject.toml [tool.ruff.lint.per-file-ignores]).
      - Hypothesis failure: Hypothesis will print the minimal counterexample — fix the implementation, not the test.

    All five gates must be green before proceeding to Task 2.
  </action>
  <verify>
    <automated>uv run pytest tests/test_strategy/ --cov=bensdorp1.strategy --cov-fail-under=95 -q && uv run pytest --cov=bensdorp1 --cov-fail-under=90 -q && uv run mypy src/ && uv run ruff check src/</automated>
  </verify>
  <done>All four commands exit 0; no output from mypy or ruff; pytest coverage gates pass at >= 95% and >= 90% respectively</done>
  <acceptance_criteria>
    - uv run pytest tests/test_strategy/ --cov=bensdorp1.strategy --cov-fail-under=95 -q exits 0 (non-zero exit means coverage below 95%)
    - uv run pytest --cov=bensdorp1 --cov-fail-under=90 -q exits 0 (non-zero exit means all-modules coverage below 90%)
    - uv run mypy src/ output is "Success: no issues found" with exit code 0
    - uv run ruff check src/ produces no output and exits 0
    - All 4 Hypothesis property tests (test_effective_stop_ge_initial, test_trailing_stop_monotonic, test_rank_candidates_max_ten, test_regime_off_when_close_le_sma200) appear in pytest -v output as PASSED
    - No test is marked xfail, skip, or error
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify public API import contract and full test suite clean run</name>
  <files></files>
  <read_first>
    - src/bensdorp1/strategy/__init__.py — confirm all 11 names in __all__ are importable
    - .planning/phases/04-strategy-logic/04-RESEARCH.md — Pitfall 6 (no_implicit_reexport requires explicit __all__), Integration Surface section (Candidate fields map to scan_candidates table)
  </read_first>
  <action>
    Step 1 — Verify public API is importable from the package surface (RESEARCH.md Pitfall 6 check):
      uv run python -c "from bensdorp1.strategy import Candidate, regime_filter, liquidity_filter, momentum_filter, rank_candidates, compute_position_size, compute_initial_stop, update_highest_close, compute_trailing_stop, compute_effective_stop, is_exit_triggered; print('API OK')"
      Expected output: "API OK". If ImportError, the __init__.py re-exports are incomplete.

    Step 2 — Verify Candidate TypedDict field names (required by Phase 7 integration contract):
      uv run python -c "from bensdorp1.strategy import Candidate; c: Candidate = {'symbol': 'AAPL', 'roc_200': 0.15, 'prev_close': 150.0, 'position_size': 10}; print(list(c.keys()))"
      Expected output: "['symbol', 'roc_200', 'prev_close', 'position_size']" (or same keys in any order).

    Step 3 — Full test suite (all phases, not just strategy/):
      uv run pytest -x -q
      Expected: exits 0. This confirms Phase 4 additions did not regress Phase 1, 2, or 3 tests.

    Step 4 — Collect and record final coverage numbers:
      uv run pytest --cov=bensdorp1 --cov-report=term-missing -q
      Note the exact line coverage percentages for: bensdorp1/strategy/screening.py, bensdorp1/strategy/positions.py, bensdorp1/strategy/__init__.py. Record these in the SUMMARY.md as evidence.

    If Step 1 fails (ImportError): update strategy/__init__.py to add the missing name to both the import line and __all__.
    If Step 3 fails on a Phase 1-3 test: strategy/ changes should have no impact on db/ or data/ tests since strategy/ imports nothing from them. If it fails, the error is likely a pytest configuration issue — check that testpaths = ["tests"] covers all subdirectories.
  </action>
  <verify>
    <automated>uv run python -c "from bensdorp1.strategy import Candidate, regime_filter, liquidity_filter, momentum_filter, rank_candidates, compute_position_size, compute_initial_stop, update_highest_close, compute_trailing_stop, compute_effective_stop, is_exit_triggered; print('API OK')" && uv run pytest -x -q</automated>
  </verify>
  <done>All 11 names importable from bensdorp1.strategy; full test suite exits 0 with no failures or errors</done>
  <acceptance_criteria>
    - uv run python -c "from bensdorp1.strategy import Candidate, ..." prints "API OK" with exit code 0
    - Candidate TypedDict has exactly 4 fields: symbol (str), roc_200 (float), prev_close (float), position_size (int)
    - uv run pytest -x -q exits 0 with no failures, errors, or skips related to strategy/ (Phase 1-3 tests unaffected)
    - Coverage report shows bensdorp1/strategy/screening.py >= 95% and bensdorp1/strategy/positions.py >= 95%
    - bensdorp1/strategy/__init__.py shows 100% coverage (all re-export lines executed by import)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner -> strategy/ | pytest invokes pure functions with in-memory test data; no external I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | Coverage gate bypass | accept | --cov-fail-under is enforced by pytest-cov exit code; gate cannot be silently bypassed. If the exit code is non-zero, the CI pipeline fails. |
| T-04-08 | Repudiation | Coverage numbers not recorded | mitigate | SUMMARY.md records actual coverage percentages from the term-missing report as evidence for the audit trail. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. Verification only. |

**Security confirmation:** This plan runs verification commands only. No new code is written (unless gap fixes are needed). No user input reaches strategy/ at any point — confirmed across all three plans. Phase 4 is a pure math layer; ASVS L1 controls are enforced at Phase 7 (the entry point that receives user-triggered scans).
</threat_model>

<verification>
Phase 4 complete when all of the following exit 0:

1. uv run pytest tests/test_strategy/ --cov=bensdorp1.strategy --cov-fail-under=95 -q
2. uv run pytest --cov=bensdorp1 --cov-fail-under=90 -q
3. uv run mypy src/
4. uv run ruff check src/
5. uv run python -c "from bensdorp1.strategy import Candidate, regime_filter, liquidity_filter, momentum_filter, rank_candidates, compute_position_size, compute_initial_stop, update_highest_close, compute_trailing_stop, compute_effective_stop, is_exit_triggered; print('API OK')"
6. uv run pytest -x -q (full suite — all phases)
</verification>

<success_criteria>
1. TEST-01 met: bensdorp1/strategy/screening.py and positions.py both >= 95% line coverage
2. TEST-02 met: all bensdorp1 modules combined >= 90% line coverage (Phase 1-3 existing coverage + Phase 4 additions)
3. TEST-03 met: all 4 Hypothesis invariants pass — effective_stop >= initial_stop, trailing stop monotonic, rank_candidates <= 10, regime_filter False when close <= SMA200
4. mypy strict: 0 errors across all strategy/ files (no # type: ignore added without documented justification)
5. ruff: 0 lint errors in strategy/ files
6. Full import contract: all 11 public names importable from bensdorp1.strategy with correct types
7. Full test suite: all Phase 1-4 tests pass, no regressions
</success_criteria>

<output>
Create .planning/phases/04-strategy-logic/04-03-SUMMARY.md when done.
Include: actual coverage percentages for screening.py, positions.py, and __init__.py; Hypothesis example counts run; mypy and ruff exit codes; any gap fixes applied and why.
</output>

---
phase: 04-strategy-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bensdorp1/strategy/__init__.py
  - src/bensdorp1/strategy/screening.py
  - tests/test_strategy/__init__.py
  - tests/test_strategy/test_screening.py
autonomous: true
requirements:
  - STRAT-01
  - STRAT-02
  - STRAT-03
  - STRAT-04
  - STRAT-05
  - TEST-01
  - TEST-03

must_haves:
  truths:
    - "regime_filter returns True when SPX close today > SMA 200, False otherwise"
    - "liquidity_filter returns only symbols whose 20-day avg volume (T-1 through T-20) is >= 75th percentile"
    - "momentum_filter returns only symbols where close today > close 200 rows ago"
    - "rank_candidates returns at most 10 Candidate dicts sorted descending by roc_200"
    - "All four functions pass mypy strict and ruff check with zero errors"
  artifacts:
    - path: "src/bensdorp1/strategy/__init__.py"
      provides: "Re-exports Candidate, regime_filter, liquidity_filter, momentum_filter, rank_candidates"
      contains: "__all__"
    - path: "src/bensdorp1/strategy/screening.py"
      provides: "4 screening functions + Candidate TypedDict"
      exports:
        - "Candidate"
        - "regime_filter"
        - "liquidity_filter"
        - "momentum_filter"
        - "rank_candidates"
    - path: "tests/test_strategy/test_screening.py"
      provides: "Unit tests for all screening branches + Hypothesis invariants 3 and 4"
      min_lines: 150
  key_links:
    - from: "src/bensdorp1/strategy/__init__.py"
      to: "src/bensdorp1/strategy/screening.py"
      via: "explicit named imports"
      pattern: "from bensdorp1.strategy.screening import"
    - from: "tests/test_strategy/test_screening.py"
      to: "src/bensdorp1/strategy/screening.py"
      via: "direct import"
      pattern: "from bensdorp1.strategy.screening import"
---

<objective>
Create the strategy/ subpackage and implement the four screening functions — regime_filter, liquidity_filter, momentum_filter, and rank_candidates — as pure functions with full unit and property tests.

Purpose: Phase 7 (Scan command) calls these functions with pre-fetched DataFrames to determine whether the regime is active and which stocks are buy candidates. This plan delivers the complete screening half of the strategy/ subpackage with >95% test coverage.

Output: src/bensdorp1/strategy/__init__.py, src/bensdorp1/strategy/screening.py, tests/test_strategy/__init__.py, tests/test_strategy/test_screening.py
</objective>

<execution_context>
@C:\Users\david\Documents\Projetos\bensdorp-1\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\PROJECT.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\ROADMAP.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\04-strategy-logic\04-CONTEXT.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\04-strategy-logic\04-RESEARCH.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\04-strategy-logic\04-PATTERNS.md

<interfaces>
<!-- Canonical patterns the executor must replicate exactly. -->

From src/bensdorp1/db/__init__.py (re-export pattern to mirror):
```python
"""Public surface of the bensdorp1.db subpackage."""

from bensdorp1.db.audit import AuditEventType, log_event
from bensdorp1.db.backup import create_backup
from bensdorp1.db.engine import get_engine, run_migrations

__all__ = [
    "AuditEventType",
    "create_backup",
    "get_engine",
    "log_event",
    "run_migrations",
]
```

From src/bensdorp1/data/calendar.py (module docstring + ValueError pattern to mirror):
```python
"""NYSE trading-day wrappers using pandas_market_calendars v5.

No I/O — pure computation over the NYSE holiday calendar.
DATA-07: NYSE market calendar via pandas_market_calendars for trading-day arithmetic.
Depends on: pandas_market_calendars>=5.3.2 (zoneinfo-based, no pytz)
Used by: data/prices.py, commands/scan.py (Phase 7), commands/init.py (Phase 6)
"""
...
    if len(days) < n:
        raise ValueError(f"Not enough trading days in range for n={n}")
```

From pyproject.toml (no new deps needed — all already present):
- pandas>=3.0.3, numpy>=2.0, hypothesis>=6.130 in dev group
- testpaths = ["tests"] already covers tests/test_strategy/ by subdirectory
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create strategy/ subpackage — screening.py + __init__.py</name>
  <files>src/bensdorp1/strategy/__init__.py, src/bensdorp1/strategy/screening.py</files>
  <read_first>
    - src/bensdorp1/db/__init__.py — exact re-export pattern to mirror (named imports + __all__)
    - src/bensdorp1/data/__init__.py — secondary example with out-of-scope note style
    - src/bensdorp1/data/calendar.py — module docstring format, ValueError pattern, explicit return annotations
    - .planning/phases/04-strategy-logic/04-RESEARCH.md — all verified function signatures, complete implementations in "Code Examples" section, anti-patterns to avoid
    - .planning/phases/04-strategy-logic/04-PATTERNS.md — TypedDict placement, __all__ ordering, import block structure
  </read_first>
  <action>
    Create src/bensdorp1/strategy/ directory with two files.

    FILE 1 — src/bensdorp1/strategy/screening.py:

    Module docstring: "No I/O — pure filter math over pre-fetched DataFrames. STRAT-01 through STRAT-05. Used by: commands/scan.py (Phase 7). Depends on: pandas>=3.0.3." Include note that D-02 prohibits any import from bensdorp1.db or bensdorp1.data.

    Imports: stdlib first (math, typing.TypedDict), then third-party (import pandas as pd). No imports from bensdorp1 subpackages.

    Define Candidate TypedDict at module level after imports, before functions:
    - symbol: str
    - roc_200: float
    - prev_close: float
    - position_size: int

    Implement exactly these four functions (signatures locked per D-01 and RESEARCH.md verified patterns):

    regime_filter(spx_closes: pd.Series[float]) -> bool
      D-09: SMA 200 = float(spx_closes.iloc[-200:].mean()). today_close = float(spx_closes.iloc[-1]).
      Guard: if len(spx_closes) < 200 raise ValueError(f"regime_filter: need >= 200 closes; got {len(spx_closes)}").
      Return today_close > sma200.

    liquidity_filter(price_dfs: dict[str, pd.DataFrame]) -> list[str]
      D-05: if not price_dfs return [].
      D-04: volume window is df.iloc[:-1].iloc[-20:] (excludes today). Guard: if len(df) < 21 raise ValueError.
      Compute avg_volumes: dict[str, float] using float(last_20["volume"].mean()).
      Threshold: float(pd.Series(list(avg_volumes.values()), dtype=float).quantile(0.75)).
      Return symbols where avg_volume >= threshold.
      NaN volume behavior: mean() with NaN produces NaN, which fails >= threshold — symbol excluded (conservative, document in docstring per open question resolution in RESEARCH.md).

    momentum_filter(price_dfs: dict[str, pd.DataFrame]) -> list[str]
      D-03: close_today = float(df["close"].iloc[-1]). close_t200 = float(df["close"].iloc[-200]).
      Guard: if len(df) < 200 raise ValueError(f"momentum_filter: {symbol} needs >= 200 rows; got {len(df)}").
      Return symbols where close_today > close_t200.

    rank_candidates(price_dfs: dict[str, pd.DataFrame], available_cash: float) -> list[Candidate]
      D-07: guard per symbol if len(df) < 200 raise ValueError.
      Compute: prev_close = float(df["close"].iloc[-1]), close_t200 = float(df["close"].iloc[-200]), roc_200 = (prev_close / close_t200) - 1.0.
      Position size: math.floor((available_cash * 0.10) / prev_close) — returns 0 when < 1 share (D-06).
      Append Candidate dict. Sort descending by roc_200, slice [:10] (STRAT-04, STRAT-05).
      Use sorted(results, key=lambda x: x["roc_200"], reverse=True)[:10].

    Do NOT use bare pd.Series annotation anywhere — always pd.Series[float] (mypy strict requirement).

    FILE 2 — src/bensdorp1/strategy/__init__.py:

    Docstring: "Public surface of the bensdorp1.strategy subpackage. All exported names are pure functions — no I/O, no DB access (D-02)."
    Explicit named imports from bensdorp1.strategy.screening: Candidate, liquidity_filter, momentum_filter, rank_candidates, regime_filter.
    Explicit named imports from bensdorp1.strategy.positions: (leave as placeholder comment "# positions exports added in Plan 04-02").
    __all__ list alphabetically sorted: Candidate, liquidity_filter, momentum_filter, rank_candidates, regime_filter.
    No wildcard imports. No implicit re-exports.
  </action>
  <verify>
    <automated>uv run mypy src/bensdorp1/strategy/screening.py src/bensdorp1/strategy/__init__.py && uv run ruff check src/bensdorp1/strategy/</automated>
  </verify>
  <done>mypy strict reports 0 errors on both files; ruff reports 0 errors; screening.py defines Candidate TypedDict and four functions with correct signatures; __init__.py re-exports all five names with __all__</done>
  <acceptance_criteria>
    - src/bensdorp1/strategy/screening.py contains: class Candidate(TypedDict), def regime_filter(spx_closes: pd.Series[float]) -> bool, def liquidity_filter(price_dfs: dict[str, pd.DataFrame]) -> list[str], def momentum_filter(price_dfs: dict[str, pd.DataFrame]) -> list[str], def rank_candidates(price_dfs: dict[str, pd.DataFrame], available_cash: float) -> list[Candidate]
    - src/bensdorp1/strategy/__init__.py contains: from bensdorp1.strategy.screening import Candidate and __all__ including "Candidate"
    - uv run mypy src/bensdorp1/strategy/ exits 0 with "Success: no issues found"
    - uv run ruff check src/bensdorp1/strategy/ exits 0 with no output
    - No import from bensdorp1.db or bensdorp1.data appears anywhere in strategy/screening.py
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_strategy/ — test_screening.py with full unit + Hypothesis coverage</name>
  <files>tests/test_strategy/__init__.py, tests/test_strategy/test_screening.py</files>
  <read_first>
    - src/bensdorp1/strategy/screening.py — (just written in Task 1) exact function signatures and behavior to test
    - .planning/phases/04-strategy-logic/04-RESEARCH.md — Validation Architecture section: branch coverage map, all test names, exact Hypothesis invariant designs for invariants 3 and 4
    - .planning/phases/04-strategy-logic/04-PATTERNS.md — test_screening.py section: module docstring, import block, test naming convention, ValueError assertion pattern, Hypothesis section placement
    - tests/test_data_calendar.py — analog test file for docstring style, pytest.raises pattern, one-line docstring per test convention
  </read_first>
  <action>
    FILE 1 — tests/test_strategy/__init__.py: empty file (no content, no imports, no docstring). Exactly matches tests/__init__.py.

    FILE 2 — tests/test_strategy/test_screening.py:

    Module docstring: describes STRAT-01 through STRAT-04 and TEST-03 invariants 3 and 4. Note "Pure computation — no db_engine fixture needed. DataFrames constructed inline."

    Import block:
      import pandas as pd
      import pytest
      from hypothesis import given, settings
      from hypothesis import strategies as st
      from bensdorp1.strategy.screening import (Candidate, liquidity_filter, momentum_filter, rank_candidates, regime_filter)

    Define module-level Hypothesis strategy:
      _price_st = st.floats(min_value=0.01, max_value=10000, allow_nan=False, allow_infinity=False)

    Helper: build_price_df(n_rows: int, close_values: list[float], volume_values: list[int] | None = None) -> pd.DataFrame — constructs DataFrame with columns "close" and "volume" of length n_rows. Volume defaults to 1_000_000 per row. Used across multiple tests.

    UNIT TESTS (one-line docstring each, test_<function>_<scenario> naming):

    test_regime_filter_on: 200-row series where last element is 1000.0 (above mean). Assert regime_filter returns True.

    test_regime_filter_off: 200-row series where last element is 0.01 (below mean). Assert regime_filter returns False.

    test_regime_filter_boundary: 200-row series of all-same float value (close == sma200). Assert returns False (today_close > sma200 is strict greater-than).

    test_regime_filter_insufficient_rows: pd.Series([1.0] * 50, dtype=float). Use pytest.raises(ValueError, match=r"need >= 200").

    test_liquidity_filter_top_quartile: 4 symbols with avg volumes 100, 200, 300, 400. Expect symbols with volumes >= 75th percentile (300.0) to be in result. Assert symbols with volume < 300 not in result.

    test_liquidity_filter_empty: liquidity_filter({}) returns [].

    test_liquidity_filter_insufficient: DataFrame with 20 rows. pytest.raises(ValueError, match=r"needs >= 21").

    test_liquidity_filter_nan_volume: DataFrame where volume column contains NaN in 20-day window. Symbol is excluded from result (conservative behavior per RESEARCH.md open question resolution).

    test_momentum_filter_pass: symbol where close at row -1 > close at row -200. Assert symbol in result.

    test_momentum_filter_reject: symbol where close at row -1 < close at row -200. Assert symbol not in result.

    test_momentum_filter_boundary: symbol where close at row -1 == close at row -200. Assert symbol not in result (strict > required).

    test_momentum_filter_insufficient: DataFrame with 199 rows. pytest.raises(ValueError, match=r"needs >= 200").

    test_rank_candidates_ordering: 15 symbols, each with 200 rows, distinct closes so roc_200 differs. Assert result[:10] is sorted descending by roc_200. Assert len(result) == 10.

    test_rank_candidates_limits_to_10: 20 symbols, all with valid 200+ rows. Assert len(result) <= 10.

    test_rank_candidates_empty: rank_candidates({}, 100000.0) returns [].

    test_rank_candidates_insufficient_rows: one symbol with 199-row DataFrame. pytest.raises(ValueError, match=r"need >= 200|needs >= 200").

    test_rank_candidates_zero_position_size: available_cash=0.01, prev_close=10000.0 — floor((0.01*0.10)/10000.0) = 0. Assert result[0]["position_size"] == 0.

    HYPOTHESIS PROPERTY TESTS (after unit tests, preceded by # --- Hypothesis property tests --- comment):

    test_rank_candidates_max_ten (Invariant 3):
      @given(st.lists(st.from_regex(r"[A-Z]{2,5}", fullmatch=True), min_size=0, max_size=600, unique=True), st.floats(min_value=1.0, max_value=10_000_000.0, allow_nan=False, allow_infinity=False))
      @settings(max_examples=200)
      Build 200-row DataFrames (close=[100.0+i for i in range(200)], volume=[1_000_000]*221).
      Assert len(rank_candidates(price_dfs, available_cash)) <= 10.

    test_regime_off_when_close_le_sma200 (Invariant 4):
      @given(st.lists(_price_st, min_size=200, max_size=200))
      @settings(max_examples=500)
      Replace last element with min(values)*0.5. Build pd.Series(bearish, dtype=float).
      Compute sma200 = float(series.iloc[-200:].mean()), today_close = float(series.iloc[-1]).
      Assert that if today_close <= sma200, regime_filter(series) is False.

    No conftest.py needed — pure function tests, no fixtures.
  </action>
  <verify>
    <automated>uv run pytest tests/test_strategy/test_screening.py -x -q && uv run pytest tests/test_strategy/ --cov=bensdorp1.strategy.screening --cov-report=term-missing</automated>
  </verify>
  <done>All tests in test_screening.py pass; coverage report shows bensdorp1/strategy/screening.py at >= 95% line coverage</done>
  <acceptance_criteria>
    - uv run pytest tests/test_strategy/test_screening.py -x -q exits 0 with all tests passing
    - Test file contains at least: test_regime_filter_on, test_regime_filter_off, test_regime_filter_insufficient_rows, test_liquidity_filter_top_quartile, test_liquidity_filter_empty, test_liquidity_filter_insufficient, test_momentum_filter_pass, test_momentum_filter_reject, test_momentum_filter_insufficient, test_rank_candidates_ordering, test_rank_candidates_limits_to_10, test_rank_candidates_empty, test_rank_candidates_insufficient_rows, test_rank_candidates_max_ten, test_regime_off_when_close_le_sma200
    - Coverage report for bensdorp1/strategy/screening.py shows >= 95%
    - uv run mypy tests/test_strategy/test_screening.py exits 0 (no type errors in test file itself)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 7 -> strategy/ | Phase 7 passes pre-validated DataFrames; strategy/ functions receive only typed Python objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | screening.py inputs | accept | No user input reaches strategy/ functions — DataFrames are constructed by Phase 7 from DB rows (price_daily), which are populated from yfinance (no user-controlled data). Pure math layer with no I/O. ASVS L1 N/A for pure math. |
| T-04-02 | Denial of Service | regime_filter with very large Series | accept | len check raises ValueError at 0-199 rows; no unbounded computation — iloc[-200:].mean() is O(200). |
| T-04-03 | Information Disclosure | Candidate TypedDict exposure | accept | No secrets in Candidate fields (symbol, roc_200, prev_close, position_size); all sourced from public market data. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. All dependencies are pre-existing project deps verified in Phase 1-3. No slopcheck needed. |

**Security confirmation:** No user input enters strategy/ functions. All parameters are pre-fetched DataFrames and scalars passed by Phase 7. This is a pure math layer — ASVS L1 trust boundary controls are enforced at the Phase 7 call site, not inside strategy/.
</threat_model>

<verification>
Full phase gate for Plan 04-01:

1. uv run pytest tests/test_strategy/ -x -q (all tests pass)
2. uv run mypy src/bensdorp1/strategy/screening.py src/bensdorp1/strategy/__init__.py (0 errors)
3. uv run ruff check src/bensdorp1/strategy/ (0 errors)
4. uv run pytest tests/test_strategy/ --cov=bensdorp1.strategy.screening --cov-report=term-missing (>= 95% on screening.py)
</verification>

<success_criteria>
1. src/bensdorp1/strategy/screening.py implements all 4 screening functions + Candidate TypedDict with correct signatures (verified by mypy strict)
2. regime_filter: True when close > SMA200, False when close <= SMA200, ValueError when len < 200
3. liquidity_filter: returns top 25% by 20-day avg volume (T-1 through T-20, excluding today), [] on empty input
4. momentum_filter: returns symbols where close[-1] > close[-200], excludes boundary (close[-1] == close[-200])
5. rank_candidates: descending roc_200, max 10 results, 0 shares when floor gives 0
6. All unit tests and both Hypothesis invariants (3 and 4) pass
7. screening.py coverage >= 95%
</success_criteria>

<output>
Create .planning/phases/04-strategy-logic/04-01-SUMMARY.md when done
</output>

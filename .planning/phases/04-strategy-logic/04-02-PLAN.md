---
phase: 04-strategy-logic
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/bensdorp1/strategy/positions.py
  - tests/test_strategy/test_positions.py
autonomous: true
requirements:
  - STRAT-06
  - STRAT-07
  - STRAT-08
  - STRAT-09
  - STRAT-10
  - TEST-01
  - TEST-03

must_haves:
  truths:
    - "compute_position_size returns floor((cash * 0.10) / prev_close), returning 0 when result is < 1"
    - "compute_initial_stop returns entry_close * 0.93 (immutable)"
    - "update_highest_close returns max(current, new_close)"
    - "compute_trailing_stop returns highest_close * 0.75"
    - "compute_effective_stop returns max(initial_stop, trailing_stop)"
    - "is_exit_triggered returns True when close <= effective_stop, False otherwise"
    - "All six functions pass mypy strict and ruff check with zero errors"
  artifacts:
    - path: "src/bensdorp1/strategy/positions.py"
      provides: "6 position management functions"
      exports:
        - "compute_position_size"
        - "compute_initial_stop"
        - "update_highest_close"
        - "compute_trailing_stop"
        - "compute_effective_stop"
        - "is_exit_triggered"
    - path: "tests/test_strategy/test_positions.py"
      provides: "Unit tests for all positions branches + Hypothesis invariants 1 and 2"
      min_lines: 100
  key_links:
    - from: "tests/test_strategy/test_positions.py"
      to: "src/bensdorp1/strategy/positions.py"
      via: "direct import"
      pattern: "from bensdorp1.strategy.positions import"
---

<objective>
Implement positions.py — all six position sizing and stop calculation functions — as pure scalar-arithmetic functions with full unit and property tests covering STRAT-06 through STRAT-09 and TEST-03 Hypothesis invariants 1 and 2.

Purpose: Phase 7 (Scan) calls these functions per open position to compute updated stops and evaluate exit triggers. Phase 8 (Buy) calls compute_initial_stop at entry time. All are stateless single-responsibility functions — Phase 7 owns persistence.

Output: src/bensdorp1/strategy/positions.py, tests/test_strategy/test_positions.py
</objective>

<execution_context>
@C:\Users\david\Documents\Projetos\bensdorp-1\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\PROJECT.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\ROADMAP.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\04-strategy-logic\04-CONTEXT.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\04-strategy-logic\04-RESEARCH.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\04-strategy-logic\04-PATTERNS.md

<interfaces>
<!-- Prior plan outputs that this plan consumes. -->

From 04-01 (strategy/ subpackage, already created):
- src/bensdorp1/strategy/__init__.py exists with placeholder comment for positions exports
- tests/test_strategy/__init__.py is an empty package marker

From src/bensdorp1/data/calendar.py (module docstring pattern):
```python
"""NYSE trading-day wrappers using pandas_market_calendars v5.

No I/O — pure computation over the NYSE holiday calendar.
...
"""
```
Apply to positions.py: "No I/O — pure stop arithmetic."

Key constraint (D-08): Two separate trailing stop functions:
  update_highest_close(current: float, new_close: float) -> float  # max(current, new_close)
  compute_trailing_stop(highest_close: float) -> float  # highest_close * 0.75

Phase 7 entry-day rule (NOT in positions.py): Phase 7 only calls update_highest_close when scan_date > entry_date. positions.py functions are stateless and know nothing about entry dates. STRAT-10 (exit triggers persist until confirmed) is Phase 7's responsibility.

DB column contract from schema.py (positions table):
  entry_close: Float  -> compute_initial_stop(entry_close)
  highest_close: Float -> update_highest_close(current, new_close) -> persisted by Phase 7
  initial_stop: Float  -> compute_effective_stop(initial_stop, trailing_stop)
  trailing_stop: Float -> compute_trailing_stop(highest_close) -> persisted by Phase 7
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create positions.py — six pure stop-arithmetic functions</name>
  <files>src/bensdorp1/strategy/positions.py</files>
  <read_first>
    - src/bensdorp1/data/calendar.py — module docstring format, single-line function docstring pattern, explicit return annotation style
    - .planning/phases/04-strategy-logic/04-RESEARCH.md — "positions.py — complete stop calculation chain" code example (verified mypy strict), anti-patterns (int() vs math.floor())
    - .planning/phases/04-strategy-logic/04-PATTERNS.md — positions.py section: import block (import math only), single-line docstring pattern, concrete signatures list
    - .planning/phases/04-strategy-logic/04-CONTEXT.md — D-06 (0-share return), D-07 (no ValueError for positions.py — these are scalar functions), D-08 (two separate trailing stop functions)
  </read_first>
  <action>
    Create src/bensdorp1/strategy/positions.py.

    Module docstring: "No I/O — pure stop arithmetic. STRAT-06 through STRAT-09. Used by: commands/scan.py (Phase 7), commands/buy.py (Phase 8). D-02: no imports from bensdorp1.db or bensdorp1.data."

    Import block: import math (stdlib only). No pandas, no numpy, no project imports.

    Implement exactly these six functions (signatures locked per D-01 and RESEARCH.md):

    compute_position_size(available_cash: float, prev_close: float) -> int
      Single-line docstring: "Return shares to buy: floor((cash * 0.10) / prev_close). Returns 0 when result < 1 (D-06)."
      Body: return math.floor((available_cash * 0.10) / prev_close)
      Use math.floor, NOT int() — financial flooring convention (RESEARCH.md anti-pattern).

    compute_initial_stop(entry_close: float) -> float
      Single-line docstring: "Return initial stop: entry_close * 0.93. Set once at entry; immutable (STRAT-07)."
      Body: return entry_close * 0.93

    update_highest_close(current: float, new_close: float) -> float
      Single-line docstring: "Return new highest close: max(current, new_close). Stateless; Phase 7 persists the result (D-08)."
      Body: return max(current, new_close)

    compute_trailing_stop(highest_close: float) -> float
      Single-line docstring: "Return trailing stop: highest_close * 0.75. Stateless; Phase 7 persists the result (D-08, STRAT-08)."
      Body: return highest_close * 0.75

    compute_effective_stop(initial_stop: float, trailing_stop: float) -> float
      Single-line docstring: "Return effective stop: max(initial_stop, trailing_stop) (STRAT-09)."
      Body: return max(initial_stop, trailing_stop)

    is_exit_triggered(close: float, effective_stop: float) -> bool
      Single-line docstring: "Return True when close <= effective_stop (exit signal). STRAT-09: trigger when daily close at or below effective stop."
      Body: return close <= effective_stop

    No additional logic in any function — these are single-expression implementations.
  </action>
  <verify>
    <automated>uv run mypy src/bensdorp1/strategy/positions.py && uv run ruff check src/bensdorp1/strategy/positions.py</automated>
  </verify>
  <done>mypy strict reports 0 errors; ruff reports 0 errors; file contains all 6 functions with correct signatures and math.floor for position sizing</done>
  <acceptance_criteria>
    - src/bensdorp1/strategy/positions.py contains: def compute_position_size(available_cash: float, prev_close: float) -> int, def compute_initial_stop(entry_close: float) -> float, def update_highest_close(current: float, new_close: float) -> float, def compute_trailing_stop(highest_close: float) -> float, def compute_effective_stop(initial_stop: float, trailing_stop: float) -> float, def is_exit_triggered(close: float, effective_stop: float) -> bool
    - File uses math.floor (not int()) for compute_position_size
    - No import from bensdorp1.db, bensdorp1.data, pandas, or numpy appears in the file
    - uv run mypy src/bensdorp1/strategy/positions.py exits 0
    - uv run ruff check src/bensdorp1/strategy/positions.py exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create test_positions.py with full unit + Hypothesis coverage and wire __init__.py</name>
  <files>tests/test_strategy/test_positions.py, src/bensdorp1/strategy/__init__.py</files>
  <read_first>
    - src/bensdorp1/strategy/positions.py — (just written in Task 1) exact function signatures and behavior to verify
    - src/bensdorp1/strategy/__init__.py — (written in Plan 04-01) needs positions exports added; replace placeholder comment with real imports
    - .planning/phases/04-strategy-logic/04-RESEARCH.md — Validation Architecture branch coverage map for positions.py; Hypothesis invariant designs 1 and 2 (complete verified code); Pitfall 5 (Hypothesis generates extreme share counts — assert int >= 0, not specific magnitude)
    - .planning/phases/04-strategy-logic/04-PATTERNS.md — test_positions.py section: module docstring, import block, unit test naming, invariant 1 and 2 structures
    - src/bensdorp1/db/__init__.py — __all__ pattern to replicate in updated __init__.py
  </read_first>
  <action>
    FILE 1 — tests/test_strategy/test_positions.py:

    Module docstring: describes STRAT-06 through STRAT-09 and TEST-03 invariants 1 and 2. Note "Pure computation — no db_engine fixture needed. All functions receive scalar float/int arguments."

    Import block:
      import math
      import pytest
      from hypothesis import given, settings
      from hypothesis import strategies as st
      from bensdorp1.strategy.positions import (compute_effective_stop, compute_initial_stop, compute_position_size, compute_trailing_stop, is_exit_triggered, update_highest_close)

    Module-level Hypothesis strategy:
      _price_st = st.floats(min_value=0.01, max_value=10000, allow_nan=False, allow_infinity=False)

    UNIT TESTS (one-line docstring per test, test_<function>_<scenario> naming per PATTERNS.md):

    test_compute_position_size_normal: cash=100000.0, prev_close=50.0. Expected: math.floor(10000.0/50.0) = 200. Assert result == 200 and isinstance(result, int).

    test_compute_position_size_zero: cash=1.0, prev_close=1000.0. Expected: math.floor(0.10/1000.0) = 0 (D-06). Assert result == 0.

    test_compute_position_size_boundary: cash=100.0, prev_close=10.0. Expected: floor(10.0/10.0) = 1. Assert result == 1.

    test_compute_initial_stop: entry_close=100.0. Expected: 93.0. Assert result == pytest.approx(93.0).

    test_compute_initial_stop_fractional: entry_close=50.75. Expected: 50.75 * 0.93. Assert result == pytest.approx(50.75 * 0.93).

    test_update_highest_close_update: current=50.0, new_close=75.0. Assert result == 75.0.

    test_update_highest_close_no_update: current=100.0, new_close=75.0. Assert result == 100.0.

    test_update_highest_close_equal: current=50.0, new_close=50.0. Assert result == 50.0.

    test_compute_trailing_stop: highest_close=100.0. Expected: 75.0. Assert result == pytest.approx(75.0).

    test_compute_effective_stop_initial_wins: initial_stop=80.0, trailing_stop=70.0. Assert result == pytest.approx(80.0).

    test_compute_effective_stop_trailing_wins: initial_stop=70.0, trailing_stop=80.0. Assert result == pytest.approx(80.0).

    test_compute_effective_stop_equal: initial_stop=75.0, trailing_stop=75.0. Assert result == pytest.approx(75.0).

    test_is_exit_triggered_true: close=50.0, effective_stop=55.0. Assert result is True.

    test_is_exit_triggered_false: close=60.0, effective_stop=55.0. Assert result is False.

    test_is_exit_triggered_boundary: close=55.0, effective_stop=55.0 (exactly equal). Assert result is True (close <= effective_stop includes equality).

    HYPOTHESIS PROPERTY TESTS (after unit tests, preceded by # --- Hypothesis property tests --- comment):

    test_effective_stop_ge_initial (Invariant 1):
      @given(_price_st, _price_st)
      @settings(max_examples=500)
      Docstring: "Effective stop is always >= initial stop regardless of trailing stop value."
      result = compute_effective_stop(initial, trailing). Assert result >= initial.

    test_trailing_stop_monotonic (Invariant 2):
      @given(st.lists(_price_st, min_size=2))
      @settings(max_examples=500)
      Docstring: "Trailing stop never decreases when new closes are applied sequentially."
      highest = 0.0, stops: list[float] = [].
      For each c in closes: highest = update_highest_close(highest, c); stops.append(compute_trailing_stop(highest)).
      For i in range(1, len(stops)): assert stops[i] >= stops[i-1], f"...decreased at index {i}...".

    FILE 2 — src/bensdorp1/strategy/__init__.py (update):

    Replace the placeholder comment for positions exports with real imports:
      from bensdorp1.strategy.positions import (compute_effective_stop, compute_initial_stop, compute_position_size, compute_trailing_stop, is_exit_triggered, update_highest_close)

    Add all six positions names to __all__ alphabetically merged with screening names:
    __all__ = [
        "Candidate",
        "compute_effective_stop",
        "compute_initial_stop",
        "compute_position_size",
        "compute_trailing_stop",
        "is_exit_triggered",
        "liquidity_filter",
        "momentum_filter",
        "rank_candidates",
        "regime_filter",
        "update_highest_close",
    ]

    Hypothesis Pitfall 5 note: position size tests must assert isinstance(result, int) and result >= 0, not a specific magnitude — Hypothesis may generate very small prev_close values yielding large share counts. This is expected behavior, not a bug.
  </action>
  <verify>
    <automated>uv run pytest tests/test_strategy/test_positions.py -x -q && uv run pytest tests/test_strategy/ --cov=bensdorp1.strategy --cov-report=term-missing</automated>
  </verify>
  <done>All test_positions.py tests pass; combined strategy/ coverage >= 95%; __init__.py exports all 11 names</done>
  <acceptance_criteria>
    - uv run pytest tests/test_strategy/test_positions.py -x -q exits 0 with all tests passing
    - Test file contains at least: test_compute_position_size_normal, test_compute_position_size_zero, test_compute_initial_stop, test_update_highest_close_update, test_update_highest_close_no_update, test_compute_trailing_stop, test_compute_effective_stop_initial_wins, test_compute_effective_stop_trailing_wins, test_is_exit_triggered_true, test_is_exit_triggered_false, test_is_exit_triggered_boundary, test_effective_stop_ge_initial, test_trailing_stop_monotonic
    - test_is_exit_triggered_boundary asserts True (equality triggers exit)
    - uv run pytest tests/test_strategy/ --cov=bensdorp1.strategy --cov-report=term-missing shows >= 95% for both screening.py and positions.py
    - src/bensdorp1/strategy/__init__.py __all__ contains all 11 names including "Candidate" and all 6 positions function names
    - uv run mypy src/bensdorp1/strategy/__init__.py exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 7/8 -> positions.py | Scalars (float, int) passed from DB rows; no user input reaches these functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | compute_position_size inputs | accept | Inputs are float scalars from DB (entry_close, available_cash). No user-controlled data path. Phase 7 is responsible for validating DB values before calling. |
| T-04-05 | Denial of Service | compute_position_size with prev_close near 0 | accept | At runtime, prev_close comes from price_daily.close (yfinance data), which is positive. Hypothesis tests with min_value=0.01 cover the low-value boundary. Division-by-zero would require price_daily.close == 0.0, which yfinance does not produce. |
| T-04-06 | Information Disclosure | Stop levels in Candidate or scalar returns | accept | No secrets. Stop levels are derived from publicly available price data. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. All deps are pre-existing. |

**Security confirmation:** positions.py accepts only float/int scalar arguments. No DataFrames, no I/O, no user input. The ASVS L1 perimeter is enforced at Phase 7 before any call into strategy/. STRAT-10 (exit triggers persist) is a Phase 7 responsibility — positions.py provides only the is_exit_triggered predicate.
</threat_model>

<verification>
Full gate for Plan 04-02:

1. uv run pytest tests/test_strategy/ -x -q (all tests pass — screening + positions)
2. uv run mypy src/bensdorp1/strategy/ (0 errors — all three files)
3. uv run ruff check src/bensdorp1/strategy/ (0 errors)
4. uv run pytest tests/test_strategy/ --cov=bensdorp1.strategy --cov-report=term-missing (>= 95% on both screening.py and positions.py)
</verification>

<success_criteria>
1. positions.py implements all 6 functions using only stdlib math; no pandas, numpy, or project imports
2. compute_position_size uses math.floor and returns 0 when result < 1 (D-06)
3. is_exit_triggered returns True for close <= effective_stop (boundary inclusive)
4. Hypothesis invariant 1 (effective_stop >= initial_stop) passes with 500 examples
5. Hypothesis invariant 2 (trailing stop monotonic) passes with 500 examples
6. strategy/__init__.py exports all 11 public names with explicit __all__ (Candidate + 4 screening + 6 positions)
7. combined strategy/ coverage >= 95%
</success_criteria>

<output>
Create .planning/phases/04-strategy-logic/04-02-SUMMARY.md when done
</output>

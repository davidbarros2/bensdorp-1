---
phase: 09-consultation-commands
plan: 03
type: execute
wave: 1
depends_on:
  - 09-01
files_modified:
  - src/bensdorp1/commands/config.py
  - tests/test_commands/test_config.py
autonomous: true
requirements:
  - CMD-12

must_haves:
  truths:
    - "`bensdorp1 config` prints cash, data directory, timezone, and version in a key-value block"
    - "Cash field shows formatted price ($X,XXX.XX) when config row exists, or `Not configured` when it does not"
    - "Version is read at runtime from importlib.metadata.version(\"bensdorp1\") — never hard-coded"
  artifacts:
    - path: "src/bensdorp1/commands/config.py"
      provides: "CMD-12 implementation"
      contains: "render_kv_block"
    - path: "tests/test_commands/test_config.py"
      provides: "CMD-12 integration test (1 scenario filled in)"
      contains: "runner.invoke(app, [\"config\"])"
  key_links:
    - from: "src/bensdorp1/commands/config.py"
      to: "config_table.c.value"
      via: "SELECT WHERE key == 'available_cash'"
      pattern: "config_table\\.c\\.key == \"available_cash\""
    - from: "src/bensdorp1/commands/config.py"
      to: "importlib.metadata.version"
      via: "pkg_version(\"bensdorp1\")"
      pattern: "pkg_version|version\\(\"bensdorp1\"\\)"
---

<objective>
Replace the `config` command stub with a full CMD-12 implementation: read cash from the `config` table (key = `"available_cash"`), display alongside `DATA_DIR`, `USER_TZ` city label, and runtime package version. Fill in the 1 Wave 0 stub in `tests/test_commands/test_config.py`.

Purpose: Give users a single-glance view of system configuration without inspecting files or environment variables.

Output: Working `config` command + 1 passing CliRunner test.

**Critical note:** The config table key for cash is `"available_cash"` (set by `init.py` line 74), NOT `"cash"` as RESEARCH.md/PATTERNS.md document. Implementation MUST use `"available_cash"`. This same correction applies to plan 09-06 (cash command).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-consultation-commands/09-CONTEXT.md
@.planning/phases/09-consultation-commands/09-RESEARCH.md
@.planning/phases/09-consultation-commands/09-PATTERNS.md

<interfaces>
<!-- Pre-existing symbols this plan consumes. -->

From bensdorp1.config:
- `DATA_DIR: Path`
- `USER_TZ: ZoneInfo` — derive city as `USER_TZ.key.split("/")[-1]` (e.g. "Lisbon" from "Europe/Lisbon")

From bensdorp1.db (public):
- `get_engine(path)`, `run_migrations(engine)`

From bensdorp1.db.schema:
- `config: Table` with columns `key (PK TEXT), value (TEXT nullable), updated_at (DATETIME tz-aware)`
- MUST import as `from bensdorp1.db.schema import config as config_table` (Pitfall 1)

From bensdorp1.ui:
- `format_price(value: float) -> str`
- `render_kv_block(data: dict[str, str], console: Console, indent: str = "") -> None`

From stdlib:
- `from importlib.metadata import version as pkg_version`

Config key for cash (VERIFIED in src/bensdorp1/commands/init.py line 74):
- `key="available_cash"` — value stored as `f"{amount:.2f}"`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement config.py + fill in 1 test stub</name>
  <files>src/bensdorp1/commands/config.py, tests/test_commands/test_config.py</files>
  <read_first>
    - src/bensdorp1/commands/config.py (current 11-line stub — replace entirely)
    - src/bensdorp1/commands/init.py (lines 65-86 for `_store_cash` — confirms key is "available_cash" and value is f"{amount:.2f}")
    - src/bensdorp1/commands/buy.py (lines 1-48 for DB entry triad and Typer registration pattern)
    - src/bensdorp1/db/schema.py (lines 22-28 for config table definition)
    - src/bensdorp1/config.py (DATA_DIR, USER_TZ, MARKET_TZ — all 11 lines)
    - src/bensdorp1/ui/styles.py (lines 39-42 for format_price; lines 146-163 for render_kv_block)
    - src/bensdorp1/ui/__init__.py (confirms format_price and render_kv_block are exported)
    - tests/test_commands/test_buy.py (lines 1-41 for runner + MagicMock pattern; lines 131-180 for db_engine + insert pattern)
    - tests/test_commands/test_config.py (Wave 0 stub from plan 09-01)
    - pyproject.toml (confirms package name is "bensdorp1" for importlib.metadata)
    - .planning/phases/09-consultation-commands/09-PATTERNS.md (sections "src/bensdorp1/commands/config.py", SP-1, SP-8)
  </read_first>
  <behavior>
    - With no cash row in config: render_kv_block shows `Cash: Not configured`, `Data directory: <DATA_DIR>`, `Timezone: <city> (BENSDORP1_USER_TZ)`, `Version: <pkg_version>`; exit 0
    - With cash row present (value=`"45000.00"`): render_kv_block shows `Cash: $45,000.00` and the other three fields; exit 0
    - Version field comes from `pkg_version("bensdorp1")` (importlib.metadata); never a hard-coded string
    - Timezone city is `USER_TZ.key.split("/")[-1]` (e.g. "Lisbon"); label always shows `" (BENSDORP1_USER_TZ)"` suffix
    - Command never writes to DB, never calls create_backup or log_event
  </behavior>
  <action>
    Replace src/bensdorp1/commands/config.py wholesale. Imports: `from importlib.metadata import version as pkg_version`; standard `typer`, `Console` from rich.console, `select` from sqlalchemy; `app` from bensdorp1._app; `DATA_DIR, USER_TZ` from bensdorp1.config; `get_engine, run_migrations` from bensdorp1.db; `from bensdorp1.db.schema import config as config_table` (Pitfall 1 — alias mandatory); `format_price, render_kv_block` from bensdorp1.ui. Keep `@app.command(rich_help_panel="System")` and signature `def config() -> None:`. Body: DB entry triad; `engine.connect()` context running `select(config_table.c.value).where(config_table.c.key == "available_cash")`. Compute `cash_str = format_price(float(cash_row.value)) if cash_row is not None and cash_row.value is not None else "Not configured"`. Build dict in this exact key order: `{"Cash": cash_str, "Data directory": str(DATA_DIR), "Timezone": f"{USER_TZ.key.split('/')[-1]} (BENSDORP1_USER_TZ)", "Version": pkg_version("bensdorp1")}`. Call `render_kv_block(data, console)`. End with `raise typer.Exit()`.
    Fill in the 1 Wave 0 stub in tests/test_commands/test_config.py. Use the db_engine fixture; INSERT one config row: `key="available_cash"`, `value="45000.00"`, `updated_at=datetime.now(UTC)`. Patch `bensdorp1.commands.config.DATA_DIR` (use tmp_path), `bensdorp1.commands.config.get_engine` (return db_engine), `bensdorp1.commands.config.run_migrations`. Invoke `runner.invoke(app, ["config"])`. Assert exit_code == 0 and all four labels appear in result.output: `"Cash:"`, `"$45,000.00"`, `"Data directory:"`, `"Timezone:"`, `"Lisbon"` (or whatever USER_TZ city is — `"BENSDORP1_USER_TZ"` is universally present in the label so assert that substring), `"Version:"`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_commands/test_config.py -x -q && uv run mypy --strict src/bensdorp1/commands/config.py && uv run ruff check src/bensdorp1/commands/config.py tests/test_commands/test_config.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/bensdorp1/commands/config.py` no longer contains "Not yet implemented"
    - File contains `from bensdorp1.db.schema import config as config_table` (alias mandatory — Pitfall 1, verifiable by grep)
    - File contains `config_table.c.key == "available_cash"` (NOT `"cash"`)
    - File contains `pkg_version("bensdorp1")` (verifiable by grep)
    - File contains `render_kv_block(` (verifiable by grep)
    - File contains `USER_TZ.key.split("/")[-1]` (verifiable by grep)
    - `tests/test_commands/test_config.py` has zero `pytest.skip` calls remaining
    - `uv run pytest tests/test_commands/test_config.py -x -q` exits 0 with 1 passing test
    - `uv run mypy --strict src/bensdorp1/commands/config.py` exits 0
    - `uv run ruff check src/bensdorp1/commands/config.py tests/test_commands/test_config.py` exits 0
    - Behavior: invoking `bensdorp1 config` prints the four labeled key-value pairs aligned by `render_kv_block`
  </acceptance_criteria>
  <done>config command works for both configured and unconfigured states; 1 CliRunner test passes; mypy strict + ruff clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env var `BENSDORP1_HOME` → DATA_DIR | Resolved at import time; printed verbatim. Single-user CLI, no traversal risk in display. |
| SQLite config row → terminal | Cash value parsed with `float()`; rendered via `format_price`; markup-safe (render_kv_block sets markup=False internally) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-T1 | Tampering | config.value → float | mitigate | Wrap `float(cash_row.value)` in standard parse; non-numeric value would raise ValueError — acceptable failure surface (data was set by init, not by user input at command time) |
| T-09-03-T2 | Tampering | DATA_DIR display | accept | DATA_DIR comes from env var resolved at import; render_kv_block uses markup=False — no injection vector |
| T-09-03-T3 | Tampering | SQL injection | mitigate | Parameterized SQLAlchemy select |
| T-09-03-I1 | Information Disclosure | cash + directory in output | accept | Single-user CLI; user is reading their own config |
| T-09-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- `uv run pytest tests/test_commands/test_config.py -x -q` → 1 passed
- `uv run mypy --strict src/bensdorp1/commands/config.py` → 0 errors
- `uv run ruff check src/bensdorp1/commands/config.py tests/test_commands/test_config.py` → 0 errors
- Visible behavior: `bensdorp1 config` prints four aligned key-value rows
</verification>

<success_criteria>
- CMD-12 fully implemented; key alias `config_table` used; cash key resolved as `"available_cash"`
- 1 Wave 0 stub filled in and passing
- No regression in other test files
</success_criteria>

<output>
Create `.planning/phases/09-consultation-commands/09-03-SUMMARY.md` when done
</output>

---
phase: 09-consultation-commands
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_commands/test_last.py
  - tests/test_commands/test_history.py
  - tests/test_commands/test_portfolio.py
  - tests/test_commands/test_detail.py
  - tests/test_commands/test_cash.py
  - tests/test_commands/test_config.py
  - tests/test_commands/test_audit.py
autonomous: true
requirements:
  - CMD-04
  - CMD-05
  - CMD-09
  - CMD-10
  - CMD-11
  - CMD-12
  - CMD-13

must_haves:
  truths:
    - "All 7 test files exist under tests/test_commands/"
    - "Each test file contains at least one failing/skip stub test per CMD requirement"
    - "Running `uv run pytest tests/test_commands/ -x -q` lists the new stubs (does not crash collection)"
  artifacts:
    - path: "tests/test_commands/test_last.py"
      provides: "Wave 0 stub for CMD-04 (2 scenarios)"
      contains: "import"
    - path: "tests/test_commands/test_history.py"
      provides: "Wave 0 stub for CMD-05 (4 scenarios)"
      contains: "import"
    - path: "tests/test_commands/test_portfolio.py"
      provides: "Wave 0 stub for CMD-09 (3 scenarios)"
      contains: "import"
    - path: "tests/test_commands/test_detail.py"
      provides: "Wave 0 stub for CMD-10 (3 scenarios)"
      contains: "import"
    - path: "tests/test_commands/test_cash.py"
      provides: "Wave 0 stub for CMD-11 (5 scenarios)"
      contains: "import"
    - path: "tests/test_commands/test_config.py"
      provides: "Wave 0 stub for CMD-12 (1 scenario)"
      contains: "import"
    - path: "tests/test_commands/test_audit.py"
      provides: "Wave 0 stub for CMD-13 (6 scenarios)"
      contains: "import"
  key_links:
    - from: "tests/test_commands/test_*.py"
      to: "bensdorp1.cli.app"
      via: "from bensdorp1.cli import app"
      pattern: "from bensdorp1.cli import app"
    - from: "tests/test_commands/test_*.py"
      to: "typer.testing.CliRunner"
      via: "module-level runner instance"
      pattern: "runner = CliRunner\\(\\)"
---

<objective>
Create Wave 0 test scaffolds for all 7 consultation commands (CMD-04, CMD-05, CMD-09, CMD-10, CMD-11, CMD-12, CMD-13). Each test file is a real pytest module that imports the existing Typer app, defines a module-level `CliRunner()`, and contains one stub test per planned scenario marked `pytest.skip("Wave 0 stub — filled in by plan 09-NN")` so collection succeeds and downstream plans can fill in real assertions.

Purpose: Establish the test surface up-front so every implementation plan in Waves 1-3 can fill in stubs alongside the command code (per RESEARCH.md Validation Architecture "Wave 0 Gaps" section). No production code is touched in this plan.

Output: 7 new test files with import scaffolding, runner instance, and skip-stub tests covering every behavior listed in RESEARCH.md "Phase Requirements → Test Map".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-consultation-commands/09-CONTEXT.md
@.planning/phases/09-consultation-commands/09-RESEARCH.md
@.planning/phases/09-consultation-commands/09-PATTERNS.md

<interfaces>
<!-- Test infrastructure already established by Phase 8. New tests must follow it. -->

From tests/conftest.py:
- `db_engine` fixture: yields a fresh file-based SQLite Engine per test (tmp_path),
  resets the cached engine, runs `metadata.create_all`, disposes on teardown.
- `record_console` fixture: Console(record=True, width=80) — used by UI tests, not needed here.

From bensdorp1.cli:
- `app: typer.Typer` — the wired Typer application; CliRunner invokes via `runner.invoke(app, ["<cmd>", ...])`.

From bensdorp1.db.schema:
- Tables: `positions, scans, scan_candidates, scan_exit_triggers, audit_log, config, price_daily, constituents_cache`
- Column names verified in `src/bensdorp1/db/schema.py` (positions has `entry_close` NOT `entry_price`).

Established test import block (test_buy.py lines 1-21):
```
from datetime import UTC, datetime
from pathlib import Path
from unittest.mock import MagicMock, patch
import pytest
from sqlalchemy import insert, select
from sqlalchemy.engine import Engine
from typer.testing import CliRunner
from bensdorp1.cli import app
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 Wave 0 test stub files</name>
  <files>tests/test_commands/test_last.py, tests/test_commands/test_history.py, tests/test_commands/test_portfolio.py, tests/test_commands/test_detail.py, tests/test_commands/test_cash.py, tests/test_commands/test_config.py, tests/test_commands/test_audit.py</files>
  <read_first>
    - tests/test_commands/test_buy.py  (canonical analog for imports, runner instance, and patch-block pattern)
    - tests/test_commands/test_sell.py (seeded-DB analog with insert() of fixture rows)
    - tests/conftest.py (db_engine + record_console fixtures available to all tests)
    - .planning/phases/09-consultation-commands/09-RESEARCH.md  (sections "Phase Requirements → Test Map" and "Wave 0 Gaps" — list of every scenario per command)
    - .planning/phases/09-consultation-commands/09-PATTERNS.md  (Test files section + SP-10 patch pattern)
    - src/bensdorp1/cli.py (confirms `app` symbol exists)
  </read_first>
  <action>
    Create each of the 7 test files using the exact import block established in tests/test_commands/test_buy.py (lines 1-21). At module level, instantiate `runner = CliRunner()`. For each scenario listed in RESEARCH.md "Phase Requirements → Test Map" for that command, define a stub function named after the scenario (e.g. `def test_empty_state_no_scans(tmp_path: Path) -> None`). Each stub body MUST be exactly one line: `pytest.skip("Wave 0 stub — filled in by plan 09-NN")` where 09-NN is the matching implementation plan (last→09-02, config→09-03, history→09-04, audit→09-05, cash→09-06, portfolio→09-07, detail→09-08). Use `Path`, `Engine`, `MagicMock`, `patch`, `insert`, `select` in the imports even if individual stubs do not use them yet (kept to match the canonical test_buy.py import block and to avoid lint churn when stubs are filled in). Per RESEARCH.md required scenario counts: test_last.py 2 scenarios, test_history.py 4 scenarios, test_portfolio.py 3 scenarios, test_detail.py 3 scenarios, test_cash.py 5 scenarios, test_config.py 1 scenario, test_audit.py 6 scenarios. The exact scenario names match the rows in the "Phase Requirements → Test Map" table.
    Add `# noqa: F401` to any unused imports in the stubs so ruff passes; alternative: include `pytest.skip(...)` only and use `_ = (MagicMock, patch, insert, select, datetime, UTC, Engine)` placeholder, but prefer noqa. Each file must also include a module docstring of the form: `"""Tests for commands/<cmd>.py — CMD-NN scenarios."""`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_commands/ --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All 7 test files exist at `tests/test_commands/test_{last,history,portfolio,detail,cash,config,audit}.py`
    - Each file has a module docstring referencing its CMD-NN requirement
    - Each file contains `runner = CliRunner()` at module scope
    - Each file imports `app` from `bensdorp1.cli` (verifiable by `grep -n "from bensdorp1.cli import app" tests/test_commands/test_last.py tests/test_commands/test_history.py tests/test_commands/test_portfolio.py tests/test_commands/test_detail.py tests/test_commands/test_cash.py tests/test_commands/test_config.py tests/test_commands/test_audit.py | wc -l` returns 7)
    - Stub counts match: 2 in test_last, 4 in test_history, 3 in test_portfolio, 3 in test_detail, 5 in test_cash, 1 in test_config, 6 in test_audit (verifiable by `grep -c "^def test_" tests/test_commands/test_<file>.py`)
    - Every stub body contains exactly `pytest.skip("Wave 0 stub` (verifiable by `grep -c "pytest.skip(\"Wave 0 stub" tests/test_commands/test_*.py` returning ≥24)
    - `uv run pytest tests/test_commands/ --collect-only -q` succeeds with no collection errors and lists all stubs
    - `uv run pytest tests/test_commands/test_last.py tests/test_commands/test_history.py tests/test_commands/test_portfolio.py tests/test_commands/test_detail.py tests/test_commands/test_cash.py tests/test_commands/test_config.py tests/test_commands/test_audit.py -q` exits 0 with all tests reported as SKIPPED
    - `uv run ruff check tests/test_commands/` exits 0
    - `uv run mypy --strict tests/test_commands/test_last.py tests/test_commands/test_history.py tests/test_commands/test_portfolio.py tests/test_commands/test_detail.py tests/test_commands/test_cash.py tests/test_commands/test_config.py tests/test_commands/test_audit.py` exits 0
  </acceptance_criteria>
  <done>All 7 stub files created with correct scenario counts, all stubs skip cleanly, ruff + mypy strict pass on the new files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → SQLite tmp file | Wave 0 stubs do not exercise this; downstream plans must use parameterized queries (already enforced project-wide) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-T1 | Tampering | Test stub bodies | accept | Stubs only call `pytest.skip()`; no DB or filesystem mutation. Downstream plans add real assertions inside the same skip placeholder. |
| T-09-01-D1 | Denial of Service | pytest collection | mitigate | Stubs return immediately via `pytest.skip()`; no long-running setup at collection time. Verified by `--collect-only` succeeding. |
| T-09-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced; RESEARCH.md "Package Legitimacy Audit" confirms zero new deps. |
</threat_model>

<verification>
- `uv run pytest tests/test_commands/ --collect-only -q` enumerates all 24+ new stubs
- `uv run pytest tests/test_commands/test_{last,history,portfolio,detail,cash,config,audit}.py -q` reports all as SKIPPED, exit 0
- `uv run ruff check tests/test_commands/` exits 0
- `uv run mypy --strict tests/test_commands/test_{last,history,portfolio,detail,cash,config,audit}.py` exits 0
</verification>

<success_criteria>
- 7 stub test files exist, collectible, lint+type clean
- Stub count per file matches RESEARCH.md Validation Architecture table
- No production source files modified by this plan
- Downstream implementation plans (09-02 through 09-08) can replace each `pytest.skip(...)` call with real assertions
</success_criteria>

<output>
Create `.planning/phases/09-consultation-commands/09-01-SUMMARY.md` when done
</output>

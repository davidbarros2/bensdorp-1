---
phase: 09-consultation-commands
plan: 08
type: execute
wave: 3
depends_on:
  - 09-01
files_modified:
  - src/bensdorp1/commands/detail.py
  - tests/test_commands/test_detail.py
autonomous: true
requirements:
  - CMD-10

must_haves:
  truths:
    - "`bensdorp1 detail SYMBOL` exits code 1 with an error referencing `bensdorp1 audit --symbol` when there is no open position for SYMBOL"
    - "On success, prints a position summary kv-block followed by a per-day stop-history table"
    - "Stop history is reconstructed by walking NYSE trading days from entry_date + 1 to the latest price_daily.trade_date for SYMBOL"
    - "Per-day running_max = max(entry_close, max(closes from entry_date+1 to D))"
    - "Per-day trailing_stop = running_max * 0.75; effective_stop = max(initial_stop, trailing_stop)"
    - "Days with no price_daily row are skipped (Pitfall 4 — data gap), not rendered with zero"
    - "No splits section in Phase 9 (D-02 — deferred to Phase 11)"
  artifacts:
    - path: "src/bensdorp1/commands/detail.py"
      provides: "CMD-10 implementation with per-day stop history walk"
      contains: "running_max = pos_row.entry_close"
    - path: "tests/test_commands/test_detail.py"
      provides: "CMD-10 integration tests (3 scenarios filled in)"
      contains: "runner.invoke(app, [\"detail\""
  key_links:
    - from: "src/bensdorp1/commands/detail.py"
      to: "positions WHERE symbol = ? AND closed_at IS NULL"
      via: "open-position guard; exit 1 + actions hint if not found"
      pattern: "print_error\\("
    - from: "src/bensdorp1/commands/detail.py"
      to: "get_trading_days(entry_date+1, last_price_date)"
      via: "walk every trading day, skip gaps"
      pattern: "get_trading_days\\("
    - from: "src/bensdorp1/commands/detail.py"
      to: "ui.render_kv_block + ui.render_table"
      via: "summary block + stop history table"
      pattern: "render_kv_block\\(|render_table\\("
---

<objective>
Replace the `detail` command stub with a full CMD-10 implementation: validate that SYMBOL refers to an open position (exit 1 with hint to `bensdorp1 audit --symbol` if not). Render a position-summary kv-block, then walk NYSE trading days from `entry_date + 1` to the latest `price_daily.trade_date` for SYMBOL and emit a stop-history table of `(Date, Close, Highest close, Trailing stop, Effective stop)` for each day with available price data. Fill in the 3 Wave 0 stubs in `tests/test_commands/test_detail.py`.

Purpose: Give the user the full day-by-day stop progression of a single open position so they can audit why a trailing stop is where it is.

Output: Working `detail` command + 3 passing CliRunner tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-consultation-commands/09-CONTEXT.md
@.planning/phases/09-consultation-commands/09-RESEARCH.md
@.planning/phases/09-consultation-commands/09-PATTERNS.md
@.planning/phases/07-scan-command/07-CONTEXT.md

<interfaces>
From bensdorp1._app:
- `app: typer.Typer`

From bensdorp1.config:
- `DATA_DIR: Path`

From bensdorp1.data (public):
- `get_trading_days(start: date, end: date) -> list[Timestamp]` — NYSE calendar; both ends inclusive

From bensdorp1.db (public):
- `get_engine, run_migrations`

From bensdorp1.db.schema:
- `positions: Table` (full column list — see plan 09-07)
- `price_daily: Table` columns: `id, symbol, trade_date (DateTime tz-aware), close, volume`

From bensdorp1.ui:
- `format_date, format_price, print_error, print_info, render_kv_block, render_table`

Stop math (Phase 4 + D-01):
- `running_max` starts at `entry_close` (initial stop is based on entry_close)
- For each trading day after entry, if `price_daily` has a row: `running_max = max(running_max, day_close); trailing_stop = running_max * 0.75; effective_stop = max(initial_stop, trailing_stop)`
- If `price_daily` has no row for a trading day: skip (Pitfall 4 — gap)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement detail.py + fill in 3 test stubs</name>
  <files>src/bensdorp1/commands/detail.py, tests/test_commands/test_detail.py</files>
  <read_first>
    - src/bensdorp1/commands/detail.py (current 11-line stub — replace entirely; note current signature has NO SYMBOL arg)
    - src/bensdorp1/commands/sell.py (full file — open-position validation pattern, print_error+actions+exit code 1)
    - src/bensdorp1/commands/buy.py (lines 1-100 imports + DB triad)
    - src/bensdorp1/commands/fix.py (render_kv_block usage and section separators via Text())
    - src/bensdorp1/db/schema.py (positions + price_daily columns)
    - src/bensdorp1/data/calendar.py (get_trading_days signature and return type — pandas Timestamps)
    - src/bensdorp1/data/__init__.py (confirms get_trading_days exported)
    - src/bensdorp1/ui/styles.py (format_date, format_price, render_kv_block)
    - src/bensdorp1/ui/__init__.py (confirms all needed names exported)
    - tests/test_commands/test_buy.py + tests/test_commands/test_sell.py (mock + db_engine + insert price_daily patterns)
    - tests/test_commands/test_detail.py (Wave 0 stubs from plan 09-01)
    - .planning/phases/09-consultation-commands/09-CONTEXT.md (D-01 reconstruction rules; D-02 omit splits; D-10 error message wording)
    - .planning/phases/09-consultation-commands/09-PATTERNS.md (section "src/bensdorp1/commands/detail.py" + SP-1, SP-4, SP-9)
    - .planning/phases/09-consultation-commands/09-RESEARCH.md (Pattern 3 detail per-day walk + Pitfall 4 price gaps)
    - .planning/phases/07-scan-command/07-CONTEXT.md (D-18 effective_stop at read time)
  </read_first>
  <behavior>
    - Signature: `def detail(symbol: str = typer.Argument(..., help="Ticker symbol of an open position.")) -> None`
    - Open-position lookup: `select(positions).where((positions.c.symbol == symbol.upper()) & (positions.c.closed_at == None))  # noqa: E711` — fetchone
    - If pos_row is None → `print_error(f"No open position for {symbol.upper()}.", actions=[f"To see history of a closed position: bensdorp1 audit --symbol {symbol.upper()}"])` + `raise typer.Exit(code=1)`
    - Summary kv-block via render_kv_block: `{"Symbol": pos_row.symbol, "Entry date": format_date(pos_row.entry_date.date()), "Entry price": format_price(pos_row.entry_close), "Shares": str(pos_row.shares), "Initial stop": format_price(pos_row.initial_stop)}` then print blank line
    - Section header: `console.print(Text("Stop history"))` then `console.print(Text("-" * 12))` (per SP-9)
    - Fetch all price_daily rows: `select(price_daily.c.trade_date, price_daily.c.close).where((price_daily.c.symbol == symbol.upper()) & (price_daily.c.trade_date > pos_row.entry_date)).order_by(price_daily.c.trade_date.asc())`
    - Build `close_map: dict[date, float] = {r.trade_date.date(): r.close for r in price_rows}`
    - If `close_map` is empty → `print_info("No price history available yet for this position.")` + `raise typer.Exit()` (exit 0 — open position with no data is a valid edge case, e.g. entry today)
    - `last_price_date = max(close_map.keys())`
    - `trading_days = get_trading_days(pos_row.entry_date.date() + timedelta(days=1), last_price_date)`
    - Walk: `running_max = pos_row.entry_close`. For each day in trading_days: `day_date = day.date()`; if `day_date not in close_map`: continue (Pitfall 4). Otherwise: `day_close = close_map[day_date]; running_max = max(running_max, day_close); trailing_stop = running_max * 0.75; effective_stop = max(pos_row.initial_stop, trailing_stop)`; append `[format_date(day_date), format_price(day_close), format_price(running_max), format_price(trailing_stop), format_price(effective_stop)]`
    - render_table columns: `[("Date","left"),("Close","right"),("Highest close","right"),("Trailing stop","right"),("Effective stop","right")]`
    - NO splits section in Phase 9 (D-02 — deferred)
  </behavior>
  <action>
    Replace src/bensdorp1/commands/detail.py wholesale. Imports: `import datetime`; `from datetime import UTC, date, timedelta`; `from typing import Any`; `typer`; `Console` from rich.console; `Text` from rich.text; `select` from sqlalchemy; `app` from bensdorp1._app; `DATA_DIR` from bensdorp1.config; `get_trading_days` from bensdorp1.data; `get_engine, run_migrations` from bensdorp1.db; `positions, price_daily` from bensdorp1.db.schema; `format_date, format_price, print_error, print_info, render_kv_block, render_table` from bensdorp1.ui. Register `@app.command(rich_help_panel="Positions")`; signature in behavior block. Body: DB entry triad. Open-position lookup (SP-4). On None → print_error with the exact actions hint above + raise typer.Exit(code=1). Render the 5-key summary block per behavior. Print blank, print section header via Text(), print Text("-"*12), print blank. Fetch all price_daily rows for SYMBOL where `trade_date > pos_row.entry_date`. Build close_map. Empty-map guard. Compute `last_price_date` and `trading_days`. Walk loop with `continue` on gap, accumulating row entries. Call render_table.
    Fill in the 3 Wave 0 stubs in tests/test_commands/test_detail.py.
    Scenario 1 (no-open-position): db_engine with no positions for NVDA; patch `bensdorp1.commands.detail.DATA_DIR/get_engine/run_migrations`; invoke `["detail", "NVDA"]`; assert exit_code == 1 and "No open position for NVDA" in output and "audit --symbol NVDA" in output.
    Scenario 2 (happy-path): db_engine; INSERT open position for "AAPL" with `entry_date=datetime(2026,3,15,tzinfo=UTC), entry_close=180.00, shares=10, initial_stop=167.40, highest_close=185.00, trailing_stop=138.75`; INSERT three price_daily rows for AAPL on consecutive trading days after entry (e.g. 2026-03-16/17/18 with closes 182.50, 185.00, 183.20); invoke `["detail", "AAPL"]`; assert exit 0; assert "AAPL" in output; assert "Stop history" in output; assert "$180.00" in output (entry); assert at least three date strings (2026-03-16/17/18) and their closes appear; verify highest-close progression: row for 2026-03-16 shows running_max=$182.50, row for 2026-03-17 shows running_max=$185.00, row for 2026-03-18 still shows running_max=$185.00; verify `format_price(185.00 * 0.75)` = "$138.75" appears for 2026-03-17 trailing_stop column.
    Scenario 3 (no-price-history): db_engine; INSERT open position for "MSFT" with entry_date = today (no price rows after entry); invoke `["detail", "MSFT"]`; assert exit 0 and "No price history available" in output and no exception.
  </action>
  <verify>
    <automated>uv run pytest tests/test_commands/test_detail.py -x -q && uv run mypy --strict src/bensdorp1/commands/detail.py && uv run ruff check src/bensdorp1/commands/detail.py tests/test_commands/test_detail.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/bensdorp1/commands/detail.py` no longer contains "Not yet implemented"
    - Command signature accepts a required `symbol: str = typer.Argument(...)` (verifiable by grep)
    - File contains `print_error(` followed by the actions list mentioning `audit --symbol` (verifiable by grep)
    - File contains `raise typer.Exit(code=1)` on the no-position branch
    - File contains `positions.c.closed_at == None  # noqa: E711` (SP-4)
    - File contains `running_max = pos_row.entry_close` (initial-state line — D-01)
    - File contains `running_max * 0.75` for trailing_stop computation (verifiable by grep)
    - File contains `max(pos_row.initial_stop, trailing_stop)` for effective_stop (verifiable by grep)
    - File contains `get_trading_days(pos_row.entry_date.date() + timedelta(days=1)` (verifiable by grep)
    - File contains `Text("Stop history")` and `Text("-" * 12)` (SP-9, verifiable by grep)
    - File does NOT contain the word "split" or "splits" in any user-visible string (D-02 — no placeholder)
    - 5-column render_table call uses exact headers: Date, Close, Highest close, Trailing stop, Effective stop
    - `tests/test_commands/test_detail.py` has zero `pytest.skip` calls remaining
    - `uv run pytest tests/test_commands/test_detail.py -x -q` exits 0 with 3 passing tests
    - `uv run mypy --strict src/bensdorp1/commands/detail.py` exits 0
    - `uv run ruff check src/bensdorp1/commands/detail.py tests/test_commands/test_detail.py` exits 0
  </acceptance_criteria>
  <done>detail command renders the summary + stop-history table; gap-handling and no-history paths covered; 3 CliRunner tests pass; mypy strict + ruff clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg SYMBOL → SQL | parameterized; `.upper()` normalized |
| price_daily rows → in-memory dict | dict lookup; no eval |
| Text("Stop history") → terminal | SP-9 wrapping prevents markup injection from literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08-T1 | Tampering | SYMBOL SQL injection | mitigate | Parameterized `positions.c.symbol == symbol.upper()`; no string interpolation |
| T-09-08-T2 | Tampering | render_table cells | mitigate | Cells pre-formatted via format_price/format_date; render_table is markup-safe |
| T-09-08-D1 | Denial of Service | unbounded trading_days loop | mitigate | Bounded by `last_price_date - entry_date`; price_daily continuous coverage by init (~220 days max in practice); negligible CPU |
| T-09-08-V1 | Tampering | unknown SYMBOL | mitigate | Open-position lookup returns None → print_error + exit 1 (no crash) |
| T-09-08-V2 | Tampering | price gaps | mitigate | `if day_date not in close_map: continue` — never KeyError, never zero-close render |
| T-09-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- `uv run pytest tests/test_commands/test_detail.py -x -q` → 3 passed
- `uv run mypy --strict src/bensdorp1/commands/detail.py` → 0 errors
- `uv run ruff check src/bensdorp1/commands/detail.py tests/test_commands/test_detail.py` → 0 errors
- Behavior: `bensdorp1 detail AAPL` on a populated position shows summary + per-day history with monotonic non-decreasing running_max
</verification>

<success_criteria>
- CMD-10 fully implemented; reconstructed per-day stop history matches Phase 4 formulas
- 3 Wave 0 stubs filled in and passing
- Error path exits code 1 and points to `audit --symbol` (D-10)
- No splits placeholder present (D-02)
</success_criteria>

<output>
Create `.planning/phases/09-consultation-commands/09-08-SUMMARY.md` when done
</output>

---
phase: 09-consultation-commands
plan: 07
type: execute
wave: 3
depends_on:
  - 09-01
files_modified:
  - src/bensdorp1/commands/portfolio.py
  - tests/test_commands/test_portfolio.py
autonomous: true
requirements:
  - CMD-09

must_haves:
  truths:
    - "`bensdorp1 portfolio` prints a 10-column table of all open positions"
    - "Empty open-position set prints `No open positions.` (UI-08)"
    - "For each position, last close comes from the most recent price_daily row for that symbol where trade_date <= today"
    - "When a position has no price_daily row, the row shows `N/A` for Last $, High $, Stop $, Dist %, and P&L"
    - "effective_stop is computed at read time as max(initial_stop, trailing_stop) — never stored (Phase 7 D-18)"
    - "Dist % uses format_pct (sign always shown); P&L uses format_pnl"
    - "Days held = len(get_trading_days(entry_date.date(), today))"
  artifacts:
    - path: "src/bensdorp1/commands/portfolio.py"
      provides: "CMD-09 implementation with 10-column table + computed metrics"
      contains: "format_pnl"
    - path: "tests/test_commands/test_portfolio.py"
      provides: "CMD-09 integration tests (3 scenarios filled in)"
      contains: "runner.invoke(app, [\"portfolio\"])"
  key_links:
    - from: "src/bensdorp1/commands/portfolio.py"
      to: "positions WHERE closed_at IS NULL"
      via: "positions.c.closed_at == None  # noqa: E711"
      pattern: "closed_at == None  # noqa: E711"
    - from: "src/bensdorp1/commands/portfolio.py"
      to: "price_daily latest close per symbol"
      via: "per-symbol sub-query ORDER BY trade_date DESC LIMIT 1"
      pattern: "price_daily\\.c\\.trade_date\\.desc"
    - from: "src/bensdorp1/commands/portfolio.py"
      to: "ui.format_pct, ui.format_pnl"
      via: "Dist % and P&L columns"
      pattern: "format_pct\\(|format_pnl\\("
---

<objective>
Replace the `portfolio` command stub with a full CMD-09 implementation: SELECT all open positions (closed_at IS NULL), for each compute days_held (NYSE trading days) + effective_stop + distance-to-stop% + unrealized P&L using the latest available close from price_daily. Render as a 10-column table with the abbreviated headers from D-06. Fill in the 3 Wave 0 stubs in `tests/test_commands/test_portfolio.py`.

Purpose: Single-glance view of every open position with the metrics needed to make hold/sell decisions in <5 minutes.

Output: Working `portfolio` command + 3 passing CliRunner tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-consultation-commands/09-CONTEXT.md
@.planning/phases/09-consultation-commands/09-RESEARCH.md
@.planning/phases/09-consultation-commands/09-PATTERNS.md
@.planning/phases/07-scan-command/07-CONTEXT.md

<interfaces>
From bensdorp1._app:
- `app: typer.Typer`

From bensdorp1.config:
- `DATA_DIR: Path`, `MARKET_TZ: ZoneInfo`

From bensdorp1.data (public):
- `get_trading_days(start: date, end: date) -> list[Timestamp]` (NYSE trading days inclusive on both ends)

From bensdorp1.db (public):
- `get_engine, run_migrations`

From bensdorp1.db.schema:
- `positions: Table` columns: `id, symbol, entry_date (DateTime tz-aware), entry_close (Float), shares (Integer), initial_stop, highest_close, trailing_stop, scan_id, closed_at (nullable), exit_price, realized_pnl, closed_reason, closed_manual_reason`
- `price_daily: Table` columns: `id, symbol, trade_date (DateTime tz-aware), close, volume`

From bensdorp1.ui:
- `format_date, format_pct, format_pnl, format_price, print_info, render_table`

Schema invariants (Phase 7 D-18, D-19; Phase 8 D-25):
- `effective_stop` is NOT a column — compute at read time as `max(initial_stop, trailing_stop)`
- Latest close lives in `price_daily`, NOT in `positions`
- Use `positions.c.closed_at == None  # noqa: E711` for IS NULL idiom
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement portfolio.py + fill in 3 test stubs</name>
  <files>src/bensdorp1/commands/portfolio.py, tests/test_commands/test_portfolio.py</files>
  <read_first>
    - src/bensdorp1/commands/portfolio.py (current 11-line stub — replace entirely)
    - src/bensdorp1/commands/buy.py (lines 1-100 DB triad + Typer registration + insert example for positions)
    - src/bensdorp1/commands/sell.py (full file — open-position query, days_held via get_trading_days, P&L computation patterns)
    - src/bensdorp1/db/schema.py (lines 42-65 positions; lines 125-139 price_daily)
    - src/bensdorp1/data/calendar.py (get_trading_days signature: takes date, returns list of pandas Timestamps)
    - src/bensdorp1/data/__init__.py (confirms get_trading_days is exported)
    - src/bensdorp1/ui/styles.py (format_pct, format_pnl, format_price, format_date)
    - src/bensdorp1/ui/__init__.py (confirms all formatters + render_table + print_info exported)
    - tests/test_commands/test_buy.py (lines 24-90 mock+db_engine patterns)
    - tests/test_commands/test_sell.py (positions insert + price_daily seeding pattern if present)
    - tests/test_commands/test_portfolio.py (Wave 0 stubs from plan 09-01)
    - .planning/phases/09-consultation-commands/09-PATTERNS.md (section "src/bensdorp1/commands/portfolio.py" + SP-1, SP-2, SP-4, SP-12)
    - .planning/phases/09-consultation-commands/09-RESEARCH.md (Pattern 6 portfolio + Pattern 7 days_held)
    - .planning/phases/07-scan-command/07-CONTEXT.md (D-18, D-19 — effective_stop computed at read time)
  </read_first>
  <behavior>
    - Signature: `def portfolio() -> None` (no arguments)
    - Empty open-position set → `print_info("No open positions.", console=console)` + `raise typer.Exit()`; exit 0
    - For each open position:
      - Compute `today_et = datetime.now(MARKET_TZ).date()`
      - Compute `today_utc_midnight = datetime(today_et.year, today_et.month, today_et.day, tzinfo=UTC)` for the price_daily comparison upper bound
      - Sub-query: `select(price_daily.c.close).where((price_daily.c.symbol == pos.symbol) & (price_daily.c.trade_date <= today_utc_midnight)).order_by(price_daily.c.trade_date.desc()).limit(1).fetchone()`
      - If price_row is None: append row `[pos.symbol, format_date(pos.entry_date.date()), str(len(get_trading_days(pos.entry_date.date(), today_et))), format_price(pos.entry_close), str(pos.shares), "N/A", "N/A", "N/A", "N/A", "N/A"]`
      - Else: compute `last_close = price_row.close; effective_stop = max(pos.initial_stop, pos.trailing_stop); dist_pct = (last_close - effective_stop) / last_close * 100.0; unrealized_pnl = (last_close - pos.entry_close) * pos.shares; days = len(get_trading_days(pos.entry_date.date(), today_et))`. Append `[pos.symbol, format_date(pos.entry_date.date()), str(days), format_price(pos.entry_close), str(pos.shares), format_price(last_close), format_price(pos.highest_close), format_price(effective_stop), format_pct(dist_pct), format_pnl(unrealized_pnl)]`
    - render_table columns (D-06 exact text and alignment): `[("Symbol","left"),("Entry date","left"),("Days","right"),("Entry $","right"),("Shares","right"),("Last $","right"),("High $","right"),("Stop $","right"),("Dist %","right"),("P&L","right")]`
    - Command never writes
  </behavior>
  <action>
    Replace src/bensdorp1/commands/portfolio.py wholesale. Imports: `import datetime`; `from datetime import UTC`; `from typing import Any`; `typer`; `Console` from rich.console; `select` from sqlalchemy; `app` from bensdorp1._app; `DATA_DIR, MARKET_TZ` from bensdorp1.config; `get_trading_days` from bensdorp1.data; `get_engine, run_migrations` from bensdorp1.db; `positions, price_daily` from bensdorp1.db.schema; `format_date, format_pct, format_pnl, format_price, print_info, render_table` from bensdorp1.ui. Register `@app.command(rich_help_panel="Positions")`; signature `def portfolio() -> None:`. Body: DB entry triad; open `engine.connect()`; execute `select(positions).where(positions.c.closed_at == None)  # noqa: E711` (SP-4); if empty list → print_info + raise typer.Exit(). Compute `today_et` and `today_utc_midnight` once outside the loop. Build `rows: list[list[str]]`. Iterate; per-position run the latest-price sub-query; branch on None vs found per behavior block. After loop ends, call render_table with the exact 10-column layout above. No explicit Exit needed on success.
    Fill in the 3 Wave 0 stubs in tests/test_commands/test_portfolio.py.
    Scenario 1 (empty): db_engine with no positions; patch `bensdorp1.commands.portfolio.DATA_DIR/get_engine/run_migrations`; invoke `["portfolio"]`; assert exit 0 and `"No open positions."` in output.
    Scenario 2 (happy-with-prices): db_engine; INSERT one open position `symbol="AAPL", entry_date=datetime(2026,3,15,tzinfo=UTC), entry_close=182.50, shares=50, initial_stop=169.73, highest_close=185.00, trailing_stop=138.75, scan_id=None, closed_at=None, exit_price=None, realized_pnl=None`; INSERT one price_daily row `symbol="AAPL", trade_date=datetime(2026,5,20,tzinfo=UTC), close=178.20, volume=1000000`; invoke `["portfolio"]`; assert exit 0; assert "AAPL" in output; assert "$182.50" in output (entry); assert "$178.20" in output (last close); assert "50" in output (shares); assert "-$215.00" in output (unrealized P&L = (178.20-182.50)*50); assert "+" in output for Dist % (since 178.20 > effective_stop = max(169.73, 138.75) = 169.73).
    Scenario 3 (no-price-data): db_engine; INSERT one open position for "NVDA" (similar shape, no matching price_daily row); invoke `["portfolio"]`; assert exit 0 and "NVDA" in output and "N/A" appears at least 5 times (one per N/A column).
  </action>
  <verify>
    <automated>uv run pytest tests/test_commands/test_portfolio.py -x -q && uv run mypy --strict src/bensdorp1/commands/portfolio.py && uv run ruff check src/bensdorp1/commands/portfolio.py tests/test_commands/test_portfolio.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/bensdorp1/commands/portfolio.py` no longer contains "Not yet implemented"
    - File contains `positions.c.closed_at == None  # noqa: E711` (SP-4, verifiable by grep)
    - File contains `max(pos.initial_stop, pos.trailing_stop)` — effective_stop computed at read time (verifiable by grep)
    - File references `pos.entry_close` (NEVER `pos.entry_price` — Pitfall referenced in key constraint 1)
    - File contains `format_pct(` for the Dist % column (NOT `format_pnl(` per SP-12)
    - File contains `format_pnl(` for the P&L column
    - File contains `get_trading_days(pos.entry_date.date(), today` for the days column (verifiable by grep)
    - File contains a 10-column render_table call with exact headers in order: Symbol, Entry date, Days, Entry $, Shares, Last $, High $, Stop $, Dist %, P&L
    - File contains `"N/A"` literal at least once (the N/A fallback path)
    - `tests/test_commands/test_portfolio.py` has zero `pytest.skip` calls remaining
    - `uv run pytest tests/test_commands/test_portfolio.py -x -q` exits 0 with 3 passing tests
    - `uv run mypy --strict src/bensdorp1/commands/portfolio.py` exits 0
    - `uv run ruff check src/bensdorp1/commands/portfolio.py tests/test_commands/test_portfolio.py` exits 0
  </acceptance_criteria>
  <done>portfolio command renders the 10-column table with N/A fallback; 3 CliRunner tests pass; mypy strict + ruff clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite positions + price_daily → terminal | render_table is markup-safe; symbol/numeric values pass through formatters |
| Floating-point arithmetic | dist_pct and unrealized_pnl computed in float; never used in security-sensitive comparisons |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-T1 | Tampering | SQL queries | mitigate | Parameterized SQLAlchemy select; `positions.c.symbol == pos.symbol` uses bindparam |
| T-09-07-T2 | Tampering | render_table cell content | mitigate | Cells already formatted via format_price/format_pct/format_pnl; render_table applies markup-safe Phase 5 primitive |
| T-09-07-D1 | Denial of Service | per-position sub-query | accept | Max 10 open positions (STRAT-05); 10 sub-queries is negligible |
| T-09-07-I1 | Information Disclosure | portfolio values | accept | Single-user CLI; user is reading own portfolio |
| T-09-07-V1 | Tampering | divide-by-zero in dist_pct | mitigate | Branch on `price_row is None` already covers no-data; positive last_close in normal data set guards `last_close / last_close * 100`; document that a zero-close from price_daily would NaN — accept (data integrity bug elsewhere) |
| T-09-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- `uv run pytest tests/test_commands/test_portfolio.py -x -q` → 3 passed
- `uv run mypy --strict src/bensdorp1/commands/portfolio.py` → 0 errors
- `uv run ruff check src/bensdorp1/commands/portfolio.py tests/test_commands/test_portfolio.py` → 0 errors
- Behavior: `bensdorp1 portfolio` shows all open positions with live-computed metrics; N/A fallback works when price_daily is missing rows
</verification>

<success_criteria>
- CMD-09 fully implemented; effective_stop computed at read time
- 3 Wave 0 stubs filled in and passing
- All 10 columns rendered in exact D-06 order with correct alignment
- N/A fallback path covered by Scenario 3
</success_criteria>

<output>
Create `.planning/phases/09-consultation-commands/09-07-SUMMARY.md` when done
</output>

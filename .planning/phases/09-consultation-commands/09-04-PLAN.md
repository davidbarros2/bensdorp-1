---
phase: 09-consultation-commands
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/bensdorp1/commands/history.py
  - tests/test_commands/test_history.py
autonomous: true
requirements:
  - CMD-05

must_haves:
  truths:
    - "`bensdorp1 history` renders a 5-column table of past scans ordered scan_date DESC"
    - "`--limit N` caps the row count (default 20)"
    - "`--since YYYY-MM-DD` filters scans on or after that date (UTC midnight)"
    - "Invalid `--since` date format exits code 1 with an error message"
    - "When no scans match, an info message is printed (not a blank table)"
    - "Bear-day scans show `—` in the Top candidates column"
    - "D-05 honored: Top candidates column is a single comma-separated column ('AAPL, MSFT, NVDA'), not three separate columns"
  artifacts:
    - path: "src/bensdorp1/commands/history.py"
      provides: "CMD-05 implementation with --limit/--since filters and per-scan top-3 sub-query"
      contains: "render_table"
    - path: "tests/test_commands/test_history.py"
      provides: "CMD-05 integration tests (4 scenarios filled in)"
      contains: "runner.invoke(app, [\"history\""
  key_links:
    - from: "src/bensdorp1/commands/history.py"
      to: "scans + scan_candidates"
      via: "outer select scans, inner subquery for top-3 ordered by rank ASC LIMIT 3"
      pattern: "scan_candidates\\.c\\.rank"
    - from: "src/bensdorp1/commands/history.py"
      to: "ui.render_table"
      via: "5 columns: Date, Regime, Exits, Candidates, Top candidates"
      pattern: "render_table\\("
---

<objective>
Replace the `history` command stub with a full CMD-05 implementation: read recent `scans` rows ordered by `scan_date DESC` with optional `--since`/`--limit` filters; for each scan, sub-query `scan_candidates` to fetch the top-3 by rank as a comma-separated list. Render as a 5-column table. Fill in the 4 Wave 0 stubs in `tests/test_commands/test_history.py`.

Purpose: Give users a compact glance at recent scan results without re-reading full raw_output.

Output: Working `history` command + 4 passing CliRunner tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-consultation-commands/09-CONTEXT.md
@.planning/phases/09-consultation-commands/09-RESEARCH.md
@.planning/phases/09-consultation-commands/09-PATTERNS.md

<interfaces>
From bensdorp1._app:
- `app: typer.Typer`

From bensdorp1.config:
- `DATA_DIR: Path`

From bensdorp1.db (public):
- `get_engine(path)`, `run_migrations(engine)`

From bensdorp1.db.schema:
- `scans: Table` with columns `id, scan_date, regime_active (Boolean), candidate_count, exit_trigger_count, raw_output, created_at`
- `scan_candidates: Table` with columns `id, scan_id (FK), symbol, rank, roc200, close, suggested_shares`

From bensdorp1.ui:
- `format_date(d: date) -> str` — ISO 8601 YYYY-MM-DD
- `print_error(title: str, *, actions: list[str] | None = None, console: Console | None = None) -> None`
- `print_info(title: str, *, console: Console | None = None) -> None`
- `render_table(columns: list[tuple[str, str]], rows: list[list[str]], console: Console) -> None`

Date parsing (Pitfall 5):
- Parse `--since` as `datetime.date.fromisoformat(since)` → wrap as `datetime(year, month, day, tzinfo=UTC)` before comparing against `scans.c.scan_date` (which is `DateTime(timezone=True)` in UTC)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement history.py + fill in 4 test stubs</name>
  <files>src/bensdorp1/commands/history.py, tests/test_commands/test_history.py</files>
  <read_first>
    - src/bensdorp1/commands/history.py (current 11-line stub — replace entirely)
    - src/bensdorp1/commands/buy.py (lines 1-65 for imports, DB triad, date parsing via fromisoformat + try/except + print_error + raise typer.Exit(code=1) from None)
    - src/bensdorp1/commands/scan.py (lines 38-63 for scans table query pattern)
    - src/bensdorp1/db/schema.py (lines 30-40 scans, lines 80-102 scan_candidates)
    - src/bensdorp1/ui/tables.py (render_table signature and column-alignment expectations)
    - src/bensdorp1/ui/__init__.py (confirm format_date, print_error, print_info, render_table all exported)
    - tests/test_commands/test_buy.py (lines 1-90 for imports + insert pattern + db_engine usage)
    - tests/test_commands/test_history.py (Wave 0 stubs from plan 09-01)
    - .planning/phases/09-consultation-commands/09-PATTERNS.md (section "src/bensdorp1/commands/history.py" + SP-1, SP-2, SP-7)
    - .planning/phases/09-consultation-commands/09-RESEARCH.md (Pattern 4 "history — Subquery for Top-3 Candidates" + Pitfall 5 "history --since date parsing")
  </read_first>
  <behavior>
    - Default `--limit` value is 20; help text describes it
    - Default `--since` is None (no filter); help text describes ISO format
    - Empty `scans` table → `print_info("No scans recorded yet. Run `bensdorp1 scan` on a trading day after 16:30 ET.")` + `raise typer.Exit()` exit 0
    - Non-empty after applying filters, zero rows → `print_info("No scans match the given filters.")` + exit 0
    - Invalid `--since` (e.g. "2026-13-99" or "foo") → `print_error(f"Invalid --since value {since!r}. Expected YYYY-MM-DD.")` + `raise typer.Exit(code=1) from None`
    - Future `--since` date → no rows match → info message branch
    - Regime label: `"Bull"` when `scan.regime_active is True`, `"Bear"` otherwise
    - Top candidates per scan: SELECT scan_candidates.symbol WHERE scan_id = scan.id ORDER BY rank ASC LIMIT 3 → comma-joined; if zero candidates show `"—"` (em dash)
    - Row data layout (in this exact order): `[format_date(scan.scan_date.date()), regime_label, str(scan.exit_trigger_count), str(scan.candidate_count), top3]`
  </behavior>
  <action>
    Replace src/bensdorp1/commands/history.py wholesale. Imports: `import datetime`; `from datetime import UTC`; `from typing import Any, Optional`; `typer`; `Console` from rich.console; `select` from sqlalchemy; `app` from bensdorp1._app; `DATA_DIR` from bensdorp1.config; `get_engine, run_migrations` from bensdorp1.db; `scan_candidates, scans` from bensdorp1.db.schema; `format_date, print_error, print_info, render_table` from bensdorp1.ui. Register `@app.command(rich_help_panel="Daily operation")`; signature: `def history(limit: int = typer.Option(20, "--limit", help="Maximum rows to show."), since: Optional[str] = typer.Option(None, "--since", help="Show scans on or after YYYY-MM-DD."), ) -> None:`. Body: DB entry triad. Parse `since` if provided using `datetime.date.fromisoformat(since)` wrapped in try/except ValueError → print_error + raise typer.Exit(code=1) from None (per Pitfall 5 wrap as `datetime(d.year, d.month, d.day, tzinfo=UTC)` for comparison). Build `filters: list[Any] = []` and append `scans.c.scan_date >= since_dt` when set. Open `engine.connect()`, execute outer `select(scans).where(*filters).order_by(scans.c.scan_date.desc()).limit(limit)`. If `scan_rows` empty: if no `since` filter and underlying scans table is also empty → print_info "No scans recorded yet…" else print_info "No scans match the given filters."; raise typer.Exit(). To detect "table empty" without an extra query just check the filter list; if filters list is empty and we got zero rows, table is empty (single source of truth). For each scan_row, sub-query `select(scan_candidates.c.symbol).where(scan_candidates.c.scan_id == scan_row.id).order_by(scan_candidates.c.rank.asc()).limit(3)`. Build `top3 = ", ".join(r.symbol for r in cand_rows) if cand_rows else "—"`. Append row list per behavior block. Call `render_table(columns=[("Date","left"),("Regime","left"),("Exits","right"),("Candidates","right"),("Top candidates","left")], rows=rows, console=console)`. End naturally (no explicit Exit needed on success).
    Fill in the 4 Wave 0 stubs in tests/test_commands/test_history.py: (1) **empty-table** — mock with fetchone/fetchall returning empty; assert exit 0 and "No scans recorded yet" substring; (2) **happy-path** — db_engine; INSERT 3 scans (dates 2026-05-19/20/21) with required columns + INSERT scan_candidates rows for the latest scan (3 symbols ranked 1/2/3); assert exit 0, all three scan_dates present in output, the comma-joined "NVDA, AAPL, MSFT" or similar substring present; (3) **`--limit 2`** — db_engine; INSERT 3 scans; invoke with `["history", "--limit", "2"]`; assert exit 0 and only 2 of the 3 date strings in output (the most recent two); (4) **`--since DATE`** — db_engine; INSERT 3 scans across boundary; invoke with `["history", "--since", "2026-05-20"]`; assert exit 0 and only 2 scan dates appear (boundary inclusive); (5) **invalid-since** is the 4th counted scenario in RESEARCH map but only 4 total scenarios required — combine: invalid `--since "not-a-date"` MUST be one of the 4. Use that as scenario 4: assert exit_code == 1 and `"Invalid --since"` in output. (If counts conflict, keep exactly 4 stubs and prefer: empty / happy / --limit / invalid-since. The `--since` filter path is also exercised inside happy-path setup if needed.)
  </action>
  <verify>
    <automated>uv run pytest tests/test_commands/test_history.py -x -q && uv run mypy --strict src/bensdorp1/commands/history.py && uv run ruff check src/bensdorp1/commands/history.py tests/test_commands/test_history.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/bensdorp1/commands/history.py` no longer contains "Not yet implemented"
    - File contains `typer.Option(20, "--limit"` and `typer.Option(None, "--since"`
    - File contains `datetime.date.fromisoformat(since)` (verifiable by grep)
    - File contains `.where(*filters)` splat (SP-7, verifiable by grep)
    - File contains `scan_candidates.c.rank.asc()` (verifiable by grep)
    - File contains 5-column render_table call with exact column headers
    - File contains `raise typer.Exit(code=1) from None` on invalid date
    - `tests/test_commands/test_history.py` has zero `pytest.skip` calls remaining
    - `uv run pytest tests/test_commands/test_history.py -x -q` exits 0 with 4 passing tests
    - `uv run mypy --strict src/bensdorp1/commands/history.py` exits 0
    - `uv run ruff check src/bensdorp1/commands/history.py tests/test_commands/test_history.py` exits 0
    - Behavior: invoking `bensdorp1 history` on a populated DB prints a table with Date/Regime/Exits/Candidates/Top candidates columns; `--limit 2` returns at most 2 rows; `--since 2099-01-01` triggers the no-match info branch
  </acceptance_criteria>
  <done>history command implements CMD-05 with both filters; 4 CliRunner tests pass; mypy strict + ruff clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (`--since`, `--limit`) → SQL | parameterized via SQLAlchemy bindparams; --since explicitly parsed with fromisoformat |
| SQLite scans rows → render_table | render_table internally sets markup safety per ui/tables.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-T1 | Tampering | --since SQL injection | mitigate | Parse with `date.fromisoformat()`; wrap in `datetime(..., tzinfo=UTC)`; pass as bindparam in `scans.c.scan_date >= since_dt` — no string interpolation |
| T-09-04-T2 | Tampering | symbol from scan_candidates → terminal | mitigate | Symbols are validated S&P constituents written by `scan`; still rendered via render_table which uses markup-safe output |
| T-09-04-D1 | Denial of Service | --limit large value | accept | Typer validates int; user-controlled in single-user CLI; SQLite handles large LIMIT efficiently — ASVS L1 does not require cap |
| T-09-04-D2 | Denial of Service | many scan_candidates sub-queries | accept | One sub-query per scan_row; bounded by --limit (default 20); negligible |
| T-09-04-V1 | Tampering | invalid date input | mitigate | try/except ValueError → print_error + typer.Exit(code=1) |
| T-09-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- `uv run pytest tests/test_commands/test_history.py -x -q` → 4 passed
- `uv run mypy --strict src/bensdorp1/commands/history.py` → 0 errors
- `uv run ruff check src/bensdorp1/commands/history.py tests/test_commands/test_history.py` → 0 errors
</verification>

<success_criteria>
- CMD-05 fully implemented with `--limit`/`--since`
- 4 Wave 0 stubs filled in and passing
- Date parsing failure path exercised and returns exit code 1
- Top-3 candidate aggregation per scan works for 0, <3, and ≥3 candidate counts
</success_criteria>

<output>
Create `.planning/phases/09-consultation-commands/09-04-SUMMARY.md` when done
</output>

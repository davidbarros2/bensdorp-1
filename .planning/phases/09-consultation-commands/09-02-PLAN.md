---
phase: 09-consultation-commands
plan: 02
type: execute
wave: 1
depends_on:
  - 09-01
files_modified:
  - src/bensdorp1/commands/last.py
  - tests/test_commands/test_last.py
autonomous: true
requirements:
  - CMD-04

must_haves:
  truths:
    - "`bensdorp1 last` prints the raw_output of the most recent scan verbatim"
    - "`bensdorp1 last` prints an info message and exits 0 when no scans exist"
    - "Rich markup characters inside raw_output are not interpreted (markup=False, highlight=False)"
  artifacts:
    - path: "src/bensdorp1/commands/last.py"
      provides: "CMD-04 implementation reading scans.raw_output"
      contains: "select(scans.c.scan_date, scans.c.raw_output)"
    - path: "tests/test_commands/test_last.py"
      provides: "CMD-04 integration tests (2 scenarios filled in)"
      contains: "runner.invoke(app, [\"last\"])"
  key_links:
    - from: "src/bensdorp1/commands/last.py"
      to: "scans.c.raw_output"
      via: "SELECT ORDER BY scan_date DESC LIMIT 1"
      pattern: "select\\(scans\\.c\\.scan_date, scans\\.c\\.raw_output\\)"
    - from: "src/bensdorp1/commands/last.py"
      to: "console.print"
      via: "markup=False, highlight=False"
      pattern: "markup=False"
---

<objective>
Replace the `last` command stub with a full CMD-04 implementation: read the most recent row from the `scans` table by `scan_date DESC LIMIT 1`, and print its `raw_output` verbatim with Rich markup disabled. When no scans exist, print an info message directing the user to run `bensdorp1 scan`. Fill in the two Wave 0 stubs in `tests/test_commands/test_last.py`.

Purpose: Give users an instant-replay of the last scan without re-running data fetch. Mirrors the existing scan.py non-trading-day branch (lines 44-63).

Output: Working `last` command + 2 passing CliRunner tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-consultation-commands/09-CONTEXT.md
@.planning/phases/09-consultation-commands/09-RESEARCH.md
@.planning/phases/09-consultation-commands/09-PATTERNS.md

<interfaces>
<!-- Pre-existing symbols this plan consumes. -->

From bensdorp1._app:
- `app: typer.Typer` — register `@app.command(rich_help_panel="Daily operation")`

From bensdorp1.config:
- `DATA_DIR: Path` — module-level constant; patched in tests

From bensdorp1.db (public surface):
- `get_engine(path: Path | None) -> Engine`
- `run_migrations(engine: Engine) -> None`

From bensdorp1.db.schema:
- `scans: Table` with columns `id, scan_date, regime_active, candidate_count, exit_trigger_count, raw_output, created_at`

From bensdorp1.ui:
- `print_info(title: str, *, console: Console | None = None) -> None`

From scan.py (non-trading-day branch, lines 44-63 — verbatim print pattern):
```
with engine.connect() as conn:
    row = conn.execute(
        select(scans.c.scan_date, scans.c.raw_output)
        .order_by(scans.c.scan_date.desc())
        .limit(1)
    ).fetchone()
console = Console()
if row is None:
    print_info("No scans recorded yet. Run `bensdorp1 scan` on a trading day after 16:30 ET.")
else:
    if row.raw_output is not None:
        console.print(row.raw_output, markup=False, highlight=False)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement last.py + fill in 2 test stubs</name>
  <files>src/bensdorp1/commands/last.py, tests/test_commands/test_last.py</files>
  <read_first>
    - src/bensdorp1/commands/last.py (current 11-line stub — replace entirely)
    - src/bensdorp1/commands/scan.py (lines 38-63: the non-trading-day branch is the canonical raw_output read+print pattern)
    - src/bensdorp1/commands/buy.py (lines 1-48: DB entry triad + imports + Typer registration)
    - src/bensdorp1/db/schema.py (lines 30-40: scans table columns)
    - src/bensdorp1/ui/__init__.py (confirm `print_info` is exported)
    - tests/test_commands/test_buy.py (lines 1-41: imports, runner, mock pattern; lines 131-180: db_engine seeded pattern)
    - tests/test_commands/test_last.py (Wave 0 stubs from plan 09-01)
    - .planning/phases/09-consultation-commands/09-PATTERNS.md (sections "src/bensdorp1/commands/last.py" and "SP-1" through "SP-3")
  </read_first>
  <behavior>
    - When `scans` table is empty: command exits 0 and stdout contains "No scans recorded yet" plus the actions hint mentioning `bensdorp1 scan`
    - When `scans` table has one or more rows: command exits 0 and stdout equals the raw_output of the row with the maximum `scan_date`, printed with `markup=False, highlight=False`
    - When the latest scan row has `raw_output IS NULL`: command exits 0 and stdout is an info message (NOT a crash, NOT empty); use `print_info("A scan record exists but has no output (the prior scan may have failed). Re-run `bensdorp1 scan --force` to retry.")` — same wording shape as scan.py line 81
    - Command never writes to DB and never calls `create_backup` or `log_event`
  </behavior>
  <action>
    Replace src/bensdorp1/commands/last.py wholesale. Import: `typer`, `Console` from rich.console, `select` from sqlalchemy, `app` from `bensdorp1._app`, `DATA_DIR` from `bensdorp1.config`, `get_engine` and `run_migrations` from `bensdorp1.db`, `scans` from `bensdorp1.db.schema`, `print_info` from `bensdorp1.ui`. Keep `@app.command(rich_help_panel="Daily operation")` and signature `def last() -> None:`. Body: DB entry triad (db_path = DATA_DIR / "data" / "bensdorp1.db"; engine = get_engine(db_path); run_migrations(engine); console = Console()). Then SELECT scans.c.scan_date, scans.c.raw_output ORDER BY scans.c.scan_date.desc() LIMIT 1 in an `engine.connect()` context. Three branches: row is None → `print_info("No scans recorded yet. Run `bensdorp1 scan` on a trading day after 16:30 ET.", console=console)`; row.raw_output is None → `print_info("A scan record exists but has no output (the prior scan may have failed). Re-run `bensdorp1 scan --force` to retry.", console=console)`; otherwise `console.print(row.raw_output, markup=False, highlight=False)`. End with `raise typer.Exit()`.
    Fill in the 2 Wave 0 stubs in tests/test_commands/test_last.py. Scenario 1 (empty state): use MagicMock pattern from test_buy.py lines 24-41 — fetchone returns None; patch `bensdorp1.commands.last.DATA_DIR`, `bensdorp1.commands.last.get_engine`, `bensdorp1.commands.last.run_migrations`; assert exit_code == 0 and `"No scans recorded yet"` in result.output. Scenario 2 (happy path): use the `db_engine` fixture, INSERT one scans row with `raw_output="Scan for 2026-05-21\n=== regime: Bull ==="` and `scan_date=datetime(2026, 5, 21, tzinfo=UTC)` plus required not-null columns (`regime_active=True, candidate_count=10, exit_trigger_count=0, created_at=datetime(2026,5,21,tzinfo=UTC)`); patch the same three symbols on the `last` module; invoke `runner.invoke(app, ["last"])`; assert exit_code == 0 and `"Scan for 2026-05-21"` in result.output.
  </action>
  <verify>
    <automated>uv run pytest tests/test_commands/test_last.py -x -q && uv run mypy --strict src/bensdorp1/commands/last.py && uv run ruff check src/bensdorp1/commands/last.py tests/test_commands/test_last.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/bensdorp1/commands/last.py` no longer contains "Not yet implemented"
    - File contains `select(scans.c.scan_date, scans.c.raw_output)` (verifiable by grep)
    - File contains `markup=False, highlight=False` on the raw_output print (verifiable by grep)
    - File contains `raise typer.Exit()` (no `sys.exit`)
    - File imports `print_info` from `bensdorp1.ui`
    - `tests/test_commands/test_last.py` has zero `pytest.skip` calls remaining (verifiable: `grep -c "pytest.skip" tests/test_commands/test_last.py` returns 0)
    - `uv run pytest tests/test_commands/test_last.py -x -q` exits 0 with 2 passing tests
    - `uv run mypy --strict src/bensdorp1/commands/last.py` exits 0
    - `uv run ruff check src/bensdorp1/commands/last.py tests/test_commands/test_last.py` exits 0
    - Behavior: invoking `bensdorp1 last` with an empty `scans` table prints "No scans recorded yet" and exits 0
    - Behavior: invoking `bensdorp1 last` after a scan returns the most recent `raw_output` verbatim
  </acceptance_criteria>
  <done>last command works in both empty and populated states; 2 CliRunner tests pass; mypy strict + ruff clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite (scans.raw_output) → terminal | raw_output is user-trusted (only written by `scan` command); still printed with markup=False to defuse any markup chars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-T1 | Tampering | scans.raw_output → terminal | mitigate | Print with `markup=False, highlight=False` to prevent Rich markup injection even from internally-trusted text (matches scan.py line 79 and Pitfall 7 family) |
| T-09-02-T2 | Tampering | SQL query | mitigate | SQLAlchemy parameterized `select()` only; no f-string SQL |
| T-09-02-I1 | Information Disclosure | scans.raw_output payload | accept | raw_output contains the user's own scan; single-user CLI; no disclosure boundary |
| T-09-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- `uv run pytest tests/test_commands/test_last.py -x -q` → 2 passed
- `uv run mypy --strict src/bensdorp1/commands/last.py` → 0 errors
- `uv run ruff check src/bensdorp1/commands/last.py tests/test_commands/test_last.py` → 0 errors
- Visible behavior: `bensdorp1 last` echoes raw_output verbatim or prints "No scans recorded yet"
</verification>

<success_criteria>
- CMD-04 fully implemented in `commands/last.py`
- Both Wave 0 stubs filled in and passing
- No production-code regression elsewhere (verified by `uv run pytest -x -q tests/test_commands/test_last.py`)
</success_criteria>

<output>
Create `.planning/phases/09-consultation-commands/09-02-SUMMARY.md` when done
</output>

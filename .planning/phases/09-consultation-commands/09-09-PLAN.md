---
phase: 09-consultation-commands
plan: 09
type: execute
wave: 4
depends_on:
  - 09-02
  - 09-03
  - 09-04
  - 09-05
  - 09-06
  - 09-07
  - 09-08
files_modified: []
autonomous: true
requirements:
  - CMD-04
  - CMD-05
  - CMD-09
  - CMD-10
  - CMD-11
  - CMD-12
  - CMD-13

must_haves:
  truths:
    - "Full pytest suite passes (no skips outside Wave 0 stubs that are already filled in)"
    - "Coverage on src/bensdorp1 is ≥ 90% (TEST-02 invariant preserved)"
    - "mypy --strict reports zero errors on the entire src/ tree"
    - "ruff check src/ tests/ reports zero issues"
    - "ruff format --check src/ tests/ reports zero diffs"
    - "All 7 Phase 9 commands respond to `--help` and produce non-empty output"
  artifacts:
    - path: ".planning/phases/09-consultation-commands/09-09-SUMMARY.md"
      provides: "Verification gate summary with coverage delta and test counts"
      contains: "coverage"
  key_links:
    - from: "verification gate"
      to: "all 7 implementation plans"
      via: "no production source modifications; pure gate"
      pattern: "files_modified: \\[\\]"
---

<objective>
Run the full Phase 9 verification gate AFTER all implementation plans (09-02 through 09-08) have completed: pytest --cov, mypy --strict, ruff check, ruff format --check. Validate that each of the 7 commands responds to `--help` non-trivially. Surface any regressions, coverage gaps, or lint/type errors so they can be remediated inside this same phase before declaring Phase 9 complete. No production source files are modified by this plan — it is a pure quality gate.

Purpose: Single point of pass/fail for Phase 9. Mirrors the gate plans used by Phases 4 (04-03), 6 (06-02), 7 (07-04), and 8 (08-05).

Output: SUMMARY.md with measured coverage %, total test count, and a verdict line confirming the gate passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-consultation-commands/09-CONTEXT.md
@.planning/phases/09-consultation-commands/09-RESEARCH.md
@.planning/phases/09-consultation-commands/09-01-PLAN.md
@.planning/phases/09-consultation-commands/09-02-PLAN.md
@.planning/phases/09-consultation-commands/09-03-PLAN.md
@.planning/phases/09-consultation-commands/09-04-PLAN.md
@.planning/phases/09-consultation-commands/09-05-PLAN.md
@.planning/phases/09-consultation-commands/09-06-PLAN.md
@.planning/phases/09-consultation-commands/09-07-PLAN.md
@.planning/phases/09-consultation-commands/09-08-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full pytest with coverage + capture results</name>
  <files></files>
  <read_first>
    - pyproject.toml (confirms `[tool.coverage.*]` and pytest configuration; identifies the coverage threshold and reporting flags)
    - .planning/phases/09-consultation-commands/09-RESEARCH.md (section "Validation Architecture" — quick-run and full-suite commands)
    - .planning/phases/08-confirmation-commands/08-05-PLAN.md if it exists (canonical example of a Phase 8 verification gate plan to mirror)
  </read_first>
  <action>
    Run `uv run pytest --cov=bensdorp1 --cov-report=term-missing -q` from the repo root. Capture exit code, total test count (passed/failed/skipped), and coverage percentage from the term-missing report. If pytest exits non-zero, surface the failing test names in this task's output — DO NOT attempt to fix in this plan. If coverage on `src/bensdorp1/` aggregate is below 90%, surface the per-file coverage table from `term-missing` so the gap is visible. If coverage on any individual Phase 9 command file (`commands/last.py`, `commands/history.py`, `commands/portfolio.py`, `commands/detail.py`, `commands/cash.py`, `commands/config.py`, `commands/audit.py`) is below 85%, surface that file's missing-line ranges.
  </action>
  <verify>
    <automated>uv run pytest --cov=bensdorp1 --cov-report=term-missing -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest --cov=bensdorp1 --cov-report=term-missing -q` exits 0
    - Total test count is ≥ pre-Phase-9 baseline + 24 (the 7 Wave 0 stubs filled in across Phase 9 sum to 2+4+3+3+5+1+6 = 24 new passing tests)
    - Aggregated coverage on `src/bensdorp1/` is ≥ 90% (TEST-02 invariant)
    - Each of the 7 Phase 9 command files has individual coverage ≥ 85% (verifiable from term-missing report)
    - Zero tests are reported as SKIPPED for any of the 7 Phase 9 test files
  </acceptance_criteria>
  <done>Pytest passes; coverage thresholds met</done>
</task>

<task type="auto">
  <name>Task 2: Run mypy strict + ruff check + ruff format check</name>
  <files></files>
  <read_first>
    - pyproject.toml (mypy and ruff configuration sections — confirms strict mode and lint/format rules)
    - .planning/phases/09-consultation-commands/09-09-PLAN.md (this plan — for traceability)
  </read_first>
  <action>
    Run three commands in sequence and capture all output: (1) `uv run mypy --strict src/`; (2) `uv run ruff check src/ tests/`; (3) `uv run ruff format --check src/ tests/`. If any command exits non-zero, surface the file-level errors (do not auto-fix here — surface them so the next iteration of the relevant plan can address them, or if trivial, capture in this plan's SUMMARY as remediation steps to apply manually before re-running). Do NOT modify any source file in this plan; this is a verification gate.
  </action>
  <verify>
    <automated>uv run mypy --strict src/ && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run mypy --strict src/` exits 0 (no type errors anywhere under src/)
    - `uv run ruff check src/ tests/` exits 0 (no lint errors)
    - `uv run ruff format --check src/ tests/` exits 0 (no formatting diffs)
    - Combined chained verify command above exits 0
  </acceptance_criteria>
  <done>mypy strict, ruff check, and ruff format all clean</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-test --help for all 7 Phase 9 commands</name>
  <files></files>
  <read_first>
    - src/bensdorp1/cli.py (confirms the 7 commands are registered)
    - src/bensdorp1/_app.py (Typer app configuration)
  </read_first>
  <action>
    For each of the 7 commands, invoke `uv run bensdorp1 <cmd> --help` and confirm exit code 0, the command name appears in output, and at least one argument/option from its signature is documented. Commands to test: `last, history, portfolio, detail, cash, config, audit`. Specifically check: `history --help` mentions `--limit` and `--since`; `audit --help` mentions `--symbol`, `--since`, `--until`, `--type`, `--limit`; `cash --help` mentions `AMOUNT` and `--note`; `detail --help` mentions `SYMBOL` argument. If any --help invocation fails or omits a documented flag, surface as a gate failure.
  </action>
  <verify>
    <automated>uv run bensdorp1 last --help && uv run bensdorp1 history --help && uv run bensdorp1 portfolio --help && uv run bensdorp1 detail --help && uv run bensdorp1 cash --help && uv run bensdorp1 config --help && uv run bensdorp1 audit --help</automated>
  </verify>
  <acceptance_criteria>
    - All 7 `--help` invocations exit 0
    - `bensdorp1 history --help` output contains `--limit` AND `--since`
    - `bensdorp1 audit --help` output contains `--symbol` AND `--since` AND `--until` AND `--type` AND `--limit`
    - `bensdorp1 cash --help` output contains `AMOUNT` (uppercase argument placeholder) AND `--note`
    - `bensdorp1 detail --help` output contains `SYMBOL`
    - `bensdorp1 config --help` and `bensdorp1 last --help` and `bensdorp1 portfolio --help` exit 0 with non-empty output
  </acceptance_criteria>
  <done>All 7 commands respond to --help with the expected flags/arguments</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner shell → pytest/mypy/ruff | Verification gate is local-only; no external network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09-T1 | Tampering | gate bypass | mitigate | All three quality tools must pass independently before SUMMARY can be written; auto-verify in `<verify>` blocks |
| T-09-09-R1 | Repudiation | unrecorded gate failure | mitigate | SUMMARY.md records measured coverage %, test count, and verdict — git-committed |
| T-09-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- All three verify commands across Tasks 1-3 exit 0
- SUMMARY.md records the actual numbers (coverage %, total tests, individual command coverage)
</verification>

<success_criteria>
- Phase 9 quality bar met: pytest green, coverage ≥ 90%, mypy strict clean, ruff check + format clean
- All 7 Phase 9 commands respond to --help with their full signature
- SUMMARY.md captures the measured numbers so they can be cross-referenced in STATE.md and ROADMAP.md
</success_criteria>

<output>
Create `.planning/phases/09-consultation-commands/09-09-SUMMARY.md` when done — include the measured coverage %, total test count delta, and a "VERDICT: PASS" line if all three gates pass.
</output>

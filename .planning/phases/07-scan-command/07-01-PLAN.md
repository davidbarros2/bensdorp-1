---
phase: 07-scan-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bensdorp1/db/schema.py
  - tests/test_commands/test_scan.py
  - .planning/phases/07-scan-command/07-VALIDATION.md
autonomous: true
requirements:
  - CMD-03
must_haves:
  truths:
    - "scan_exit_triggers table is created by run_migrations (create_all picks it up automatically)"
    - "All 10 test stubs pass with exit code 0 so Wave 2 executor has a green test baseline"
    - "07-VALIDATION.md wave_0_complete is set to true"
  artifacts:
    - path: "src/bensdorp1/db/schema.py"
      provides: "scan_exit_triggers Table + ix_scan_exit_triggers_position Index"
      contains: "scan_exit_triggers"
    - path: "tests/test_commands/test_scan.py"
      provides: "All 10 stub test functions from the validation test map"
      min_lines: 60
  key_links:
    - from: "src/bensdorp1/db/schema.py scan_exit_triggers"
      to: "scans.id + positions.id"
      via: "ForeignKey constraints"
      pattern: "ForeignKey.*scans\\.id.*ForeignKey.*positions\\.id"
---

<objective>
Extend the DB schema with the scan_exit_triggers table and create the Wave 0 test stub file that all subsequent plans will fill in.

Purpose: The scan_exit_triggers table is the only new DDL this phase requires (per D-05). It must exist before the engine can insert trigger rows. The test stub file is the Nyquist Wave 0 contract — all 10 test names are declared now so later plans have target test IDs to implement against.

Output: Updated schema.py with the scan_exit_triggers table + Index; tests/test_commands/test_scan.py with 10 stubs (all pass); 07-VALIDATION.md updated with wave_0_complete: true.
</objective>

<execution_context>
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\execute-plan.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\summary.md
</execution_context>

<context>
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\PROJECT.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\ROADMAP.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\07-CONTEXT.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\07-RESEARCH.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\07-PATTERNS.md

<interfaces>
<!-- From src/bensdorp1/db/schema.py — current state before modification -->

scan_candidates: Table = Table(
    "scan_candidates",
    metadata,
    Column("id", Integer, primary_key=True, autoincrement=True),
    Column("scan_id", Integer, ForeignKey("scans.id"), nullable=False),
    Column("symbol", Text, nullable=False),
    Column("rank", Integer, nullable=False),
    Column("roc200", Float, nullable=False),
    Column("close", Float, nullable=False),
    Column("suggested_shares", Integer, nullable=False),
)
Index("ix_scan_candidates_scan_rank", scan_candidates.c.scan_id, scan_candidates.c.rank, unique=True)
Index("ix_scan_candidates_scan_symbol", scan_candidates.c.scan_id, scan_candidates.c.symbol, unique=True)

constituents_cache: Table = Table(
    "constituents_cache",
    metadata,
    ...
)
# scan_exit_triggers does NOT exist yet — insert it between scan_candidates and constituents_cache
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scan_exit_triggers table to schema.py (per D-05)</name>
  <files>src/bensdorp1/db/schema.py</files>
  <read_first>
    - src/bensdorp1/db/schema.py — read the full file; insert new table after scan_candidates block (after the ix_scan_candidates_scan_symbol Index), before constituents_cache
    - .planning/phases/07-scan-command/07-RESEARCH.md — Pattern 4 (scan_exit_triggers DDL) and the "New table to add" spec block; note triggered_date column from planning guidance
    - .planning/phases/07-scan-command/07-CONTEXT.md — D-05 (table spec), D-09 (triggered_date needed for accurate "Triggered on" display)
  </read_first>
  <action>
    Insert a new scan_exit_triggers Table definition into src/bensdorp1/db/schema.py immediately after the ix_scan_candidates_scan_symbol Index call and before the constituents_cache Table definition.

    The table must have exactly these columns:
    - id: Integer, primary_key=True, autoincrement=True
    - scan_id: Integer, ForeignKey("scans.id"), nullable=False — the scan during which the trigger was first detected (today's scan even if catch-up)
    - position_id: Integer, ForeignKey("positions.id"), nullable=False
    - reason: Text, nullable=False — stores exactly "Trailing stop" or "Initial stop" (sentence case)
    - effective_stop: Float, nullable=False
    - triggered_date: DateTime(timezone=True), nullable=False — the specific trading date the trigger fired (the missed day for catch-up, or today for same-day triggers); needed for accurate "Triggered on" display per D-09

    After the Table definition, add one Index:
    - Index("ix_scan_exit_triggers_position", scan_exit_triggers.c.position_id)

    Follow the exact same style as scan_candidates: positional Table(...) call, then Index(...) calls immediately after. The DateTime import is already present in schema.py. No other imports are needed.

    Do not modify any other table definition. Do not add the triggered_date column to any other table.
  </action>
  <verify>
    <automated>uv run python -c "from bensdorp1.db.schema import scan_exit_triggers, metadata; print([c.name for c in scan_exit_triggers.columns])" 2>&1 | grep -i "triggered_date"</automated>
  </verify>
  <acceptance_criteria>
    - schema.py contains: scan_exit_triggers = Table(
    - scan_exit_triggers has columns: id, scan_id, position_id, reason, effective_stop, triggered_date
    - scan_exit_triggers.c.scan_id has ForeignKey("scans.id")
    - scan_exit_triggers.c.position_id has ForeignKey("positions.id")
    - scan_exit_triggers.c.triggered_date is DateTime(timezone=True)
    - Index("ix_scan_exit_triggers_position", scan_exit_triggers.c.position_id) present immediately after the Table definition
    - schema.py has no syntax errors: uv run python -c "from bensdorp1.db.schema import scan_exit_triggers" exits 0
    - uv run mypy src/bensdorp1/db/schema.py --strict exits 0
    - uv run ruff check src/bensdorp1/db/schema.py exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create test_scan.py with all 10 Wave 0 stubs + update 07-VALIDATION.md</name>
  <files>tests/test_commands/test_scan.py, .planning/phases/07-scan-command/07-VALIDATION.md</files>
  <read_first>
    - tests/test_commands/test_init.py — use this as the structural template: file docstring, imports block, runner = CliRunner() module-level, then test functions
    - .planning/phases/07-scan-command/07-VALIDATION.md — the 10 test function names listed in the "Per-Task Verification Map" table; these are the exact names to stub
    - .planning/phases/07-scan-command/07-PATTERNS.md — "File header + runner pattern" section for import block template
    - tests/conftest.py — confirm db_engine fixture signature (tests use `db_engine: Engine` parameter)
  </read_first>
  <action>
    Create tests/test_commands/test_scan.py. The file must contain exactly the following 10 stub functions matching the validation map names. Each stub body is `pytest.skip("Wave 0 stub — implemented in Plan 07-03")` to make pytest collect and pass them cleanly (skip counts as pass, does not fail the suite).

    Test function names (these exact names, no deviations):
    1. test_schema_has_exit_triggers_table — unit; no parameters
    2. test_time_gate — integration; parameter: tmp_path: Path
    3. test_happy_path_bull — integration; parameter: tmp_path: Path
    4. test_bearish_regime — integration; parameter: tmp_path: Path
    5. test_idempotent_same_day — integration; parameter: tmp_path: Path
    6. test_force_reruns_scan — integration; parameter: tmp_path: Path
    7. test_non_trading_day — integration; parameter: tmp_path: Path
    8. test_catchup_stop_updates — unit; parameter: db_engine: Engine
    9. test_stop_freeze_after_trigger — unit; parameter: db_engine: Engine
    10. test_exit_trigger_on_missed_day — unit; parameter: db_engine: Engine

    Import block must include: from pathlib import Path, import pytest, from sqlalchemy.engine import Engine, from typer.testing import CliRunner, from bensdorp1.cli import app. Module-level: runner = CliRunner().

    After creating test_scan.py, update .planning/phases/07-scan-command/07-VALIDATION.md: change wave_0_complete: false to wave_0_complete: true in the YAML frontmatter.
  </action>
  <verify>
    <automated>uv run pytest tests/test_commands/test_scan.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_commands/test_scan.py exists
    - uv run pytest tests/test_commands/test_scan.py -x exits 0 (all 10 stubs collected and skipped)
    - pytest output shows "10 skipped" (not "10 passed" — stubs use pytest.skip)
    - All 10 function names present: grep "def test_" tests/test_commands/test_scan.py shows exactly 10 matches
    - 07-VALIDATION.md frontmatter contains wave_0_complete: true
    - uv run mypy tests/test_commands/test_scan.py --strict exits 0
    - uv run ruff check tests/test_commands/test_scan.py exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.py → SQLite | New table DDL is static Python objects; no user input reaches DDL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | scan_exit_triggers inserts | mitigate | All inserts in scan engine will use SQLAlchemy bound parameters; the Table definition itself has no injection surface — parameterized by design |
| T-7-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all existing packages verified in prior phases |
</threat_model>

<verification>
After both tasks complete:

```
uv run pytest tests/test_commands/test_scan.py -x
uv run mypy src/bensdorp1/db/schema.py tests/test_commands/test_scan.py --strict
uv run ruff check src/bensdorp1/db/schema.py tests/test_commands/test_scan.py
```

All three must exit 0.
</verification>

<success_criteria>
- scan_exit_triggers table defined in schema.py with 6 columns (id, scan_id, position_id, reason, effective_stop, triggered_date) and one index
- run_migrations() picks up the new table automatically via create_all(checkfirst=True) — verified by test_schema_has_exit_triggers_table stub being in place
- 10 test stubs exist in test_scan.py; uv run pytest tests/test_commands/test_scan.py -x exits 0
- 07-VALIDATION.md wave_0_complete: true
</success_criteria>

<output>
Create .planning/phases/07-scan-command/07-01-SUMMARY.md when done
</output>

---
phase: 07-scan-command
plan: 04
type: execute
wave: 4
depends_on:
  - "07-03"
files_modified:
  - .planning/phases/07-scan-command/07-VALIDATION.md
autonomous: true
requirements:
  - CMD-03
must_haves:
  truths:
    - "uv run pytest --cov=bensdorp1 --cov-report=term-missing exits 0 with total coverage >= 90%"
    - "uv run mypy src/ --strict exits 0 (zero type errors across the whole repo)"
    - "uv run ruff check src/ tests/ exits 0 (zero lint errors)"
    - "uv run ruff format --check src/ tests/ exits 0 (all files formatted)"
    - "07-VALIDATION.md frontmatter nyquist_compliant: true"
  artifacts:
    - path: ".planning/phases/07-scan-command/07-VALIDATION.md"
      provides: "Phase 7 validation sign-off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "pytest coverage report"
      to: "src/bensdorp1/commands/_scan_engine.py + scan.py"
      via: "--cov=bensdorp1 --cov-report=term-missing"
      pattern: "TOTAL.*9[0-9]%"
---

<objective>
Full integration verification gate: confirm the whole repo is green on pytest (coverage >= 90%), mypy strict, ruff check, and ruff format before closing Phase 7.

Purpose: Phases 1–6 established a >= 90% coverage contract. Phase 7 adds two substantial modules (_scan_engine.py and scan.py) and 10 new tests. This gate ensures the new code does not drop coverage below threshold and that the type checker and linter are satisfied across the entire repo. If coverage drops below 90%, targeted tests are added to bring it back up.

Output: All four quality gates pass. 07-VALIDATION.md updated to nyquist_compliant: true.
</objective>

<execution_context>
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\execute-plan.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\summary.md
</execution_context>

<context>
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\PROJECT.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\ROADMAP.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\07-CONTEXT.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\07-VALIDATION.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full verification suite; add targeted tests if coverage < 90%</name>
  <files>.planning/phases/07-scan-command/07-VALIDATION.md</files>
  <read_first>
    - .planning/phases/07-scan-command/07-VALIDATION.md — current frontmatter state; verify wave_0_complete: true from Plan 07-01 before proceeding
    - tests/test_commands/test_scan.py — inspect which _scan_engine.py code paths are exercised; identify uncovered branches if coverage report shows < 90%
    - src/bensdorp1/commands/_scan_engine.py — identify any helper functions that may lack test coverage (e.g., _run_preflight error paths, _render_output bearish branch, D-22 no-affordable-candidates note)
  </read_first>
  <action>
    Run the four quality checks in sequence:

    Step 1: uv run pytest --cov=bensdorp1 --cov-report=term-missing
    - If exit code != 0: read the failure output, fix the failing test, re-run. Do not proceed to step 2 until step 1 is green.
    - If TOTAL coverage line shows < 90%: read the --cov-report output to identify uncovered lines in _scan_engine.py and scan.py. Add targeted tests to tests/test_commands/test_scan.py for the identified uncovered branches. Focus on: _run_preflight coverage < 95% edge (price coverage abort path), _render_output no-exit-triggers branch, _render_output bearish regime branch, _get_available_cash None return (cash = 0.0 path). Re-run until >= 90%.

    Step 2: uv run mypy src/ --strict
    - If errors exist: read each error message. Fix the type error in the source file indicated. Common patterns: missing type annotations, Any return type in a function that should return a specific type, incompatible assignment. Do NOT use # type: ignore unless the error is a known mypy limitation (e.g., SQLAlchemy Row field access). Re-run until clean.

    Step 3: uv run ruff check src/ tests/
    - If errors exist: run uv run ruff check src/ tests/ --fix for auto-fixable issues. For non-auto-fixable: read the rule ID and fix manually. Re-run until clean.

    Step 4: uv run ruff format --check src/ tests/
    - If any files need reformatting: run uv run ruff format src/ tests/ (without --check) to apply formatting. Re-run --check to confirm.

    Step 5: Update 07-VALIDATION.md frontmatter: change nyquist_compliant: false to nyquist_compliant: true. Update the Validation Sign-Off checklist — mark all checkboxes as [x]. Set approval: phase-7-complete.

    Note: Do not modify any files outside src/, tests/, and .planning/phases/07-scan-command/07-VALIDATION.md. Do not change pyproject.toml unless a specific ruff or mypy configuration issue is identified that requires it (and even then, read pyproject.toml first to understand the current config).
  </action>
  <verify>
    <automated>uv run pytest --cov=bensdorp1 --cov-report=term-missing && uv run mypy src/ --strict && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest --cov=bensdorp1 --cov-report=term-missing exits 0
    - Coverage TOTAL line in pytest output shows >= 90% (grep "TOTAL" shows a value like "91%" or higher)
    - uv run mypy src/ --strict exits 0 with "Success: no issues found" in output
    - uv run ruff check src/ tests/ exits 0 with no output (zero errors)
    - uv run ruff format --check src/ tests/ exits 0 (no files need reformatting)
    - 07-VALIDATION.md frontmatter contains nyquist_compliant: true
    - 07-VALIDATION.md Validation Sign-Off section: all checkboxes marked [x]
    - 07-VALIDATION.md approval: phase-7-complete
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/test runner | Verification-only plan; no new production code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | Any targeted tests added for coverage | mitigate | New tests must follow the same parameterized-query and Text() markup-safety patterns established in Plans 07-02/07-03; no raw string SQL in test fixtures |
| T-7-SC | Tampering | npm/pip/cargo installs | accept | No new packages; all verified in prior phases |
</threat_model>

<verification>
Final composite command — all four gates in one invocation:

```
uv run pytest --cov=bensdorp1 --cov-report=term-missing && uv run mypy src/ --strict && uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/
```

All four must exit 0. The pipeline short-circuits on the first failure.
</verification>

<success_criteria>
- Full test suite passes: uv run pytest --cov=bensdorp1 exits 0
- Coverage >= 90% (TOTAL line in coverage report)
- mypy strict exits 0 on entire src/ tree
- ruff check exits 0 on src/ and tests/
- ruff format --check exits 0 on src/ and tests/
- 07-VALIDATION.md nyquist_compliant: true and approval: phase-7-complete
- Phase 7 is complete and ready for /gsd-verify-work
</success_criteria>

<output>
Create .planning/phases/07-scan-command/07-04-SUMMARY.md when done
</output>

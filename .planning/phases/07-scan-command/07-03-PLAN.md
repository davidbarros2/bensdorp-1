---
phase: 07-scan-command
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - src/bensdorp1/commands/scan.py
  - tests/test_commands/test_scan.py
autonomous: true
requirements:
  - CMD-03
must_haves:
  truths:
    - "bensdorp1 scan before 16:30 ET exits 1 with error message containing '16:30 ET'"
    - "bensdorp1 scan on a non-trading day prints Info message and last raw_output, exits 0"
    - "Same-day second call shows raw_output without calling run_scan (D-01)"
    - "bensdorp1 scan --force calls run_scan even when same-day scan exists (D-02)"
    - "All 10 test stubs are replaced with real test implementations; uv run pytest tests/test_commands/test_scan.py -x exits 0"
    - "scan.py registers @app.command(rich_help_panel='Daily operation') with --force option"
  artifacts:
    - path: "src/bensdorp1/commands/scan.py"
      provides: "Full Typer scan command: time gate, trading-day check, idempotency, --force, delegates to run_scan"
      min_lines: 50
    - path: "tests/test_commands/test_scan.py"
      provides: "All 10 tests implemented — no pytest.skip stubs remain"
      min_lines: 180
  key_links:
    - from: "src/bensdorp1/commands/scan.py"
      to: "bensdorp1.commands._scan_engine.run_scan"
      via: "direct import + call"
      pattern: "from bensdorp1.commands._scan_engine import run_scan"
    - from: "tests/test_commands/test_scan.py"
      to: "bensdorp1.commands.scan"
      via: "patch targets for module-level names"
      pattern: "bensdorp1.commands.scan\\."
---

<objective>
Replace the scan.py stub with the full Typer command entry point and implement all 10 tests in test_scan.py.

Purpose: scan.py is the user-facing entry point. It must perform the three pre-engine checks (time gate, trading-day, idempotency) before delegating to run_scan. The tests validate all 6 CliRunner integration scenarios (D-23) and 4 unit tests for complex engine logic (D-24).

Output: scan.py fully implemented with --force flag, time gate, non-trading-day handling, idempotency check, and engine delegation. All 10 test functions implemented with no pytest.skip stubs remaining. All tests pass.
</objective>

<execution_context>
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\execute-plan.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\summary.md
</execution_context>

<context>
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\PROJECT.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\ROADMAP.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\07-CONTEXT.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\07-RESEARCH.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\07-PATTERNS.md
@C:\Users\david\Documents\Projetos\bensdorp-1\.planning\phases\07-scan-command\07-02-SUMMARY.md

<interfaces>
<!-- scan.py imports (from PATTERNS.md + RESEARCH.md) -->
# from datetime import UTC, date, datetime
# from zoneinfo import ZoneInfo
# import typer
# from rich.console import Console
# from rich.text import Text
# from sqlalchemy import select
# from sqlalchemy.engine import Engine
# from bensdorp1._app import app
# from bensdorp1.config import DATA_DIR, MARKET_TZ
# from bensdorp1.commands._scan_engine import run_scan
# from bensdorp1.data import is_trading_day
# from bensdorp1.db import get_engine, run_migrations
# from bensdorp1.db.schema import scans

<!-- Time gate logic (from PATTERNS.md) -->
# now_et = datetime.now(MARKET_TZ)
# if now_et.hour < 16 or (now_et.hour == 16 and now_et.minute < 30):
#     print_error("Market has not closed yet.", data={...})
#     raise typer.Exit(code=1)

<!-- Idempotency check (from PATTERNS.md) -->
# scan_date_utc = datetime(today.year, today.month, today.day, tzinfo=UTC)
# SELECT scans.raw_output WHERE scans.scan_date == scan_date_utc
# if existing is not None and not force: print + raise typer.Exit()

<!-- Engine path -->
# db_path = DATA_DIR / "data" / "bensdorp1.db"
# engine = get_engine(db_path)
# run_migrations(engine)
# output = run_scan(engine, force=force, console=console)
# console.print(output, markup=False, highlight=False)

<!-- CliRunner test pattern (from PATTERNS.md + test_init.py) -->
# runner = CliRunner()
# patch targets: "bensdorp1.commands.scan.DATA_DIR", "bensdorp1.commands.scan.datetime",
#                "bensdorp1.commands.scan.is_trading_day", "bensdorp1.commands.scan.get_engine",
#                "bensdorp1.commands.scan.run_migrations", "bensdorp1.commands.scan.run_scan"

<!-- Unit test targets for _scan_engine internals (D-24) -->
# from bensdorp1.commands._scan_engine import _update_position_stops, _detect_exit_triggers
# Unit tests use db_engine: Engine fixture from conftest.py (not mocks)
# db_engine is already migrated via run_migrations in the conftest fixture
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scan.py Typer command (per D-01 through D-04, D-15)</name>
  <files>src/bensdorp1/commands/scan.py</files>
  <read_first>
    - src/bensdorp1/commands/scan.py — current stub (1-line implementation; replace entirely)
    - src/bensdorp1/commands/init.py — guard pattern (lines 96–107), console creation (line 109), engine creation (lines 169–170), raise typer.Exit patterns
    - .planning/phases/07-scan-command/07-PATTERNS.md — scan.py section: all 5 patterns (imports, decorator, time gate, non-trading-day, idempotency, engine call, early exit)
    - .planning/phases/07-scan-command/07-CONTEXT.md — D-01 (idempotent replay), D-02 (--force UPDATE), D-03 (non-trading day), D-04 (--force + ON CONFLICT), D-15 (thin Typer layer)
    - src/bensdorp1/commands/_scan_engine.py — verify run_scan signature: run_scan(engine, *, force, console) -> str
  </read_first>
  <action>
    Replace src/bensdorp1/commands/scan.py entirely. The file is currently a 10-line stub. The new implementation follows the PATTERNS.md scan.py section exactly.

    Command decorator: @app.command(rich_help_panel="Daily operation") with function signature: scan(force: bool = typer.Option(False, "--force", help="Re-run even if scan already ran today.")) -> None. Docstring: "Run daily end-of-day screening for exit triggers and buy candidates."

    Execution order inside scan():
    1. Time gate: now_et = datetime.now(MARKET_TZ). If before 16:30 ET (hour < 16 or (hour == 16 and minute < 30)): call print_error("Market has not closed yet.", data={"Market closes at": "16:00 ET", "Scan available after": "16:30 ET", "Current time": f"{now_et:%H:%M} ET"}), then raise typer.Exit(code=1). This check runs BEFORE any DB access.

    2. Trading-day check: today = datetime.now(MARKET_TZ).date(). If not is_trading_day(today): create engine, query scans table for most recent row (ORDER BY scan_date DESC LIMIT 1). If row exists: print_info(f"Today is not a trading day. Showing last scan from {last_date}."), then console.print(row.raw_output, markup=False, highlight=False) if raw_output is not None. Else: print_info("No scans recorded yet. Run `bensdorp1 scan` on a trading day after 16:30 ET."). raise typer.Exit() (clean exit).

    3. Idempotency check (only on trading days): get_engine + run_migrations. Query scans for today's scan_date (midnight UTC datetime). If row exists and not force: console.print(existing.raw_output, markup=False, highlight=False). raise typer.Exit() (clean exit, no re-run).

    4. Engine delegation: console = Console(). output = run_scan(engine, force=force, console=console). console.print(output, markup=False, highlight=False).

    DB path: DATA_DIR / "data" / "bensdorp1.db". Reuse the same engine instance for both idempotency check and run_scan call (do not call get_engine twice).

    Import print_error and print_info from bensdorp1.ui. Do NOT import run_migrations inside the if-block — call it once at the top of the trading-day branch before the idempotency check.

    scan_date idempotency: store as datetime(today.year, today.month, today.day, tzinfo=UTC) to match _persist_scan's storage format.

    No modifications to cli.py or commands/__init__.py — scan.py is already registered from Phase 1.
  </action>
  <verify>
    <automated>uv run mypy src/bensdorp1/commands/scan.py --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - uv run mypy src/bensdorp1/commands/scan.py --strict exits 0
    - uv run ruff check src/bensdorp1/commands/scan.py exits 0
    - uv run ruff format --check src/bensdorp1/commands/scan.py exits 0
    - scan.py contains: def scan(force: bool = typer.Option(False, "--force"
    - scan.py contains: raise typer.Exit(code=1) (time gate failure path)
    - scan.py contains: raise typer.Exit() (at least 2 clean-exit paths: non-trading day + idempotent)
    - scan.py contains: print_error("Market has not closed yet."
    - scan.py contains: from bensdorp1.commands._scan_engine import run_scan
    - scan.py does NOT contain: typer.echo("Not yet implemented.")
    - scan.py does NOT contain: from bensdorp1.commands._scan_engine import _  (only run_scan imported)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement all 10 tests in test_scan.py (D-23 + D-24)</name>
  <files>tests/test_commands/test_scan.py</files>
  <read_first>
    - tests/test_commands/test_scan.py — current file with 10 pytest.skip stubs (read to see exact function signatures)
    - tests/test_commands/test_init.py — complete file; extract: module-level patch pattern, MagicMock engine usage, mock_conn.execute chaining pattern, CliRunner invocation with input=
    - src/bensdorp1/commands/scan.py — just implemented; verify exact import names to use as patch targets
    - src/bensdorp1/commands/_scan_engine.py — verify _update_position_stops and _detect_exit_triggers signatures for D-24 unit tests
    - tests/conftest.py — db_engine fixture: understand what it provides (migrated Engine in tmp_path, calls run_migrations)
    - .planning/phases/07-scan-command/07-PATTERNS.md — test_scan.py section: all 5 test patterns
    - .planning/phases/07-scan-command/07-CONTEXT.md — D-23 (6 CliRunner tests), D-24 (4 unit tests)
    - .planning/phases/07-scan-command/07-RESEARCH.md — Pitfall 5 (engine singleton), Pitfall 7 (Windows teardown), Pattern 2 (CliRunner with module-level patches)
  </read_first>
  <action>
    Replace all 10 pytest.skip stubs with real test implementations. Keep the same function names and parameter signatures. Add needed imports at the top: datetime, zoneinfo.ZoneInfo, and any additional imports required. All 10 tests must be deterministic and not depend on network access, clock, or filesystem (use patches and fixtures).

    CliRunner tests (tests 2–7) use module-level patches on bensdorp1.commands.scan.* targets:

    test_time_gate(tmp_path): patch DATA_DIR + datetime. mock_dt.now.return_value = datetime(2026, 5, 21, 15, 0, tzinfo=MARKET_TZ) (before 16:30). Assert exit_code == 1. Assert "16:30 ET" in result.output.

    test_happy_path_bull(tmp_path): patch DATA_DIR, datetime (17:00 ET), is_trading_day=True, get_engine=mock_engine, run_migrations, run_scan returning "Scan for 2026-05-21\nBull market\n". Mock the scans query to return None (no existing scan). Assert exit_code == 0. Assert "Scan for 2026-05-21" in result.output.

    test_bearish_regime(tmp_path): patch the same set; run_scan returns output without buy candidate tables. Assert exit_code == 0 and "Buy candidates" NOT in result.output.

    test_idempotent_same_day(tmp_path): patch get_engine; mock scans query to return MagicMock(raw_output="Stored output"). Assert exit_code == 0. Assert "Stored output" in result.output. Assert mock_run_scan.assert_not_called() (D-01: no re-run).

    test_force_reruns_scan(tmp_path): same setup as idempotent but invoke with ["scan", "--force"]. Assert run_scan WAS called (mock_run_scan.assert_called_once()). Assert only one scans row (D-02 behavior tested via mock call count).

    test_non_trading_day(tmp_path): patch is_trading_day=False; mock scans query returns MagicMock(scan_date=datetime(2026,5,20,tzinfo=UTC), raw_output="Last scan output"). Assert exit_code == 0. Assert "not a trading day" in result.output (case-insensitive). Assert "Last scan output" in result.output.

    Unit tests (tests 1, 8–10) use db_engine fixture:

    test_schema_has_exit_triggers_table(db_engine): from bensdorp1.db.schema import metadata; assert "scan_exit_triggers" in metadata.tables. Inspect columns: assert "triggered_date" in [c.name for c in metadata.tables["scan_exit_triggers"].columns].

    test_catchup_stop_updates(db_engine): Insert a positions row (highest_close=100.0, trailing_stop=75.0, initial_stop=93.0, closed_at=None). Insert price_daily rows for 3 consecutive trading dates (closes: 102.0, 104.0, 106.0). Call _update_position_stops(db_engine, [pos_row], [day1, day2, day3], today, price_dfs, set()). SELECT updated positions row. Assert highest_close == 106.0. Assert trailing_stop == 106.0 * 0.75.

    test_stop_freeze_after_trigger(db_engine): Insert position with initial_stop=100.0, trailing_stop=75.0, highest_close=100.0. Add to triggered_position_ids set before calling. Insert price_daily for today with close=90.0. Call _update_position_stops. SELECT position. Assert highest_close unchanged == 100.0 (freeze verified).

    test_exit_trigger_on_missed_day(db_engine): Insert a scans row for today. Insert positions row with initial_stop=100.0, trailing_stop=100.0 (effective_stop=100.0). Insert price_daily for a missed day date with close=95.0 (below effective_stop). Call _update_position_stops — this should add pos.id to triggered_position_ids. Then call _detect_exit_triggers(db_engine, [pos], triggered_ids, scan_id, today, price_dfs). SELECT from scan_exit_triggers. Assert one row exists. Assert scan_exit_triggers row has triggered_date matching the missed day's date (not today).

    For mock_conn patterns: study test_init.py lines 28–53 carefully — mock_engine.connect.return_value.__enter__.return_value is the connection context. mock_conn.execute.return_value.fetchone.return_value = MagicMock(...).

    All tests must be importable at collection time (no module-level side effects). All tests must pass without network access.
  </action>
  <verify>
    <automated>uv run pytest tests/test_commands/test_scan.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/test_commands/test_scan.py -x -v exits 0
    - All 10 tests pass (0 skipped — no pytest.skip stubs remain)
    - test_time_gate: exit_code == 1 and "16:30 ET" in output
    - test_idempotent_same_day: mock_run_scan.assert_not_called() passes
    - test_force_reruns_scan: mock_run_scan.assert_called_once() passes
    - test_schema_has_exit_triggers_table: "scan_exit_triggers" confirmed in metadata.tables
    - test_exit_trigger_on_missed_day: scan_exit_triggers row has triggered_date matching the missed day
    - grep -c "pytest.skip" tests/test_commands/test_scan.py returns 0 (no remaining stubs)
    - uv run mypy tests/test_commands/test_scan.py --strict exits 0
    - uv run ruff check tests/test_commands/test_scan.py exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → scan.py | --force is a bool (Typer validates); no user-supplied strings enter the engine |
| scans.raw_output → console.print | Stored text from prior scan is printed with markup=False, highlight=False |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | scans query in scan.py | mitigate | SELECT uses SQLAlchemy select() with bound parameters: .where(scans.c.scan_date == scan_date_utc) — no string interpolation |
| T-7-02 | Tampering | raw_output replay | mitigate | console.print(raw_output, markup=False, highlight=False) — markup disabled; stored output from our own rendering pipeline (no external strings) |
| T-7-03 | Tampering | DATA_DIR / "data" / "bensdorp1.db" path | mitigate | Path constructed via Path join (DATA_DIR / "data" / "bensdorp1.db"), not string concatenation |
| T-7-SC | Tampering | npm/pip/cargo installs | accept | No new packages; all verified in prior phases |
</threat_model>

<verification>
After both tasks complete:

```
uv run pytest tests/test_commands/test_scan.py -x -v
uv run mypy src/bensdorp1/commands/scan.py tests/test_commands/test_scan.py --strict
uv run ruff check src/bensdorp1/commands/scan.py tests/test_commands/test_scan.py
uv run ruff format --check src/bensdorp1/commands/scan.py tests/test_commands/test_scan.py
```

All must exit 0. 10 tests pass, 0 skipped.
</verification>

<success_criteria>
- scan.py: full Typer command with --force, time gate (exit 1), non-trading-day info, idempotency replay, engine delegation
- test_scan.py: all 10 tests pass, no pytest.skip stubs remain
- mypy strict, ruff check, ruff format --check all exit 0 on both files
- test_exit_trigger_on_missed_day verifies triggered_date is the missed day date, not today (D-09)
- test_idempotent_same_day verifies run_scan is not called on second invocation (D-01)
</success_criteria>

<output>
Create .planning/phases/07-scan-command/07-03-SUMMARY.md when done
</output>

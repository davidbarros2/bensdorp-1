---
phase: 02-database-and-migrations
plan: 05
type: execute
wave: 5
depends_on:
  - 02-03
  - 02-04
files_modified:
  - src/bensdorp1/db/__init__.py
  - tests/test_db_positions.py
autonomous: true
requirements:
  - STATE-06

must_haves:
  truths:
    - "db/__init__.py re-exports get_engine, run_migrations, create_backup, log_event, AuditEventType as the public surface of the db subpackage"
    - "Inserting a second open position for the same symbol raises sqlalchemy.exc.IntegrityError — enforced by the partial unique index at DB level"
    - "Closing a position (setting closed_at to a datetime) and then opening a new position in the same symbol succeeds — sequential positions are allowed"
    - "from bensdorp1.db import get_engine, run_migrations, create_backup, log_event, AuditEventType works without error"
  artifacts:
    - path: "src/bensdorp1/db/__init__.py"
      provides: "Re-exports: get_engine, run_migrations, create_backup, log_event, AuditEventType"
    - path: "tests/test_db_positions.py"
      provides: "STATE-06 tests: IntegrityError on duplicate open position, sequential positions allowed"
  key_links:
    - from: "src/bensdorp1/db/__init__.py"
      to: "src/bensdorp1/db/engine.py, backup.py, audit.py"
      via: "from bensdorp1.db.{module} import ..."
      pattern: "__all__"
    - from: "tests/test_db_positions.py"
      to: "src/bensdorp1/db/schema.py positions table"
      via: "from bensdorp1.db.schema import positions; insert(positions).values(closed_at=None)"
      pattern: "IntegrityError"
---

<objective>
Wire the db/ package public interface by completing db/__init__.py with real re-exports from Waves 2–4, then confirm STATE-06 enforcement with test_db_positions.py.

Purpose: db/__init__.py is the API surface that all later phases import from. Completing it makes the import surface stable before Phase 3 begins. The positions tests confirm the partial unique index works end-to-end at DB level — not just that it was defined in schema.py.

Output: db/__init__.py (complete re-exports), test_db_positions.py (2 STATE-06 tests confirming IntegrityError and sequential success)
</objective>

<execution_context>
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-RESEARCH.md
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-PATTERNS.md
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-03-SUMMARY.md
@C:/Users/david/Documents\Projetos\bensdorp-1\.planning\phases\02-database-and-migrations\02-04-SUMMARY.md
</execution_context>

<context>
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/REQUIREMENTS.md
@C:/Users/david/Documents/Projetos/bensdorp-1/pyproject.toml
@C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/db/__init__.py
@C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/db/engine.py
@C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/db/backup.py
@C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/db/audit.py
@C:/Users/david/Documents/Projetos/bensdorp-1/tests/conftest.py

<interfaces>
<!-- Completed modules from Waves 2–4 that __init__.py re-exports -->

From src/bensdorp1/db/engine.py:
  def get_engine(path: Optional[Path] = None) -> Engine
  def run_migrations(engine: Engine) -> None

From src/bensdorp1/db/backup.py:
  def create_backup(engine: Engine, backups_dir: Path) -> Path

From src/bensdorp1/db/audit.py:
  class AuditEventType(StrEnum)  # 17 members
  def log_event(engine: Engine, event_type: AuditEventType, symbol: str | None = None, payload: dict[str, Any] | None = None) -> None

From PATTERNS.md (db/__init__.py re-export pattern from cli.py analog):
  from bensdorp1.db.audit import AuditEventType, log_event
  from bensdorp1.db.backup import create_backup
  from bensdorp1.db.engine import get_engine, run_migrations
  __all__ = ["get_engine", "run_migrations", "create_backup", "log_event", "AuditEventType"]

From RESEARCH.md — test_duplicate_open_position_rejected pattern (verified end-to-end):
  with pytest.raises(IntegrityError):
      conn.execute(insert(positions).values(symbol="AAPL", ..., closed_at=None))
  # second insert with closed_at=None for same symbol raises IntegrityError

From RESEARCH.md — test_sequential_positions_allowed pattern:
  insert AAPL with closed_at=None → commit → update AAPL set closed_at=now → commit → insert AAPL with closed_at=None → commit (succeeds)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete db/__init__.py with real re-exports</name>
  <files>src/bensdorp1/db/__init__.py</files>
  <read_first>
    - C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/db/__init__.py — read the current placeholder content from Wave 1 before replacing
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-PATTERNS.md — db/__init__.py re-export pattern (from cli.py analog)
    - C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/cli.py — existing re-export pattern to match style
    - C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/db/engine.py — confirm get_engine, run_migrations are exported
    - C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/db/backup.py — confirm create_backup is exported
    - C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/db/audit.py — confirm AuditEventType, log_event are exported
  </read_first>
  <action>
    Replace src/bensdorp1/db/__init__.py with the complete public surface of the db subpackage:

    Imports (use from ... import style — no bare module imports needed; no # noqa: F401 required with from-style):
    - from bensdorp1.db.audit import AuditEventType, log_event
    - from bensdorp1.db.backup import create_backup
    - from bensdorp1.db.engine import get_engine, run_migrations

    Declare __all__ with exactly these 5 names:
    __all__ = ["AuditEventType", "create_backup", "get_engine", "log_event", "run_migrations"]

    Add a module docstring: "Public surface of the bensdorp1.db subpackage."

    This is the complete file — no other content. Three import lines + __all__ + docstring.
  </action>
  <verify>
    <automated>uv run python -c "from bensdorp1.db import get_engine, run_migrations, create_backup, log_event, AuditEventType; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - from bensdorp1.db import get_engine exits 0
    - from bensdorp1.db import run_migrations exits 0
    - from bensdorp1.db import create_backup exits 0
    - from bensdorp1.db import log_event exits 0
    - from bensdorp1.db import AuditEventType exits 0
    - __all__ contains exactly ["AuditEventType", "create_backup", "get_engine", "log_event", "run_migrations"] (5 names, alphabetically sorted or in definition order — either is acceptable)
    - uv run mypy src/bensdorp1/db/__init__.py exits 0
    - uv run ruff check src/bensdorp1/db/__init__.py exits 0
  </acceptance_criteria>
  <done>db/__init__.py exposes the public surface; all 5 imports resolve without error; mypy strict and ruff pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_db_positions.py confirming STATE-06 partial index enforcement</name>
  <files>tests/test_db_positions.py</files>
  <read_first>
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-RESEARCH.md — "Test for STATE-06 enforcement" code example (complete test functions, verified end-to-end); Pattern 4 (ix_positions_open_symbol partial index)
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-PATTERNS.md — test_db_positions.py section (pytest.raises(IntegrityError) pattern)
    - C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/db/schema.py — positions table columns (confirm: symbol, entry_date, entry_close, shares, initial_stop, highest_close, trailing_stop, closed_at are all required)
    - C:/Users/david/Documents/Projetos/bensdorp-1/tests/conftest.py — db_engine fixture signature
  </read_first>
  <behavior>
    - insert AAPL with closed_at=None → commit → insert AAPL with closed_at=None → raises IntegrityError (duplicate open position)
    - insert MSFT with closed_at=None → commit → update MSFT set closed_at=now() → commit → insert MSFT with closed_at=None → commit → no error raised (sequential positions allowed)
    - insert AAPL with closed_at=None and insert GOOG with closed_at=None in same session → both succeed (different symbols)
  </behavior>
  <action>
    Create tests/test_db_positions.py with these test functions:

    Imports: pytest, datetime and timezone from datetime, insert and update from sqlalchemy, IntegrityError from sqlalchemy.exc, Engine from sqlalchemy.engine, positions from bensdorp1.db.schema

    Helper values used across tests:
    - nowval = datetime.now(timezone.utc)
    - Base row dict for positions: symbol="AAPL", entry_date=nowval, entry_close=150.0, shares=10, initial_stop=139.5, highest_close=150.0, trailing_stop=112.5, closed_at=None

    - test_duplicate_open_position_rejected(db_engine: Engine) -> None:
      with db_engine.connect() as conn:
        conn.execute(insert(positions).values(symbol="AAPL", entry_date=now, entry_close=150.0, shares=10, initial_stop=139.5, highest_close=150.0, trailing_stop=112.5, closed_at=None))
        conn.commit()
        with pytest.raises(IntegrityError):
          conn.execute(insert(positions).values(symbol="AAPL", entry_date=now, entry_close=155.0, shares=10, initial_stop=144.15, highest_close=155.0, trailing_stop=116.25, closed_at=None))
      NOTE: the inner pytest.raises block does NOT call conn.commit() — the insert itself should raise IntegrityError synchronously from SQLite

    - test_sequential_positions_allowed(db_engine: Engine) -> None:
      now = datetime.now(timezone.utc)
      with db_engine.connect() as conn:
        conn.execute(insert(positions).values(symbol="MSFT", entry_date=now, entry_close=300.0, shares=5, initial_stop=279.0, highest_close=300.0, trailing_stop=225.0, closed_at=None))
        conn.commit()
        conn.execute(update(positions).where(positions.c.symbol == "MSFT").values(closed_at=now, exit_price=310.0, realized_pnl=50.0))
        conn.commit()
        conn.execute(insert(positions).values(symbol="MSFT", entry_date=now, entry_close=315.0, shares=5, initial_stop=292.95, highest_close=315.0, trailing_stop=236.25, closed_at=None))
        conn.commit()
        # No exception raised = test passes

    - test_different_symbols_can_both_be_open(db_engine: Engine) -> None:
      now = datetime.now(timezone.utc)
      with db_engine.connect() as conn:
        conn.execute(insert(positions).values(symbol="AAPL", entry_date=now, entry_close=150.0, shares=10, initial_stop=139.5, highest_close=150.0, trailing_stop=112.5, closed_at=None))
        conn.execute(insert(positions).values(symbol="GOOG", entry_date=now, entry_close=2800.0, shares=1, initial_stop=2604.0, highest_close=2800.0, trailing_stop=2100.0, closed_at=None))
        conn.commit()
        # Both inserts succeed — no exception

    All functions: -> None return type; db_engine: Engine typed fixture parameter.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db_positions.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_db_positions.py exists with 3 test functions
    - uv run pytest tests/test_db_positions.py -x -q exits 0 (all 3 tests pass)
    - test_duplicate_open_position_rejected passes: second insert(positions) with closed_at=None for same symbol raises IntegrityError
    - test_sequential_positions_allowed passes: close position then re-enter same symbol succeeds without error
    - test_different_symbols_can_both_be_open passes: AAPL and GOOG can both have closed_at=None simultaneously
    - uv run pytest tests/ -x -q exits 0 (complete suite: Phase 1 tests + all Phase 2 waves)
    - uv run ruff check . exits 0 (full project)
    - uv run mypy src/ exits 0 (full src/ including all db/ modules)
  </acceptance_criteria>
  <done>STATE-06 confirmed at DB level: IntegrityError raised on duplicate open symbol; sequential and multi-symbol open positions allowed; complete test suite exits 0; mypy strict and ruff pass on full src/</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| db/__init__.py → caller | Clean re-export surface; no business logic; all security enforcement in individual modules |
| positions table → partial unique index | DB-level constraint preventing simultaneous same-symbol open positions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | positions DML via schema.py insert | mitigate | All DML through SQLAlchemy parameterized insert().values(); no string-format SQL in test helpers or production code |
| T-2-02 | Tampering | AuditEventType bypass | mitigate | Confirmed mitigated in Wave 4; __init__.py re-exports AuditEventType as the only valid path to the enum |
| T-2-03 | Tampering | backup integrity | mitigate | Confirmed mitigated in Wave 3; __init__.py re-exports create_backup as the only path to the backup function |
| T-2-06 | Tampering | STATE-06 bypass (same-symbol open position) | mitigate | ix_positions_open_symbol partial unique index enforced at SQLite level — no INSERT can bypass it regardless of code path; confirmed by test_duplicate_open_position_rejected |
| T-2-SC | Tampering | pip installs | accept | No new packages in this plan |
</threat_model>

<verification>
1. uv run pytest tests/test_db_positions.py -v exits 0 (3 tests pass)
2. uv run pytest tests/ -x -q exits 0 (full suite: Phase 1 + all Phase 2 waves)
3. uv run ruff check . exits 0
4. uv run mypy src/ exits 0
5. uv run ruff format --check . exits 0
6. python -c "from bensdorp1.db import get_engine, run_migrations, create_backup, log_event, AuditEventType; print('public surface OK')"
</verification>

<success_criteria>
- db/__init__.py re-exports get_engine, run_migrations, create_backup, log_event, AuditEventType
- test_duplicate_open_position_rejected: IntegrityError raised when inserting second open position for same symbol
- test_sequential_positions_allowed: close position then re-enter same symbol — no error
- test_different_symbols_can_both_be_open: AAPL and GOOG can both be open simultaneously — no error
- Full test suite exits 0 (Phase 1 unchanged + all Phase 2 waves)
- uv run ruff check . exits 0 (full project)
- uv run mypy src/ exits 0 (all db/ modules pass strict mode)
</success_criteria>

<output>
Create .planning/phases/02-database-and-migrations/02-05-SUMMARY.md when done
</output>

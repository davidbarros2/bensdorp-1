---
phase: 02-database-and-migrations
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
files_modified:
  - src/bensdorp1/db/audit.py
  - tests/test_db_audit.py
autonomous: true
requirements:
  - STATE-04

must_haves:
  truths:
    - "AuditEventType is a StrEnum with exactly 17 members whose string values match STATE-04 exactly"
    - "str(AuditEventType.BUY_CONFIRMED) == 'buy_confirmed' — StrEnum returns the value, not 'AuditEventType.BUY_CONFIRMED'"
    - "log_event() inserts a row into audit_log with event_type as TEXT, occurred_at as UTC datetime, optional symbol, and optional JSON payload"
    - "All 17 event types can be inserted into audit_log and queried back by their string value"
    - "log_event() uses parameterized insert().values() — no string interpolation in SQL"
  artifacts:
    - path: "src/bensdorp1/db/audit.py"
      provides: "AuditEventType(StrEnum) with 17 members + log_event() function"
      exports: ["AuditEventType", "log_event"]
    - path: "tests/test_db_audit.py"
      provides: "STATE-04 tests: all 17 event types insertable, log_event with payload, query by event_type"
  key_links:
    - from: "src/bensdorp1/db/audit.py"
      to: "src/bensdorp1/db/schema.py audit_log table"
      via: "from bensdorp1.db.schema import audit_log"
      pattern: "insert(audit_log).values"
    - from: "tests/test_db_audit.py"
      to: "src/bensdorp1/db/audit.py"
      via: "from bensdorp1.db.audit import AuditEventType, log_event"
      pattern: "log_event"
---

<objective>
Implement audit.py (AuditEventType StrEnum + log_event() function) and its test file. The AuditEventType StrEnum must have exactly 17 members with the exact string values from STATE-04. log_event() must use SQLAlchemy Core parameterized inserts.

Purpose: audit.py is the structured event log primitive. Every command that changes state (buy, sell, fix, cash, scan, init, restore) writes to audit_log. Correctness here prevents silent data corruption and SQL injection via user-supplied symbols or payloads.

Output: audit.py (AuditEventType + log_event), test_db_audit.py (parametrized over all 17 event types + payload test)
</objective>

<execution_context>
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-RESEARCH.md
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-PATTERNS.md
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-02-SUMMARY.md
</execution_context>

<context>
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/REQUIREMENTS.md
@C:/Users/david/Documents/Projetos/bensdorp-1/pyproject.toml
@C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/db/schema.py
@C:/Users/david/Documents/Projetos/bensdorp-1/tests/conftest.py
@C:/Users/david/Documents/Projetos/bensdorp-1/tests/test_cli.py

<interfaces>
<!-- Key contracts audit.py depends on -->

From src/bensdorp1/db/schema.py (Wave 1 output):
  audit_log: Table  # table with columns: id, event_type, occurred_at, symbol, payload
  import as: from bensdorp1.db.schema import audit_log

From sqlalchemy (runtime imports):
  from sqlalchemy import insert, select
  from sqlalchemy.engine import Engine

From RESEARCH.md Pattern 6 (AuditEventType — use StrEnum, not str+Enum):
  from enum import StrEnum
  class AuditEventType(StrEnum):
      SYSTEM_INITIALIZED = "system_initialized"
      ... (17 members total — all lowercase snake_case values)
  str(AuditEventType.BUY_CONFIRMED) == "buy_confirmed"  # True with StrEnum

From RESEARCH.md Pattern 7 (log_event — verified under mypy strict):
  def log_event(engine: Engine, event_type: AuditEventType,
                symbol: str | None = None, payload: dict[str, Any] | None = None) -> None
  Uses: insert(audit_log).values(event_type=str(event_type), occurred_at=datetime.now(timezone.utc), ...)
  conn.commit() called inside with engine.connect() as conn block

From RESEARCH.md Pitfall 5 (CRITICAL):
  Do NOT use class AuditEventType(str, Enum) — Python 3.11 changed __str__ to return "ClassName.MEMBER"
  Must use class AuditEventType(StrEnum) from enum module

From tests/test_cli.py (parametrize pattern to reuse for 17 event types):
  @pytest.mark.parametrize("cmd", [...])
  def test_stub_exits_cleanly(cmd: str) -> None: ...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement audit.py with AuditEventType StrEnum and log_event()</name>
  <files>src/bensdorp1/db/audit.py</files>
  <read_first>
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-RESEARCH.md — Pattern 6 (AuditEventType — all 17 values listed verbatim); Pattern 7 (log_event — complete implementation verified under mypy strict); Pitfall 5 (StrEnum vs str+Enum — CRITICAL); Security Domain section (T-2-01 parameterized queries, T-2-02 StrEnum prevents bypass)
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-PATTERNS.md — audit.py section
    - C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/db/schema.py — confirm audit_log Table and its columns
  </read_first>
  <behavior>
    - str(AuditEventType.SYSTEM_INITIALIZED) == "system_initialized"
    - str(AuditEventType.BUY_CONFIRMED) == "buy_confirmed"
    - str(AuditEventType.POSITION_CLOSED_MANUAL) == "position_closed_manual"
    - len(list(AuditEventType)) == 17
    - log_event(engine, AuditEventType.SCAN_PERFORMED) inserts a row; selecting from audit_log where event_type=="scan_performed" returns 1 row
    - log_event(engine, AuditEventType.BUY_CONFIRMED, symbol="AAPL", payload={"price": 150.0}) stores JSON payload queryable by select(audit_log)
    - log_event() called twice inserts two rows (no unique constraint on audit_log)
  </behavior>
  <action>
    Create src/bensdorp1/db/audit.py with these exact contents:

    Imports:
    - json from stdlib
    - datetime, timezone from datetime stdlib
    - Any from typing
    - StrEnum from enum stdlib
    - insert from sqlalchemy
    - Engine from sqlalchemy.engine
    - audit_log from bensdorp1.db.schema

    Class AuditEventType(StrEnum) with exactly these 17 members (values are the string that gets stored in SQLite):
    - SYSTEM_INITIALIZED = "system_initialized"
    - SCAN_PERFORMED = "scan_performed"
    - BUY_CONFIRMED = "buy_confirmed"
    - SELL_CONFIRMED = "sell_confirmed"
    - SELL_MANUAL = "sell_manual"
    - TRANSACTION_CORRECTED = "transaction_corrected"
    - CASH_UPDATED = "cash_updated"
    - CONSTITUENTS_UPDATED = "constituents_updated"
    - CONSTITUENTS_DISCREPANCY = "constituents_discrepancy"
    - SPLIT_APPLIED = "split_applied"
    - POSITION_DELISTED_FROM_INDEX = "position_delisted_from_index"
    - REGIME_CHANGE_BULL_TO_BEAR = "regime_change_bull_to_bear"
    - REGIME_CHANGE_BEAR_TO_BULL = "regime_change_bear_to_bull"
    - DATA_FETCH_FAILED = "data_fetch_failed"
    - CATCH_UP_PERFORMED = "catch_up_performed"
    - RESTORE_PERFORMED = "restore_performed"
    - POSITION_CLOSED_MANUAL = "position_closed_manual"

    Function log_event(engine: Engine, event_type: AuditEventType, symbol: str | None = None, payload: dict[str, Any] | None = None) -> None:
    - Body: with engine.connect() as conn: conn.execute(insert(audit_log).values(event_type=str(event_type), occurred_at=datetime.now(timezone.utc), symbol=symbol, payload=json.dumps(payload) if payload is not None else None)); conn.commit()
    - Use parameterized insert().values() — NEVER string interpolation or f-strings in SQL (T-2-01 mitigation)
    - str(event_type) ensures the StrEnum value string is stored, not the enum repr

    Docstring on log_event: "Insert one structured event into the audit log. Uses parameterized SQL — no injection risk."
    Docstring on AuditEventType: "All 17 audit event types from STATE-04. StrEnum members are strings — no .value needed for SQLite TEXT storage."
  </action>
  <verify>
    <automated>uv run mypy src/bensdorp1/db/audit.py && uv run ruff check src/bensdorp1/db/audit.py</automated>
  </verify>
  <acceptance_criteria>
    - src/bensdorp1/db/audit.py is importable (python -c "from bensdorp1.db.audit import AuditEventType, log_event" exits 0)
    - len(list(AuditEventType)) == 17 (python -c "from bensdorp1.db.audit import AuditEventType; assert len(list(AuditEventType)) == 17")
    - str(AuditEventType.BUY_CONFIRMED) == "buy_confirmed" (not "AuditEventType.BUY_CONFIRMED")
    - StrEnum is used (grep for "class AuditEventType(StrEnum)" matches; grep for "(str, Enum)" returns no match)
    - insert(audit_log).values(...) is used in log_event (grep for "insert(audit_log)" matches)
    - No f-string SQL in log_event (no f"SELECT" or f"INSERT" patterns)
    - uv run mypy src/bensdorp1/db/audit.py exits 0 under strict mode
    - uv run ruff check src/bensdorp1/db/audit.py exits 0
  </acceptance_criteria>
  <done>audit.py has AuditEventType with exactly 17 StrEnum members and log_event() with parameterized insert; mypy strict and ruff both pass with zero errors</done>
</task>

<task type="auto">
  <name>Task 2: Create test_db_audit.py covering all 17 event types (STATE-04)</name>
  <files>tests/test_db_audit.py</files>
  <read_first>
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-RESEARCH.md — Validation Architecture: test_all_event_types_insertable, test_log_event_with_payload
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-PATTERNS.md — test_db_audit.py section (parametrize over AuditEventType pattern)
    - C:/Users/david/Documents/Projetos/bensdorp-1/tests/test_cli.py — @pytest.mark.parametrize pattern to replicate
    - C:/Users/david/Documents/Projetos/bensdorp-1/tests/conftest.py — confirm db_engine fixture signature
  </read_first>
  <action>
    Create tests/test_db_audit.py with these test functions:

    Imports: pytest, select from sqlalchemy, Engine from sqlalchemy.engine, AuditEventType and log_event from bensdorp1.db.audit, audit_log from bensdorp1.db.schema

    - test_all_event_types_insertable(db_engine: Engine, event_type: AuditEventType) -> None: decorated with @pytest.mark.parametrize("event_type", list(AuditEventType)); body: log_event(db_engine, event_type); with db_engine.connect() as conn: row = conn.execute(select(audit_log).where(audit_log.c.event_type == str(event_type))).fetchone(); assert row is not None

    - test_log_event_with_symbol(db_engine: Engine) -> None: log_event(db_engine, AuditEventType.BUY_CONFIRMED, symbol="AAPL"); with db_engine.connect() as conn: row = conn.execute(select(audit_log).where(audit_log.c.symbol == "AAPL")).fetchone(); assert row is not None; assert row.symbol == "AAPL"  # type: ignore[union-attr]

    - test_log_event_with_payload(db_engine: Engine) -> None: import json; log_event(db_engine, AuditEventType.SCAN_PERFORMED, payload={"regime_active": True, "candidate_count": 5}); with db_engine.connect() as conn: row = conn.execute(select(audit_log).where(audit_log.c.event_type == "scan_performed")).fetchone(); assert row is not None; decoded = json.loads(row.payload)  # type: ignore[union-attr]; assert decoded["regime_active"] is True; assert decoded["candidate_count"] == 5

    - test_log_event_without_symbol_or_payload(db_engine: Engine) -> None: log_event(db_engine, AuditEventType.REGIME_CHANGE_BULL_TO_BEAR); with db_engine.connect() as conn: row = conn.execute(select(audit_log).where(audit_log.c.event_type == "regime_change_bull_to_bear")).fetchone(); assert row is not None; assert row.symbol is None  # type: ignore[union-attr]; assert row.payload is None  # type: ignore[union-attr]

    - test_log_event_inserts_multiple_rows(db_engine: Engine) -> None: log_event(db_engine, AuditEventType.DATA_FETCH_FAILED); log_event(db_engine, AuditEventType.DATA_FETCH_FAILED); with db_engine.connect() as conn: rows = conn.execute(select(audit_log).where(audit_log.c.event_type == "data_fetch_failed")).fetchall(); assert len(rows) == 2

    - test_audit_event_type_str_value(db_engine: Engine) -> None: assert str(AuditEventType.SYSTEM_INITIALIZED) == "system_initialized"; assert str(AuditEventType.POSITION_CLOSED_MANUAL) == "position_closed_manual"; assert str(AuditEventType.BUY_CONFIRMED) == "buy_confirmed"

    All functions must have -> None return types. Fixture parameters explicitly typed.
    The row attribute accesses (row.symbol, row.payload) need # type: ignore[union-attr] because SQLAlchemy Row attributes return Any from mypy's perspective.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db_audit.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_db_audit.py exists with 6 test functions (5 regular + 1 parametrized that expands to 17 cases)
    - uv run pytest tests/test_db_audit.py -x -q exits 0 (all 22 test cases pass: 17 parametrized + 5 regular)
    - test_all_event_types_insertable passes for all 17 AuditEventType members — each can be inserted and queried by its string value
    - test_log_event_with_payload passes: JSON payload round-trips correctly through TEXT column
    - test_log_event_without_symbol_or_payload passes: symbol=None, payload=None stored as SQL NULL
    - test_log_event_inserts_multiple_rows passes: audit_log has no unique constraint; two inserts produce two rows
    - test_audit_event_type_str_value confirms StrEnum behavior: "system_initialized" not "AuditEventType.SYSTEM_INITIALIZED"
    - uv run pytest tests/ -x -q exits 0 (full suite including Phase 1 and Phase 2 Waves 1-4)
  </acceptance_criteria>
  <done>All 22 audit test cases pass (17 parametrized + 5 direct); STATE-04 requirement confirmed: all 17 event types insertable and queryable; full suite exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → log_event() | Caller supplies event_type (StrEnum-constrained), symbol (str or None), payload (dict or None) |
| log_event() → SQLite audit_log | Insert with parameterized values — no SQL concatenation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | log_event() symbol/payload parameters | mitigate | insert(audit_log).values(symbol=symbol, payload=...) uses SQLAlchemy parameterized binding — no string interpolation; symbol and payload are bound parameters, not concatenated SQL |
| T-2-02 | Tampering | audit_log.event_type column | mitigate | AuditEventType(StrEnum) with exactly 17 members; type system rejects any value not in the enum at call sites; str(event_type) produces a known value string |
| T-2-03 | Tampering | backup on write | accept | Handled in backup.py (Wave 3); audit.py does not call create_backup |
| T-2-SC | Tampering | pip installs | accept | No new packages; only stdlib enum, json, datetime used in addition to already-present sqlalchemy |
</threat_model>

<verification>
1. uv run pytest tests/test_db_audit.py -v exits 0 (22 cases: 17 parametrized + 5 direct)
2. uv run pytest tests/ -x -q exits 0 (full suite)
3. python -c "from bensdorp1.db.audit import AuditEventType; assert len(list(AuditEventType)) == 17; assert str(AuditEventType.BUY_CONFIRMED) == 'buy_confirmed'"
4. uv run ruff check src/bensdorp1/db/audit.py exits 0
5. uv run mypy src/bensdorp1/db/audit.py exits 0
</verification>

<success_criteria>
- audit.py defines AuditEventType(StrEnum) with exactly 17 members matching STATE-04 string values
- str(AuditEventType.member) returns the lowercase snake_case value (StrEnum behavior confirmed)
- log_event() uses insert(audit_log).values() — parameterized, no string interpolation (T-2-01 mitigated)
- All 17 event types confirmed insertable and queryable by parametrized test
- JSON payload round-trips correctly through TEXT column
- mypy strict and ruff both pass with zero errors on audit.py
</success_criteria>

<output>
Create .planning/phases/02-database-and-migrations/02-04-SUMMARY.md when done
</output>

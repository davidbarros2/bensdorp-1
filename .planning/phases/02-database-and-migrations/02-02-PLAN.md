---
phase: 02-database-and-migrations
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/bensdorp1/db/engine.py
  - tests/conftest.py
  - tests/test_db_schema.py
  - tests/test_db_engine.py
autonomous: true
requirements:
  - STATE-01

must_haves:
  truths:
    - "get_engine() returns a cached SQLAlchemy Engine; second call returns the same object without rebuilding"
    - "run_migrations(engine) calls metadata.create_all(engine, checkfirst=True) and is idempotent — second call does not raise"
    - "BENSDORP1_HOME env var resolves to a custom base directory; default is ~/bensdorp1/data/bensdorp1.db"
    - "Tests use a tmp_path file-based SQLite engine via the db_engine fixture; engine.dispose() is called in teardown"
    - "All 7 tables are confirmed present after run_migrations() via sqlalchemy.inspect"
  artifacts:
    - path: "src/bensdorp1/db/engine.py"
      provides: "get_engine(), run_migrations(), _build_engine(), _resolve_db_path(), _reset_engine_for_testing()"
      exports: ["get_engine", "run_migrations", "_build_engine", "_resolve_db_path", "_reset_engine_for_testing"]
    - path: "tests/conftest.py"
      provides: "db_engine fixture (tmp_path-based, with create_all and engine.dispose() in finally)"
    - path: "tests/test_db_schema.py"
      provides: "tests for STATE-01: all 7 tables present, create_all idempotent, partial index present"
    - path: "tests/test_db_engine.py"
      provides: "tests for STATE-01: BENSDORP1_HOME override, get_engine lazy cache, path override"
  key_links:
    - from: "src/bensdorp1/db/engine.py"
      to: "src/bensdorp1/db/schema.py"
      via: "from bensdorp1.db.schema import metadata"
      pattern: "metadata.create_all"
    - from: "tests/conftest.py"
      to: "src/bensdorp1/db/engine.py"
      via: "engine_module._build_engine(db_path) + _reset_engine_for_testing()"
      pattern: "_reset_engine_for_testing"
    - from: "tests/test_db_schema.py"
      to: "src/bensdorp1/db/engine.py"
      via: "db_engine fixture"
      pattern: "db_engine"
---

<objective>
Implement engine.py (the lazy-cached SQLAlchemy engine singleton with BENSDORP1_HOME resolution) and create the test infrastructure: conftest.py with db_engine fixture, test_db_schema.py for STATE-01 schema assertions, and test_db_engine.py for engine behavior.

Purpose: engine.py is the second link in the dependency chain (schema.py → engine.py). The db_engine fixture in conftest.py is required by every test module in this phase. Tests run against a real file-based SQLite engine in tmp_path to catch Windows file-handle issues.

Output: engine.py (complete), conftest.py (db_engine fixture), test_db_schema.py (10 STATE-01 assertions), test_db_engine.py (3+ STATE-01 behavioral tests)
</objective>

<execution_context>
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-RESEARCH.md
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-PATTERNS.md
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-01-SUMMARY.md
</execution_context>

<context>
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/ROADMAP.md
@C:/Users/david/Documents/Projetos/bensdorp-1/pyproject.toml
@C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/_app.py
@C:/Users/david/Documents/Projetos/bensdorp-1/tests/test_cli.py
@C:/Users/david/Documents/Projetos/bensdorp-1/tests/test_repo.py

<interfaces>
<!-- Contracts from Wave 1 that engine.py depends on -->

From src/bensdorp1/db/schema.py (Wave 1 output):
  metadata: MetaData = MetaData()  # import as: from bensdorp1.db.schema import metadata

From RESEARCH.md Pattern 3 (engine.py — full implementation, verified locally):
  Module-level variables:
    _engine: Optional[Engine] = None
    _engine_lock: threading.Lock = threading.Lock()

  Public functions:
    def get_engine(path: Optional[Path] = None) -> Engine
    def run_migrations(engine: Engine) -> None
    def _build_engine(path: Path) -> Engine
    def _resolve_db_path(override: Optional[Path]) -> Path
    def _reset_engine_for_testing(replacement: Optional[Engine] = None) -> None

From RESEARCH.md conftest.py example (verified locally on Windows):
  @pytest.fixture()
  def db_engine(tmp_path: Path) -> Generator[Engine, None, None]:
      db_path = tmp_path / "test.db"
      engine = engine_module._build_engine(db_path)
      engine_module._reset_engine_for_testing(engine)
      metadata.create_all(engine, checkfirst=True)
      try:
          yield engine
      finally:
          engine_module._reset_engine_for_testing()
          engine.dispose()  # CRITICAL on Windows: prevents [WinError 32] in tmp_path cleanup

From PATTERNS.md — test_db_schema.py pattern (sqlalchemy.inspect usage):
  from sqlalchemy import inspect
  def test_all_tables_created(db_engine: Engine) -> None:
      insp = inspect(db_engine)
      tables = set(insp.get_table_names())
      expected = {"config", "positions", "audit_log", "scans", "scan_candidates", "constituents_cache", "price_daily"}
      assert expected == tables
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement engine.py with lazy-cached engine singleton</name>
  <files>src/bensdorp1/db/engine.py</files>
  <read_first>
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-RESEARCH.md — Pattern 2 (URL.create), Pattern 3 (full engine.py implementation), Anti-Patterns section (never use f"sqlite:///{path}")
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-PATTERNS.md — "engine.py" section (full pattern listing)
    - C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/db/schema.py — confirm metadata is exported (import in run_migrations)
  </read_first>
  <behavior>
    - get_engine(path=None) called twice returns the same Engine object (identity: first_call is second_call)
    - get_engine(path=tmp_path/"test.db") returns an Engine whose URL contains the given path string
    - _resolve_db_path(None) with BENSDORP1_HOME="C:/custom" resolves to Path("C:/custom") / "data" / "bensdorp1.db"
    - _resolve_db_path(None) with no env var resolves to Path.home() / "bensdorp1" / "data" / "bensdorp1.db"
    - _resolve_db_path(Path("/explicit/path.db")) returns Path("/explicit/path.db") unchanged
    - run_migrations(engine) calls metadata.create_all(engine, checkfirst=True) — calling it twice does not raise
  </behavior>
  <action>
    Create src/bensdorp1/db/engine.py with these exact signatures and behaviors (per D-08):

    Imports: os, threading from stdlib; pathlib.Path; typing.Optional; sqlalchemy.engine.URL, Engine, create_engine; bensdorp1.db.schema.metadata (lazy import inside run_migrations to avoid circular import risk).

    Module-level variables:
    - _engine: Optional[Engine] = None
    - _engine_lock: threading.Lock = threading.Lock()

    Functions (all with -> None or explicit return type, per mypy strict disallow_incomplete_defs):
    - _resolve_db_path(override: Optional[Path]) -> Path: if override is not None return override; else read os.environ.get("BENSDORP1_HOME"); if set use Path(home_env) as base else Path.home() / "bensdorp1"; return base / "data" / "bensdorp1.db"
    - _build_engine(path: Path) -> Engine: url = URL.create("sqlite+pysqlite", database=str(path)); return create_engine(url). Do NOT use f"sqlite:///{path}" — use URL.create() only (per Anti-Patterns in RESEARCH.md).
    - get_engine(path: Optional[Path] = None) -> Engine: double-checked locking pattern — if _engine is None: acquire _engine_lock; if _engine is None: _engine = _build_engine(_resolve_db_path(path)); return _engine. Uses global _engine declaration.
    - run_migrations(engine: Engine) -> None: import metadata inside function body as "from bensdorp1.db.schema import metadata" to avoid circular import; call metadata.create_all(engine, checkfirst=True). Docstring: "Create all tables idempotently. Called only from bensdorp1 init (Phase 6)."
    - _reset_engine_for_testing(replacement: Optional[Engine] = None) -> None: with _engine_lock: if _engine is not None: _engine.dispose(); set _engine = replacement. Uses global _engine declaration.

    All functions must have -> return type annotation. No bare functions without return type.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db_engine.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/bensdorp1/db/engine.py exists and is importable
    - get_engine, run_migrations, _build_engine, _resolve_db_path, _reset_engine_for_testing are all defined
    - uv run mypy src/bensdorp1/db/engine.py exits 0 under strict mode
    - uv run ruff check src/bensdorp1/db/engine.py exits 0
    - _resolve_db_path(None) with BENSDORP1_HOME set returns Path(env_value) / "data" / "bensdorp1.db"
    - _resolve_db_path(override=Path("x")) returns Path("x") without reading env vars
    - URL.create("sqlite+pysqlite", ...) is used — no f-string URL construction
  </acceptance_criteria>
  <done>engine.py passes mypy strict and ruff check with zero errors; all behavioral tests in test_db_engine.py pass</done>
</task>

<task type="auto">
  <name>Task 2: Create conftest.py fixture and STATE-01 test files</name>
  <files>tests/conftest.py, tests/test_db_schema.py, tests/test_db_engine.py</files>
  <read_first>
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-RESEARCH.md — "Test fixture pattern (conftest.py)" code example; "Test for STATE-06 enforcement" pattern (use same import style); Validation Architecture test map for STATE-01 test names
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-PATTERNS.md — conftest.py, test_db_schema.py, test_db_engine.py pattern sections
    - C:/Users/david/Documents/Projetos/bensdorp-1/tests/test_cli.py — existing test conventions (import style, -> None, parametrize pattern)
    - C:/Users/david/Documents/Projetos/bensdorp-1/tests/test_repo.py — existence-assertion test pattern
    - C:/Users/david/Documents/Projetos/bensdorp-1/pyproject.toml — confirm [tool.pytest.ini_options] testpaths = ["tests"]
  </read_first>
  <action>
    Create tests/conftest.py containing:
    - Imports: pytest, pathlib.Path, typing.Generator, sqlalchemy.engine.Engine, bensdorp1.db.engine as engine_module, bensdorp1.db.schema.metadata
    - db_engine fixture with signature: def db_engine(tmp_path: Path) -> Generator[Engine, None, None]
    - Body: db_path = tmp_path / "test.db"; engine = engine_module._build_engine(db_path); engine_module._reset_engine_for_testing(engine); metadata.create_all(engine, checkfirst=True); try: yield engine; finally: engine_module._reset_engine_for_testing(); engine.dispose()
    - The engine.dispose() in finally is CRITICAL on Windows to prevent [WinError 32] during tmp_path cleanup

    Create tests/test_db_schema.py containing these test functions (all -> None, all accept db_engine: Engine):
    - test_all_tables_created(db_engine: Engine) -> None: use sqlalchemy.inspect(db_engine).get_table_names(); assert set equals {"config", "positions", "audit_log", "scans", "scan_candidates", "constituents_cache", "price_daily"}
    - test_create_all_idempotent(db_engine: Engine) -> None: import metadata from bensdorp1.db.schema; call metadata.create_all(db_engine, checkfirst=True) a second time; assert no exception raised
    - test_partial_index_exists(db_engine: Engine) -> None: insp = inspect(db_engine); index_names = {idx["name"] for idx in insp.get_indexes("positions")}; assert "ix_positions_open_symbol" in index_names
    - test_price_daily_unique_index_exists(db_engine: Engine) -> None: insp = inspect(db_engine); indexes = insp.get_indexes("price_daily"); names = {i["name"] for i in indexes}; assert "ix_price_daily_symbol_date" in names
    - test_audit_log_indexes_exist(db_engine: Engine) -> None: insp = inspect(db_engine); names = {i["name"] for i in insp.get_indexes("audit_log")}; assert {"ix_audit_log_occurred_at", "ix_audit_log_symbol", "ix_audit_log_event_type"}.issubset(names)
    - test_positions_columns(db_engine: Engine) -> None: insp = inspect(db_engine); cols = {c["name"] for c in insp.get_columns("positions")}; assert {"id", "symbol", "entry_date", "entry_close", "shares", "initial_stop", "highest_close", "trailing_stop", "scan_id", "closed_at", "exit_price", "realized_pnl"} == cols

    Create tests/test_db_engine.py containing:
    - test_build_engine_returns_engine(tmp_path: Path) -> None: import engine_module; engine = engine_module._build_engine(tmp_path / "t.db"); from sqlalchemy.engine import Engine; assert isinstance(engine, Engine); engine.dispose()
    - test_resolve_db_path_uses_bensdorp1_home(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None: import engine_module; _reset_engine_for_testing() first; monkeypatch.setenv("BENSDORP1_HOME", str(tmp_path)); resolved = engine_module._resolve_db_path(None); assert resolved == tmp_path / "data" / "bensdorp1.db"
    - test_resolve_db_path_override(tmp_path: Path) -> None: import engine_module; override = tmp_path / "custom.db"; resolved = engine_module._resolve_db_path(override); assert resolved == override
    - test_run_migrations_idempotent(db_engine: Engine) -> None: from bensdorp1.db.engine import run_migrations; run_migrations(db_engine); run_migrations(db_engine)  # second call must not raise

    All test functions must have -> None return type annotations. All fixture parameters typed explicitly (db_engine: Engine, tmp_path: Path, monkeypatch: pytest.MonkeyPatch). Add a [[tool.mypy.overrides]] for module = "tests.*" with disallow_untyped_decorators = false to pyproject.toml if mypy strict fires on fixture parameters — but prefer explicit annotations first.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db_schema.py tests/test_db_engine.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py exists; db_engine fixture yields an Engine; engine.dispose() is in finally block
    - uv run pytest tests/test_db_schema.py -x -q exits 0 (all 6 schema tests pass)
    - uv run pytest tests/test_db_engine.py -x -q exits 0 (all 4 engine tests pass)
    - uv run pytest tests/ -x -q exits 0 (full suite including existing Phase 1 tests)
    - uv run mypy src/bensdorp1/db/ exits 0 (engine.py passes strict mode)
    - test_all_tables_created passes: exactly 7 tables found matching expected set
    - test_partial_index_exists passes: ix_positions_open_symbol in positions indexes
    - test_resolve_db_path_uses_bensdorp1_home passes: monkeypatch env var correctly changes resolution
  </acceptance_criteria>
  <done>All schema and engine tests pass; conftest.py db_engine fixture is available for all downstream test files in Waves 3–5; full test suite (Phase 1 + Phase 2 Wave 2) exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BENSDORP1_HOME env var → Path resolution | Untrusted input (env var) used to construct filesystem path for DB location |
| engine → SQLite file | Engine creates/opens a file at the resolved path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | engine.py DML (none in this plan) | accept | engine.py defines no DML; all DML is in backup.py and audit.py (Waves 3–4) with parameterized queries |
| T-2-02 | Tampering | AuditEventType bypass | accept | Not in scope for this plan; mitigated in Wave 4 (audit.py StrEnum) |
| T-2-03 | Tampering | backup via file copy | accept | Not in scope for this plan; mitigated in Wave 3 (backup.py) |
| T-2-04 | Spoofing | BENSDORP1_HOME path traversal | accept | Single-user personal CLI; local filesystem only; no privilege boundary crossed; Path() construction is safe |
| T-2-SC | Tampering | pip installs | accept | No new packages; all dependencies already in pyproject.toml with verified versions |
</threat_model>

<verification>
1. uv run pytest tests/test_db_schema.py tests/test_db_engine.py -v exits 0
2. uv run pytest tests/ -x -q exits 0 (includes Phase 1 tests)
3. uv run ruff check src/bensdorp1/db/ tests/ exits 0
4. uv run mypy src/bensdorp1/db/ exits 0
</verification>

<success_criteria>
- engine.py implements get_engine(), run_migrations(), _build_engine(), _resolve_db_path(), _reset_engine_for_testing() — all with explicit return types
- BENSDORP1_HOME env var correctly overrides the DB path resolution (confirmed by test_resolve_db_path_uses_bensdorp1_home)
- run_migrations() is idempotent: calling it twice in test_run_migrations_idempotent does not raise
- conftest.py db_engine fixture uses engine.dispose() in finally — safe on Windows
- All 7 tables confirmed present via sqlalchemy.inspect in test_all_tables_created
- Partial index ix_positions_open_symbol confirmed present via sqlalchemy.inspect
- Full test suite exits 0 (Phase 1 + Phase 2 Wave 2 tests)
</success_criteria>

<output>
Create .planning/phases/02-database-and-migrations/02-02-SUMMARY.md when done
</output>

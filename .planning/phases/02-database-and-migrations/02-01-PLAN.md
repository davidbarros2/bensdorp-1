---
phase: 02-database-and-migrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bensdorp1/db/__init__.py
  - src/bensdorp1/db/schema.py
autonomous: true
requirements:
  - STATE-01
  - STATE-06

must_haves:
  truths:
    - "All 7 SQLite tables are defined with their exact columns and constraints in a single schema.py module"
    - "The partial unique index ix_positions_open_symbol prevents simultaneous open positions in the same symbol at the DB level"
    - "schema.py has no imports from the db/ package — it is a pure DDL file"
    - "metadata is a shared MetaData() singleton that engine.py, backup.py, and audit.py import"
    - "db/__init__.py exposes a clean public surface for downstream phases"
  artifacts:
    - path: "src/bensdorp1/db/__init__.py"
      provides: "Package init with re-export stubs (placeholders until engine.py/backup.py/audit.py exist)"
    - path: "src/bensdorp1/db/schema.py"
      provides: "All 7 Table objects + shared MetaData singleton + all indexes"
      contains: "metadata: MetaData = MetaData()"
  key_links:
    - from: "src/bensdorp1/db/schema.py"
      to: "positions table"
      via: "partial unique index ix_positions_open_symbol"
      pattern: "ix_positions_open_symbol"
    - from: "src/bensdorp1/db/schema.py"
      to: "src/bensdorp1/db/engine.py (Wave 2)"
      via: "from bensdorp1.db.schema import metadata"
      pattern: "metadata"
---

<objective>
Create the db/ subpackage foundation: the shared metadata object and all 7 table definitions with their columns and indexes. This is the single source of truth for all DDL — every other db/ module imports from here.

Purpose: schema.py must exist before engine.py, backup.py, or audit.py can be written. The partial unique index on positions enforces STATE-06 at the database level, requiring no application-layer check.

Output: src/bensdorp1/db/schema.py (complete), src/bensdorp1/db/__init__.py (placeholder with comment stubs for Wave 2+ imports)
</objective>

<execution_context>
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-RESEARCH.md
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-PATTERNS.md
</execution_context>

<context>
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/ROADMAP.md
@C:/Users/david/Documents/Projetos/bensdorp-1/.planning/REQUIREMENTS.md
@C:/Users/david/Documents/Projetos/bensdorp-1/pyproject.toml
@C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/_app.py

<interfaces>
<!-- Key patterns from existing codebase that schema.py must follow -->

From src/bensdorp1/_app.py (module-level singleton pattern):
  app = typer.Typer(name="bensdorp1", ...)  # module-level singleton — apply same pattern to metadata

From RESEARCH.md Pattern 1 (MetaData singleton):
  metadata: MetaData = MetaData()

From RESEARCH.md Pattern 4 (partial unique index — MANDATORY noqa):
  Index("ix_positions_open_symbol", positions.c.symbol, unique=True,
        sqlite_where=(positions.c.closed_at == None))  # noqa: E711

From RESEARCH.md Code Examples (complete schema.py skeleton — use verbatim column definitions):
  Table definition order: config → scans → positions → audit_log → scan_candidates → constituents_cache → price_daily

SQLAlchemy imports needed (all from sqlalchemy flat namespace):
  Boolean, Column, DateTime, Float, ForeignKey, Index, Integer, MetaData, Table, Text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/bensdorp1/db/ subpackage with schema.py</name>
  <files>src/bensdorp1/db/__init__.py, src/bensdorp1/db/schema.py</files>
  <read_first>
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-RESEARCH.md — "Code Examples" section: complete schema.py skeleton (use verbatim column definitions)
    - C:/Users/david/Documents/Projetos/bensdorp-1/.planning/phases/02-database-and-migrations/02-PATTERNS.md — "Pattern Assignments" for schema.py and db/__init__.py
    - C:/Users/david/Documents/Projetos/bensdorp-1/pyproject.toml — confirm sqlalchemy>=2.0.49,<2.1 is in [project.dependencies] (already present)
    - C:/Users/david/Documents/Projetos/bensdorp-1/src/bensdorp1/_app.py — module-level singleton pattern to replicate for metadata
  </read_first>
  <action>
    Create src/bensdorp1/db/__init__.py as a minimal package init. It will re-export db/ public symbols once the other modules exist. For Wave 1, declare __all__ as a placeholder comment list and add a module docstring. Do NOT import from engine.py, backup.py, or audit.py yet — they do not exist. Content: module docstring stating "Public surface of the db subpackage. Imports added in Waves 2–4." and __all__ = [] as a placeholder.

    Create src/bensdorp1/db/schema.py containing:
    1. Imports: Boolean, Column, DateTime, Float, ForeignKey, Index, Integer, MetaData, Table, Text — all from sqlalchemy flat namespace. No other imports (no stdlib, no local imports).
    2. Module-level singleton: metadata: MetaData = MetaData()
    3. Seven Table objects defined in this exact order (parent tables before child tables):
       - config: Column("key", Text, primary_key=True), Column("value", Text, nullable=True), Column("updated_at", DateTime, nullable=False)
       - scans: Column("id", Integer, primary_key=True, autoincrement=True), Column("scan_date", DateTime, nullable=False, unique=True), Column("regime_active", Boolean, nullable=False), Column("candidate_count", Integer, nullable=False), Column("exit_trigger_count", Integer, nullable=False), Column("raw_output", Text, nullable=True), Column("created_at", DateTime, nullable=False)
       - positions: Column("id", Integer, primary_key=True, autoincrement=True), Column("symbol", Text, nullable=False), Column("entry_date", DateTime, nullable=False), Column("entry_close", Float, nullable=False), Column("shares", Integer, nullable=False), Column("initial_stop", Float, nullable=False), Column("highest_close", Float, nullable=False), Column("trailing_stop", Float, nullable=False), Column("scan_id", Integer, ForeignKey("scans.id"), nullable=True), Column("closed_at", DateTime, nullable=True), Column("exit_price", Float, nullable=True), Column("realized_pnl", Float, nullable=True)
       - audit_log: Column("id", Integer, primary_key=True, autoincrement=True), Column("event_type", Text, nullable=False), Column("occurred_at", DateTime, nullable=False), Column("symbol", Text, nullable=True), Column("payload", Text, nullable=True)
       - scan_candidates: Column("id", Integer, primary_key=True, autoincrement=True), Column("scan_id", Integer, ForeignKey("scans.id"), nullable=False), Column("symbol", Text, nullable=False), Column("rank", Integer, nullable=False), Column("roc200", Float, nullable=False), Column("close", Float, nullable=False), Column("suggested_shares", Integer, nullable=False)
       - constituents_cache: Column("id", Integer, primary_key=True, autoincrement=True), Column("symbol", Text, nullable=False, unique=True), Column("company_name", Text, nullable=True), Column("fetched_at", DateTime, nullable=False)
       - price_daily: Column("id", Integer, primary_key=True, autoincrement=True), Column("symbol", Text, nullable=False), Column("trade_date", DateTime, nullable=False), Column("close", Float, nullable=False), Column("volume", Integer, nullable=True)
    4. Indexes defined immediately after their parent table (not grouped at the bottom):
       - After positions: Index("ix_positions_open_symbol", positions.c.symbol, unique=True, sqlite_where=(positions.c.closed_at == None))  # noqa: E711
       - After audit_log: Index("ix_audit_log_occurred_at", audit_log.c.occurred_at), Index("ix_audit_log_symbol", audit_log.c.symbol), Index("ix_audit_log_event_type", audit_log.c.event_type)
       - After price_daily: Index("ix_price_daily_symbol_date", price_daily.c.symbol, price_daily.c.trade_date, unique=True)
    5. All 7 table variables annotated as Table: config: Table = Table(...), scans: Table = Table(...), etc.

    Critical constraints:
    - The # noqa: E711 comment on the sqlite_where line is mandatory — do NOT change == None to is None
    - Do NOT add the SQLAlchemy mypy plugin to pyproject.toml (it is deprecated and must never appear)
    - No __all__ in schema.py — all 7 Table objects and metadata are exported implicitly (imported by name by engine.py etc.)
    - No functions in schema.py — pure DDL declarations only
  </action>
  <verify>
    <automated>uv run python -c "from bensdorp1.db.schema import metadata, config, scans, positions, audit_log, scan_candidates, constituents_cache, price_daily; print('Tables:', sorted(metadata.tables.keys())); assert len(metadata.tables) == 7"</automated>
  </verify>
  <acceptance_criteria>
    - src/bensdorp1/db/__init__.py exists and is importable (python -c "import bensdorp1.db" exits 0)
    - src/bensdorp1/db/schema.py exists and is importable (python -c "from bensdorp1.db.schema import metadata" exits 0)
    - metadata.tables contains exactly 7 keys: config, scans, positions, audit_log, scan_candidates, constituents_cache, price_daily
    - positions table has columns: id, symbol, entry_date, entry_close, shares, initial_stop, highest_close, trailing_stop, scan_id, closed_at, exit_price, realized_pnl (12 columns)
    - audit_log table has columns: id, event_type, occurred_at, symbol, payload (5 columns)
    - scans table has scan_date with unique=True
    - ix_positions_open_symbol index is registered in metadata (confirmed by: python -c "from bensdorp1.db.schema import positions; print([i.name for i in positions.indexes])" showing "ix_positions_open_symbol")
    - ix_price_daily_symbol_date index is registered (unique=True, two columns: symbol, trade_date)
    - ix_audit_log_occurred_at, ix_audit_log_symbol, ix_audit_log_event_type indexes are registered
    - uv run ruff check src/bensdorp1/db/schema.py exits 0 (E711 suppressed with noqa, all other rules pass)
    - uv run mypy src/bensdorp1/db/schema.py exits 0 under strict mode
  </acceptance_criteria>
  <done>schema.py defines all 7 tables with correct columns, 6 indexes (1 partial unique, 1 composite unique, 3 audit, 1 scan_date uniqueness is inline), and mypy + ruff both pass clean on the file</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.py → SQLite | Table/Column/Index definitions executed by SQLAlchemy at create_all() time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | positions, audit_log tables | mitigate | SQLAlchemy Core parameterized insert().values() is enforced in audit.py (Wave 4) and positions tests (Wave 5); schema itself defines no DML |
| T-2-02 | Tampering | audit_log.event_type column | mitigate | AuditEventType StrEnum (Wave 4) restricts insertable values to the 17 valid strings; schema enforces NOT NULL |
| T-2-03 | Tampering | backup operations | mitigate | backup.py (Wave 3) uses sqlite3.Connection.backup() — transactionally safe; file copy is never used |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 2; all packages already in pyproject.toml and audited in RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
1. uv run python -c "from bensdorp1.db.schema import metadata; assert len(metadata.tables) == 7, metadata.tables.keys()"
2. uv run python -c "from bensdorp1.db.schema import positions; names = [i.name for i in positions.indexes]; assert 'ix_positions_open_symbol' in names, names"
3. uv run ruff check src/bensdorp1/db/ exits 0
4. uv run mypy src/bensdorp1/db/ exits 0
</verification>

<success_criteria>
- src/bensdorp1/db/schema.py is importable and defines exactly 7 tables with correct column specifications
- The partial unique index ix_positions_open_symbol is defined with sqlite_where=(positions.c.closed_at == None) and # noqa: E711
- ix_price_daily_symbol_date composite unique index on (symbol, trade_date) is defined
- Three audit_log indexes are defined: ix_audit_log_occurred_at, ix_audit_log_symbol, ix_audit_log_event_type
- ruff check and mypy strict both pass with zero errors on src/bensdorp1/db/
- db/__init__.py exists and is importable (package recognized by Python)
</success_criteria>

<output>
Create .planning/phases/02-database-and-migrations/02-01-SUMMARY.md when done
</output>

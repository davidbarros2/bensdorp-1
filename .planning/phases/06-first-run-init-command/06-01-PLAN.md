---
phase: 06-first-run-init-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bensdorp1/commands/init.py
  - tests/test_cli.py
autonomous: true
requirements:
  - CMD-01

must_haves:
  truths:
    - "Running `bensdorp1 init` on a clean system produces a populated database with constituents, price history, and cash"
    - "Re-running `bensdorp1 init` when the DB file already exists prints an error mentioning both recovery paths and exits with code 1"
    - "Pressing Ctrl+C during cash entry prints 'Operation aborted. No changes were made.' and exits cleanly"
    - "Cash ≤ 0 triggers a re-prompt without aborting; only a positive float accepted"
    - "The system_initialized audit event is written with constituent_count, cash, and history_days=220 in the payload"
    - "The completion summary displays tilde-form paths, formatted cash, and total elapsed time"
  artifacts:
    - path: "src/bensdorp1/commands/init.py"
      provides: "Full interactive init command replacing the stub"
      min_lines: 80
    - path: "tests/test_cli.py"
      provides: "test_stub_exits_cleanly no longer includes 'init'"
  key_links:
    - from: "src/bensdorp1/commands/init.py"
      to: "bensdorp1.db"
      via: "get_engine, run_migrations, log_event, create_backup, AuditEventType"
      pattern: "from bensdorp1.db import"
    - from: "src/bensdorp1/commands/init.py"
      to: "bensdorp1.data"
      via: "get_constituents, update_price_data"
      pattern: "from bensdorp1.data import"
    - from: "src/bensdorp1/commands/init.py"
      to: "bensdorp1.ui"
      via: "feedback, confirm_prompt, number_prompt, print_error, format_price"
      pattern: "from bensdorp1.ui import"
---

<objective>
Implement the `init` command in full, replacing the one-liner stub with the complete interactive first-run setup flow defined in spec §7.1. Also update `test_cli.py` to remove `"init"` from the stub-only parametrize list so the test suite stays green.

Purpose: Phase 6's primary deliverable — a user can run `bensdorp1 init` on a clean machine and end with a populated, ready-to-scan database.
Output: `src/bensdorp1/commands/init.py` (full implementation) + `tests/test_cli.py` (init removed from stub list).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-first-run-init-command/06-CONTEXT.md
@.planning/phases/06-first-run-init-command/06-RESEARCH.md
@.planning/phases/06-first-run-init-command/06-PATTERNS.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Read from codebase. -->

From src/bensdorp1/config.py:
  DATA_DIR: Path = Path(os.environ.get("BENSDORP1_HOME", str(Path.home() / "bensdorp1")))

From src/bensdorp1/db/__init__.py (all re-exported — import from bensdorp1.db):
  AuditEventType.SYSTEM_INITIALIZED = "system_initialized"
  get_engine(path: Path | None = None) -> Engine          # lazy singleton; path must be DATA_DIR/"data"/"bensdorp1.db"
  run_migrations(engine: Engine) -> None                  # idempotent, checkfirst=True
  log_event(engine, event_type, symbol=None, payload=None) -> None
  create_backup(engine: Engine, backups_dir: Path) -> Path  # backups_dir is NOT auto-resolved

From src/bensdorp1/db/schema.py (NOT re-exported; import directly):
  config: Table  # columns: key (Text PK), value (Text nullable), updated_at (DateTime with tz)

From src/bensdorp1/data/__init__.py (all re-exported — import from bensdorp1.data):
  get_constituents(engine: Engine) -> dict[str, str]       # {symbol: company_name}; includes cross-check internally
  update_price_data(engine: Engine, symbols: list[str], start=None, end=None) -> None

From src/bensdorp1/ui/__init__.py (public surface — import from bensdorp1.ui ONLY):
  feedback                                                  # namespace; feedback.multi_step(total, console=None) -> MultiStepContext
  MultiStepContext.__enter__() -> MultiStepContext
  MultiStepContext.step(description: str, total: int | None = None) -> SpinnerContext | TrackContext
    # total=None → SpinnerContext; total=N → TrackContext
    # Prints "[N/TOTAL] description" at __enter__; "[N/TOTAL] description... done." at __exit__
    # Detail lines must be printed AFTER the with-block exits (not inside it)
  TrackContext.advance(label: str) -> None
  confirm_prompt(message: str, console: Console | None = None) -> bool
  number_prompt(label: str, unit: str, console: Console | None = None) -> float  # handles non-numeric re-prompt internally
  print_error(title: str, *, actions: list[str] | None = None, console: Console | None = None) -> None
  format_price(value: float) -> str                        # returns "$X,XXX.XX"

From src/bensdorp1/ui/styles.py (private — one allowed direct import for completion summary):
  _render_kv_block(data: dict[str, str], console: Console, indent: str = "") -> None
    # Aligns values at max_key_len + 1 (colon) + 2 spaces; markup=False, highlight=False

From sqlalchemy.dialects.sqlite:
  insert as sqlite_insert
  # .on_conflict_do_update(index_elements=["key"], set_={"value": ..., "updated_at": ...})
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement init.py — full interactive flow</name>
  <files>src/bensdorp1/commands/init.py</files>

  <read_first>
    - src/bensdorp1/commands/init.py (current stub — replace entirely)
    - src/bensdorp1/commands/help.py (command decorator + exit pattern analog)
    - src/bensdorp1/config.py (DATA_DIR resolution)
    - src/bensdorp1/db/audit.py (log_event signature + AuditEventType members)
    - src/bensdorp1/db/backup.py (create_backup signature)
    - src/bensdorp1/db/schema.py (config table columns)
    - src/bensdorp1/data/prices.py (update_price_data signature, lines 222-255)
    - src/bensdorp1/ui/progress.py (MultiStepContext.step + TrackContext.advance, lines 272-310)
    - src/bensdorp1/ui/prompts.py (confirm_prompt + number_prompt signatures)
    - src/bensdorp1/ui/styles.py (_render_kv_block signature, lines 146-163)
    - .planning/Bensdorp_1.md §7.1 (exact terminal output layout — every string must match)
    - .planning/phases/06-first-run-init-command/06-PATTERNS.md (all patterns: imports, helpers, loop, kv block)
  </read_first>

  <action>
Replace src/bensdorp1/commands/init.py entirely. The file must contain:

IMPORTS — exactly these, in ruff-ordered sections:
  stdlib: import time; from datetime import UTC, datetime; from pathlib import Path
  rich: from rich.console import Console; from rich.text import Text
  sqlalchemy: from sqlalchemy.dialects.sqlite import insert as sqlite_insert; from sqlalchemy.engine import Engine
  typer: import typer
  bensdorp1._app: from bensdorp1._app import app
  bensdorp1.config: from bensdorp1.config import DATA_DIR
  bensdorp1.db: from bensdorp1.db import AuditEventType, create_backup, get_engine, log_event, run_migrations
  bensdorp1.db.schema: from bensdorp1.db.schema import config as config_table
  bensdorp1.data: from bensdorp1.data import get_constituents, update_price_data
  bensdorp1.ui: from bensdorp1.ui import confirm_prompt, feedback, format_price, number_prompt, print_error
  bensdorp1.ui.styles (private, allowed once): from bensdorp1.ui.styles import _render_kv_block
    Add inline comment: "# direct import of private helper — completion summary requires column-aligned kv pairs (no public equivalent)"

MODULE-LEVEL CONSTANTS:
  SEPARATOR: str = "=" * 64   # matches spec §7.1 exactly — 64 '=' characters
  CASH_HEADER: str = "Available cash"
  CASH_SEPARATOR: str = "-" * len(CASH_HEADER)   # 14 dashes, matching spec §7.1 Step 2

PRIVATE HELPERS (module-level, before the command function):

  def _tilde_path(p: Path) -> str:
    Replaces Path.home() prefix with "~/". Falls back to str(p) if p is not under home.
    Implementation: try "~/" + str(p.relative_to(Path.home())); except ValueError: return str(p)

  def _format_elapsed(seconds: float) -> str:
    Formats total elapsed time as "Xm Ys" when >= 60 seconds, or "Xs" when < 60 seconds.
    Use integer division: minutes = int(seconds) // 60; secs = int(seconds) % 60.
    If minutes > 0: return f"{minutes}m {secs}s"; else: return f"{secs}s"

  def _store_cash(engine: Engine, amount: float) -> None:
    Upserts key="available_cash" into the config table. Value stored as f"{amount:.2f}" (per Pitfall 5 — deterministic precision).
    Use sqlite_insert(config_table).values(key="available_cash", value=f"{amount:.2f}", updated_at=datetime.now(UTC)).on_conflict_do_update(index_elements=["key"], set_={"value": f"{amount:.2f}", "updated_at": datetime.now(UTC)}).
    Execute and commit inside engine.connect() context.

COMMAND FUNCTION:

  @app.command(rich_help_panel="Setup")
  def init() -> None — docstring: "First-run setup: create data directory, download history, record cash."

  Body structure (EXACT order — no deviations):

  1. GUARD (per D-01, D-02):
     db_path = DATA_DIR / "data" / "bensdorp1.db"
     if db_path.exists(): call print_error("System already initialized.", actions=["Delete the file and run `bensdorp1 init` again.", "Run `bensdorp1 restore PATH` to replace with a backup."]); raise typer.Exit(1)

  2. CONSOLE: console = Console()

  3. WELCOME SCREEN (Step 1 per spec §7.1 — exact strings):
     console.print(Text(SEPARATOR))
     console.print(Text("bensdorp1 — System #1: Trend following on S&P 500 stocks"))
     console.print(Text("First-time setup"))
     console.print(Text(SEPARATOR))
     console.print()
     console.print(Text("This is a one-time setup. It will:"))
     console.print()
     console.print(Text("  1. Create local database in ~/bensdorp1/"))
     console.print(Text("  2. Download current S&P 500 constituents list"))
     console.print(Text("  3. Download 220 days of price history for all constituents"))
     console.print(Text("  4. Register your initial available cash"))
     console.print()
     console.print(Text("Step 3 may take 5 to 10 minutes depending on connection speed."))
     console.print()

  4. PRE-CONFIRMATION PROMPT (per D-07):
     if not confirm_prompt("Continue?", console=console): raise typer.Exit()
     console.print()

  5. CASH DECLARATION BLOCK (per D-03, D-04, D-05; Step 2 per spec §7.1):
     Wrap the entire block in try/except KeyboardInterrupt. On KeyboardInterrupt: console.print(); console.print(Text("Operation aborted. No changes were made.")); raise typer.Exit()
     Inside try:
       console.print(Text(CASH_HEADER))
       console.print(Text(CASH_SEPARATOR))
       console.print(Text("Enter the amount of cash you have available for trading."))
       console.print(Text("This is the amount the system will use to size new positions."))
       console.print()
       while True loop:
         cash = number_prompt("Available cash", "USD", console=console)
         if cash <= 0: console.print(Text("Error: Cash must be greater than zero.")); continue
         console.print()
         console.print(Text(f"Available cash: {format_price(cash)}"))
         console.print()
         if confirm_prompt("Confirm?", console=console): break
         # user said n — loop back to re-enter
     console.print()

  6. SETUP EXECUTION (Step 3 per spec §7.1):
     start_time = time.monotonic()
     engine = get_engine(db_path)
     run_migrations(engine)
     console.print(Text("Setting up your system"))
     console.print()
     with feedback.multi_step(3, console=console) as ms:
       Step [1/3] — Fetching S&P 500 constituents (SpinnerContext, total=None):
         with ms.step("Fetching S&P 500 constituents"):
           constituents = get_constituents(engine)
         # Print detail lines AFTER the with-block (not inside — Pitfall 1):
         console.print(Text("      Source: Wikipedia"))
         console.print(Text(f"      Stocks found: {len(constituents)}"))
         console.print()
       Step [2/3] — Verifying against secondary source (SpinnerContext, total=None):
         with ms.step("Verifying against secondary source"):
           pass  # cross-check already happened inside get_constituents(); step 2 is a display beat
         console.print()
       Step [3/3] — Downloading price history (TrackContext, total=len(constituents)):
         symbols = list(constituents.keys())
         with ms.step("Downloading price history", total=len(symbols)) as track:
           for symbol in symbols:
             update_price_data(engine, [symbol])
             track.advance(symbol)
     elapsed = time.monotonic() - start_time

  7. POST-DOWNLOAD STATE:
     _store_cash(engine, cash)
     log_event(engine, AuditEventType.SYSTEM_INITIALIZED, payload={"constituent_count": len(constituents), "cash": cash, "history_days": 220})
     create_backup(engine, DATA_DIR / "backups")

  8. COMPLETION SUMMARY (Step 4 per spec §7.1 — exact strings):
     console.print()
     console.print(Text(SEPARATOR))
     console.print(Text("Setup complete"))
     console.print(Text(SEPARATOR))
     console.print()
     _render_kv_block({"Database created": _tilde_path(db_path), "Backups location": _tilde_path(DATA_DIR / "backups") + "/", "Constituents": f"{len(constituents)} stocks", "History downloaded": "220 trading days", "Available cash": format_price(cash), "Total time": _format_elapsed(elapsed)}, console)
     console.print()
     console.print(Text("Next steps:"))
     console.print(Text("  1. Wait for end-of-day market close (16:00 ET / 21:00 Lisbon)"))
     console.print(Text("  2. Run `bensdorp1 scan` to see today's buy candidates and exit triggers"))
     console.print(Text("  3. Run `bensdorp1 help` to see all available commands"))

  MYPY STRICT compliance requirements:
  - All functions must have explicit return type annotations (-> None, -> str)
  - cash variable must be typed or inferable as float (number_prompt returns float)
  - constituents variable type is dict[str, str] (get_constituents return type)
  - No bare except clauses
  - track variable from ms.step() is TrackContext when total=N — import TrackContext if needed to satisfy mypy; OR use # type: ignore[assignment] only if mypy cannot resolve it
  - Engine import is from sqlalchemy.engine import Engine (already in imports list above)

  RUFF compliance: LF line endings; imports ordered per ruff isort; no unused imports.
  NO fenced code blocks inline in the file's docstring or comments.
  </action>

  <verify>
    <automated>uv run python -c "from bensdorp1.commands.init import init; print('import ok')"</automated>
    <automated>uv run mypy src/bensdorp1/commands/init.py --strict</automated>
    <automated>uv run ruff check src/bensdorp1/commands/init.py</automated>
    <automated>uv run ruff format --check src/bensdorp1/commands/init.py</automated>
  </verify>

  <acceptance_criteria>
    - src/bensdorp1/commands/init.py imports from bensdorp1.db (not submodules), bensdorp1.data, bensdorp1.ui (not submodules), except for the one allowed _render_kv_block private import from bensdorp1.ui.styles
    - db_path = DATA_DIR / "data" / "bensdorp1.db" — guard checks db_path.exists()
    - print_error called with actions=["Delete the file...", "Run `bensdorp1 restore PATH`..."] when guard fires
    - SEPARATOR = "=" * 64 constant present at module level
    - _tilde_path, _format_elapsed, _store_cash are module-level private helpers
    - _store_cash stores value as f"{amount:.2f}" (not str(amount))
    - Per-symbol loop: for symbol in symbols: update_price_data(engine, [symbol]); track.advance(symbol)
    - log_event called with AuditEventType.SYSTEM_INITIALIZED and payload containing constituent_count, cash, history_days keys
    - create_backup called with (engine, DATA_DIR / "backups") — explicit backups_dir argument
    - Detail lines printed AFTER with ms.step() exits, not inside the with-block
    - try/except KeyboardInterrupt wraps the cash-entry while loop; abort message is "Operation aborted. No changes were made."
    - uv run mypy src/bensdorp1/commands/init.py --strict exits 0
    - uv run ruff check src/bensdorp1/commands/init.py exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update test_cli.py — remove init from stub parametrize list</name>
  <files>tests/test_cli.py</files>

  <read_first>
    - tests/test_cli.py (full file — read current parametrize lists before editing)
  </read_first>

  <action>
In tests/test_cli.py, find the `test_stub_exits_cleanly` parametrize decorator (currently at lines 34-53). Remove `"init"` from the list of command strings in that decorator only.

Do NOT modify the `test_help_subcommand_shows_help` parametrize list — that test checks help text, not stub behavior, and `init` should remain in it.

The resulting `test_stub_exits_cleanly` list should contain all the original entries EXCEPT `"init"`. Order of remaining entries must be preserved exactly.

Do NOT modify any other part of test_cli.py.
  </action>

  <verify>
    <automated>uv run pytest tests/test_cli.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_cli.py no longer contains "init" inside the test_stub_exits_cleanly parametrize list
    - tests/test_cli.py still contains "init" inside the test_help_subcommand_shows_help parametrize list
    - uv run pytest tests/test_cli.py -x exits 0 (all existing CLI tests pass)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→CLI (cash input) | User-supplied float string enters via number_prompt() → display |
| config→CLI (DATA_DIR) | BENSDORP1_HOME env var resolves DATA_DIR at import time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | cash display in completion summary | mitigate | format_price(cash) returns a safe "$X,XXX.XX" string; _render_kv_block uses markup=False, highlight=False; no user string passed to Rich markup parser |
| T-06-02 | Information Disclosure | path display in completion summary | mitigate | _tilde_path() only replaces Path.home() prefix; never passes user-controlled path strings to file operations |
| T-06-03 | Elevation of Privilege | DATA_DIR resolution from env var | accept | DATA_DIR is resolved once at import time from BENSDORP1_HOME; init.py never takes a path argument; scope is single-user CLI |
| T-06-04 | Denial of Service | partial DB left on Ctrl+C or network failure | accept | D-06: guard error on re-run explains both recovery paths (delete + re-init, or restore from backup); no sensitive data at risk |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs zero new packages; no package legitimacy check needed |
</threat_model>

<verification>
After both tasks complete:

1. uv run pytest tests/test_cli.py -x — all CLI tests pass (stub list no longer includes init)
2. uv run mypy src/bensdorp1/commands/init.py --strict — 0 errors
3. uv run ruff check src/bensdorp1/commands/init.py — 0 errors
4. uv run ruff format --check src/bensdorp1/commands/init.py — no formatting changes needed
5. Manual spot-check: uv run python -c "from bensdorp1.commands.init import init" — no import errors
</verification>

<success_criteria>
- init.py replaces the stub with the full §7.1 interactive flow
- Guard fires on re-run (db_path.exists()) with both recovery paths in the error message
- Cash validation loop (> 0) re-prompts on invalid input, exits on Ctrl+C with abort message
- Per-symbol loop drives TrackContext progress for step [3/3]
- _store_cash writes f"{amount:.2f}" to config table key="available_cash"
- log_event writes AuditEventType.SYSTEM_INITIALIZED with correct payload
- create_backup called with explicit DATA_DIR / "backups"
- Completion summary uses _tilde_path() for paths and _format_elapsed() for total time
- test_cli.py: "init" removed from test_stub_exits_cleanly only; test_help_subcommand_shows_help unchanged
- mypy --strict, ruff check, ruff format --check all exit 0
</success_criteria>

<output>
Create .planning/phases/06-first-run-init-command/06-01-SUMMARY.md when done.
</output>

---
phase: 08-confirmation-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bensdorp1/db/engine.py
  - tests/test_commands/test_buy.py
  - tests/test_commands/test_sell.py
  - tests/test_commands/test_fix.py
autonomous: true
requirements:
  - CMD-06
  - CMD-07
  - CMD-08

must_haves:
  truths:
    - "After run_migrations, the positions table has closed_reason TEXT and closed_manual_reason TEXT columns"
    - "run_migrations is idempotent — running it twice does not raise OperationalError"
    - "test_buy.py, test_sell.py, test_fix.py exist with named skeleton test functions corresponding to every requirement scenario"
    - "Test stubs use pytest.skip() so the test suite still runs green before implementation lands"
  artifacts:
    - path: "src/bensdorp1/db/engine.py"
      provides: "ALTER TABLE migration for closed_reason and closed_manual_reason"
      contains: "ALTER TABLE positions ADD COLUMN closed_reason TEXT"
    - path: "src/bensdorp1/db/engine.py"
      provides: "Idempotent ALTER for closed_manual_reason"
      contains: "ALTER TABLE positions ADD COLUMN closed_manual_reason TEXT"
    - path: "tests/test_commands/test_buy.py"
      provides: "5 named CMD-06 test stubs (pytest.skip until Plan 02)"
    - path: "tests/test_commands/test_sell.py"
      provides: "3 named CMD-07 test stubs (pytest.skip until Plan 03)"
    - path: "tests/test_commands/test_fix.py"
      provides: "3 named CMD-08 test stubs (pytest.skip until Plan 04)"
  key_links:
    - from: "src/bensdorp1/db/engine.py"
      to: "SQLite positions table"
      via: "sqlalchemy text + OperationalError catch"
      pattern: "ALTER TABLE positions ADD COLUMN"
    - from: "tests/test_commands/test_buy.py"
      to: "bensdorp1.cli.app"
      via: "CliRunner import (skeleton ready for Plan 02)"
      pattern: "from typer.testing import CliRunner"
---

<objective>
Add the two new columns to the positions table via idempotent ALTER TABLE migrations in `db/engine.py`, and create test skeleton files (with named, skipped test functions) for buy, sell, and fix. This unblocks Plans 02-04 to implement in parallel against pre-declared test names.

Purpose: D-01 requires the schema migration before any buy/sell/fix write paths exist. Wave-0 test scaffolds prevent later plans from having to invent test names mid-implementation and let each test file be owned by exactly one downstream plan.

Output: Updated `run_migrations()` with ALTER TABLE statements; three new test files with skipped skeleton tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-confirmation-commands/08-CONTEXT.md
@.planning/phases/08-confirmation-commands/08-RESEARCH.md
@.planning/phases/08-confirmation-commands/08-PATTERNS.md
@src/bensdorp1/db/engine.py
@src/bensdorp1/db/schema.py
@tests/test_commands/test_init.py
@tests/conftest.py
</context>

<interfaces>
<!-- Existing exports the new tests will import -->

From src/bensdorp1/db/engine.py (current):
```python
def get_engine(path: Path | None = None) -> Engine
def run_migrations(engine: Engine) -> None
def _reset_engine_for_testing(replacement: Engine | None = None) -> None
def _build_engine(path: Path) -> Engine
def _resolve_db_path(override: Path | None) -> Path
```

From bensdorp1.cli:
```python
app: typer.Typer  # has buy, sell, fix command stubs registered
```

From bensdorp1.db.schema:
```python
positions: Table  # columns: id, symbol, entry_date, entry_close, shares,
                  # initial_stop, highest_close, trailing_stop, scan_id,
                  # closed_at, exit_price, realized_pnl
                  # (closed_reason and closed_manual_reason added via ALTER TABLE only)
```

From tests/conftest.py:
```python
@pytest.fixture
def db_engine(tmp_path: Path) -> Generator[Engine, None, None]  # real SQLite engine
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add idempotent ALTER TABLE migrations for closed_reason and closed_manual_reason</name>
  <files>src/bensdorp1/db/engine.py</files>
  <read_first>
    - src/bensdorp1/db/engine.py (current run_migrations is one-line; you append the ALTER TABLE loop after metadata.create_all)
    - src/bensdorp1/db/schema.py (confirm positions table definition does NOT declare these columns — they live ONLY in the migration per D-01 and PATTERNS.md `schema.py` section)
    - .planning/phases/08-confirmation-commands/08-CONTEXT.md §D-01 and §specifics for the exact pattern
    - .planning/phases/08-confirmation-commands/08-PATTERNS.md "db/engine.py" section for the target pattern
  </read_first>
  <behavior>
    - Test: After run_migrations on a fresh engine, PRAGMA table_info(positions) includes 'closed_reason' and 'closed_manual_reason' (TEXT, nullable)
    - Test: run_migrations is idempotent — calling it twice on the same engine raises no exception
    - Test: After run_migrations on an engine where the columns already exist (simulating an upgrade), the function does not raise and leaves data intact
  </behavior>
  <action>
    Modify `src/bensdorp1/db/engine.py`:
    1. Add new imports at the top: `from sqlalchemy import text` and `from sqlalchemy.exc import OperationalError`.
    2. Extend `run_migrations(engine)` so that after `metadata.create_all(engine, checkfirst=True)` it opens an `engine.connect()` context and executes two ALTER TABLE statements inside a `for stmt in [...]` loop. Statements must be exactly:
       - `"ALTER TABLE positions ADD COLUMN closed_reason TEXT"`
       - `"ALTER TABLE positions ADD COLUMN closed_manual_reason TEXT"`
    3. Each `conn.execute(text(stmt))` followed by `conn.commit()` must be wrapped in `try/except OperationalError: pass` for idempotency. SQLite raises `OperationalError: duplicate column name: <col>` if the column already exists.
    4. Update the docstring: the function is now called by all state-changing commands (not only `init`), and it applies incremental migrations idempotently in addition to creating the schema.
    5. Add a test-only unit test in `tests/test_db/test_db_engine.py` named `test_run_migrations_adds_closed_reason_columns` that uses the `db_engine` fixture, runs `run_migrations(db_engine)` a second time (the fixture already ran create_all once), then queries `PRAGMA table_info(positions)` via `engine.connect().execute(text("PRAGMA table_info(positions)"))` and asserts both `'closed_reason'` and `'closed_manual_reason'` appear in the column-name list. Also call `run_migrations(db_engine)` a third time and assert no exception is raised.

    Do NOT modify `db/schema.py`. The columns live only in the migration per D-01 and the PATTERNS.md decision for schema.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db/test_db_engine.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/bensdorp1/db/engine.py` contains the literal string `ALTER TABLE positions ADD COLUMN closed_reason TEXT`
    - `src/bensdorp1/db/engine.py` contains the literal string `ALTER TABLE positions ADD COLUMN closed_manual_reason TEXT`
    - `src/bensdorp1/db/engine.py` imports `text` from sqlalchemy and `OperationalError` from sqlalchemy.exc
    - `src/bensdorp1/db/engine.py` contains `except OperationalError` in run_migrations
    - `tests/test_db/test_db_engine.py` contains a test named `test_run_migrations_adds_closed_reason_columns` that passes under `uv run pytest tests/test_db/test_db_engine.py::test_run_migrations_adds_closed_reason_columns -x`
    - `uv run mypy src/bensdorp1/db/engine.py --strict` exits 0
    - `uv run ruff check src/bensdorp1/db/engine.py` exits 0
  </acceptance_criteria>
  <done>
    `run_migrations` adds both columns idempotently; positive unit test passes; mypy strict and ruff clean on the changed file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test skeleton files for buy, sell, fix with all named scenarios</name>
  <files>tests/test_commands/test_buy.py, tests/test_commands/test_sell.py, tests/test_commands/test_fix.py</files>
  <read_first>
    - tests/test_commands/test_init.py (canonical CliRunner pattern: runner = CliRunner(), patch at "bensdorp1.commands.<mod>.*", uses input="y\n", asserts on result.exit_code and result.output)
    - tests/test_commands/test_scan.py lines 1-100 (canonical multi-scenario layout, MagicMock(conn.execute.return_value.fetchone.side_effect))
    - tests/conftest.py (db_engine fixture signature)
    - .planning/phases/08-confirmation-commands/08-RESEARCH.md §"Phase Requirements → Test Map" for the EXACT test function names
  </read_first>
  <action>
    Create three new test files. Each file:
    - Module docstring `"""Tests for commands/<name>.py — CMD-XX scenarios."""`
    - Imports: `from pathlib import Path`, `from unittest.mock import MagicMock, patch`, `import pytest`, `from sqlalchemy.engine import Engine`, `from typer.testing import CliRunner`, `from bensdorp1.cli import app`
    - Module-level `runner = CliRunner()`
    - One named test function per scenario from 08-RESEARCH.md's `Phase Requirements → Test Map`. Each function body is a single line: `pytest.skip("Implementation pending in Plan 0X")` where X is the implementing plan number.
    - Each function uses the exact name listed in the test map. Signatures take `tmp_path: Path` (mocked-engine tests) or `db_engine: Engine` (real-DB tests) so Plan 02-04 implementers can fill them in without renaming.

    `tests/test_commands/test_buy.py` — 5 functions for CMD-06, all skipped with `"Implementation pending in Plan 02"`:
      - `test_invalid_constituent(tmp_path: Path) -> None`
      - `test_duplicate_open_position(db_engine: Engine) -> None`
      - `test_off_signal_warning(tmp_path: Path) -> None`
      - `test_happy_path_on_signal(tmp_path: Path) -> None`
      - `test_off_signal_abort(tmp_path: Path) -> None`

    `tests/test_commands/test_sell.py` — 3 functions for CMD-07, all skipped with `"Implementation pending in Plan 03"`:
      - `test_no_exit_trigger(tmp_path: Path) -> None`
      - `test_happy_path_normal(tmp_path: Path) -> None`
      - `test_manual_sell(tmp_path: Path) -> None`

    `tests/test_commands/test_fix.py` — 3 functions for CMD-08, all skipped with `"Implementation pending in Plan 04"`:
      - `test_no_transaction(tmp_path: Path) -> None`
      - `test_no_changes(tmp_path: Path) -> None`
      - `test_price_change_updates_stop(db_engine: Engine) -> None`

    Each function MUST appear in the file with the exact name shown above. Names match the `pytest -k` selectors referenced in downstream plan acceptance criteria — do not rename.

    Mark unused imports with `# noqa: F401` if necessary (e.g. MagicMock, patch, Engine, app) since stubs don't reference all of them yet, OR import them only in the implementing plans. Prefer using `# noqa: F401` so Plans 02-04 don't have to re-add the imports.
  </action>
  <verify>
    <automated>uv run pytest tests/test_commands/test_buy.py tests/test_commands/test_sell.py tests/test_commands/test_fix.py -v</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist at the listed paths
    - `uv run pytest tests/test_commands/test_buy.py -v` reports exactly 5 tests, all SKIPPED
    - `uv run pytest tests/test_commands/test_sell.py -v` reports exactly 3 tests, all SKIPPED
    - `uv run pytest tests/test_commands/test_fix.py -v` reports exactly 3 tests, all SKIPPED
    - `uv run ruff check tests/test_commands/test_buy.py tests/test_commands/test_sell.py tests/test_commands/test_fix.py` exits 0
    - Test files import `from bensdorp1.cli import app` and `from typer.testing import CliRunner`
    - Each test function name matches the names listed exactly (use `grep -c "def test_invalid_constituent" tests/test_commands/test_buy.py` returns 1, etc.)
    - The full test suite still runs green: `uv run pytest -x` exits 0
  </acceptance_criteria>
  <done>
    Three skeleton test files exist with 11 named, skipped tests total. Plans 02-04 fill in bodies in place without inventing names.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → DB | Symbol/price/shares inputs cross this boundary in Plans 02-04 (not this plan) |
| Migration SQL → DB | run_migrations sends static ALTER TABLE statements only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | run_migrations | accept | ALTER TABLE statements are static string literals (no interpolation); no caller-supplied data flows through the migration |
| T-08-02 | DoS | run_migrations | mitigate | Idempotent via try/except OperationalError — second-run failures are swallowed cleanly, preventing crash loops on commands that call it on entry |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed in this phase (verified RESEARCH.md Package Legitimacy Audit — empty table) |
</threat_model>

<verification>
- `uv run pytest tests/test_db/test_db_engine.py -x` passes the new migration test
- `uv run pytest tests/test_commands/test_buy.py tests/test_commands/test_sell.py tests/test_commands/test_fix.py -v` shows 11 SKIPPED tests
- `uv run pytest -x` exits 0 (suite stays green)
- `uv run mypy src/bensdorp1/db/engine.py --strict` exits 0
- `uv run ruff check src/bensdorp1/db/engine.py tests/test_commands/test_buy.py tests/test_commands/test_sell.py tests/test_commands/test_fix.py` exits 0
</verification>

<success_criteria>
- positions table has closed_reason and closed_manual_reason columns after `run_migrations`
- run_migrations is idempotent
- Three skeleton test files exist with exactly 11 named, skipped tests
- All gates green (pytest, mypy strict, ruff) on the changed files
</success_criteria>

<output>
Create `.planning/phases/08-confirmation-commands/08-01-SUMMARY.md` when done. Summary must include:
- Confirm both ALTER TABLE statements present in engine.py and idempotency test passing
- Confirm 11 skeleton tests in the three test files
- Note any encountered idiosyncrasies for Plans 02-04 (e.g. specific SQLite OperationalError message text on this Python version)
</output>

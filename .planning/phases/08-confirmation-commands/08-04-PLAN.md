---
phase: 08-confirmation-commands
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/bensdorp1/commands/fix.py
  - tests/test_commands/test_fix.py
autonomous: true
requirements:
  - CMD-08

must_haves:
  truths:
    - "User can run `bensdorp1 fix SYMBOL` against an open position and edit entry_date / entry_close / shares; after confirmation, positions is updated in-place"
    - "User running fix on a symbol with no open AND no closed position sees an error and exits with code 1 (D-19)"
    - "When fix targets a closed position, editable fields are closed_at (date) / exit_price (price) / closed_manual_reason (only if closed_reason == 'manual')"
    - "Pressing Enter at every field prompt leaves all values unchanged and prints 'No changes detected. Nothing to update.' via print_info; exit code 0; NO DB write occurs (D-23)"
    - "Changing the price of an OPEN position recalculates initial_stop = new_price * 0.93; trailing_stop and highest_close are NOT changed (D-21, D-22)"
    - "Changing price or shares of a CLOSED position recalculates realized_pnl = (exit_price - new_price) * new_shares (D-21)"
    - "After confirmed correction, audit_log contains a transaction_corrected event with payload {before: {...}, after: {...}} (D-24)"
    - "After confirmed correction, a new file appears in the backups directory"
    - "Before/after diff is rendered as a render_table showing only changed fields (Field / Before / After columns)"
  artifacts:
    - path: "src/bensdorp1/commands/fix.py"
      provides: "Full CMD-08 implementation (replaces stub)"
      contains: "def fix("
    - path: "src/bensdorp1/commands/fix.py"
      provides: "No-changes early exit per D-23"
      contains: "No changes detected"
    - path: "src/bensdorp1/commands/fix.py"
      provides: "Initial-stop recalculation on price change (D-21)"
      contains: "new_price * 0.93"
    - path: "src/bensdorp1/commands/fix.py"
      provides: "Audit before/after payload per D-24"
      contains: "\"before\""
    - path: "tests/test_commands/test_fix.py"
      provides: "3 implemented CMD-08 scenarios — no skips remain"
      contains: "def test_price_change_updates_stop"
  key_links:
    - from: "src/bensdorp1/commands/fix.py"
      to: "positions table"
      via: "in-place UPDATE"
      pattern: "update\\(positions\\)"
    - from: "src/bensdorp1/commands/fix.py"
      to: "audit_log"
      via: "log_event(TRANSACTION_CORRECTED, payload={before, after})"
      pattern: "TRANSACTION_CORRECTED"
    - from: "src/bensdorp1/commands/fix.py"
      to: "ui.render_table"
      via: "before/after diff"
      pattern: "render_table\\("
---

<objective>
Implement the full `bensdorp1 fix SYMBOL [--date DATE]` command (CMD-08) per spec §7.5 and decisions D-19–D-24. Replace the existing stub; fill in all 3 skeleton tests in `tests/test_commands/test_fix.py`.

Purpose: Interactively correct the last transaction for a symbol. Identifies the target (open position OR most recent closed position), runs field-by-field input prompts with current values as defaults, shows a before/after diff table plus an "Impact on this position" block for derived values, writes the in-place UPDATE only after confirmation, and persists the full before/after payload in the audit log.

Output: Working fix command with full integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-confirmation-commands/08-CONTEXT.md
@.planning/phases/08-confirmation-commands/08-RESEARCH.md
@.planning/phases/08-confirmation-commands/08-PATTERNS.md
@.planning/Bensdorp_1.md
@src/bensdorp1/commands/init.py
@src/bensdorp1/commands/fix.py
@src/bensdorp1/db/schema.py
@src/bensdorp1/db/audit.py
@src/bensdorp1/ui/__init__.py
@src/bensdorp1/ui/messages.py
@src/bensdorp1/ui/styles.py
@src/bensdorp1/ui/tables.py
@src/bensdorp1/ui/prompts.py
@src/bensdorp1/config.py
@tests/test_commands/test_init.py
@tests/test_commands/test_fix.py
@tests/conftest.py
</context>

<interfaces>
<!-- Key contracts the executor consumes -->

From bensdorp1._app:
```python
app: typer.Typer
```

From bensdorp1.config:
```python
DATA_DIR: Path
MARKET_TZ: ZoneInfo
```

From bensdorp1.db (public surface):
```python
class AuditEventType(StrEnum):
    TRANSACTION_CORRECTED = "transaction_corrected"
    # ...
def get_engine(path: Path | None = None) -> Engine
def run_migrations(engine: Engine) -> None
def log_event(engine: Engine, event_type: AuditEventType, ...) -> None
def create_backup(engine: Engine, backups_dir: Path) -> Path
```

From bensdorp1.db.schema:
```python
positions: Table  # full column list as above; closed_at, exit_price, closed_reason,
                  # closed_manual_reason populated only on closed rows
```

From bensdorp1.ui:
```python
def confirm_prompt(message: str, *, console: Console | None = None) -> bool
def render_kv_block(data: dict[str, str], console: Console, indent: str = "") -> None
def render_table(columns: list[tuple[str, Literal["left","right"]]],
                 rows: list[list[str]], *, console: Console | None = None) -> None
def format_price(value: float) -> str
def format_date(d: date) -> str
def print_error(title, *, data, body, impact, actions, console) -> None
def print_info(title, *, data, body, impact, actions, console) -> None
def print_success(title, *, data, body, impact, actions, console) -> None
```

Stub being replaced (src/bensdorp1/commands/fix.py):
```python
@app.command(rich_help_panel="Confirmations")
def fix() -> None:
    typer.echo("Not yet implemented.")
    raise typer.Exit()
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fix.py — full CMD-08 flow</name>
  <files>src/bensdorp1/commands/fix.py</files>
  <read_first>
    - src/bensdorp1/commands/fix.py (current stub being replaced)
    - src/bensdorp1/commands/init.py (canonical SEPARATOR, KeyboardInterrupt wrapping, create_backup + log_event sequence)
    - src/bensdorp1/db/schema.py (positions columns; remember `entry_close` not `entry_price`; Pitfall 6: closed sells use closed_at/exit_price/closed_manual_reason)
    - src/bensdorp1/db/audit.py (TRANSACTION_CORRECTED enum member exists)
    - src/bensdorp1/ui/prompts.py (text_prompt and number_prompt have their OWN prompt formats; fix uses raw input() per Assumption A1 — different prompt format `"Field   [current]:  "`)
    - src/bensdorp1/ui/tables.py (render_table signature: columns list[tuple[str, Literal["left","right"]]], rows list[list[str]])
    - src/bensdorp1/ui/messages.py (print_info signature)
    - .planning/phases/08-confirmation-commands/08-CONTEXT.md §"Fix command" decisions D-19 through D-24
    - .planning/phases/08-confirmation-commands/08-RESEARCH.md §"Code Examples — Fix: Field-by-Field Input..." and §"Code Examples — Fix: Before/After Diff Table" and §"Common Pitfalls" 6
    - .planning/phases/08-confirmation-commands/08-PATTERNS.md `src/bensdorp1/commands/fix.py` section
    - .planning/Bensdorp_1.md §7.5 (fix spec)
  </read_first>
  <behavior>
    Test cases (covered in Task 2):
    - Test: no open AND no closed position for symbol → print_error("No transaction found for SYMBOL.") + exit code 1 (D-19)
    - Test: open position exists; all fields kept (Enter at every prompt) → print_info("No changes detected. Nothing to update.") + exit code 0; NO update to positions; NO create_backup; NO log_event (D-23)
    - Test: open position; change price from 432.50 to 432.75 → positions row updated with entry_close=432.75 and initial_stop=432.75 * 0.93 (recalculated per D-21); trailing_stop and highest_close unchanged (D-22); audit_log has transaction_corrected event with payload before/after containing the old and new values
    - Test: open position; change shares only → no stop recalculation (D-21); positions.shares updated; audit event payload reflects the change
    - Test: open position; change date only → entry_date updated; no stop recalculation; audit event payload reflects the change
    - Test: closed position; change price → exit_price updated; realized_pnl recalculated (D-21)
    - Test: Ctrl+C during field input → "Operation aborted. No changes were made." printed; exit code 0; no DB write
  </behavior>
  <action>
    REPLACE `src/bensdorp1/commands/fix.py` entirely. Follow the structure laid out in 08-PATTERNS.md `src/bensdorp1/commands/fix.py` section.

    Module structure:
    1. Docstring: `"""Interactively correct the last transaction for a symbol."""`
    2. Imports:
       - `import datetime`
       - `from datetime import UTC`
       - `from typing import Any`
       - `import typer`
       - `from rich.console import Console`
       - `from rich.text import Text`
       - `from sqlalchemy import select, update`
       - `from bensdorp1._app import app`
       - `from bensdorp1.config import DATA_DIR`
       - `from bensdorp1.db import AuditEventType, create_backup, get_engine, log_event, run_migrations`
       - `from bensdorp1.db.schema import positions`
       - `from bensdorp1.ui import confirm_prompt, format_date, format_price, print_error, print_info, print_success, render_kv_block, render_table`
    3. Module-level constant: `SEPARATOR: str = "=" * 64`
    4. Command signature:
       - `@app.command(rich_help_panel="Confirmations")`
       - `def fix(symbol: str = typer.Argument(..., help="Ticker symbol to correct."), date: str | None = typer.Option(None, "--date", help="(Reserved; unused in Phase 8.)")) -> None:`
       - Docstring: `"""Interactively correct the last transaction for a symbol."""`
       - The `date` parameter is accepted but unused in Phase 8 (Open Question 1 in RESEARCH.md). Bind to local `_ = date` to silence ruff unused-var warning; OR include in docstring "Reserved for future use." and add `_unused = date` line.

    Command body, in order:

    A. DB entry triad (db_path, get_engine, run_migrations, console).

    B. Identify the transaction (D-19):
       - First query open position: `select(positions.c.id, positions.c.entry_date, positions.c.entry_close, positions.c.shares, positions.c.initial_stop).where((positions.c.symbol == symbol.upper()) & (positions.c.closed_at == None))` (with `# noqa: E711`). fetchone.
       - If found: target_kind = "buy"; capture id, entry_date, entry_close, shares, initial_stop.
       - Else: query most recent closed position: `select(positions.c.id, positions.c.entry_close, positions.c.shares, positions.c.closed_at, positions.c.exit_price, positions.c.closed_reason, positions.c.closed_manual_reason).where((positions.c.symbol == symbol.upper()) & (positions.c.closed_at != None)).order_by(positions.c.closed_at.desc()).limit(1)`. fetchone.
       - If neither: `print_error(f"No transaction found for {symbol.upper()}.", console=console)` + `raise typer.Exit(code=1)`.
       - Else: target_kind = "sell"; capture id, entry_close, shares, closed_at, exit_price, closed_reason, closed_manual_reason. Note that for closed positions we will edit `closed_at` (date) and `exit_price` (price), and ONLY `closed_manual_reason` if closed_reason == "manual" (D-20).

    C. Identification confirmation prompt (spec §7.5 Step 1):
       - In `try/except KeyboardInterrupt: raise typer.Exit() from None`:
         - Print SEPARATOR / "Confirm fix target" header / SEPARATOR / blank line
         - Use render_kv_block to show: "Transaction" -> e.g. `f"Buy {symbol.upper()} on {format_date(current_entry_date.date())}"` for buy, or `f"Sell {symbol.upper()} on {format_date(current_closed_at.date())}"` for sell. Include current values: "Date", "Price", "Shares" (and "Manual reason" if applicable).
         - `confirmed = confirm_prompt("Is this the transaction to fix?", console=console)`
       - If not confirmed → `raise typer.Exit()` (code 0).

    D. Field-by-field input loop (D-20, RESEARCH.md Code Examples — raw `input()`):
       - In `try / except KeyboardInterrupt: console.print(); console.print(Text("Operation aborted. No changes were made.")); raise typer.Exit() from None`:
         - For buy target:
           - `raw_date = input(f"Date    [{format_date(current_entry_date.date())}]:  ").strip()`
             - If raw_date: parse via `datetime.date.fromisoformat(raw_date)` inside an inner try/except ValueError that re-prompts (single re-prompt OK; or print Error and accept current). Simpler approach acceptable: if invalid, print "Error: Expected YYYY-MM-DD." and exit code 1.
             - Else new_date = current_entry_date.date()
           - `raw_price = input(f"Price   [{format_price(current_entry_close)}]:  ").strip()`
             - clean = `raw_price.lstrip("$").replace(",", "")`
             - If clean: new_price = float(clean). Else new_price = current_entry_close.
           - `raw_shares = input(f"Shares  [{current_shares}]:  ").strip()`
             - If raw_shares: new_shares = int(raw_shares). Else new_shares = current_shares.
         - For sell target:
           - Same prompts but: Date column = closed_at.date(); Price column = exit_price; Shares NOT editable (per D-20: "Sell" editable fields are date/price/manual_reason). Skip shares prompt.
           - If closed_reason == "manual": prompt `raw_manual = input(f"Manual reason  [{current_closed_manual_reason or ''}]:  ").strip()`; if empty, keep current.

    E. Detect changes:
       - For buy: any_changes = (new_date != current_entry_date.date()) or (new_price != current_entry_close) or (new_shares != current_shares)
       - For sell: any_changes = (new_date != current_closed_at.date()) or (new_price != current_exit_price) or (manual changed)
       - If not any_changes → `print_info("No changes detected. Nothing to update.", console=console)` and `raise typer.Exit()` (code 0). DO NOT update, backup, or log.

    F. Recalculations (D-21, D-22):
       - For buy with price change: `new_initial_stop = new_price * 0.93`. Otherwise: new_initial_stop = current_initial_stop.
       - For sell with price or shares change: `new_realized_pnl = (new_price - current_entry_close) * <new_shares for sell? D-21 says new_shares but sell editable fields don't include shares — use existing current_shares for sell>`. Per D-21 wording, "price or shares change on a CLOSED position" — but for sell the shares aren't editable per D-20. Reconcile: for sell, only price changes affect realized_pnl. Use `new_realized_pnl = (new_price - current_entry_close) * current_shares` for sell when price changed.
       - Stops on closed positions are NOT recalculated (D-22).
       - trailing_stop and highest_close NEVER change (D-22).

    G. Before/after diff (D-23, render_table):
       - SEPARATOR / "Confirm correction" header / SEPARATOR / blank line
       - Show "Transaction:" line via render_kv_block.
       - Build diff_rows for changed fields only:
         - if new_price != current_entry_close (buy) or != current_exit_price (sell): `["Price", format_price(current), format_price(new)]`
         - if new_shares != current_shares (buy only): `["Shares", str(current), str(new)]`
         - if new_date != current_date: `["Date", format_date(current_date), format_date(new_date)]`
       - render_table with columns `[("Field", "left"), ("Before", "left"), ("After", "left")]` and `rows=diff_rows`.
       - If any derived value changed (initial_stop for buy / realized_pnl for sell): print "Impact on this position" subsection header + render_kv_block with the derived diffs (e.g. `{"Initial stop": f"{format_price(current_initial_stop)} -> {format_price(new_initial_stop)}"}`).
       - Print body lines: "This correction will be recorded in the audit log." and "The original entry will be preserved as a historical record."

    H. Confirmation prompt:
       - In `try / except KeyboardInterrupt: raise typer.Exit() from None`:
         - `confirmed = confirm_prompt("Confirm correction?", console=console)`
       - If not confirmed → `raise typer.Exit()` (code 0).

    I. State-changing write (D-24):
       - For buy:
         - new_entry_dt = `datetime.datetime(new_date.year, new_date.month, new_date.day, tzinfo=UTC)`
         - UPDATE: `update(positions).where(positions.c.id == position_id).values(entry_date=new_entry_dt, entry_close=new_price, shares=new_shares, initial_stop=new_initial_stop)`
       - For sell:
         - new_closed_dt = same UTC construction with new_date
         - UPDATE: `update(positions).where(positions.c.id == position_id).values(closed_at=new_closed_dt, exit_price=new_price, realized_pnl=new_realized_pnl, closed_manual_reason=new_manual_reason_or_current)`
       - `conn.commit()` inside `engine.connect()`
       - `create_backup(engine, DATA_DIR / "backups")`
       - Build payload per D-24:
         - For buy: `payload: dict[str, Any] = {"target_kind": "buy", "before": {"entry_close": current_entry_close, "shares": current_shares, "entry_date": current_entry_date.date().isoformat(), "initial_stop": current_initial_stop}, "after": {"entry_close": new_price, "shares": new_shares, "entry_date": new_date.isoformat(), "initial_stop": new_initial_stop}}`
         - For sell: `payload = {"target_kind": "sell", "before": {"exit_price": current_exit_price, "closed_at": current_closed_at.date().isoformat(), "closed_manual_reason": current_closed_manual_reason, "realized_pnl": current_realized_pnl}, "after": {"exit_price": new_price, "closed_at": new_date.isoformat(), "closed_manual_reason": new_manual_reason_or_current, "realized_pnl": new_realized_pnl}}`
       - `log_event(engine, AuditEventType.TRANSACTION_CORRECTED, symbol=symbol.upper(), payload=payload)`
       - `print_success("Transaction corrected.", console=console)`

    Style requirements (same as buy.py/sell.py): Text() wrapping, parameterized SQL, raise typer.Exit() patterns. The raw `input()` calls in step D are intentional per Assumption A1 (different prompt format than text_prompt/number_prompt).
  </action>
  <verify>
    <automated>uv run mypy src/bensdorp1/commands/fix.py --strict && uv run ruff check src/bensdorp1/commands/fix.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/bensdorp1/commands/fix.py` contains `def fix(` with the 2-parameter signature (symbol, date)
    - File contains the literal string `"No changes detected. Nothing to update."` (D-23)
    - File contains the literal string `"No transaction found for"` (D-19)
    - File contains the literal `new_price * 0.93` (D-21 stop recalculation on buy price change)
    - File contains `AuditEventType.TRANSACTION_CORRECTED`
    - File contains `render_table(` for the diff
    - File contains `"before"` AND `"after"` payload keys (D-24)
    - File contains `create_backup(engine, DATA_DIR / "backups")`
    - File contains `input(` (raw input for field-by-field prompts per Assumption A1)
    - File contains at least ONE `update(positions)` call
    - File uses `positions.c.entry_close` not `positions.c.entry_price`
    - File does NOT mutate `trailing_stop` or `highest_close` in any UPDATE: `grep "trailing_stop=" src/bensdorp1/commands/fix.py` returns 0 lines and `grep "highest_close=" src/bensdorp1/commands/fix.py` returns 0 lines (D-22)
    - `uv run mypy src/bensdorp1/commands/fix.py --strict` exits 0
    - `uv run ruff check src/bensdorp1/commands/fix.py` exits 0
  </acceptance_criteria>
  <done>
    `bensdorp1 fix` command is fully implemented per CMD-08 / spec §7.5 / decisions D-19–D-24. mypy strict and ruff clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement test_fix.py — fill all 3 skeleton tests</name>
  <files>tests/test_commands/test_fix.py</files>
  <read_first>
    - tests/test_commands/test_fix.py (skeleton from Plan 01 — 3 named tests with pytest.skip)
    - tests/test_commands/test_init.py (canonical mocked-engine pattern)
    - tests/test_commands/test_scan.py (real-DB seeding pattern with insert(table).values(...))
    - tests/conftest.py (db_engine fixture)
    - src/bensdorp1/commands/fix.py (the implementation under test from Task 1)
    - src/bensdorp1/db/schema.py (positions columns)
    - .planning/phases/08-confirmation-commands/08-PATTERNS.md `tests/test_commands/test_fix.py` section
  </read_first>
  <behavior>
    - test_no_transaction: tmp_path mocked engine; both open-position and closed-position fetchones return None → exit code 1; "No transaction found for" in output
    - test_no_changes: tmp_path or db_engine; user presses Enter to keep every field default → print_info "No changes detected. Nothing to update." in output; exit code 0; no UPDATE to positions; no create_backup mock call; no log_event mock call
    - test_price_change_updates_stop: db_engine fixture; seed open position; user enters new price → positions row updated with entry_close=new_price AND initial_stop=new_price*0.93; trailing_stop and highest_close UNCHANGED; audit_log has transaction_corrected event whose payload before/after dicts capture the change
  </behavior>
  <action>
    REPLACE the body of each of the 3 test functions in `tests/test_commands/test_fix.py` (do not rename — names are pinned by Plan 01).

    Imports needed:
    ```
    from datetime import UTC, datetime
    from pathlib import Path
    from unittest.mock import MagicMock, patch
    import pytest
    from sqlalchemy import insert, select
    from sqlalchemy.engine import Engine
    from typer.testing import CliRunner
    from bensdorp1.cli import app
    from bensdorp1.db.schema import audit_log, positions
    ```

    Module-level: `runner = CliRunner()`.

    Implementation guidance per test:

    **test_no_transaction(tmp_path: Path) -> None**
    - Mocked engine; mock_conn.execute.return_value.fetchone.side_effect = [None, None]  # open lookup, then closed lookup
    - patch DATA_DIR, get_engine (return mock_engine), run_migrations, create_backup, log_event at "bensdorp1.commands.fix.*"
    - `result = runner.invoke(app, ["fix", "ZZZ"])`
    - assert result.exit_code == 1
    - assert "No transaction found for" in result.output

    **test_no_changes(db_engine: Engine) -> None** (use db_engine for accurate INSERT-then-no-UPDATE assertion)
    - NOTE: Plan 01 declared this with `tmp_path: Path`. Keep the signature `tmp_path: Path` and use a MagicMock engine. The fetchone returns an open position row; fetchone for the no-changes path is exhausted after the lookup.
    - mock_engine fetchone returns MagicMock(id=1, entry_date=datetime(2026, 5, 1, tzinfo=UTC), entry_close=432.50, shares=23, initial_stop=402.225) for the open-position lookup.
    - patch DATA_DIR, get_engine, run_migrations, create_backup, log_event at "bensdorp1.commands.fix.*".
    - Input simulates: "y\n\n\n\n" — y to "Is this the transaction to fix?", then three empty Enter presses for Date, Price, Shares.
    - `result = runner.invoke(app, ["fix", "NVDA"], input="y\n\n\n\n")`
    - assert result.exit_code == 0
    - assert "No changes detected" in result.output
    - assert mock_create_backup.called is False
    - assert mock_log_event.called is False
    - The mocked UPDATE call: assert that no `update(positions)` was executed. With a MagicMock, capture `mock_conn.execute.call_count` before the no-changes branch and verify no additional execute occurred (or assert that no call's first positional arg was an update statement via inspection).

    **test_price_change_updates_stop(db_engine: Engine) -> None**
    - Insert an open position: symbol="NVDA", entry_date=datetime(2026, 5, 20, tzinfo=UTC), entry_close=432.50, shares=23, initial_stop=402.225, highest_close=440.00, trailing_stop=330.00, scan_id=None, closed_at=None, exit_price=None, realized_pnl=None. Capture position_id.
    - patch DATA_DIR (to tmp_path implicit), get_engine return db_engine, create_backup MagicMock. Do NOT mock log_event.
    - Input: "y\n\n432.75\n\ny\n" — y to "Is this the transaction to fix?", Enter to keep Date, "432.75" for Price, Enter to keep Shares, y to "Confirm correction?"
    - `result = runner.invoke(app, ["fix", "NVDA"], input="y\n\n432.75\n\ny\n")`
    - assert result.exit_code == 0
    - assert "Transaction corrected" in result.output
    - Query positions row by id == position_id:
      - entry_close == pytest.approx(432.75)
      - initial_stop == pytest.approx(432.75 * 0.93)
      - highest_close == pytest.approx(440.00)  # UNCHANGED (D-22)
      - trailing_stop == pytest.approx(330.00)  # UNCHANGED (D-22)
      - shares == 23
    - Query audit_log: at least one row with event_type == "transaction_corrected" and symbol == "NVDA"; parse the payload JSON and assert payload["before"]["entry_close"] == 432.50 and payload["after"]["entry_close"] == 432.75 and payload["after"]["initial_stop"] == pytest.approx(432.75 * 0.93).

    For the JSON payload check use `import json; payload = json.loads(row.payload)`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_commands/test_fix.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_commands/test_fix.py -v` reports 3 PASSED, 0 SKIPPED, 0 FAILED
    - `uv run pytest tests/test_commands/test_fix.py::test_no_transaction -x` exits 0
    - `uv run pytest tests/test_commands/test_fix.py::test_no_changes -x` exits 0
    - `uv run pytest tests/test_commands/test_fix.py::test_price_change_updates_stop -x` exits 0
    - No skips remain: `grep -c "pytest.skip" tests/test_commands/test_fix.py` == 0
    - `uv run ruff check tests/test_commands/test_fix.py` exits 0
    - `uv run mypy tests/test_commands/test_fix.py --strict` exits 0
    - Trailing-stop / highest-close immutability is asserted in test_price_change_updates_stop
  </acceptance_criteria>
  <done>
    All 3 fix tests pass; no skips; type/lint clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args / interactive input → fix.py | symbol, --date, and each field prompt value originate from the shell user |
| fix.py → DB | All writes via parameterized SQLAlchemy UPDATE |
| fix.py → audit_log | Full before/after payload preserves history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | symbol in SQL queries | mitigate | All queries use SQLAlchemy parameterized .where / .values; no f-string SQL — verifiable via `grep -c "text(f\"" src/bensdorp1/commands/fix.py` == 0 |
| T-08-04-02 | Tampering | Field-prompt values in Rich output | mitigate | render_table wraps each cell in Text() internally; render_kv_block uses markup=False/highlight=False |
| T-08-04-03 | DoS | Invalid date or price input via raw input() ValueError crash | mitigate | datetime.date.fromisoformat and float() wrapped in try/except ValueError with print_error + typer.Exit(code=1) (per Code Examples) |
| T-08-04-04 | Tampering | Trailing-stop or highest-close mutated by fix | mitigate | D-22 enforced by NEVER including trailing_stop or highest_close in any UPDATE .values(); verified by acceptance_criteria grep |
| T-08-04-05 | Repudiation | Corrections not recoverable | mitigate | log_event(TRANSACTION_CORRECTED) writes full before/after payload to audit_log; original values preserved per D-24 |
| T-08-04-06 | Repudiation | No-changes path silently exits | accept | Intentional per D-23 — no DB write means nothing to audit; the print_info message is the only feedback. Acceptable for a single-user tool. |
| T-08-04-07 | Tampering | Closed-position fields edited that should not be (e.g. closed_reason) | mitigate | D-20 enforced: only closed_at, exit_price, and (conditionally) closed_manual_reason are prompted for sell targets. closed_reason is NEVER prompted nor included in the sell-branch UPDATE .values() |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed (RESEARCH.md Package Legitimacy Audit — empty) |
</threat_model>

<verification>
- `uv run pytest tests/test_commands/test_fix.py -v` → 3 PASSED
- `uv run mypy src/bensdorp1/commands/fix.py tests/test_commands/test_fix.py --strict` exits 0
- `uv run ruff check src/bensdorp1/commands/fix.py tests/test_commands/test_fix.py` exits 0
- `uv run pytest -x` exits 0 (full suite stays green)
</verification>

<success_criteria>
- `bensdorp1 fix NVDA` against an open position runs the field-by-field flow and writes an in-place UPDATE only when at least one field changed and the user confirms
- No-changes path prints `Info: No changes detected. Nothing to update.` and exits cleanly without DB writes
- Price change recalculates initial_stop on open positions / realized_pnl on closed positions
- trailing_stop and highest_close are NEVER mutated by fix
- audit_log captures the full before/after payload
- All 3 CMD-08 scenarios pass
- All gates green
</success_criteria>

<output>
Create `.planning/phases/08-confirmation-commands/08-04-SUMMARY.md` when done. Summary must include:
- Confirm fix.py replaces the stub
- Confirm 3/3 test scenarios pass
- Note how Open Question 1 was resolved (--date parameter unused, accepted but ignored)
- Note how Open Question 2 was resolved (display label = original trigger reason from scan_exit_triggers, but fix doesn't touch sell-trigger display; closing-reason display label is sell.py's concern, not fix.py's)
- Note any handling of invalid date / price input from raw input() prompts
</output>

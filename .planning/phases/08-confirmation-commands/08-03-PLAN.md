---
phase: 08-confirmation-commands
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/bensdorp1/commands/sell.py
  - tests/test_commands/test_sell.py
autonomous: true
requirements:
  - CMD-07

must_haves:
  truths:
    - "User can run `bensdorp1 sell SYMBOL PRICE` against an open position with a recorded exit trigger and, after [y/n] confirmation, the position is closed (closed_at, exit_price, realized_pnl, closed_reason populated)"
    - "User running sell on an open position with NO scan_exit_triggers row sees an error and exits with code 1 (D-13)"
    - "User can run `bensdorp1 sell SYMBOL PRICE --manual REASON` to bypass the exit-trigger requirement; closed_reason = 'manual', closed_manual_reason = REASON"
    - "Sell on a non-existent open position prints an error and exits 1"
    - "Sell price <= 0 prints an error and exits 1"
    - "--date earlier than entry_date prints an error and exits 1"
    - "Realized P&L is computed correctly: (sell_price - entry_close) * shares and (sell_price / entry_close - 1) * 100"
    - "Days held is computed as len(get_trading_days(entry_date.date(), sell_date)) NYSE trading days"
    - "After a confirmed sell, audit_log contains a sell_confirmed (or sell_manual) event with payload including symbol, sell_price, shares, entry_price, realized_pnl, realized_pnl_pct, closed_reason, closed_manual_reason"
  artifacts:
    - path: "src/bensdorp1/commands/sell.py"
      provides: "Full CMD-07 implementation (replaces stub)"
      contains: "def sell("
    - path: "src/bensdorp1/commands/sell.py"
      provides: "Reason mapping per D-12"
      contains: "stop_trailing"
    - path: "src/bensdorp1/commands/sell.py"
      provides: "Earliest trigger query per D-12 / Pitfall 2"
      contains: "scan_exit_triggers.c.id.asc()"
    - path: "tests/test_commands/test_sell.py"
      provides: "3 implemented CMD-07 scenarios — no skips remain"
      contains: "def test_happy_path_normal"
  key_links:
    - from: "src/bensdorp1/commands/sell.py"
      to: "positions table"
      via: "update closing row"
      pattern: "update\\(positions\\)"
    - from: "src/bensdorp1/commands/sell.py"
      to: "scan_exit_triggers table"
      via: "earliest-row ASC ORDER BY id query"
      pattern: "scan_exit_triggers.c.id.asc\\(\\)"
    - from: "src/bensdorp1/commands/sell.py"
      to: "audit_log"
      via: "log_event with SELL_CONFIRMED or SELL_MANUAL"
      pattern: "SELL_CONFIRMED|SELL_MANUAL"
    - from: "src/bensdorp1/commands/sell.py"
      to: "data.calendar.get_trading_days"
      via: "len(get_trading_days(entry_date.date(), sell_date))"
      pattern: "get_trading_days"
---

<objective>
Implement the full `bensdorp1 sell SYMBOL PRICE [--date DATE] [--manual REASON]` command (CMD-07) per spec §7.4 and decisions D-12–D-18. Replace the existing stub; fill in all 3 skeleton tests in `tests/test_commands/test_sell.py`.

Purpose: Records confirmed sells (normal exit-trigger-driven or `--manual`), computes realized P&L, closes the position by UPDATE, writes the appropriate audit event (SELL_CONFIRMED or SELL_MANUAL), and creates a backup.

Output: Working sell command with full integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-confirmation-commands/08-CONTEXT.md
@.planning/phases/08-confirmation-commands/08-RESEARCH.md
@.planning/phases/08-confirmation-commands/08-PATTERNS.md
@.planning/Bensdorp_1.md
@src/bensdorp1/commands/init.py
@src/bensdorp1/commands/sell.py
@src/bensdorp1/db/schema.py
@src/bensdorp1/db/audit.py
@src/bensdorp1/ui/__init__.py
@src/bensdorp1/ui/messages.py
@src/bensdorp1/ui/styles.py
@src/bensdorp1/data/calendar.py
@src/bensdorp1/config.py
@tests/test_commands/test_init.py
@tests/test_commands/test_sell.py
@tests/conftest.py
</context>

<interfaces>
<!-- Key contracts the executor consumes -->

From bensdorp1._app:
```python
app: typer.Typer
```

From bensdorp1.config:
```python
DATA_DIR: Path
MARKET_TZ: ZoneInfo
```

From bensdorp1.db (public surface):
```python
class AuditEventType(StrEnum):
    SELL_CONFIRMED = "sell_confirmed"
    SELL_MANUAL = "sell_manual"
    # ...
def get_engine(path: Path | None = None) -> Engine
def run_migrations(engine: Engine) -> None
def log_event(engine: Engine, event_type: AuditEventType,
              symbol: str | None = None, payload: dict[str, Any] | None = None) -> None
def create_backup(engine: Engine, backups_dir: Path) -> Path
```

From bensdorp1.db.schema:
```python
positions: Table             # c.id, c.symbol, c.entry_date, c.entry_close, c.shares,
                             # c.initial_stop, c.highest_close, c.trailing_stop, c.scan_id,
                             # c.closed_at, c.exit_price, c.realized_pnl
                             # (+ closed_reason, closed_manual_reason via ALTER TABLE)
scan_exit_triggers: Table    # c.id, c.scan_id, c.position_id, c.reason,
                             # c.effective_stop, c.triggered_date
                             # reason values: literally "Trailing stop" or "Initial stop"
```

From bensdorp1.data:
```python
def get_trading_days(start: date, end: date) -> pd.DatetimeIndex  # inclusive
```

From bensdorp1.ui:
```python
def confirm_prompt(message: str, *, console: Console | None = None) -> bool
def render_kv_block(data: dict[str, str], console: Console, indent: str = "") -> None
def format_price(value: float) -> str  # "$X,XXX.XX"
def format_pnl(value: float) -> str    # "+/-$X,XXX.XX"
def format_pct(value: float) -> str    # "+/-X.X%"
def format_days(n: int) -> str         # "N days" / "1 day"
def format_date(d: date) -> str        # "YYYY-MM-DD"
def print_error(title: str, *, data, body, impact, actions, console) -> None
def print_success(title: str, *, data, body, impact, actions, console) -> None
```

Stub being replaced (src/bensdorp1/commands/sell.py):
```python
@app.command(rich_help_panel="Confirmations")
def sell() -> None:
    typer.echo("Not yet implemented.")
    raise typer.Exit()
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sell.py — full CMD-07 flow</name>
  <files>src/bensdorp1/commands/sell.py</files>
  <read_first>
    - src/bensdorp1/commands/sell.py (current stub being replaced)
    - src/bensdorp1/commands/init.py (canonical SEPARATOR, KeyboardInterrupt wrapping, render_kv_block, create_backup + log_event sequence)
    - src/bensdorp1/commands/scan.py (canonical validation pattern; DATA_DIR / "data" / "bensdorp1.db" path)
    - src/bensdorp1/db/schema.py lines 42-112 (positions and scan_exit_triggers columns)
    - src/bensdorp1/db/audit.py (SELL_CONFIRMED and SELL_MANUAL enum members exist; log_event signature)
    - src/bensdorp1/data/calendar.py (get_trading_days inclusive endpoints; takes `date` objects — Common Pitfall 4)
    - src/bensdorp1/ui/styles.py (format_price, format_pnl, format_pct, format_days, format_date)
    - .planning/phases/08-confirmation-commands/08-CONTEXT.md §"Sell command" decisions D-12 through D-18
    - .planning/phases/08-confirmation-commands/08-RESEARCH.md §"Code Examples — Sell: Trigger Lookup..." and §"Common Pitfalls" 2, 3, 4
    - .planning/phases/08-confirmation-commands/08-PATTERNS.md `src/bensdorp1/commands/sell.py` section
    - .planning/Bensdorp_1.md §7.4 (sell spec)
  </read_first>
  <behavior>
    Test cases (covered in Task 2):
    - Test: no open position for symbol → print_error + exit code 1
    - Test: price <= 0 → print_error + exit code 1
    - Test: --date earlier than entry_date → print_error + exit code 1
    - Test: open position exists but no scan_exit_triggers row → print_error("No exit trigger on record for SYMBOL.") + exit code 1 (D-13)
    - Test: open position + trigger with reason "Trailing stop" → closed_reason="stop_trailing", closed_manual_reason=NULL, event SELL_CONFIRMED, realized_pnl computed, position closed
    - Test: open position + trigger with reason "Initial stop" → closed_reason="stop_initial"
    - Test: --manual "Stop tightened ahead of earnings" → bypasses trigger lookup; closed_reason="manual", closed_manual_reason="Stop tightened ahead of earnings", event SELL_MANUAL
    - Test: realized_pnl = (sell_price - entry_close) * shares (verified to within float precision)
    - Test: days_held = len(get_trading_days(entry_date.date(), sell_date))
    - Test: Ctrl+C during confirmation prints abort message (printed by confirm_prompt itself) and exits 0
  </behavior>
  <action>
    REPLACE `src/bensdorp1/commands/sell.py` entirely with the full CMD-07 implementation. Follow the structure laid out in 08-PATTERNS.md `src/bensdorp1/commands/sell.py` section.

    Module structure:
    1. Docstring: `"""Record confirmed sell transaction."""`
    2. Imports (alphabetical within stdlib/third-party/first-party groups):
       - `import datetime`
       - `from datetime import UTC`
       - `from typing import Any`
       - `import typer`
       - `from rich.console import Console`
       - `from rich.text import Text`
       - `from sqlalchemy import select, update`
       - `from bensdorp1._app import app`
       - `from bensdorp1.config import DATA_DIR, MARKET_TZ`
       - `from bensdorp1.data import get_trading_days`
       - `from bensdorp1.db import AuditEventType, create_backup, get_engine, log_event, run_migrations`
       - `from bensdorp1.db.schema import positions, scan_exit_triggers`
       - `from bensdorp1.ui import confirm_prompt, format_date, format_days, format_pct, format_pnl, format_price, print_error, print_success, render_kv_block`
    3. Module-level constants:
       - `SEPARATOR: str = "=" * 64`
       - `_REASON_MAP: dict[str, str] = {"Trailing stop": "stop_trailing", "Initial stop": "stop_initial"}`
    4. Command signature:
       - `@app.command(rich_help_panel="Confirmations")`
       - `def sell(symbol: str = typer.Argument(..., help="Ticker symbol (e.g. AAPL)."), price: float = typer.Argument(..., help="Sell price per share."), date: str | None = typer.Option(None, "--date", help="Sell date (YYYY-MM-DD). Defaults to today ET."), manual: str | None = typer.Option(None, "--manual", help="Manual sell reason (skips exit trigger lookup).")) -> None:`
       - Docstring: `"""Record a confirmed sell transaction."""`

    Command body, in order:

    A. DB entry triad: db_path, get_engine, run_migrations, console (same as buy.py).

    B. Resolve sell_date and sell_dt (per D-10 semantics):
       - If `date` is None: `sell_date = datetime.datetime.now(MARKET_TZ).date()`.
       - Else: `try: sell_date = datetime.date.fromisoformat(date)` and on ValueError → print_error + typer.Exit(code=1).
       - `sell_dt = datetime.datetime(sell_date.year, sell_date.month, sell_date.day, tzinfo=UTC)`.

    C. Validation per D-18:
       1. Open position lookup: `select(positions.c.id, positions.c.entry_close, positions.c.shares, positions.c.entry_date).where((positions.c.symbol == symbol.upper()) & (positions.c.closed_at == None))` (with `# noqa: E711` for the `== None`). fetchone.
          - If None: `print_error(f"No open position for {symbol.upper()}.", actions=["Use `bensdorp1 portfolio` to view current positions."], console=console)` + `raise typer.Exit(code=1)`.
          - Else: extract `position_id: int`, `entry_close: float`, `shares: int`, `entry_date: datetime` (the column is timezone-aware datetime).
       2. price > 0: if `price <= 0` → print_error(...) + exit code 1.
       3. sell_date >= entry_date.date(): if `sell_date < entry_date.date()` → `print_error(f"--date {sell_date.isoformat()} is earlier than entry date {entry_date.date().isoformat()}.", console=console)` + exit code 1.

    D. Determine closed_reason / closed_manual_reason / display_reason / event_type (D-12, D-14, D-15):
       - If `manual is not None`: closed_reason = "manual"; closed_manual_reason = manual; display_reason = "Manual"; event_type = AuditEventType.SELL_MANUAL.
       - Else: query earliest trigger per D-12 / Pitfall 2:
         - `select(scan_exit_triggers.c.reason).where(scan_exit_triggers.c.position_id == position_id).order_by(scan_exit_triggers.c.id.asc()).limit(1)`. fetchone.
         - If None: `print_error(f"No exit trigger on record for {symbol.upper()}.", body=["To record a manual sell, use: bensdorp1 sell SYMBOL PRICE --manual REASON"], console=console)` + `raise typer.Exit(code=1)`.
         - Else: closed_reason = _REASON_MAP.get(trigger_row.reason, "stop_trailing"); closed_manual_reason = None; display_reason = trigger_row.reason; event_type = AuditEventType.SELL_CONFIRMED.

    E. Computed values (D-16, Pitfall 4):
       - `sell_value: float = price * shares`
       - `entry_value: float = entry_close * shares`
       - `days_held: int = len(get_trading_days(entry_date.date(), sell_date))`
       - `realized_pnl: float = (price - entry_close) * shares`
       - `realized_pnl_pct: float = (price / entry_close - 1) * 100.0`
       - `pnl_display: str = f"{format_pnl(realized_pnl)} ({format_pct(realized_pnl_pct)})"`

    F. Confirmation block (D-16, spec §7.4):
       - In try / except KeyboardInterrupt: raise typer.Exit() from None:
         - `console.print(Text(SEPARATOR))`
         - Header text: `"Confirm sell (manual)"` when manual is not None, else `"Confirm sell"`.
         - `console.print(Text(SEPARATOR))`; `console.print()`
         - Build dict with keys (sentence case, matching spec §7.4):
           - "Symbol": symbol.upper()
           - "Sell price": format_price(price)
           - "Sell value": format_price(sell_value)
           - "Shares sold": str(shares)
           - "Entry price": format_price(entry_close)
           - "Entry value": format_price(entry_value)
           - "Days held": format_days(days_held)
           - "Realized P&L": pnl_display
           - "Closing reason": display_reason
         - When manual is not None, also add: "Manual reason": manual
         - `render_kv_block({...}, console)`
         - `console.print()`
         - `confirmed = confirm_prompt("Confirm sell?", console=console)`
       - If not confirmed → `raise typer.Exit()`.

    G. State-changing write (D-17):
       - In `engine.connect()`: `update(positions).where(positions.c.id == position_id).values(closed_at=sell_dt, exit_price=price, realized_pnl=realized_pnl, closed_reason=closed_reason, closed_manual_reason=closed_manual_reason)`. Then `conn.commit()`.
       - `create_backup(engine, DATA_DIR / "backups")`
       - `payload: dict[str, Any] = {"symbol": symbol.upper(), "sell_price": price, "shares": shares, "entry_price": entry_close, "realized_pnl": realized_pnl, "realized_pnl_pct": realized_pnl_pct, "closed_reason": closed_reason, "closed_manual_reason": closed_manual_reason}`
       - `log_event(engine, event_type, symbol=symbol.upper(), payload=payload)`
       - `print_success("Sell recorded.", console=console)`

    Style requirements (same as buy.py): Text() wrapping for all `console.print` literals; parameterized SQL only; `raise typer.Exit(code=1)` for errors; `raise typer.Exit() from None` after caught KeyboardInterrupt; mypy strict locals typed.

    The column name is `entry_close` (NOT `entry_price`) — Pitfall 3. Use `positions.c.entry_close` in queries; map to UI label "Entry price" in render_kv_block dict.
  </action>
  <verify>
    <automated>uv run mypy src/bensdorp1/commands/sell.py --strict && uv run ruff check src/bensdorp1/commands/sell.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/bensdorp1/commands/sell.py` contains `def sell(` with the 4-parameter signature (symbol, price, date, manual)
    - File contains the literal `_REASON_MAP` mapping with both keys `"Trailing stop"` and `"Initial stop"`
    - File uses `.order_by(scan_exit_triggers.c.id.asc()).limit(1)` (earliest trigger — D-12 / Pitfall 2)
    - File contains exactly ONE `update(positions)` call (state change is an UPDATE, never INSERT)
    - File contains `AuditEventType.SELL_CONFIRMED` AND `AuditEventType.SELL_MANUAL`
    - File contains the string literal `"No exit trigger on record for"` (D-13)
    - File contains `"To record a manual sell, use: bensdorp1 sell SYMBOL PRICE --manual REASON"` in the body of the no-trigger error
    - File uses `get_trading_days(entry_date.date(), sell_date)` (Pitfall 4 — .date() called on the datetime)
    - File contains `(price - entry_close) * shares` (realized_pnl formula)
    - File contains `(price / entry_close - 1) * 100` (realized_pnl_pct formula; constant may be `100.0`)
    - File uses `positions.c.entry_close` (not `entry_price`): `grep -c "entry_close" src/bensdorp1/commands/sell.py` >= 1; `grep -c "entry_price" src/bensdorp1/commands/sell.py` == 0 within DB query lines (payload key `"entry_price"` IS allowed for the audit log payload)
    - File contains `create_backup(engine, DATA_DIR / "backups")`
    - File contains `SEPARATOR: str = "=" * 64`
    - `uv run mypy src/bensdorp1/commands/sell.py --strict` exits 0
    - `uv run ruff check src/bensdorp1/commands/sell.py` exits 0
  </acceptance_criteria>
  <done>
    `bensdorp1 sell` command is fully implemented per CMD-07 / spec §7.4 / decisions D-12–D-18. mypy strict and ruff clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement test_sell.py — fill all 3 skeleton tests</name>
  <files>tests/test_commands/test_sell.py</files>
  <read_first>
    - tests/test_commands/test_sell.py (skeleton from Plan 01 — 3 named tests with pytest.skip)
    - tests/test_commands/test_init.py (canonical mocked-engine pattern)
    - tests/conftest.py (db_engine fixture)
    - src/bensdorp1/commands/sell.py (the implementation under test from Task 1)
    - src/bensdorp1/db/schema.py (positions, scan_exit_triggers, scans for fixture seeding)
    - .planning/phases/08-confirmation-commands/08-PATTERNS.md `tests/test_commands/test_sell.py` section
  </read_first>
  <behavior>
    - test_no_exit_trigger: open position exists for AAPL but scan_exit_triggers has no row for that position_id → exit code 1 and "No exit trigger on record" in output; position remains open (closed_at IS NULL after the call)
    - test_happy_path_normal: open position for AAPL + a scan_exit_triggers row with reason="Trailing stop" → invoking `["sell", "AAPL", "178.20"]` with input "y\n" → exit code 0; positions row updated: closed_at set, exit_price=178.20, realized_pnl correct, closed_reason="stop_trailing", closed_manual_reason IS NULL; audit_log has sell_confirmed event
    - test_manual_sell: open position + invocation `["sell", "AAPL", "178.20", "--manual", "Stop tightened ahead of earnings"]` with input "y\n" → exit code 0; closed_reason="manual", closed_manual_reason="Stop tightened ahead of earnings"; audit event = "sell_manual"
  </behavior>
  <action>
    REPLACE the body of each of the 3 test functions in `tests/test_commands/test_sell.py` (do not rename — names are pinned by Plan 01).

    Imports needed:
    ```
    from datetime import UTC, datetime, timedelta
    from pathlib import Path
    from unittest.mock import MagicMock, patch
    import pytest
    from sqlalchemy import insert, select
    from sqlalchemy.engine import Engine
    from typer.testing import CliRunner
    from bensdorp1.cli import app
    from bensdorp1.db.schema import audit_log, positions, scan_exit_triggers, scans
    ```

    Module-level: `runner = CliRunner()`.

    Implementation guidance per test:

    **test_no_exit_trigger(tmp_path: Path) -> None**
    - Use db_engine fixture instead of tmp_path; rename signature accordingly to `(db_engine: Engine) -> None` if the test map allows (it does — exit-trigger absence is best verified with a real DB). NOTE: Plan 01 declared the signature as `tmp_path: Path` — keep `tmp_path: Path` to match Plan 01 and use a MagicMock engine where the first fetchone (open position) returns a row and the second fetchone (trigger query) returns None.
    - patch DATA_DIR, get_engine (return mock_engine), run_migrations at "bensdorp1.commands.sell.*".
    - `mock_conn = mock_engine.connect.return_value.__enter__.return_value`
    - `mock_conn.execute.return_value.fetchone.side_effect = [MagicMock(id=1, entry_close=182.50, shares=50, entry_date=datetime(2026, 4, 1, tzinfo=UTC)), None]`
    - `result = runner.invoke(app, ["sell", "AAPL", "178.20"])`
    - assert result.exit_code == 1
    - assert "No exit trigger on record" in result.output

    **test_happy_path_normal(db_engine: Engine) -> None**
    - Use db_engine fixture. Insert positions row: symbol="AAPL", entry_date=datetime(2026, 4, 1, tzinfo=UTC), entry_close=182.50, shares=50, initial_stop=169.725, highest_close=185.00, trailing_stop=138.75, scan_id=None, closed_at=None, exit_price=None, realized_pnl=None. Capture the returned position_id.
    - Insert a scans row first (FK requirement for scan_exit_triggers.scan_id): scan_date=datetime(2026, 5, 20, tzinfo=UTC), regime_active=True, candidate_count=10, exit_trigger_count=1, created_at=datetime.now(UTC). Capture scan_id.
    - Insert scan_exit_triggers row: scan_id=that scan_id, position_id=position_id, reason="Trailing stop", effective_stop=170.0, triggered_date=datetime(2026, 5, 20, tzinfo=UTC).
    - Patch DATA_DIR (to tmp_path implicitly via fixture path), get_engine return db_engine, create_backup to MagicMock. Do NOT mock log_event.
    - `result = runner.invoke(app, ["sell", "AAPL", "178.20"], input="y\n")`
    - assert result.exit_code == 0
    - assert "Sell recorded" in result.output
    - Query positions: row with id == position_id has closed_at IS NOT NULL, exit_price == pytest.approx(178.20), realized_pnl == pytest.approx((178.20 - 182.50) * 50), closed_reason == "stop_trailing", closed_manual_reason IS NULL.
    - Query audit_log: at least one row with event_type == "sell_confirmed" and symbol == "AAPL".

    **test_manual_sell(db_engine: Engine) -> None**
    - Same seeding as happy path but DO NOT insert a scan_exit_triggers row (verifies --manual bypasses trigger lookup).
    - Insert position row as above, capture position_id.
    - `result = runner.invoke(app, ["sell", "AAPL", "178.20", "--manual", "Stop tightened ahead of earnings"], input="y\n")`
    - assert result.exit_code == 0
    - Query positions: closed_reason == "manual", closed_manual_reason == "Stop tightened ahead of earnings"
    - Query audit_log: event_type == "sell_manual"

    Use pytest.approx for floats; cast db rows to typed accessors (`row.closed_reason`).

    Note: `test_no_exit_trigger` keeps signature `tmp_path: Path` (per Plan 01 stub) — implementation uses MagicMock engine. The other two tests have the signature `db_engine: Engine` per Plan 01.
  </action>
  <verify>
    <automated>uv run pytest tests/test_commands/test_sell.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_commands/test_sell.py -v` reports 3 PASSED, 0 SKIPPED, 0 FAILED
    - `uv run pytest tests/test_commands/test_sell.py::test_no_exit_trigger -x` exits 0
    - `uv run pytest tests/test_commands/test_sell.py::test_happy_path_normal -x` exits 0
    - `uv run pytest tests/test_commands/test_sell.py::test_manual_sell -x` exits 0
    - No skips remain: `grep -c "pytest.skip" tests/test_commands/test_sell.py` == 0
    - `uv run ruff check tests/test_commands/test_sell.py` exits 0
    - `uv run mypy tests/test_commands/test_sell.py --strict` exits 0
  </acceptance_criteria>
  <done>
    All 3 sell tests pass; no skips; type/lint clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → sell.py | symbol, price, --date, --manual originate from shell |
| sell.py → DB | All writes via parameterized SQLAlchemy queries |
| sell.py → console | All literal output Text-wrapped; user-supplied --manual reason flows into render_kv_block (markup=False inside) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | symbol / manual reason in SQL | mitigate | All queries use .where(col == bindparam) and .values(col=value); no f-string SQL anywhere — verifiable via `grep -c "text(f\"" src/bensdorp1/commands/sell.py` == 0 |
| T-08-03-02 | Tampering | --manual reason in Rich output | mitigate | render_kv_block uses markup=False/highlight=False internally; the reason is shown only via render_kv_block, never via direct console.print of an unwrapped string |
| T-08-03-03 | Tampering | Negative/zero sell price | mitigate | Explicit `price <= 0` check → print_error + typer.Exit(code=1) before any DB write |
| T-08-03-04 | DoS | Invalid --date ValueError crash | mitigate | datetime.date.fromisoformat in try/except ValueError → print_error + typer.Exit(code=1) |
| T-08-03-05 | Tampering | --date earlier than entry_date allows backdating to past | mitigate | Explicit check `sell_date >= entry_date.date()` → print_error + typer.Exit(code=1) |
| T-08-03-06 | Repudiation | Manual sells not auditable | mitigate | AuditEventType.SELL_MANUAL with full payload (including manual reason) appended to audit_log on every confirmed manual sell |
| T-08-03-07 | Tampering | Wrong trigger row used (latest vs earliest) | mitigate | Pitfall 2: `.order_by(scan_exit_triggers.c.id.asc()).limit(1)` returns the EARLIEST trigger — original reason — per D-12 |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed (RESEARCH.md Package Legitimacy Audit — empty) |
</threat_model>

<verification>
- `uv run pytest tests/test_commands/test_sell.py -v` → 3 PASSED
- `uv run mypy src/bensdorp1/commands/sell.py tests/test_commands/test_sell.py --strict` exits 0
- `uv run ruff check src/bensdorp1/commands/sell.py tests/test_commands/test_sell.py` exits 0
- `uv run pytest -x` exits 0 (full suite stays green)
</verification>

<success_criteria>
- `bensdorp1 sell AAPL 178.20` against an open position with a trigger closes the position cleanly and writes a SELL_CONFIRMED audit event
- `bensdorp1 sell AAPL 178.20 --manual "<reason>"` closes the position and writes a SELL_MANUAL audit event
- Missing trigger without --manual → exit code 1 with the documented error message
- Realized P&L formula and days_held calculation match the verified formulas
- All 3 CMD-07 scenarios pass
- All gates green
</success_criteria>

<output>
Create `.planning/phases/08-confirmation-commands/08-03-SUMMARY.md` when done. Summary must include:
- Confirm sell.py replaces the stub
- Confirm 3/3 test scenarios pass
- Note the realized_pnl float precision approach (pytest.approx tolerance)
- Note how the trigger lookup ordering was verified in tests (single-row scenario, but explicit ASC ordering)
</output>

---
phase: 08-confirmation-commands
plan: 05
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
  - 08-04
files_modified: []
autonomous: true
requirements:
  - CMD-06
  - CMD-07
  - CMD-08

must_haves:
  truths:
    - "Full pytest suite passes with no failures or unexpected skips"
    - "Combined coverage on bensdorp1 package is >= 90% (TEST-02)"
    - "mypy strict reports zero errors across the whole src/ tree"
    - "ruff check reports zero errors across src/ and tests/"
    - "ruff format check reports no diff (formatting clean)"
    - "All 8 phase 8 success criteria from ROADMAP.md are verifiable from the running CLI"
  artifacts: []
  key_links: []
---

<objective>
Whole-repo verification gate for Phase 8. No production code is changed in this plan — only check that the implementations from Plans 02/03/04 (sitting on top of the migration from Plan 01) pass every quality gate together.

Purpose: TEST-02 (>= 90% coverage), mypy strict, ruff, and the three success criteria from ROADMAP.md Phase 8 must ALL pass before the phase is marked complete. Catches integration regressions (e.g. test_init.py breaking because of a migration side-effect) that single-plan verification could miss.

Output: A pass/fail report (committed as 08-05-SUMMARY.md). Phase 8 is only complete when this plan exits clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-confirmation-commands/08-CONTEXT.md
@.planning/phases/08-confirmation-commands/08-RESEARCH.md
@.planning/phases/08-confirmation-commands/08-01-PLAN.md
@.planning/phases/08-confirmation-commands/08-02-PLAN.md
@.planning/phases/08-confirmation-commands/08-03-PLAN.md
@.planning/phases/08-confirmation-commands/08-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full quality gate (pytest with coverage, mypy strict, ruff check, ruff format check)</name>
  <files></files>
  <read_first>
    - .planning/REQUIREMENTS.md TEST-02 (>= 90% coverage target)
    - pyproject.toml `[tool.coverage.*]`, `[tool.mypy]`, `[tool.ruff*]` sections to confirm config paths
    - .planning/phases/08-confirmation-commands/08-RESEARCH.md §"Validation Architecture" (sampling rate and full-suite command)
  </read_first>
  <action>
    Run the following commands sequentially, capturing exit code and output for each. DO NOT modify any source or test file as part of this task — this is a verification-only plan.

    1. `uv run pytest --cov=bensdorp1 --cov-report=term-missing -x`
       - Must exit 0 with all tests passing
       - Coverage on `bensdorp1` package must be >= 90% (TEST-02)
       - Phase 8 specifically: tests/test_commands/test_buy.py (5 passed), test_sell.py (3 passed), test_fix.py (3 passed) — total 11 new passing tests

    2. `uv run pytest tests/test_commands/test_buy.py tests/test_commands/test_sell.py tests/test_commands/test_fix.py -v`
       - Verify: 11 PASSED, 0 SKIPPED, 0 FAILED
       - This is the focused phase-8 regression suite

    3. `uv run mypy src --strict`
       - Must exit 0 with zero errors across all src/ files including the three new command implementations

    4. `uv run mypy tests --strict`
       - Must exit 0 with zero errors across all tests/ files (the new test files must type-check)

    5. `uv run ruff check src tests`
       - Must exit 0 with zero lint errors

    6. `uv run ruff format --check src tests`
       - Must exit 0 (no formatting diffs)

    If ANY of these commands exits non-zero:
    - Capture the exit code and failing output
    - Identify which Plan (01/02/03/04) introduced the regression
    - Return a `## REGRESSION DETECTED` block in the SUMMARY indicating which plan needs follow-up work
    - DO NOT silently patch or skip — this is a verification gate, not a fix-everything plan

    If all six commands pass:
    - Record per-module coverage percentages for the three new command files in the SUMMARY
    - Record the total test count delta from before Phase 8 (use `git log -1 --stat` on tests/ if helpful)
    - Confirm the phase 8 ROADMAP success criteria are observable: buy creates a position, sell with trigger closes it, sell --manual closes it, fix corrects in-place with audit trail
  </action>
  <verify>
    <automated>uv run pytest --cov=bensdorp1 --cov-report=term-missing -x && uv run mypy src --strict && uv run ruff check src tests && uv run ruff format --check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -x` exits 0 with no FAILED or unexpected SKIPPED tests (the 5 buy + 3 sell + 3 fix tests are PASSED)
    - `uv run pytest --cov=bensdorp1 --cov-report=term-missing` reports total coverage >= 90%
    - `uv run mypy src --strict` exits 0
    - `uv run mypy tests --strict` exits 0
    - `uv run ruff check src tests` exits 0
    - `uv run ruff format --check src tests` exits 0
    - The Phase 8 success-criteria observations (buy works, sell normal works, sell --manual works, fix works with audit trail) are recorded in the SUMMARY
  </acceptance_criteria>
  <done>
    All quality gates green. Phase 8 ready to mark complete in ROADMAP.md and STATE.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (verification only) | This plan executes read-only test/lint commands — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | (n/a) | Verification gate | accept | Plan executes no production code changes; failure modes are detection (gate fails loud) rather than risk introduction |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | No new packages — all gates run via `uv run` against the existing locked environment |
</threat_model>

<verification>
The plan IS the verification. If task 1 exits 0, Phase 8 passes the quality gate.
</verification>

<success_criteria>
- All six gate commands exit 0
- Phase 8 ROADMAP success-criteria observable via the integration tests
- 08-05-SUMMARY.md records the pass/fail status of each gate command
</success_criteria>

<output>
Create `.planning/phases/08-confirmation-commands/08-05-SUMMARY.md` when done. Summary must include:
- Per-command exit codes (pytest with coverage, mypy src strict, mypy tests strict, ruff check, ruff format check)
- Coverage percentage for the three new command files individually (buy.py, sell.py, fix.py)
- Total project coverage percentage
- Confirmation that the three ROADMAP success criteria are observably satisfied
- If any regression: which plan introduced it and what command revealed it
</output>

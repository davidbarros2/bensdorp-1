---
phase: 08-confirmation-commands
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bensdorp1/commands/fix.py
  - src/bensdorp1/commands/sell.py
autonomous: true
gap_closure: true
requirements:
  - CMD-08
must_haves:
  truths:
    - "fix.py rejects new_price <= 0 in buy path with exit code 1 before any DB write"
    - "fix.py rejects new_shares <= 0 in buy path with exit code 1 before any DB write"
    - "fix.py rejects new_price <= 0 in sell path with exit code 1 before any DB write"
    - "fix.py always recomputes realized_pnl from new_price/current_entry_close in sell path"
    - "sell.py raises explicit error (not silent fallback) for unrecognised exit trigger reason"
  artifacts:
    - path: "src/bensdorp1/commands/fix.py"
      provides: "Range checks after float()/int() parse in buy and sell branches"
      contains: "new_price <= 0"
    - path: "src/bensdorp1/commands/sell.py"
      provides: "Explicit error for unknown _REASON_MAP key"
      contains: "Unrecognised exit trigger reason"
  key_links:
    - from: "fix.py buy path"
      to: "positions.entry_close"
      via: "update(positions).values(entry_close=new_price)"
      pattern: "new_price <= 0"
    - from: "fix.py sell path"
      to: "positions.realized_pnl"
      via: "update(positions).values(realized_pnl=new_realized_pnl)"
      pattern: "new_realized_pnl = .new_price - current_entry_close. \\* current_shares"
---

<objective>
Close CR-01 (missing input validation in fix.py) and address WR-01 (silent fallback
in sell.py) and WR-02 (NULL propagation in fix.py sell path).

Purpose: Prevent corrupt values (zero, negative, NULL realized_pnl) from being
written to the database and prevent silent audit-trail corruption for unknown
trigger reason strings.

Output: Three guard additions to fix.py (6 lines total) + one WR-02 fix in fix.py
sell-path recalc block + one WR-01 fix in sell.py _REASON_MAP lookup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-confirmation-commands/08-VERIFICATION.md
@.planning/phases/08-confirmation-commands/08-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add range guards to fix.py (CR-01 + WR-02)</name>
  <files>src/bensdorp1/commands/fix.py</files>

  <read_first>
  Read src/bensdorp1/commands/fix.py in full before making any edits.
  Key locations:
  - Line 176-184: buy path price parse block (raw_price / float(clean_price))
  - Line 187-197: buy path shares parse block (raw_shares / int(raw_shares))
  - Line 213-224: sell path price parse block (raw_price / float(clean_price))
  - Lines 265-270: sell path recalculation block (new_realized_pnl)

  Also read src/bensdorp1/commands/buy.py lines 97-104 to confirm the guard
  pattern to mirror.
  </read_first>

  <action>
  Make four targeted edits to src/bensdorp1/commands/fix.py.

  **Edit 1 — CR-01 buy path price guard (after line 179 `new_price: float = float(clean_price)`):**

  Immediately after the `new_price: float = float(clean_price)` assignment in the
  buy branch (the `if raw_price:` block, which ends with `raise typer.Exit(code=1)
  from None` for ValueError), add:

      if new_price <= 0:
          print_error("Price must be greater than zero.", console=console)
          raise typer.Exit(code=1) from None

  This guard goes BEFORE the closing `else: new_price = current_entry_close` branch.
  It sits inside the `if raw_price:` block, after the `except ValueError` block.

  **Edit 2 — CR-01 buy path shares guard (after line 190 `new_shares: int = int(raw_shares)`):**

  Immediately after the `new_shares: int = int(raw_shares)` assignment in the buy
  branch, add:

      if new_shares <= 0:
          print_error("Shares must be greater than zero.", console=console)
          raise typer.Exit(code=1) from None

  This guard goes BEFORE the closing `else: new_shares = current_shares` branch.

  **Edit 3 — CR-01 sell path price guard (after line 219 `new_price = float(clean_price)` in sell branch):**

  Immediately after the `new_price = float(clean_price)` assignment in the sell
  branch, add:

      if new_price <= 0:
          print_error("Price must be greater than zero.", console=console)
          raise typer.Exit(code=1) from None

  This guard goes BEFORE the closing `else: new_price = current_exit_price` branch.

  **Edit 4 — WR-02 sell path realized_pnl recalculation (lines 265-270):**

  Change the sell-path recalculation block from:

      else:
          new_initial_stop = 0.0  # not used for sell
          if new_price != current_exit_price:
              new_realized_pnl = (new_price - current_entry_close) * current_shares
          else:
              new_realized_pnl = current_realized_pnl

  To:

      else:
          new_initial_stop = 0.0  # not used for sell
          new_realized_pnl = (new_price - current_entry_close) * current_shares

  This always recomputes realized_pnl from current values, eliminating NULL
  propagation and removing the conditional branch. The type of new_realized_pnl
  changes from `float | None` to `float` — update the type annotation on line 264
  (`new_realized_pnl: float | None = None`) to `new_realized_pnl: float | None = None`
  keeping it as-is (the buy path still assigns None for new_realized_pnl, so the
  union type must remain). The sell-path value is always a float after this change.

  After edits, run: `uv run ruff check src/bensdorp1/commands/fix.py && uv run ruff format --check src/bensdorp1/commands/fix.py && uv run mypy src/bensdorp1/commands/fix.py --strict`

  Fix any ruff or mypy errors before proceeding.
  </action>

  <verify>
    <automated>
      uv run python -c "import ast, pathlib; src = pathlib.Path('src/bensdorp1/commands/fix.py').read_text(); assert 'new_price <= 0' in src, 'CR-01 buy price guard missing'; assert 'new_shares <= 0' in src, 'CR-01 buy shares guard missing'; print('Guards present')"
      uv run ruff check src/bensdorp1/commands/fix.py
      uv run mypy src/bensdorp1/commands/fix.py --strict
    </automated>
  </verify>

  <acceptance_criteria>
  - `grep -c "new_price <= 0" src/bensdorp1/commands/fix.py` returns 2 (buy path + sell path)
  - `grep -c "new_shares <= 0" src/bensdorp1/commands/fix.py` returns 1 (buy path)
  - `grep -c "new_realized_pnl = (new_price" src/bensdorp1/commands/fix.py` returns 1 and the conditional `if new_price != current_exit_price:` no longer wraps it
  - `uv run ruff check src/bensdorp1/commands/fix.py` exits 0
  - `uv run mypy src/bensdorp1/commands/fix.py --strict` exits 0 with "Success: no issues found"
  - The existing 3 fix.py tests still pass: `uv run pytest tests/test_commands/test_fix.py -v` shows 3 passed
  </acceptance_criteria>

  <done>
  fix.py rejects zero/negative price in both buy and sell paths, rejects zero/negative
  shares in buy path, and always recomputes realized_pnl from new_price in sell path.
  All guards exit code 1. ruff and mypy pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix sell.py _REASON_MAP silent fallback (WR-01)</name>
  <files>src/bensdorp1/commands/sell.py</files>

  <read_first>
  Read src/bensdorp1/commands/sell.py lines 144-165 — specifically the trigger_row
  lookup and the _REASON_MAP.get() call at line 162.

  The current code is:
      closed_reason = _REASON_MAP.get(trigger_row.reason, "stop_trailing")
      closed_manual_reason = None
      display_reason = trigger_row.reason
      event_type = AuditEventType.SELL_CONFIRMED

  This silently maps unknown reason strings to "stop_trailing", corrupting the
  audit trail.
  </read_first>

  <action>
  Replace the single `_REASON_MAP.get(trigger_row.reason, "stop_trailing")` call
  (line 162) with an explicit None-check pattern.

  Change from:

      closed_reason = _REASON_MAP.get(trigger_row.reason, "stop_trailing")
      closed_manual_reason = None
      display_reason = trigger_row.reason
      event_type = AuditEventType.SELL_CONFIRMED

  To:

      mapped = _REASON_MAP.get(trigger_row.reason)
      if mapped is None:
          print_error(
              f"Unrecognised exit trigger reason: {trigger_row.reason!r}.",
              body=["Use --manual to record this sell with a custom reason."],
              console=console,
          )
          raise typer.Exit(code=1)
      closed_reason = mapped
      closed_manual_reason = None
      display_reason = trigger_row.reason
      event_type = AuditEventType.SELL_CONFIRMED

  The `print_error` function in sell.py already has access to `body` kwarg — confirm
  by checking existing call sites in the file (line 155 uses `body=[...]`).

  After the edit run: `uv run ruff check src/bensdorp1/commands/sell.py && uv run ruff format --check src/bensdorp1/commands/sell.py && uv run mypy src/bensdorp1/commands/sell.py --strict`
  </action>

  <verify>
    <automated>
      uv run python -c "import pathlib; src = pathlib.Path('src/bensdorp1/commands/sell.py').read_text(); assert 'Unrecognised exit trigger reason' in src, 'WR-01 fix missing'; assert '_REASON_MAP.get(trigger_row.reason, ' not in src, 'Silent fallback still present'; print('WR-01 fixed')"
      uv run ruff check src/bensdorp1/commands/sell.py
      uv run mypy src/bensdorp1/commands/sell.py --strict
    </automated>
  </verify>

  <acceptance_criteria>
  - `grep "stop_trailing" src/bensdorp1/commands/sell.py` does NOT include a `.get(` call with a default fallback
  - `grep "Unrecognised exit trigger reason" src/bensdorp1/commands/sell.py` returns 1 match
  - `uv run ruff check src/bensdorp1/commands/sell.py` exits 0
  - `uv run mypy src/bensdorp1/commands/sell.py --strict` exits 0 with "Success: no issues found"
  - The 3 existing sell.py tests still pass: `uv run pytest tests/test_commands/test_sell.py -v` shows 3 passed
  </acceptance_criteria>

  <done>
  sell.py raises typer.Exit(code=1) with an explicit error message for any trigger
  reason string not in _REASON_MAP. The silent fallback default is removed.
  ruff and mypy pass. All existing sell tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → fix.py field loop | Raw strings from input() cross into float()/int() and DB write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Tampering | fix.py new_price/new_shares parse | mitigate | CR-01 guards: `if new_price <= 0` / `if new_shares <= 0` immediately after parse, before any DB write |
| T-08-06-02 | Information Disclosure | sell.py _REASON_MAP silent fallback | mitigate | WR-01 fix: raise explicit Exit(code=1) so caller knows the reason string was unrecognised |
</threat_model>

<verification>
Run after both tasks complete:

```
uv run pytest tests/test_commands/test_fix.py tests/test_commands/test_sell.py -v
```

Expected: 6 passed (3 fix + 3 sell), 0 failed.

```
uv run ruff check src/bensdorp1/commands/fix.py src/bensdorp1/commands/sell.py
uv run mypy src/bensdorp1/commands/fix.py src/bensdorp1/commands/sell.py --strict
```

Both exit 0.
</verification>

<success_criteria>
- fix.py contains exactly 2 occurrences of `new_price <= 0` (buy path + sell path)
- fix.py contains exactly 1 occurrence of `new_shares <= 0` (buy path)
- fix.py sell-path recalc block computes `new_realized_pnl` unconditionally (no `if new_price != current_exit_price` guard)
- sell.py contains `Unrecognised exit trigger reason` error message
- sell.py has no `.get(trigger_row.reason, "stop_trailing")` with a default
- All 6 existing phase 8 buy/fix/sell tests pass
- ruff check and mypy strict pass on both modified files
</success_criteria>

<output>
Create `.planning/phases/08-confirmation-commands/08-06-SUMMARY.md` when done.
</output>

---
phase: 08-confirmation-commands
plan: 07
type: execute
wave: 2
depends_on: ["08-06"]
files_modified:
  - tests/test_commands/test_fix.py
  - tests/test_commands/test_buy.py
  - tests/test_commands/test_sell.py
autonomous: true
gap_closure: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "test_fix.py covers the sell-side fix path (exit_price, realized_pnl, audit_log updated)"
    - "test_fix.py covers buy-path new_price <= 0 rejection"
    - "test_fix.py covers buy-path new_shares <= 0 rejection"
    - "test_fix.py covers sell-path new_price <= 0 rejection"
    - "test_buy.py covers price <= 0 or shares <= 0 rejection"
    - "test_buy.py covers invalid --date rejection"
    - "test_sell.py covers no open position error path"
    - "Total measured coverage for fix.py, buy.py, sell.py reaches >= 90%"
  artifacts:
    - path: "tests/test_commands/test_fix.py"
      provides: "Sell-path integration test + 3 validation rejection tests"
      contains: "test_fix_sell_path"
    - path: "tests/test_commands/test_buy.py"
      provides: "Error-path tests for price/shares <= 0 and invalid --date"
      contains: "test_price_zero_rejected"
    - path: "tests/test_commands/test_sell.py"
      provides: "No open position error path test"
      contains: "test_no_open_position"
  key_links:
    - from: "test_fix_sell_path"
      to: "positions.exit_price / realized_pnl"
      via: "db_engine SELECT after runner.invoke fix SYMBOL"
      pattern: "row.exit_price == pytest.approx"
    - from: "test_price_zero_rejected (buy)"
      to: "buy.py exit code 1"
      via: "runner.invoke app ['buy', 'NVDA', '0.0', '10']"
      pattern: "exit_code == 1"
---

<objective>
Close TEST-02 by adding tests that cover the previously untested branches across
fix.py, buy.py, and sell.py to bring total coverage to >= 90%.

Purpose: The sell-side fix path and several error-exit branches in buy/sell have zero
test coverage, leaving CR-01 and WR-01/WR-02 regressions undetectable. These tests
also exercise the validations added by Plan 08-06, providing a regression safety net.

Output: 7 new test functions across the three test files. Tests use the real SQLite
db_engine fixture for integration tests and MagicMock for unit-level error paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-confirmation-commands/08-VERIFICATION.md

<!-- Key interfaces from existing code the executor must know -->
<interfaces>
From tests/conftest.py:
```python
@pytest.fixture
def db_engine(tmp_path: Path) -> Generator[Engine, None, None]:
    """Fresh file-based SQLite engine per test. metadata.create_all applied."""
    # yields Engine pointing at tmp_path/test.db
```

From src/bensdorp1/commands/buy.py:
```python
# Validation order (post Plan 08-06):
# B. Resolve buy_date from --date (fromisoformat; Exit code=1 on ValueError)
# C.1 constituent_cache check → Exit code=1 if None
# C.2 no-open-position check → Exit code=1 if already open
# C.3 price/shares > 0 → Exit code=1 if either <= 0  (line ~98)
```

From src/bensdorp1/commands/fix.py (post Plan 08-06):
```python
# Buy path field loop:
#   new_price = float(clean_price) → if new_price <= 0: print_error + Exit(1)
#   new_shares = int(raw_shares)   → if new_shares <= 0: print_error + Exit(1)
# Sell path field loop:
#   new_price = float(clean_price) → if new_price <= 0: print_error + Exit(1)
# Sell-path recalc (WR-02 fixed):
#   new_realized_pnl = (new_price - current_entry_close) * current_shares  # always

# DB columns for closed position:
#   positions.c.id, entry_close, shares, closed_at, exit_price,
#   closed_reason, closed_manual_reason, realized_pnl
```

From tests/test_fix.py (existing pattern for mock-based tests):
```python
mock_engine = MagicMock()
mock_conn = MagicMock()
mock_open_row = MagicMock()
mock_open_row.id = 1
mock_open_row.entry_date = datetime(2026, 5, 1, tzinfo=UTC)
mock_open_row.entry_close = 432.50
mock_open_row.shares = 23
mock_open_row.initial_stop = 402.225
mock_conn.execute.return_value.fetchone.return_value = mock_open_row
mock_engine.connect.return_value.__enter__ = MagicMock(return_value=mock_conn)
mock_engine.connect.return_value.__exit__ = MagicMock(return_value=False)
```

From tests/test_sell.py (existing pattern for no-position mock):
```python
mock_conn.execute.return_value.fetchone.side_effect = [
    MagicMock(id=1, entry_close=182.50, shares=50, entry_date=...),  # pos row
    None,  # trigger row
]
```

From src/bensdorp1/db/schema.py (closed-position columns relevant for sell-path fix test):
The `closed_at`, `exit_price`, `realized_pnl` columns are in the positions Table
metadata. `closed_reason` and `closed_manual_reason` were added via ALTER TABLE
(run_migrations), so they must be queried with `text("SELECT * FROM positions WHERE id = :id")`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sell-path and validation tests to test_fix.py</name>
  <files>tests/test_commands/test_fix.py</files>

  <read_first>
  Read tests/test_commands/test_fix.py in full — understand existing fixtures,
  imports, and the mock patterns used in test_no_transaction, test_no_changes,
  and test_price_change_updates_stop.

  Read src/bensdorp1/commands/fix.py lines 66-113 (closed-position branch: how
  closed_row is fetched and what fields are read), lines 176-197 (buy-path
  field loop with new guards), and lines 213-224 (sell-path price field loop
  with new guard).

  Note: the db_engine fixture from conftest.py does NOT call run_migrations() —
  it only calls metadata.create_all(). For the sell-path integration test,
  call `from bensdorp1.db import run_migrations` and invoke
  `run_migrations(db_engine)` inside the test to create the closed_reason /
  closed_manual_reason columns, mirroring the pattern in test_sell.py
  test_happy_path_normal.
  </read_first>

  <action>
  Append four new test functions to tests/test_commands/test_fix.py. Do NOT
  modify any existing tests or imports — only append new functions at the end
  of the file.

  Add these imports at the top of the file if not already present:
  - `from bensdorp1.db import run_migrations` (check; may already be imported)
  - `from sqlalchemy import text` (check; may already be imported)

  **Test 1 — test_fix_sell_path (integration, uses db_engine):**

  Scenario: seed a closed position, invoke `fix SYMBOL` (sell-path), change price,
  confirm; assert exit_price, realized_pnl, and audit_log updated.

  Setup:
  1. Call `run_migrations(db_engine)` so closed_reason / closed_manual_reason columns exist.
  2. Insert a closed position via `insert(positions).values(...)` with:
     - symbol="AAPL", entry_close=182.50, shares=50
     - closed_at=datetime(2026, 5, 20, tzinfo=UTC)
     - exit_price=178.20, realized_pnl=(178.20-182.50)*50
     - Set closed_reason and closed_manual_reason via `text("UPDATE positions SET closed_reason='stop_trailing', closed_manual_reason=NULL WHERE id=:id")`
  3. Patch `bensdorp1.commands.fix.get_engine` to return `db_engine`
  4. Patch `bensdorp1.commands.fix.create_backup`

  Invocation:
  - `runner.invoke(app, ["fix", "AAPL"], input="y\n\n180.00\ny\n")`
    (y = confirm fix target; Enter = keep Date; "180.00" = new price; y = confirm correction)

  Assertions:
  - `result.exit_code == 0`
  - "Transaction corrected" in result.output
  - DB row: `row.exit_price == pytest.approx(180.00)`
  - DB row: `row.realized_pnl == pytest.approx((180.00 - 182.50) * 50)`
  - audit_log has a row with event_type == "transaction_corrected" and symbol == "AAPL"
  - audit_log payload["after"]["exit_price"] == pytest.approx(180.00)
  - audit_log payload["before"]["exit_price"] == pytest.approx(178.20)

  Query the position row with:
  `conn.execute(text("SELECT * FROM positions WHERE id = :id"), {"id": position_id}).fetchone()`
  (needed because closed_reason column not in schema metadata).

  **Test 2 — test_buy_path_price_zero_rejected (mock-based):**

  Scenario: open position found; user enters "0" for price in the buy-path field
  loop; command exits code 1 with error message before any DB write.

  Setup (mirror test_no_changes mock pattern):
  - mock_open_row.id = 1, entry_date = datetime(2026, 5, 1, tzinfo=UTC),
    entry_close = 432.50, shares = 23, initial_stop = 402.225
  - mock_conn.execute.return_value.fetchone.return_value = mock_open_row

  Invocation:
  - `runner.invoke(app, ["fix", "NVDA"], input="y\n\n0\n")`
    (y = confirm target; Enter = keep Date; "0" = zero price)

  Assertions:
  - `result.exit_code == 1`
  - "Price must be greater than zero" in result.output
  - `mock_conn.execute.call_count` is consistent with only the initial SELECT
    (no UPDATE call made — mock_conn.execute should NOT have been called with
    an update statement; verify via `mock_create_backup.called is False` and
    `mock_log_event.called is False`)

  **Test 3 — test_buy_path_shares_zero_rejected (mock-based):**

  Scenario: open position found; user enters valid price "432.75", then "0" for
  shares; command exits code 1 with error message.

  Setup: same mock pattern as test_buy_path_price_zero_rejected.

  Invocation:
  - `runner.invoke(app, ["fix", "NVDA"], input="y\n\n432.75\n0\n")`
    (y = confirm target; Enter = keep Date; valid price; zero shares)

  Assertions:
  - `result.exit_code == 1`
  - "Shares must be greater than zero" in result.output

  **Test 4 — test_sell_path_price_zero_rejected (mock-based):**

  Scenario: no open position (returns None), closed position found; user enters "0"
  for price in the sell-path field loop; command exits code 1.

  Setup:
  - mock_conn.execute.return_value.fetchone.side_effect = [
      None,           # open_row lookup → no open position
      mock_closed,    # closed_row lookup → closed position found
    ]
  - mock_closed: id=2, entry_close=182.50, shares=50,
    closed_at=datetime(2026, 5, 20, tzinfo=UTC),
    exit_price=178.20, closed_reason="stop_trailing",
    closed_manual_reason=None, realized_pnl=(178.20-182.50)*50

  Invocation:
  - `runner.invoke(app, ["fix", "AAPL"], input="y\n\n0\n")`
    (y = confirm target; Enter = keep Date; "0" = zero price)

  Assertions:
  - `result.exit_code == 1`
  - "Price must be greater than zero" in result.output

  After writing all four tests, run:
  `uv run pytest tests/test_commands/test_fix.py -v`
  All 7 tests (3 existing + 4 new) must pass.

  Run also:
  `uv run ruff check tests/test_commands/test_fix.py && uv run mypy tests/test_commands/test_fix.py --strict`
  Fix any issues before completing.
  </action>

  <verify>
    <automated>
      uv run pytest tests/test_commands/test_fix.py -v
      uv run ruff check tests/test_commands/test_fix.py
    </automated>
  </verify>

  <acceptance_criteria>
  - `uv run pytest tests/test_commands/test_fix.py -v` shows 7 passed, 0 failed, 0 skipped
  - test_fix_sell_path is present and uses `db_engine` fixture with `run_migrations(db_engine)` call
  - test_buy_path_price_zero_rejected asserts exit_code == 1 and "Price must be greater than zero" in output
  - test_buy_path_shares_zero_rejected asserts exit_code == 1 and "Shares must be greater than zero" in output
  - test_sell_path_price_zero_rejected uses side_effect=[None, mock_closed] for the two fetchone calls
  - ruff check exits 0 on test_fix.py
  </acceptance_criteria>

  <done>
  test_fix.py has 7 tests covering the buy-path happy path (existing), sell-path
  integration (new), and 3 validation rejection scenarios (new). All pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add error-path tests to test_buy.py and test_sell.py</name>
  <files>tests/test_commands/test_buy.py, tests/test_commands/test_sell.py</files>

  <read_first>
  Read tests/test_commands/test_buy.py in full — understand existing mock pattern
  for test_invalid_constituent (single fetchone None mock with constituent check).
  Note that price/shares <= 0 validation in buy.py fires AFTER two DB round-trips
  (constituent check + open-position check), so the mock must supply both fetchone
  values.

  Read tests/test_commands/test_sell.py in full — understand test_no_exit_trigger
  mock pattern. The no-open-position test needs fetchone to return None on the
  FIRST call (the pos_row lookup, sell.py line 91-100).

  Read src/bensdorp1/commands/buy.py lines 44-104 to confirm the exact validation
  sequence: B (date parse), C.1 (constituent), C.2 (open position), C.3 (price/shares).
  </read_first>

  <action>
  **Additions to tests/test_commands/test_buy.py:**

  Append two new test functions at the end of test_buy.py.

  **Test 5 — test_invalid_date (mock-based):**

  Scenario: `--date` with an invalid format string causes exit code 1 before any
  DB query.

  Setup:
  - Use a simple MagicMock engine (constituent check will never be reached because
    date parsing happens at step B, before step C.1)
  - Patch DATA_DIR, get_engine, run_migrations, create_backup, log_event

  Invocation:
  - `runner.invoke(app, ["buy", "NVDA", "432.50", "23", "--date", "not-a-date"])`

  Assertions:
  - `result.exit_code == 1`
  - "Invalid --date value" in result.output

  **Test 6 — test_price_zero_rejected (mock-based):**

  Scenario: price=0 is passed; command exits code 1 after two DB round-trips
  (constituent + open-position checks pass) but before any insert.

  Setup:
  - mock_conn.execute.return_value.fetchone.side_effect = [
      MagicMock(symbol="NVDA"),   # C.1 constituent check passes
      None,                        # C.2 no open position found
    ]
  - Patch DATA_DIR, get_engine, run_migrations, create_backup, log_event

  Invocation:
  - `runner.invoke(app, ["buy", "NVDA", "0.0", "23"])`

  Assertions:
  - `result.exit_code == 1`
  - "Price and shares must be greater than zero" in result.output
  - mock_create_backup.called is False

  After writing, run:
  `uv run pytest tests/test_commands/test_buy.py -v`
  All 7 tests (5 existing + 2 new) must pass.

  ---

  **Additions to tests/test_commands/test_sell.py:**

  Append one new test function at the end of test_sell.py.

  **Test 7 — test_no_open_position (mock-based):**

  Scenario: symbol has no open position; sell exits code 1 with "No open position"
  error before any other lookup.

  Setup:
  - mock_conn.execute.return_value.fetchone.return_value = None
    (the first fetchone is pos_row lookup; returning None triggers the guard)
  - Patch DATA_DIR, get_engine, run_migrations

  Invocation:
  - `runner.invoke(app, ["sell", "AAPL", "178.20"])`

  Assertions:
  - `result.exit_code == 1`
  - "No open position for" in result.output

  After writing, run:
  `uv run pytest tests/test_commands/test_sell.py -v`
  All 4 tests (3 existing + 1 new) must pass.

  ---

  After both files are updated, run the full coverage report:

  `uv run pytest tests/test_commands/ --cov=src/bensdorp1/commands --cov-report=term-missing -v`

  Examine the coverage output. If total is still below 90%, identify the remaining
  uncovered lines from the `--cov-report=term-missing` output and add targeted
  tests to cover them. The goal is >= 90% total for the commands package.

  Common remaining gaps to check:
  - sell.py: price <= 0 branch (line 116-121) — needs `runner.invoke(app, ["sell", "AAPL", "0.0"])` after seeding open position
  - sell.py: sell_date < entry_date branch (line 124-130)

  If these branches are needed for >= 90%: add `test_sell_price_zero_rejected`
  and/or `test_sell_date_before_entry` to test_sell.py following the
  test_no_open_position mock pattern extended with side_effect for two fetchone
  calls (pos_row found, then no trigger needed because validation fires first).

  Run ruff and mypy on both files after all additions:
  `uv run ruff check tests/test_commands/test_buy.py tests/test_commands/test_sell.py`
  `uv run mypy tests/test_commands/test_buy.py tests/test_commands/test_sell.py --strict`
  </action>

  <verify>
    <automated>
      uv run pytest tests/test_commands/test_buy.py tests/test_commands/test_sell.py -v
      uv run pytest tests/test_commands/ --cov=src/bensdorp1/commands --cov-report=term-missing -q 2>&1 | tail -20
      uv run ruff check tests/test_commands/test_buy.py tests/test_commands/test_sell.py
    </automated>
  </verify>

  <acceptance_criteria>
  - `uv run pytest tests/test_commands/test_buy.py -v` shows >= 7 passed (5 existing + 2 new), 0 failed
  - `uv run pytest tests/test_commands/test_sell.py -v` shows >= 4 passed (3 existing + 1 new), 0 failed
  - test_invalid_date asserts exit_code == 1 and "Invalid --date value" in output
  - test_price_zero_rejected (buy) asserts exit_code == 1 and "Price and shares must be greater than zero" in output
  - test_no_open_position (sell) asserts exit_code == 1 and "No open position for" in output
  - Coverage report shows total for commands package >= 90%
  - ruff check exits 0 on both test files
  </acceptance_criteria>

  <done>
  test_buy.py has >= 7 tests; test_sell.py has >= 4 tests. Total phase 8 command
  coverage reaches >= 90%. All tests pass. ruff clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real SQLite | Integration tests write to tmp_path DB via db_engine fixture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | test isolation | mitigate | db_engine fixture provides fresh tmp_path DB per test; no shared state between tests |
</threat_model>

<verification>
Run the complete phase 8 test suite with coverage after both tasks complete:

```
uv run pytest tests/test_commands/ --cov=src/bensdorp1/commands --cov-report=term-missing -v
```

Expected outcome:
- All tests pass (>= 12 total: 7 fix + 7+ buy + 4+ sell), 0 failed
- Coverage for src/bensdorp1/commands: >= 90% total
- fix.py: >= 90% (was 56%)
- buy.py: >= 90% (was 82%)
- sell.py: >= 90% (was 84%)

Also run the full suite to confirm no regressions:
```
uv run pytest tests/ -v
```
</verification>

<success_criteria>
- test_fix.py: 7 tests pass (3 existing + 4 new)
- test_buy.py: >= 7 tests pass (5 existing + 2 new)
- test_sell.py: >= 4 tests pass (3 existing + 1 new)
- Total coverage for commands package: >= 90%
- fix.py coverage: >= 90%
- buy.py coverage: >= 90%
- sell.py coverage: >= 90%
- ruff check clean on all 3 test files
- Full test suite (`uv run pytest tests/`) exits green
</success_criteria>

<output>
Create `.planning/phases/08-confirmation-commands/08-07-SUMMARY.md` when done.
</output>
